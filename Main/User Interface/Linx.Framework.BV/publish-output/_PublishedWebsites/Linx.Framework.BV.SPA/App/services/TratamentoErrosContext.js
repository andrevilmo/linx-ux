define(['durandal/system', 'pkg_linx-framework-bv-spa/services/DataDomains', 'services/logger', 'breeze', 'durandal/app', 'managers/__auth', 'viewmodels/shared/modal', 'viewmodels/shared/modal2'],
function (system, dataDomains, logger, breeze, app, managerAuth, modal, modal2) {
var result = function () {
    var getPivotLayouts = function (params, success, error) {
        return $.ajax({
            messageUser: 'Busca dos layouts exportados',
            contentType: 'application/json; charset=UTF-8',
            headers: managerAuth.getHeaders(),
            url: getServiceAddress('linxframeworkobjeto') + '/GetPivotLayouts?' +
                                                                   'rootNameSpace=' + params.rootNamespace +
                                                                   '&viewName=' + params.viewName +
                                                                   '&pivotName=' + params.pivotName +
                                                                   '&pivotDataSource=' + params.pivotDataSource,
            async: true,
            cache: false,
            error: error,
            success: success
        });
    };
    
    var getServiceAddress = function(apiPart) {
       return managerAuth.getServiceAddress(apiPart, businessAssemblyName);
    };
    var getBaseServiceAddress = function(apiPart) {
       return managerAuth.getBaseServiceAddress(apiPart, businessAssemblyName);
    };
    var getAccessGroup = function() {
       return '00000000-0000-0000-0000-000000000000';
    };
    var getNewGuid = function() {
       return breeze.core.getUuid();
    };
    var getDataFeedUrl = function() {
       return getServiceAddress('LinxFrameworkTratamentoErrosOData');
    };
    var getDataServiceUrl = function () {
       return getServiceAddress(controllerName);
    };
    var setServiceBusUrl = function (url) {
       if (dataService) { dataService.serviceName = (isNullOrEmpty(url) ? getDataServiceUrl() : url + controllerName); }
    };
    var initializePOCO = function(ownerReference, entityName) {
       if (ownerReference && !ownerReference.RowDataId) { eval(entityName + 'Initializer(ownerReference, true);'); }
    };
    var businessAssemblyName = 'Linx.Framework.BV';
    var controllerName = 'LinxFrameworkTratamentoErros';
    var dataService = new breeze.DataService({
        serviceName: getDataServiceUrl(),
        hasServerMetadata: false // don't ask the server for metadata
    });
    var manager = new breeze.EntityManager({ dataService: dataService });
    manager.entityChanged.subscribe(function(changeArgs) {
        if (changeArgs.entityAction === breeze.EntityAction.PropertyChange) {
            if ((typeof changeArgs.args.newValue) === 'number' && changeArgs.args.oldValue < 0 && changeArgs.args.newValue > 0 && changeArgs.entity.isPrimaryKey(changeArgs.args.propertyName)) vm.replaceInnerUIsKeys(changeArgs.entity, changeArgs.args.propertyName, changeArgs.args.oldValue, changeArgs.args.newValue);
            if (typeof changeArgs.entity.OnPropertyChanged == 'function')
                changeArgs.entity.OnPropertyChanged(changeArgs.args.propertyName, changeArgs.args.oldValue, changeArgs.args.newValue);
        }
    });
    var enableChangeTrack = true;
    var entityPropChanged = function(entity, propName, oldVal, newVal) {
        if (!enableChangeTrack) return true;
        var result = true;
        if ((typeof entity.OnPropertyChanged == 'function') && oldVal !== newVal)
            result = (entity.OnPropertyChanged(propName, oldVal, newVal) !== false);
        if (result && ['U', 'I', 'D'].indexOf(entity.ChangeState) < 0) { entity.createOriginal(propName, oldVal); entity.ChangeState = 'U'; if (entity.setParentAsModified) entity.setParentAsModified(); }
        if (result && (typeof newVal) === 'number' && oldVal < 0 && newVal > 0 && entity.isPrimaryKey(propName)) vm.replaceInnerUIsKeys(entity, propName, oldVal, newVal);
        return result;
    }
    var metadataStore = manager.metadataStore;
    var EntityQuery = breeze.EntityQuery;
    // Extract Breeze metadata definition types
    var DataType = breeze.DataType;
    var AutoGeneratedKeyType = breeze.AutoGeneratedKeyType;
    var Validator = breeze.Validator;
    Validator.hasValueValidator = new breeze.Validator('hasValueValidator', hasValueValidationFn, { messageTemplate: "'%displayName%' é requerido" });
    //#region Metadata Info
    var metadataInfo = [];
    var dataExportInfo = [];
    var entityNames = [];
    var lookUpNames = [];
    var entitylookUps = [];
    entityNames.push('TcsLogErrosDash');
    metadataInfo['TcsLogErrosDash'] = [
        { key: 'EntityUniqueKey', maxLength: 0, isPartOfKey: true, headerText: 'EntityUniqueKey', width: '50px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'DataErro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: false, headerText: 'Data', width: '120px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'DescricaoAmbiente', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'DescricaoAmbiente', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Ambiente', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DescricaoAplicacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'DescricaoAplicacao', lookupVisibleColumns: '', maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Aplicação', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'EnderecoWeb', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 8000, validateMaxLength: true, isPartOfKey: false, headerText: 'Endereço Web', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Gpecon', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'Gpecon', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Grupo Econômico', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdAplicacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdAplicacao', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Aplicacao', width: '192px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdLinxEmpresa', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdLinxEmpresa', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Linx Empresa', width: '231px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdLinxGpecon', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdLinxGpecon', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Linx Gpecon', width: '218px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdTcsAmbiente', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdTcsAmbiente', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Tcs Ambiente', width: '231px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdTcsLogErros', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id', width: '130px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdUsuario', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Usuario', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'MensagemExcecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 1000, validateMaxLength: true, isPartOfKey: false, headerText: 'Exceção', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'MensagemExcecaoInterna', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 1000, validateMaxLength: true, isPartOfKey: false, headerText: 'Exceção Interna', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'MetodoHttp', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 6, validateMaxLength: true, isPartOfKey: false, headerText: 'Método Http', width: '179px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeAcao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 256, validateMaxLength: true, isPartOfKey: false, headerText: 'Ação', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeControlador', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 200, validateMaxLength: true, isPartOfKey: false, headerText: 'Controlador', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeEmpresa', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'NomeEmpresa', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Empresa', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeServidor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 256, validateMaxLength: true, isPartOfKey: false, headerText: 'Servidor', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'PilhaExcecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Pilha Exceção', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'UsuarioWindows', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 256, validateMaxLength: true, isPartOfKey: false, headerText: 'Usuário Servidor', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeAutenticacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'NomeAutenticacao', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Nome Autenticação', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'NomeUsuario', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Usuário', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ChangeState', isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: '', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null }
    ];
    dataExportInfo['TcsLogErrosDash'] = [ 
        { name: 'TcsLogErrosDash', canExportMedia: false , canExportReport: false, actionExport: 'LinxFrameworkTratamentoErros/GetTcsLogErrosDashToExcel', actionReport: 'LinxFrameworkTratamentoErros/GetTcsLogErrosDashToReportXml', actionFeed: 'LinxFrameworkTratamentoErrosOData/TcsLogErrosDash', actionName: 'LinxFrameworkTratamentoErros/GetTcsLogErrosDashByEntitySearchNoAssociations', display: 'TcsLogErrosDash',  metaData: function() { return metadataInfo['TcsLogErrosDash']; } }
        , { name: 'LogFile', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkTratamentoErros/GetLogFileToExcel', actionReport: 'LinxFrameworkTratamentoErros/GetLogFileToReportXml', actionFeed: 'LinxFrameworkTratamentoErrosOData/LogFile', actionName: 'LinxFrameworkTratamentoErros/GetLogFileByEntitySearchNoAssociations', display: 'Arquivo',  metaData: function() { return metadataInfo['LogFile']; } }
        , { name: 'TcsLogErros', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkTratamentoErros/GetTcsLogErrosToExcel', actionReport: 'LinxFrameworkTratamentoErros/GetTcsLogErrosToReportXml', actionFeed: 'LinxFrameworkTratamentoErrosOData/TcsLogErros', actionName: 'LinxFrameworkTratamentoErros/GetTcsLogErrosByEntitySearchNoAssociations', display: 'Banco de Dados',  metaData: function() { return metadataInfo['TcsLogErros']; } }
    ];
    entitylookUps.push('TcsLogErrosDash');
    entitylookUps['TcsLogErrosDash'] = [];
    entitylookUps['TcsLogErrosDash'].push('LookUpGpecon');
    lookUpNames.push('LookUpGpecon');
    metadataInfo['LookUpGpecon'] = [
        { key: 'IdLinxGpecon', relatedKey: 'IdLinxGpecon', maxLength: 12, isPartOfKey: true, headerText: 'Id Linx Gpecon', width: '218px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'Gpecon', relatedKey: 'Gpecon', maxLength: 250, isPartOfKey: false, headerText: 'Grupo Economico', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    entitylookUps['TcsLogErrosDash'].push('LookUpTcsAmbiente');
    lookUpNames.push('LookUpTcsAmbiente');
    metadataInfo['LookUpTcsAmbiente'] = [
        { key: 'DescricaoAmbiente', relatedKey: 'DescricaoAmbiente', maxLength: 250, isPartOfKey: false, headerText: 'Descricao Ambiente', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdTcsAmbiente', relatedKey: 'IdTcsAmbiente', maxLength: 12, isPartOfKey: true, headerText: 'Id Tcs Ambiente', width: '231px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null }
    ];
    entitylookUps['TcsLogErrosDash'].push('LookUpTcsAplicacao');
    lookUpNames.push('LookUpTcsAplicacao');
    metadataInfo['LookUpTcsAplicacao'] = [
        { key: 'DescricaoAplicacao', relatedKey: 'DescricaoAplicacao', maxLength: 60, isPartOfKey: false, headerText: 'Descricao Aplicacao', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdAplicacao', relatedKey: 'IdAplicacao', maxLength: 12, isPartOfKey: true, headerText: 'Id Aplicacao', width: '192px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null }
    ];
    entitylookUps['TcsLogErrosDash'].push('LookUpTcsEmpresaAutenticacao');
    lookUpNames.push('LookUpTcsEmpresaAutenticacao');
    metadataInfo['LookUpTcsEmpresaAutenticacao'] = [
        { key: 'IdLinxEmpresa', relatedKey: 'IdLinxEmpresa', maxLength: 12, isPartOfKey: true, headerText: 'Id Linx Empresa', width: '231px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'NomeEmpresa', relatedKey: 'NomeEmpresa', maxLength: 250, isPartOfKey: false, headerText: 'Nome Empresa', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    entitylookUps['TcsLogErrosDash'].push('LookUpTcsUsuarioAutenticacao');
    lookUpNames.push('LookUpTcsUsuarioAutenticacao');
    metadataInfo['LookUpTcsUsuarioAutenticacao'] = [
        { key: 'IdUsuario', relatedKey: 'IdUsuario', maxLength: 24, isPartOfKey: true, headerText: 'Id Usuario', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'NomeAutenticacao', relatedKey: 'NomeAutenticacao', maxLength: 250, isPartOfKey: false, headerText: 'Usuário Autenticação', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeUsuario', relatedKey: 'NomeUsuario', maxLength: 250, isPartOfKey: false, headerText: 'Nome', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    entityNames.push('LogFile');
    metadataInfo['LogFile'] = [
        { key: 'EntityParentUniqueKey', maxLength: 0, isPartOfKey: false, headerText: 'EntityUniqueKey', width: '50px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'FileName', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 100, validateMaxLength: true, isPartOfKey: true, headerText: 'FileName', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Download', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 13, validateMaxLength: true, isPartOfKey: false, headerText: 'Download', width: '130px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ChangeState', isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: '', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null }
    ];
    dataExportInfo['LogFile'] = [ 
        { name: 'LogFile', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkTratamentoErros/GetLogFileToExcel', actionReport: 'LinxFrameworkTratamentoErros/GetLogFileToReportXml', actionFeed: 'LinxFrameworkTratamentoErrosOData/LogFile', actionName: 'LinxFrameworkTratamentoErros/GetLogFileByEntitySearchNoAssociations', display: 'Arquivo',  metaData: function() { return metadataInfo['LogFile']; } }
    ];
    entitylookUps.push('LogFile');
    entitylookUps['LogFile'] = [];
    entityNames.push('TcsLogErros');
    metadataInfo['TcsLogErros'] = [
        { key: 'EntityParentUniqueKey', maxLength: 0, isPartOfKey: false, headerText: 'EntityUniqueKey', width: '50px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'DataErro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: false, headerText: 'Data', width: '120px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'DescricaoAmbiente', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Ambiente', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DescricaoAplicacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Aplicação', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Empresa', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Empresa', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'EnderecoWeb', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 8000, validateMaxLength: true, isPartOfKey: false, headerText: 'Endereço Web', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Gpecon', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Grupo Econômico', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdAplicacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Aplicacao', width: '192px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdLinxEmpresa', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Linx Empresa', width: '231px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdLinxGpecon', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Linx Gpecon', width: '218px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdTcsAmbiente', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Tcs Ambiente', width: '231px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdTcsLogErros', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 12, isPartOfKey: true, headerText: 'Id', width: '130px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Usuario', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'MensagemExcecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 1000, validateMaxLength: true, isPartOfKey: false, headerText: 'Exceção', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'MensagemExcecaoInterna', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 1000, validateMaxLength: true, isPartOfKey: false, headerText: 'Exceção Interna', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'MetodoHttp', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 6, validateMaxLength: true, isPartOfKey: false, headerText: 'Método Http', width: '179px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeAcao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 256, validateMaxLength: true, isPartOfKey: false, headerText: 'Ação', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeControlador', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 200, validateMaxLength: true, isPartOfKey: false, headerText: 'Controlador', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeServidor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 256, validateMaxLength: true, isPartOfKey: false, headerText: 'Servidor', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'PilhaExcecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Pilha Exceção', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'UsuarioWindows', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 256, validateMaxLength: true, isPartOfKey: false, headerText: 'Usuário Servidor', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeAutenticacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Nome Autenticação', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Usuário', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ChangeState', isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: '', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null }
    ];
    dataExportInfo['TcsLogErros'] = [ 
        { name: 'TcsLogErros', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkTratamentoErros/GetTcsLogErrosToExcel', actionReport: 'LinxFrameworkTratamentoErros/GetTcsLogErrosToReportXml', actionFeed: 'LinxFrameworkTratamentoErrosOData/TcsLogErros', actionName: 'LinxFrameworkTratamentoErros/GetTcsLogErrosByEntitySearchNoAssociations', display: 'Banco de Dados',  metaData: function() { return metadataInfo['TcsLogErros']; } }
    ];
    entitylookUps.push('TcsLogErros');
    entitylookUps['TcsLogErros'] = [];
    var lookUpProperties = [];
    //#endregion Metadata Info
    //#region dataParameters
    var dataParameters = {
        isLoaded: false,
        parameters: [],
        registerParameters: function (parameterList, complete) {
            if (parameterList !== '') {
                var variation = '{TBC_GRUPO_ECONOMICO|' + managerAuth.loginInfo.IdLinxGrupoEconomico.toString() + '|TCS_USUARIO|' + managerAuth.loginInfo.UidUsuario + (vm != null && vm.getBandeiraRede() > 0 ? '|TBC_BANDEIRA_REDE|' + vm.getBandeiraRede().toString() : '') + '}';
                $.ajax({
                    type: 'GET',
                    url: getServiceAddress('LinxFrameworkParametro') + '/GetParameterValue?serializedParameterList=' + stringReplace(parameterList, '{}', variation),
                    dataType: 'json',
                    cache: false,
                    headers: managerAuth.getHeaders(),
                    error: function (jqXHR, textStatus, errorThrown) {
                        var msg = 'Os seguintes parâmetros não foram pesquisados: [' + parameterList + ']';
                        app.showMessage(msg, 'Alerta', ['Ok']);
                        dataParameters.isLoaded = true;
                    },
                    success: function (data) {
                        var parametersName = '';
                        var parameters = data.split('#');
                        for (var idx in parameters) {
                            var values = parameters[idx].split('|');
                            var pName = values[0];
                            var pValue = values[1];
                            dataParameters.parameters[pName] = pValue;
                        }
                        dataParameters.isLoaded = true;
                        if (complete) complete();
                    }
                });
            }
        }
    };
    //#endregion dataParameters
    //#region Classes Map
    var sequences = [];
    var getNextSequence = function(entityName) {
        if (!sequences[entityName]) resetSequence(entityName);
        sequence = sequences[entityName];
        sequences[entityName]++;
        return sequence;
    };
    var resetSequence = function(entityName) {
        sequences[entityName] = 0;
    };
    var getSequence = function(entityName) {
        if (!sequences[entityName]) resetSequence(entityName);
        return sequences[entityName];
    };
    
    // Configure TcsLogErrosDash data type
    metadataStore.addEntityType({
    shortName: "TcsLogErrosDash",
    namespace: "Linx.Framework.BV.TratamentoErros",
    autoGeneratedKeyType: AutoGeneratedKeyType.None,
    dataProperties: {
    EntityUniqueKey: { dataType: DataType.Guid, isNullable: false, isPartOfKey: true, validators: [ ]  }
    ,DataErro: { dataType: DataType.DateTime, isNullable: false, isPartOfKey: false, defaultValue: '', validators: [ Validator.hasValueValidator]  }
    ,DescricaoAmbiente: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,DescricaoAplicacao: { dataType: DataType.String, maxLength: 60, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 60})]  }
    ,EnderecoWeb: { dataType: DataType.String, maxLength: 8000, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 8000})]  }
    ,Gpecon: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,IdAplicacao: { dataType: DataType.Int32, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IdLinxEmpresa: { dataType: DataType.Int32, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IdLinxGpecon: { dataType: DataType.Int32, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IdTcsAmbiente: { dataType: DataType.Int32, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IdTcsLogErros: { dataType: DataType.Int32, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdUsuario: { dataType: DataType.Int64, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,MensagemExcecao: { dataType: DataType.String, maxLength: 1000, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 1000})]  }
    ,MensagemExcecaoInterna: { dataType: DataType.String, maxLength: 1000, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 1000})]  }
    ,MetodoHttp: { dataType: DataType.String, maxLength: 6, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 6})]  }
    ,NomeAcao: { dataType: DataType.String, maxLength: 256, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 256})]  }
    ,NomeControlador: { dataType: DataType.String, maxLength: 200, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 200})]  }
    ,NomeEmpresa: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,NomeServidor: { dataType: DataType.String, maxLength: 256, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 256})]  }
    ,PilhaExcecao: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,UsuarioWindows: { dataType: DataType.String, maxLength: 256, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 256})]  }
    ,NomeAutenticacao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,NomeUsuario: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,ChangeState: { dataType: DataType.String, isNullable: true, isPartOfKey: false, validators: [] }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
    LogFileList: { entityTypeName: "LogFile:#Linx.Framework.BV.TratamentoErros", isScalar: false, invForeignKeyNames: ["EntityUniqueKey"], associationName: "FK_TcsLogErrosDash_LogFile" }
    ,TcsLogErrosList: { entityTypeName: "TcsLogErros:#Linx.Framework.BV.TratamentoErros", isScalar: false, invForeignKeyNames: ["EntityUniqueKey"], associationName: "FK_TcsLogErrosDash_TcsLogErros" }
                          }
    });
    lookUpProperties['TcsLogErrosDash'] = {DescricaoAmbiente: 'LookUpTcsAmbiente', DescricaoAplicacao: 'LookUpTcsAplicacao', Gpecon: 'LookUpGpecon', IdAplicacao: 'LookUpTcsAplicacao', IdLinxEmpresa: 'LookUpTcsEmpresaAutenticacao', IdLinxGpecon: 'LookUpGpecon', IdTcsAmbiente: 'LookUpTcsAmbiente', IdUsuario: 'LookUpTcsUsuarioAutenticacao', NomeEmpresa: 'LookUpTcsEmpresaAutenticacao', NomeAutenticacao: 'LookUpTcsUsuarioAutenticacao', NomeUsuario: 'LookUpTcsUsuarioAutenticacao'};
    var TcsLogErrosDashInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsLogErrosDash') : ko.observable(getNextSequence('TcsLogErrosDash')));
        //Start Property Definitions
        var _entityUniqueKey = ownerReference.EntityUniqueKey;
        Object.defineProperty(ownerReference, 'EntityUniqueKey', {
          get: function() { return _entityUniqueKey; },
          set: function(newValue) { var oldValue = _entityUniqueKey; _entityUniqueKey = newValue; if (!entityPropChanged(ownerReference, 'EntityUniqueKey', oldValue, newValue)) { _entityUniqueKey = oldValue; } }
        });
        var _dataErro = ownerReference.DataErro;
        Object.defineProperty(ownerReference, 'DataErro', {
          get: function() { return _dataErro; },
          set: function(newValue) { var oldValue = _dataErro; _dataErro = newValue; if (!entityPropChanged(ownerReference, 'DataErro', oldValue, newValue)) { _dataErro = oldValue; } }
        });
        var _descricaoAmbiente = ownerReference.DescricaoAmbiente;
        Object.defineProperty(ownerReference, 'DescricaoAmbiente', {
          get: function() { return _descricaoAmbiente; },
          set: function(newValue) { var oldValue = _descricaoAmbiente; _descricaoAmbiente = newValue; if (!entityPropChanged(ownerReference, 'DescricaoAmbiente', oldValue, newValue)) { _descricaoAmbiente = oldValue; } }
        });
        var _descricaoAplicacao = ownerReference.DescricaoAplicacao;
        Object.defineProperty(ownerReference, 'DescricaoAplicacao', {
          get: function() { return _descricaoAplicacao; },
          set: function(newValue) { var oldValue = _descricaoAplicacao; _descricaoAplicacao = newValue; if (!entityPropChanged(ownerReference, 'DescricaoAplicacao', oldValue, newValue)) { _descricaoAplicacao = oldValue; } }
        });
        var _enderecoWeb = ownerReference.EnderecoWeb;
        Object.defineProperty(ownerReference, 'EnderecoWeb', {
          get: function() { return _enderecoWeb; },
          set: function(newValue) { var oldValue = _enderecoWeb; _enderecoWeb = newValue; if (!entityPropChanged(ownerReference, 'EnderecoWeb', oldValue, newValue)) { _enderecoWeb = oldValue; } }
        });
        var _gpecon = ownerReference.Gpecon;
        Object.defineProperty(ownerReference, 'Gpecon', {
          get: function() { return _gpecon; },
          set: function(newValue) { var oldValue = _gpecon; _gpecon = newValue; if (!entityPropChanged(ownerReference, 'Gpecon', oldValue, newValue)) { _gpecon = oldValue; } }
        });
        var _idAplicacao = ownerReference.IdAplicacao;
        Object.defineProperty(ownerReference, 'IdAplicacao', {
          get: function() { return _idAplicacao; },
          set: function(newValue) { var oldValue = _idAplicacao; _idAplicacao = newValue; if (!entityPropChanged(ownerReference, 'IdAplicacao', oldValue, newValue)) { _idAplicacao = oldValue; } }
        });
        var _idLinxEmpresa = ownerReference.IdLinxEmpresa;
        Object.defineProperty(ownerReference, 'IdLinxEmpresa', {
          get: function() { return _idLinxEmpresa; },
          set: function(newValue) { var oldValue = _idLinxEmpresa; _idLinxEmpresa = newValue; if (!entityPropChanged(ownerReference, 'IdLinxEmpresa', oldValue, newValue)) { _idLinxEmpresa = oldValue; } }
        });
        var _idLinxGpecon = ownerReference.IdLinxGpecon;
        Object.defineProperty(ownerReference, 'IdLinxGpecon', {
          get: function() { return _idLinxGpecon; },
          set: function(newValue) { var oldValue = _idLinxGpecon; _idLinxGpecon = newValue; if (!entityPropChanged(ownerReference, 'IdLinxGpecon', oldValue, newValue)) { _idLinxGpecon = oldValue; } }
        });
        var _idTcsAmbiente = ownerReference.IdTcsAmbiente;
        Object.defineProperty(ownerReference, 'IdTcsAmbiente', {
          get: function() { return _idTcsAmbiente; },
          set: function(newValue) { var oldValue = _idTcsAmbiente; _idTcsAmbiente = newValue; if (!entityPropChanged(ownerReference, 'IdTcsAmbiente', oldValue, newValue)) { _idTcsAmbiente = oldValue; } }
        });
        var _idTcsLogErros = ownerReference.IdTcsLogErros;
        Object.defineProperty(ownerReference, 'IdTcsLogErros', {
          get: function() { return _idTcsLogErros; },
          set: function(newValue) { var oldValue = _idTcsLogErros; _idTcsLogErros = newValue; if (!entityPropChanged(ownerReference, 'IdTcsLogErros', oldValue, newValue)) { _idTcsLogErros = oldValue; } }
        });
        var _idUsuario = ownerReference.IdUsuario;
        Object.defineProperty(ownerReference, 'IdUsuario', {
          get: function() { return _idUsuario; },
          set: function(newValue) { var oldValue = _idUsuario; _idUsuario = newValue; if (!entityPropChanged(ownerReference, 'IdUsuario', oldValue, newValue)) { _idUsuario = oldValue; } }
        });
        var _mensagemExcecao = ownerReference.MensagemExcecao;
        Object.defineProperty(ownerReference, 'MensagemExcecao', {
          get: function() { return _mensagemExcecao; },
          set: function(newValue) { var oldValue = _mensagemExcecao; _mensagemExcecao = newValue; if (!entityPropChanged(ownerReference, 'MensagemExcecao', oldValue, newValue)) { _mensagemExcecao = oldValue; } }
        });
        var _mensagemExcecaoInterna = ownerReference.MensagemExcecaoInterna;
        Object.defineProperty(ownerReference, 'MensagemExcecaoInterna', {
          get: function() { return _mensagemExcecaoInterna; },
          set: function(newValue) { var oldValue = _mensagemExcecaoInterna; _mensagemExcecaoInterna = newValue; if (!entityPropChanged(ownerReference, 'MensagemExcecaoInterna', oldValue, newValue)) { _mensagemExcecaoInterna = oldValue; } }
        });
        var _metodoHttp = ownerReference.MetodoHttp;
        Object.defineProperty(ownerReference, 'MetodoHttp', {
          get: function() { return _metodoHttp; },
          set: function(newValue) { var oldValue = _metodoHttp; _metodoHttp = newValue; if (!entityPropChanged(ownerReference, 'MetodoHttp', oldValue, newValue)) { _metodoHttp = oldValue; } }
        });
        var _nomeAcao = ownerReference.NomeAcao;
        Object.defineProperty(ownerReference, 'NomeAcao', {
          get: function() { return _nomeAcao; },
          set: function(newValue) { var oldValue = _nomeAcao; _nomeAcao = newValue; if (!entityPropChanged(ownerReference, 'NomeAcao', oldValue, newValue)) { _nomeAcao = oldValue; } }
        });
        var _nomeControlador = ownerReference.NomeControlador;
        Object.defineProperty(ownerReference, 'NomeControlador', {
          get: function() { return _nomeControlador; },
          set: function(newValue) { var oldValue = _nomeControlador; _nomeControlador = newValue; if (!entityPropChanged(ownerReference, 'NomeControlador', oldValue, newValue)) { _nomeControlador = oldValue; } }
        });
        var _nomeEmpresa = ownerReference.NomeEmpresa;
        Object.defineProperty(ownerReference, 'NomeEmpresa', {
          get: function() { return _nomeEmpresa; },
          set: function(newValue) { var oldValue = _nomeEmpresa; _nomeEmpresa = newValue; if (!entityPropChanged(ownerReference, 'NomeEmpresa', oldValue, newValue)) { _nomeEmpresa = oldValue; } }
        });
        var _nomeServidor = ownerReference.NomeServidor;
        Object.defineProperty(ownerReference, 'NomeServidor', {
          get: function() { return _nomeServidor; },
          set: function(newValue) { var oldValue = _nomeServidor; _nomeServidor = newValue; if (!entityPropChanged(ownerReference, 'NomeServidor', oldValue, newValue)) { _nomeServidor = oldValue; } }
        });
        var _pilhaExcecao = ownerReference.PilhaExcecao;
        Object.defineProperty(ownerReference, 'PilhaExcecao', {
          get: function() { return _pilhaExcecao; },
          set: function(newValue) { var oldValue = _pilhaExcecao; _pilhaExcecao = newValue; if (!entityPropChanged(ownerReference, 'PilhaExcecao', oldValue, newValue)) { _pilhaExcecao = oldValue; } }
        });
        var _usuarioWindows = ownerReference.UsuarioWindows;
        Object.defineProperty(ownerReference, 'UsuarioWindows', {
          get: function() { return _usuarioWindows; },
          set: function(newValue) { var oldValue = _usuarioWindows; _usuarioWindows = newValue; if (!entityPropChanged(ownerReference, 'UsuarioWindows', oldValue, newValue)) { _usuarioWindows = oldValue; } }
        });
        var _nomeAutenticacao = ownerReference.NomeAutenticacao;
        Object.defineProperty(ownerReference, 'NomeAutenticacao', {
          get: function() { return _nomeAutenticacao; },
          set: function(newValue) { var oldValue = _nomeAutenticacao; _nomeAutenticacao = newValue; if (!entityPropChanged(ownerReference, 'NomeAutenticacao', oldValue, newValue)) { _nomeAutenticacao = oldValue; } }
        });
        var _nomeUsuario = ownerReference.NomeUsuario;
        Object.defineProperty(ownerReference, 'NomeUsuario', {
          get: function() { return _nomeUsuario; },
          set: function(newValue) { var oldValue = _nomeUsuario; _nomeUsuario = newValue; if (!entityPropChanged(ownerReference, 'NomeUsuario', oldValue, newValue)) { _nomeUsuario = oldValue; } }
        });
        //End Property Definitions
       ownerReference.currentLogFile = ko.observable(null);
       ownerReference.currentTcsLogErros = ko.observable(null);
       //Adjust details for a POCO reference
       if (isPOCO === true) {
           ownerReference.LogFileList = ko.observableArray(ownerReference.LogFileList);
           ownerReference.TcsLogErrosList = ko.observableArray(ownerReference.TcsLogErrosList);
       }
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           if (noDetails !== true && ownerReference.TcsLogErrosList && ownerReference.TcsLogErrosList().length > 0) {
             var detailExpr = ownerReference.TcsLogErrosList()[0].getJExpression(listFilterRange);
             if (detailExpr === 'Error') return detailExpr;
             jExpression += detailExpr;
           }
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               if (isNullOrEmpty(propertyName)) {
                   for (var i = 0; i < ownerReference.LogFileList().length; i++) {
                       var detail = ownerReference.LogFileList()[i];
                       errors = errors.concat(detail.getValidationErrors());
                   }
               }
               if (isNullOrEmpty(propertyName)) {
                   for (var i = 0; i < ownerReference.TcsLogErrosList().length; i++) {
                       var detail = ownerReference.TcsLogErrosList()[i];
                       errors = errors.concat(detail.getValidationErrors());
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           if (loadDetails) {
               result.LogFileList = [];
               var sourceList = getAbsoluteValue(ownerReference.LogFileList);
               if (sourceList && sourceList.length > 0) {
                   for (var i = 0; i < sourceList.length; i++) {
                       if (['U', 'I', 'D'].indexOf(sourceList[i].ChangeState) >= 0) result.LogFileList.push(sourceList[i].getPrimitiveDTO(sourceList[i].ChangeState != 'D'));
                   }
               }
               result.TcsLogErrosList = [];
               var sourceList = getAbsoluteValue(ownerReference.TcsLogErrosList);
               if (sourceList && sourceList.length > 0) {
                   for (var i = 0; i < sourceList.length; i++) {
                       if (['U', 'I', 'D'].indexOf(sourceList[i].ChangeState) >= 0) result.TcsLogErrosList.push(sourceList[i].getPrimitiveDTO(sourceList[i].ChangeState != 'D'));
                   }
               }
           }
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           var _LogFileList = getAbsoluteValue(ownerReference.LogFileList);
           if (_LogFileList && _LogFileList.length > 0) {
               for (var i = 0; i < _LogFileList.length; i++) {
                   var detail = _LogFileList[i];
                   if (['U', 'I', 'D'].indexOf(detail.ChangeState) >= 0) {
                       result.push(detail);
                       result = result.concat(detail.getAllDetailChanges());
                   }
               }
           }
           var _TcsLogErrosList = getAbsoluteValue(ownerReference.TcsLogErrosList);
           if (_TcsLogErrosList && _TcsLogErrosList.length > 0) {
               for (var i = 0; i < _TcsLogErrosList.length; i++) {
                   var detail = _TcsLogErrosList[i];
                   if (['U', 'I', 'D'].indexOf(detail.ChangeState) >= 0) {
                       result.push(detail);
                       result = result.concat(detail.getAllDetailChanges());
                   }
               }
           }
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
           if (copyDetails) {
               if (ownerReference.LogFileList && originData.LogFileList) {
                   var toList = getAbsoluteValue(ownerReference.LogFileList);
                   var fromList = getAbsoluteValue(originData.LogFileList);
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                      if (toList[idxElem].ChangeState === 'D') toList.splice(idxElem, 1);
                   }
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                          if (toList[idxElem].ChangeState !== 'N') {
                               var fromObj = _.where(fromList, { FileName: toList[idxElem]['FileName'] });
                               if (fromObj.length > 0) toList[idxElem].copyDataFrom(fromObj[0], true);
                          }
                   }
               }
               if (ownerReference.TcsLogErrosList && originData.TcsLogErrosList) {
                   var toList = getAbsoluteValue(ownerReference.TcsLogErrosList);
                   var fromList = getAbsoluteValue(originData.TcsLogErrosList);
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                      if (toList[idxElem].ChangeState === 'D') toList.splice(idxElem, 1);
                   }
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                          if (toList[idxElem].ChangeState !== 'N') {
                               var fromObj = [];
                               if (toList[idxElem].ChangeState == 'I') {
                                   fromObj = _.where(fromList, { TemporaryIdTcsLogErros: toList[idxElem]['IdTcsLogErros'] });
                               } else {
                                   fromObj = _.where(fromList, { IdTcsLogErros: toList[idxElem]['IdTcsLogErros'] });
                               }
                               if (fromObj.length > 0) toList[idxElem].copyDataFrom(fromObj[0], true);
                          }
                   }
               }
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
              vm.dataBind('LogFileList', true);
              if (ownerReference.currentLogFile()) ownerReference.currentLogFile().commitDetailsVisualPendings();
              vm.dataBind('TcsLogErrosList', true);
              if (ownerReference.currentTcsLogErros()) ownerReference.currentTcsLogErros().commitDetailsVisualPendings();
          }
          ownerReference.refreshData = function(noWait, succeeded) {
           if (succeeded) { succeeded({results: [ownerReference]}); }
           return { then: function(thenMethod) { if (thenMethod) thenMethod(); }, fin: function(finMethod) { if (finMethod) finMethod(); } }; 
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return ownerReference.ChangeState === 'I'; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return ownerReference.ChangeState === 'D'; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return ownerReference.ChangeState === 'U'; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return ownerReference.ChangeState === 'N'; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() { ownerReference.ChangeState = 'U'; } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() { ownerReference.ChangeState = 'N'; } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['DataErro'] = 'T';
       ownerReference.serverDataType['DescricaoAmbiente'] = 'S';
       ownerReference.serverDataType['DescricaoAplicacao'] = 'S';
       ownerReference.serverDataType['EnderecoWeb'] = 'S';
       ownerReference.serverDataType['Gpecon'] = 'S';
       ownerReference.serverDataType['IdAplicacao'] = 'I';
       ownerReference.serverDataType['IdLinxEmpresa'] = 'I';
       ownerReference.serverDataType['IdLinxGpecon'] = 'I';
       ownerReference.serverDataType['IdTcsAmbiente'] = 'I';
       ownerReference.serverDataType['IdTcsLogErros'] = 'I';
       ownerReference.serverDataType['IdUsuario'] = 'L';
       ownerReference.serverDataType['MensagemExcecao'] = 'S';
       ownerReference.serverDataType['MensagemExcecaoInterna'] = 'S';
       ownerReference.serverDataType['MetodoHttp'] = 'S';
       ownerReference.serverDataType['NomeAcao'] = 'S';
       ownerReference.serverDataType['NomeControlador'] = 'S';
       ownerReference.serverDataType['NomeEmpresa'] = 'S';
       ownerReference.serverDataType['NomeServidor'] = 'S';
       ownerReference.serverDataType['PilhaExcecao'] = 'S';
       ownerReference.serverDataType['UsuarioWindows'] = 'S';
       ownerReference.serverDataType['NomeAutenticacao'] = 'S';
       ownerReference.serverDataType['NomeUsuario'] = 'S';
       ownerReference.typeName = 'TcsLogErrosDash';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [  ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       //#region Lookup Extended Methods
       ownerReference.getLookupPropertyName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null && !isNullOrEmpty(property.lookupPropertyName) ? property.lookupPropertyName : propertyName);
       }
       ownerReference.getLookupVisibleColumns = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.lookupVisibleColumns : '');
       }
       ownerReference.getLookUpClientFilterExpressions = function (lookupName, lookupInfo) {
           return '';
       };
    
       ownerReference.getLookupDisplay = function (lookupName) {
           var displayName = '';
           if (lookupName === 'LookUpGpecon') {
               displayName = ' de Tcs_Empresa_Autenticacao';
           }
           if (lookupName === 'LookUpTcsAmbiente') {
               displayName = ' de Tcs_Ambiente';
           }
           if (lookupName === 'LookUpTcsAplicacao') {
               displayName = ' de Tcs_Aplicacao';
           }
           if (lookupName === 'LookUpTcsEmpresaAutenticacao') {
               displayName = ' de Tcs_Empresa_Autenticacao';
           }
           if (lookupName === 'LookUpTcsUsuarioAutenticacao') {
               displayName = ' de Tcs_Usuario_Autenticacao';
           }
           return 'Seleção' + displayName;
       };
    
       ownerReference.getSpecializedLookup = function (lookupName, lookupInfo, fieldToSearch, valueToSearch, ownerReference, allowMultiSelectionInSearch) {
           var specializedLookup = '';
           return specializedLookup;
       };
    
       ownerReference.getSubQueryFilterFromLookUpGpecon = function (propertyName) {
           var filter = '';
           return filter;
       }
       ownerReference.getSubQueryFilterFromLookUpTcsAmbiente = function (propertyName) {
           var filter = '';
           return filter;
       }
       ownerReference.getSubQueryFilterFromLookUpTcsAplicacao = function (propertyName) {
           var filter = '';
           return filter;
       }
       ownerReference.getSubQueryFilterFromLookUpTcsEmpresaAutenticacao = function (propertyName) {
           var filter = '';
           return filter;
       }
       ownerReference.getSubQueryFilterFromLookUpTcsUsuarioAutenticacao = function (propertyName) {
           var filter = '';
           return filter;
       }
       ownerReference.canGetClientFilter = function (lookupName) {
           return true;
       }
       ownerReference.validatedlookupsArray = [];
       ownerReference.internalLookupSearch = function (lookupName, fieldToSearch, operation, querySucceeded, lookupInfo, valueToSearch, beforeGettingLookup, referencefield) {
           if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); querySucceeded(null); return lookupInfo; }
           if (isNullOrEmpty(lookupInfo.lastJEntityExpression)) {
               if (isNullOrEmpty(referencefield)) referencefield = fieldToSearch;
               if ((typeof valueToSearch) === 'undefined')
                   valueToSearch = getAbsoluteValue(ownerReference[referencefield]);
               var extraFilters = '';
               if (ownerReference.canGetClientFilter(lookupName)) {
                   extraFilters = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
                   dataContext.lastClientFilterExpressions[lookupName] = extraFilters;
                   if (extraFilters === 'Error') { querySucceeded(null); return lookupInfo; }
                   if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                       var customFilter = ownerReference['BeforeGet' + lookupName + 'Query'](fieldToSearch, lookupInfo);
                       if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                       if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                   }
                   if (typeof ownerReference['getSubQueryFilterFrom' + lookupName] == 'function') {
                       var customFilter = ownerReference['getSubQueryFilterFrom' + lookupName](referencefield);
                       if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                       if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                   }
               }
               var completeExpression = getLookUpJEntityExpression(lookupName, ownerReference, fieldToSearch, valueToSearch, extraFilters, referencefield, app, lookupInfo.vm.useLikeCommandAsDefault);
               if (completeExpression === 'Error') { querySucceeded(null); return lookupInfo; }
               lookupInfo.lastJEntityExpression = completeExpression;
           }
           switch (operation) {
               case 'F':
                   lookupInfo.pageSkip = 0;
                   break;
               case 'B':
                   lookupInfo.pageSkip = lookupInfo.pageSkip - 1;
                   break;
               case 'N':
                   lookupInfo.pageSkip = lookupInfo.pageSkip + 1;
                   break;
               case 'L':
                   lookupInfo.pageSkip = lookupInfo.totalPages();
                   break;
               default:
           }
    
           var e = { cancel: false, lookupName: lookupName, jEntitySearch: lookupInfo.lastJEntityExpression, entity: ownerReference, viewModel: lookupInfo.vm };
           if (beforeGettingLookup) beforeGettingLookup(e);
           if (e.cancel) { querySucceeded(null); return lookupInfo; }
           if(lookupInfo.lastJEntityExpression !== e.jEntitySearch)
               lookupInfo.lastJEntityExpression = e.jEntitySearch;
           if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(true);
           var returnQueryResult = function (data) { lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount); querySucceeded(data); };
           eval('dataContext.get' + lookupName + 'ByEntitySearch(lookupInfo.lastJEntityExpression, (isNullOrEmpty(lookupInfo.fieldToSort) ? fieldToSearch : lookupInfo.fieldToSort), lookupInfo.pageSize*lookupInfo.pageSkip, lookupInfo.pageSize, lookupInfo.sortDirection, fieldToSearch).then(returnQueryResult).fail(queryFailed)').fin(function(){ if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(false); });
           return lookupInfo;
       };
    
       ownerReference.hasValidClientFilter = function (lookupName, lookupInfo) {
           var checkClientFilter = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
           if (checkClientFilter === 'Error') { return false; }
           if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
               checkClientFilter = ownerReference['BeforeGet' + lookupName + 'Query']('', lookupInfo);
               if (checkClientFilter === 'Error') { return false; }
           }
           return true;
       }
    
       ownerReference.executeLookUp = function (lookupName, fieldToSearch, beforeGettingLookup, vm, valueToSearch, finished, comboCallBack, allowMultiSelectionInSearch) {
           if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); if (finished) finished(false, null); return; }
           var lookupFieldName = ownerReference.getLookupPropertyName(fieldToSearch);
           vm.dataBind('', true);
           var lookupInfo = new lookupInformation();
           lookupInfo.visibleColumns = ownerReference.getLookupVisibleColumns(fieldToSearch);
           lookupInfo.vm = vm;
           lookupInfo.isMultiSelection = lookupName.in([]);
           var specializedLookup = ownerReference.getSpecializedLookup(lookupName, lookupInfo, lookupFieldName, valueToSearch, ownerReference, allowMultiSelectionInSearch);
           if (isNullOrEmpty(specializedLookup)) {
                   ownerReference.internalLookupSearch(lookupName, lookupFieldName, 'F',
                       function querySucceeded(data) {
                           if (typeof ownerReference['OnLoading' + lookupName + 'Query'] == 'function') {
                               ownerReference['OnLoading' + lookupName + 'Query'](data);
                           }
                           if ((typeof comboCallBack) === 'function') {
                               return comboCallBack(data ? data.results : null);
                           }
                           else if (data == null || data.results == null || data.results.length == 0) {
                               if (finished) finished(false, null);
                               ownerReference.clearLookUp(lookupName);
                               if (data != null) app.showMessage('A informação de Lookup [' + ownerReference.getDisplayName(fieldToSearch) + '] não foi encontrada!', 'Informação', ['Ok']);
                               return;
                           }
                           lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount);
                           showLookUp(dataContext, ownerReference, ownerReference.getLookupDisplay(lookupName), lookupName, lookupFieldName, ownerReference.internalLookupSearch, lookupInfo, 
                               function (confirm, values) {
                                   var results = '';
                                   if (values != null && values.length > 1) {
                                       $.each(values, function (index, item) { results += (index == 0 ? '' : ',') + item[lookupFieldName].toString().trim() });
                                       results = '[' + results + ']';
                                       ownerReference[fieldToSearch](results);
                                       if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                           vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                       else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                       document.dispatchEvent(dataUpdateEvent);
                                   }
                                   if (finished) finished(confirm, results);
                               }, data.results, allowMultiSelectionInSearch);
                   }, lookupInfo, valueToSearch, beforeGettingLookup, fieldToSearch);
           }
           else {
                   var currentModal = (modal.inUse ? modal2 : modal);
                   if (currentModal.inUse === false) {
                       //Check Client Validations
                       if (!ownerReference.hasValidClientFilter(lookupName, lookupInfo)) { if (finished) finished(false); return; }
                       //Show External Lookup
                       currentModal.showModal(specializedLookup.moduleName, specializedLookup.uiSettings, ownerReference.getLookupDisplay(lookupName), ['Ok', 'Cancelar'], 'large').then(function (r, data) {
                       if (r == 'Ok') {
                           if (!currentModal.internalUIs || currentModal.internalUIs.length != 1) { if (finished) finished(false); return; }
                           var lookupVM = currentModal[currentModal.internalUIs[0]]; 
                           if (!lookupVM) return; 
                           if (typeof lookupVM == 'function') lookupVM = lookupVM(); 
                           currentModal[currentModal.internalUIs[0]] = null; 
                           var selectedItems = lookupVM.getSpecializedLookupItems(); 
                           if (vm.status() == 'C' && selectedItems != null && selectedItems.length > 1) { 
                               var results = '';
                               $.each(selectedItems, function (index, item) { results += (index == 0 ? '' : ',') + (typeof item[lookupFieldName] == 'function' ? item[lookupFieldName]() : item[lookupFieldName]).toString().trim() });
                               results = '[' + results + ']'
                               ownerReference[fieldToSearch](results);
                               if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                   vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                               else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                               document.dispatchEvent(dataUpdateEvent);
                               if (finished) finished(true, results);
                           }
                           else if (selectedItems.length > 0) { dataContext['finalizeAll' + lookupName](ownerReference, selectedItems, '', lookupInfo); }
                       }
                       if (finished) finished(r === 'Ok');
                   });
               }
               if (finished) finished(false);
           }
       };
       ownerReference.clearLookUp = function (lookupName) {
           return eval('dataContext.clear' + lookupName + '(ownerReference)');
       };
       //#endregion Lookup Extended Methods
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (!isNullOrEmpty(ownerReference.LogFileList()) && ownerReference.LogFileList().length > 0) {
              var details = [].concat(ownerReference.LogFileList());
              for (var idx = 0; idx < details.length; idx++) {
                details[idx].delete();
              }
           }
           if (!isNullOrEmpty(ownerReference.TcsLogErrosList()) && ownerReference.TcsLogErrosList().length > 0) {
              var details = [].concat(ownerReference.TcsLogErrosList());
              for (var idx = 0; idx < details.length; idx++) {
                details[idx].delete();
              }
           }
           if (ownerReference.ChangeState == 'I') {
               if (parent && (typeof parent.TcsLogErrosDashList === 'function')) { 
                   parent.TcsLogErrosDashList.remove(ownerReference); 
               }
               else {
                   vm.dataView.remove(ownerReference);
               }
           }
           else {
               if (ownerReference.ChangeState == 'N') { ownerReference.createOriginal(); }
               ownerReference.ChangeState = 'D'; // mark for deletion
           }
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.TratamentoErros';
       ownerReference.myProperties = [ ,'DataErro','DescricaoAmbiente','DescricaoAplicacao','EnderecoWeb','Gpecon','IdAplicacao','IdLinxEmpresa','IdLinxGpecon','IdTcsAmbiente','IdTcsLogErros','IdUsuario','MensagemExcecao','MensagemExcecaoInterna','MetodoHttp','NomeAcao','NomeControlador','NomeEmpresa','NomeServidor','PilhaExcecao','UsuarioWindows','NomeAutenticacao','NomeUsuario' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
       if (!isNullOrEmpty(ownerReference.currentLogFile())) { result = result.concat(ownerReference.currentLogFile().getCurrentElements()); }
       if (!isNullOrEmpty(ownerReference.currentTcsLogErros())) { result = result.concat(ownerReference.currentTcsLogErros().getCurrentElements()); }
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.GetJsWhereDetailRelationForLogFile = function(customParentRelation) {
       return vm.lastJEntitySearch() + 'LogFile{' + '}';    
       }
       ownerReference.GetJsWhereDetailRelationForTcsLogErros = function(customParentRelation) {
       return vm.lastJEntitySearch() + 'TcsLogErros{' + '}';    
       }
       ownerReference.LogFileIsLoaded = false;
       ownerReference.TcsLogErrosIsLoaded = false;
       ownerReference.detailsLoaded = function() {
           return ownerReference.LogFileIsLoaded && ownerReference.TcsLogErrosIsLoaded;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return ownerReference.LogFileIsLoaded || ownerReference.TcsLogErrosIsLoaded;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
           ownerReference.LogFileIsLoaded = value;
           if (value === false && ownerReference.isPOCO)
               ownerReference.LogFileList([]);
           ownerReference.TcsLogErrosIsLoaded = value;
           if (value === false && ownerReference.isPOCO)
               ownerReference.TcsLogErrosList([]);
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (ownerReference.isAdded()) {
            ownerReference.LogFileIsLoaded = true;
            ownerReference.TcsLogErrosIsLoaded = true;
          }
          var _LogFileRemoteComplete = false;
          var _TcsLogErrosRemoteComplete = false;
          var detachList_LogFile = [];
          if (force) {
               if (isNullOrEmpty(detailName) || detailName == 'LogFile') ownerReference.LogFileIsLoaded = false;
               if ((isNullOrEmpty(detailName) || detailName == 'LogFile') && ownerReference.LogFileList && ownerReference.LogFileList().length > 0) {
                   if (ownerReference.isPOCO) {
                       ownerReference.LogFileList([]);
                   } else {
                       var detailList = ownerReference.LogFileList();
                       for (var idx = detailList.length - 1; idx >= 0; idx--) {
                           detachList_LogFile.push(detailList[idx]);
                       }
                   }
               }
          }
    
          if (!ownerReference.LogFileIsLoaded) {
            //Load LogFileList
            if (isNullOrEmpty(detailName) || detailName === 'LogFile') {
              ownerReference.LogFileIsLoaded = true;
              _LogFileRemoteComplete = (ownerReference.LogFileList && ownerReference.LogFileList().length > 0);
              if ((force || !ownerReference.LogFileList || ownerReference.LogFileList().length === 0)) {
                var navQuery = EntityQuery.from('GetLogFileByEntitySearchNoAssociations').noTracking(ownerReference.isPOCO === true)
                .orderBy('FileName asc')
                    .withParameters({ jEntitySearch: ownerReference.GetJsWhereDetailRelationForLogFile(customParentRelation) })    ;
                if (!vm.dataToolbar._noBusyLoading) vm.showProcessing('Pesquisando detalhes...');
                manager.executeQuery(navQuery).then(function (data) { if (ownerReference.isPOCO) { for (var idx = 0; idx < data.results.length; idx++) { initializePOCO(data.results[idx], 'LogFile'); data.results[idx].TcsLogErrosDash = ko.observable(ownerReference); } ownerReference.LogFileList(data.results); }  for (var idx = 0; idx < data.results.length; idx++) { setAbsoluteValue(data.results[idx], 'EntityParentUniqueKey', getAbsoluteValue(ownerReference.EntityUniqueKey)); if (!ownerReference.isPOCO) { data.results[idx].TcsLogErrosDash(ownerReference); data.results[idx].entityAspect.entityState === breeze.EntityState.Unchanged; } }
                   if (!ownerReference.isPOCO && detachList_LogFile.length > 0)
                   {
                       for (var idx = 0; idx < detachList_LogFile.length; idx++)
                       {
                           if (!data.results.contains(detachList_LogFile[idx]))
                               detachEntity(detachList_LogFile[idx]);
                           else {
                               if (force && detachList_LogFile[idx].atLeastOneDetailLoaded())
                                   detachList_LogFile[idx].fillDetails(force, '', false, noWait);
                           }
                       }
                   }
                   ownerReference.setCurrentDetails('LogFile'); notifyPresentation('LogFileList');
                   _LogFileRemoteComplete = true;
                   if (callback && (!isNullOrEmpty(detailName) || (_LogFileRemoteComplete && _TcsLogErrosRemoteComplete))) { callback(); }
                }).fail(queryFailed).fin(function() { if (!vm.dataToolbar._noBusyLoading) vm.closeProcessing(); });
              } else { ownerReference.setCurrentDetails('LogFile'); notifyPresentation('LogFileList'); }
            } else { _LogFileRemoteComplete = true; if (!ownerReference.LogFileIsLoaded && ownerReference.LogFileList && ownerReference.LogFileList().length > 0) { ownerReference.LogFileIsLoaded = true; ownerReference.setCurrentDetails('LogFile'); } }
          } else { 
            if (isNullOrEmpty(detailName) || detailName == 'LogFile') {
               notifyPresentation('LogFileList');
               ownerReference.setCurrentDetails('LogFile');
            }
            _LogFileRemoteComplete = true;
          }
          var detachList_TcsLogErros = [];
          if (force) {
               if (isNullOrEmpty(detailName) || detailName == 'TcsLogErros') ownerReference.TcsLogErrosIsLoaded = false;
               if ((isNullOrEmpty(detailName) || detailName == 'TcsLogErros') && ownerReference.TcsLogErrosList && ownerReference.TcsLogErrosList().length > 0) {
                   if (ownerReference.isPOCO) {
                       ownerReference.TcsLogErrosList([]);
                   } else {
                       var detailList = ownerReference.TcsLogErrosList();
                       for (var idx = detailList.length - 1; idx >= 0; idx--) {
                           detachList_TcsLogErros.push(detailList[idx]);
                       }
                   }
               }
          }
    
          if (!ownerReference.TcsLogErrosIsLoaded) {
            //Load TcsLogErrosList
            if (isNullOrEmpty(detailName) || detailName === 'TcsLogErros') {
              ownerReference.TcsLogErrosIsLoaded = true;
              _TcsLogErrosRemoteComplete = (ownerReference.TcsLogErrosList && ownerReference.TcsLogErrosList().length > 0);
              if ((force || !ownerReference.TcsLogErrosList || ownerReference.TcsLogErrosList().length === 0)) {
                var navQuery = EntityQuery.from('GetTcsLogErrosByEntitySearchNoAssociations').noTracking(ownerReference.isPOCO === true)
                .orderBy('DataErro desc')
                    .withParameters({ jEntitySearch: ownerReference.GetJsWhereDetailRelationForTcsLogErros(customParentRelation) })    ;
                if (!vm.dataToolbar._noBusyLoading) vm.showProcessing('Pesquisando detalhes...');
                manager.executeQuery(navQuery).then(function (data) { if (ownerReference.isPOCO) { for (var idx = 0; idx < data.results.length; idx++) { initializePOCO(data.results[idx], 'TcsLogErros'); data.results[idx].TcsLogErrosDash = ko.observable(ownerReference); } ownerReference.TcsLogErrosList(data.results); }  for (var idx = 0; idx < data.results.length; idx++) { setAbsoluteValue(data.results[idx], 'EntityParentUniqueKey', getAbsoluteValue(ownerReference.EntityUniqueKey)); if (!ownerReference.isPOCO) { data.results[idx].TcsLogErrosDash(ownerReference); data.results[idx].entityAspect.entityState === breeze.EntityState.Unchanged; } }
                   if (!ownerReference.isPOCO && detachList_TcsLogErros.length > 0)
                   {
                       for (var idx = 0; idx < detachList_TcsLogErros.length; idx++)
                       {
                           if (!data.results.contains(detachList_TcsLogErros[idx]))
                               detachEntity(detachList_TcsLogErros[idx]);
                           else {
                               if (force && detachList_TcsLogErros[idx].atLeastOneDetailLoaded())
                                   detachList_TcsLogErros[idx].fillDetails(force, '', false, noWait);
                           }
                       }
                   }
                   ownerReference.setCurrentDetails('TcsLogErros'); notifyPresentation('TcsLogErrosList');
                   _TcsLogErrosRemoteComplete = true;
                   if (callback && (!isNullOrEmpty(detailName) || (_LogFileRemoteComplete && _TcsLogErrosRemoteComplete))) { callback(); }
                }).fail(queryFailed).fin(function() { if (!vm.dataToolbar._noBusyLoading) vm.closeProcessing(); });
              } else { ownerReference.setCurrentDetails('TcsLogErros'); notifyPresentation('TcsLogErrosList'); }
            } else { _TcsLogErrosRemoteComplete = true; if (!ownerReference.TcsLogErrosIsLoaded && ownerReference.TcsLogErrosList && ownerReference.TcsLogErrosList().length > 0) { ownerReference.TcsLogErrosIsLoaded = true; ownerReference.setCurrentDetails('TcsLogErros'); } }
          } else { 
            if (isNullOrEmpty(detailName) || detailName == 'TcsLogErros') {
               notifyPresentation('TcsLogErrosList');
               ownerReference.setCurrentDetails('TcsLogErros');
            }
            _TcsLogErrosRemoteComplete = true;
          }
          if (callback && ((!isNullOrEmpty(detailName) && (eval('_' + detailName + 'RemoteComplete && ownerReference.' + detailName + 'IsLoaded') == true)) || (isNullOrEmpty(detailName) && (_LogFileRemoteComplete && _TcsLogErrosRemoteComplete)))) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
          if ((isNullOrEmpty(detailName) || detailName === 'LogFile')) {
               if (ownerReference.LogFileList().length > 0) { ownerReference.currentLogFile(ownerReference.LogFileList()[0]); if (clearing == null || clearing === false) ownerReference.currentLogFile().fillDetails(); }
               else { ownerReference.currentLogFile(null); ownerReference.notifyEmptyDetails('LogFile'); }
          }
          if ((isNullOrEmpty(detailName) || detailName === 'TcsLogErros')) {
               if (ownerReference.TcsLogErrosList().length > 0) { ownerReference.currentTcsLogErros(ownerReference.TcsLogErrosList()[0]); if (clearing == null || clearing === false) ownerReference.currentTcsLogErros().fillDetails(); }
               else { ownerReference.currentTcsLogErros(null); ownerReference.notifyEmptyDetails('TcsLogErros'); }
          }
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
          if (detailName === 'LogFile') {
               notifyPresentation('LogFileList');
               vm.queryInnerUIs(null, 'LogFile');
          }
          if (detailName === 'TcsLogErros') {
               notifyPresentation('TcsLogErrosList');
               vm.queryInnerUIs(null, 'TcsLogErros');
          }
       };
    //#region Adjust details already loaded for a POCO reference
       if (isPOCO === true) {
           if ((typeof ownerReference.LogFileList === 'function') && ownerReference.LogFileList().length > 0) {
                for(var idx = 0; idx < ownerReference.LogFileList().length; idx++) { LogFileInitializer(ownerReference.LogFileList()[idx], isPOCO); }
           }
           if ((typeof ownerReference.TcsLogErrosList === 'function') && ownerReference.TcsLogErrosList().length > 0) {
                for(var idx = 0; idx < ownerReference.TcsLogErrosList().length; idx++) { TcsLogErrosInitializer(ownerReference.TcsLogErrosList()[idx], isPOCO); }
           }
       }
    //#endregion Adjust details already loaded for a POCO reference
    };
    metadataStore.registerEntityTypeCtor("TcsLogErrosDash", null, TcsLogErrosDashInitializer);
    
    // Configure LogFile data type
    metadataStore.addEntityType({
    shortName: "LogFile",
    namespace: "Linx.Framework.BV.TratamentoErros",
    autoGeneratedKeyType: AutoGeneratedKeyType.None,
    dataProperties: {
    EntityParentUniqueKey: { dataType: DataType.Guid, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,FileName: { dataType: DataType.String, maxLength: 100, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 100})]  }
    ,Download: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,ChangeState: { dataType: DataType.String, isNullable: true, isPartOfKey: false, validators: [] }
                    },
    navigationProperties: {
    // Returns a single parent and associates with Details
    TcsLogErrosDash: { entityTypeName: "TcsLogErrosDash:#Linx.Framework.BV.TratamentoErros", isScalar: true, foreignKeyNames: ["EntityParentUniqueKey"], associationName: "FK_TcsLogErrosDash_LogFile" }
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['LogFile'] = {};
    var LogFileInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('LogFile') : ko.observable(getNextSequence('LogFile')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'LogFile{' + 'FileName#==#S' + getAbsoluteValue(ownerReference.FileName).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getLogFileByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return ownerReference.ChangeState === 'I'; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return ownerReference.ChangeState === 'D'; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return ownerReference.ChangeState === 'U'; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return ownerReference.ChangeState === 'N'; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() { ownerReference.ChangeState = 'U'; } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() { ownerReference.ChangeState = 'N'; } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['FileName'] = 'S';
       ownerReference.serverDataType['Download'] = 'S';
       ownerReference.typeName = 'LogFile';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'FileName' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           var parent = getAbsoluteValue(ownerReference.TcsLogErrosDash);
           if (ownerReference.ChangeState == 'I') {
               if (parent && (typeof parent.LogFileList === 'function')) { 
                   parent.LogFileList.remove(ownerReference); 
               }
               else {
                   vm.dataView.remove(ownerReference);
               }
               delete ownerReference.TcsLogErrosDash;
           }
           else {
               if (ownerReference.ChangeState == 'N') { ownerReference.createOriginal(); }
               ownerReference.ChangeState = 'D'; // mark for deletion
           }
           if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.LogFileList === 'function') && parent.LogFileList().length == 0) parent.setCurrentDetails('LogFile');
       };
       ownerReference.setParentAsModified = function() {
       var parent = getAbsoluteValue(ownerReference.TcsLogErrosDash);
       if (parent) {
           if (parent.isUnchanged()) {
               parent.setModified(); 
           }
           parent.setParentAsModified();
       }
       };
       ownerReference.getParent = function() {
           return getAbsoluteValue(ownerReference.TcsLogErrosDash);
       };
       ownerReference.getSelfList = function() {
           var parent = ownerReference.getParent();
           if (!isNullOrEmpty(parent)) {
               return getAbsoluteValue(parent.LogFileList);
           } else { return null; }
       };
       ownerReference.namespace = 'Linx.Framework.BV.TratamentoErros';
       ownerReference.myProperties = [ ,'FileName','Download' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("LogFile", null, LogFileInitializer);
    
    // Configure TcsLogErros data type
    metadataStore.addEntityType({
    shortName: "TcsLogErros",
    namespace: "Linx.Framework.BV.TratamentoErros",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    EntityParentUniqueKey: { dataType: DataType.Guid, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,DataErro: { dataType: DataType.DateTime, isNullable: false, isPartOfKey: false, defaultValue: '', validators: [ Validator.hasValueValidator]  }
    ,DescricaoAmbiente: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,DescricaoAplicacao: { dataType: DataType.String, maxLength: 60, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 60})]  }
    ,Empresa: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,EnderecoWeb: { dataType: DataType.String, maxLength: 8000, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 8000})]  }
    ,Gpecon: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,IdAplicacao: { dataType: DataType.Int32, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IdLinxEmpresa: { dataType: DataType.Int32, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IdLinxGpecon: { dataType: DataType.Int32, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IdTcsAmbiente: { dataType: DataType.Int32, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IdTcsLogErros: { dataType: DataType.Int32, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,IdUsuario: { dataType: DataType.Int64, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,MensagemExcecao: { dataType: DataType.String, maxLength: 1000, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 1000})]  }
    ,MensagemExcecaoInterna: { dataType: DataType.String, maxLength: 1000, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 1000})]  }
    ,MetodoHttp: { dataType: DataType.String, maxLength: 6, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 6})]  }
    ,NomeAcao: { dataType: DataType.String, maxLength: 256, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 256})]  }
    ,NomeControlador: { dataType: DataType.String, maxLength: 200, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 200})]  }
    ,NomeServidor: { dataType: DataType.String, maxLength: 256, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 256})]  }
    ,PilhaExcecao: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,UsuarioWindows: { dataType: DataType.String, maxLength: 256, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 256})]  }
    ,NomeAutenticacao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,NomeUsuario: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,ChangeState: { dataType: DataType.String, isNullable: true, isPartOfKey: false, validators: [] }
                    },
    navigationProperties: {
    // Returns a single parent and associates with Details
    TcsLogErrosDash: { entityTypeName: "TcsLogErrosDash:#Linx.Framework.BV.TratamentoErros", isScalar: true, foreignKeyNames: ["EntityParentUniqueKey"], associationName: "FK_TcsLogErrosDash_TcsLogErros" }
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsLogErros'] = {};
    var TcsLogErrosInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsLogErros') : ko.observable(getNextSequence('TcsLogErros')));
        //Start Property Definitions
        var _entityParentUniqueKey = ownerReference.EntityParentUniqueKey;
        Object.defineProperty(ownerReference, 'EntityParentUniqueKey', {
          get: function() { return _entityParentUniqueKey; },
          set: function(newValue) { var oldValue = _entityParentUniqueKey; _entityParentUniqueKey = newValue; if (!entityPropChanged(ownerReference, 'EntityParentUniqueKey', oldValue, newValue)) { _entityParentUniqueKey = oldValue; } }
        });
        var _dataErro = ownerReference.DataErro;
        Object.defineProperty(ownerReference, 'DataErro', {
          get: function() { return _dataErro; },
          set: function(newValue) { var oldValue = _dataErro; _dataErro = newValue; if (!entityPropChanged(ownerReference, 'DataErro', oldValue, newValue)) { _dataErro = oldValue; } }
        });
        var _descricaoAmbiente = ownerReference.DescricaoAmbiente;
        Object.defineProperty(ownerReference, 'DescricaoAmbiente', {
          get: function() { return _descricaoAmbiente; },
          set: function(newValue) { var oldValue = _descricaoAmbiente; _descricaoAmbiente = newValue; if (!entityPropChanged(ownerReference, 'DescricaoAmbiente', oldValue, newValue)) { _descricaoAmbiente = oldValue; } }
        });
        var _descricaoAplicacao = ownerReference.DescricaoAplicacao;
        Object.defineProperty(ownerReference, 'DescricaoAplicacao', {
          get: function() { return _descricaoAplicacao; },
          set: function(newValue) { var oldValue = _descricaoAplicacao; _descricaoAplicacao = newValue; if (!entityPropChanged(ownerReference, 'DescricaoAplicacao', oldValue, newValue)) { _descricaoAplicacao = oldValue; } }
        });
        var _empresa = ownerReference.Empresa;
        Object.defineProperty(ownerReference, 'Empresa', {
          get: function() { return _empresa; },
          set: function(newValue) { var oldValue = _empresa; _empresa = newValue; if (!entityPropChanged(ownerReference, 'Empresa', oldValue, newValue)) { _empresa = oldValue; } }
        });
        var _enderecoWeb = ownerReference.EnderecoWeb;
        Object.defineProperty(ownerReference, 'EnderecoWeb', {
          get: function() { return _enderecoWeb; },
          set: function(newValue) { var oldValue = _enderecoWeb; _enderecoWeb = newValue; if (!entityPropChanged(ownerReference, 'EnderecoWeb', oldValue, newValue)) { _enderecoWeb = oldValue; } }
        });
        var _gpecon = ownerReference.Gpecon;
        Object.defineProperty(ownerReference, 'Gpecon', {
          get: function() { return _gpecon; },
          set: function(newValue) { var oldValue = _gpecon; _gpecon = newValue; if (!entityPropChanged(ownerReference, 'Gpecon', oldValue, newValue)) { _gpecon = oldValue; } }
        });
        var _idAplicacao = ownerReference.IdAplicacao;
        Object.defineProperty(ownerReference, 'IdAplicacao', {
          get: function() { return _idAplicacao; },
          set: function(newValue) { var oldValue = _idAplicacao; _idAplicacao = newValue; if (!entityPropChanged(ownerReference, 'IdAplicacao', oldValue, newValue)) { _idAplicacao = oldValue; } }
        });
        var _idLinxEmpresa = ownerReference.IdLinxEmpresa;
        Object.defineProperty(ownerReference, 'IdLinxEmpresa', {
          get: function() { return _idLinxEmpresa; },
          set: function(newValue) { var oldValue = _idLinxEmpresa; _idLinxEmpresa = newValue; if (!entityPropChanged(ownerReference, 'IdLinxEmpresa', oldValue, newValue)) { _idLinxEmpresa = oldValue; } }
        });
        var _idLinxGpecon = ownerReference.IdLinxGpecon;
        Object.defineProperty(ownerReference, 'IdLinxGpecon', {
          get: function() { return _idLinxGpecon; },
          set: function(newValue) { var oldValue = _idLinxGpecon; _idLinxGpecon = newValue; if (!entityPropChanged(ownerReference, 'IdLinxGpecon', oldValue, newValue)) { _idLinxGpecon = oldValue; } }
        });
        var _idTcsAmbiente = ownerReference.IdTcsAmbiente;
        Object.defineProperty(ownerReference, 'IdTcsAmbiente', {
          get: function() { return _idTcsAmbiente; },
          set: function(newValue) { var oldValue = _idTcsAmbiente; _idTcsAmbiente = newValue; if (!entityPropChanged(ownerReference, 'IdTcsAmbiente', oldValue, newValue)) { _idTcsAmbiente = oldValue; } }
        });
        var _idTcsLogErros = ownerReference.IdTcsLogErros;
        Object.defineProperty(ownerReference, 'IdTcsLogErros', {
          get: function() { return _idTcsLogErros; },
          set: function(newValue) { var oldValue = _idTcsLogErros; _idTcsLogErros = newValue; if (!entityPropChanged(ownerReference, 'IdTcsLogErros', oldValue, newValue)) { _idTcsLogErros = oldValue; } }
        });
        var _idUsuario = ownerReference.IdUsuario;
        Object.defineProperty(ownerReference, 'IdUsuario', {
          get: function() { return _idUsuario; },
          set: function(newValue) { var oldValue = _idUsuario; _idUsuario = newValue; if (!entityPropChanged(ownerReference, 'IdUsuario', oldValue, newValue)) { _idUsuario = oldValue; } }
        });
        var _mensagemExcecao = ownerReference.MensagemExcecao;
        Object.defineProperty(ownerReference, 'MensagemExcecao', {
          get: function() { return _mensagemExcecao; },
          set: function(newValue) { var oldValue = _mensagemExcecao; _mensagemExcecao = newValue; if (!entityPropChanged(ownerReference, 'MensagemExcecao', oldValue, newValue)) { _mensagemExcecao = oldValue; } }
        });
        var _mensagemExcecaoInterna = ownerReference.MensagemExcecaoInterna;
        Object.defineProperty(ownerReference, 'MensagemExcecaoInterna', {
          get: function() { return _mensagemExcecaoInterna; },
          set: function(newValue) { var oldValue = _mensagemExcecaoInterna; _mensagemExcecaoInterna = newValue; if (!entityPropChanged(ownerReference, 'MensagemExcecaoInterna', oldValue, newValue)) { _mensagemExcecaoInterna = oldValue; } }
        });
        var _metodoHttp = ownerReference.MetodoHttp;
        Object.defineProperty(ownerReference, 'MetodoHttp', {
          get: function() { return _metodoHttp; },
          set: function(newValue) { var oldValue = _metodoHttp; _metodoHttp = newValue; if (!entityPropChanged(ownerReference, 'MetodoHttp', oldValue, newValue)) { _metodoHttp = oldValue; } }
        });
        var _nomeAcao = ownerReference.NomeAcao;
        Object.defineProperty(ownerReference, 'NomeAcao', {
          get: function() { return _nomeAcao; },
          set: function(newValue) { var oldValue = _nomeAcao; _nomeAcao = newValue; if (!entityPropChanged(ownerReference, 'NomeAcao', oldValue, newValue)) { _nomeAcao = oldValue; } }
        });
        var _nomeControlador = ownerReference.NomeControlador;
        Object.defineProperty(ownerReference, 'NomeControlador', {
          get: function() { return _nomeControlador; },
          set: function(newValue) { var oldValue = _nomeControlador; _nomeControlador = newValue; if (!entityPropChanged(ownerReference, 'NomeControlador', oldValue, newValue)) { _nomeControlador = oldValue; } }
        });
        var _nomeServidor = ownerReference.NomeServidor;
        Object.defineProperty(ownerReference, 'NomeServidor', {
          get: function() { return _nomeServidor; },
          set: function(newValue) { var oldValue = _nomeServidor; _nomeServidor = newValue; if (!entityPropChanged(ownerReference, 'NomeServidor', oldValue, newValue)) { _nomeServidor = oldValue; } }
        });
        var _pilhaExcecao = ownerReference.PilhaExcecao;
        Object.defineProperty(ownerReference, 'PilhaExcecao', {
          get: function() { return _pilhaExcecao; },
          set: function(newValue) { var oldValue = _pilhaExcecao; _pilhaExcecao = newValue; if (!entityPropChanged(ownerReference, 'PilhaExcecao', oldValue, newValue)) { _pilhaExcecao = oldValue; } }
        });
        var _usuarioWindows = ownerReference.UsuarioWindows;
        Object.defineProperty(ownerReference, 'UsuarioWindows', {
          get: function() { return _usuarioWindows; },
          set: function(newValue) { var oldValue = _usuarioWindows; _usuarioWindows = newValue; if (!entityPropChanged(ownerReference, 'UsuarioWindows', oldValue, newValue)) { _usuarioWindows = oldValue; } }
        });
        var _nomeAutenticacao = ownerReference.NomeAutenticacao;
        Object.defineProperty(ownerReference, 'NomeAutenticacao', {
          get: function() { return _nomeAutenticacao; },
          set: function(newValue) { var oldValue = _nomeAutenticacao; _nomeAutenticacao = newValue; if (!entityPropChanged(ownerReference, 'NomeAutenticacao', oldValue, newValue)) { _nomeAutenticacao = oldValue; } }
        });
        var _nomeUsuario = ownerReference.NomeUsuario;
        Object.defineProperty(ownerReference, 'NomeUsuario', {
          get: function() { return _nomeUsuario; },
          set: function(newValue) { var oldValue = _nomeUsuario; _nomeUsuario = newValue; if (!entityPropChanged(ownerReference, 'NomeUsuario', oldValue, newValue)) { _nomeUsuario = oldValue; } }
        });
        //End Property Definitions
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsLogErros{' + 'IdTcsLogErros#==#I' + getAbsoluteValue(ownerReference.IdTcsLogErros).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsLogErrosByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return ownerReference.ChangeState === 'I'; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return ownerReference.ChangeState === 'D'; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return ownerReference.ChangeState === 'U'; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return ownerReference.ChangeState === 'N'; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() { ownerReference.ChangeState = 'U'; } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() { ownerReference.ChangeState = 'N'; } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['DataErro'] = 'T';
       ownerReference.serverDataType['DescricaoAmbiente'] = 'S';
       ownerReference.serverDataType['DescricaoAplicacao'] = 'S';
       ownerReference.serverDataType['Empresa'] = 'S';
       ownerReference.serverDataType['EnderecoWeb'] = 'S';
       ownerReference.serverDataType['Gpecon'] = 'S';
       ownerReference.serverDataType['IdAplicacao'] = 'I';
       ownerReference.serverDataType['IdLinxEmpresa'] = 'I';
       ownerReference.serverDataType['IdLinxGpecon'] = 'I';
       ownerReference.serverDataType['IdTcsAmbiente'] = 'I';
       ownerReference.serverDataType['IdTcsLogErros'] = 'I';
       ownerReference.serverDataType['IdUsuario'] = 'L';
       ownerReference.serverDataType['MensagemExcecao'] = 'S';
       ownerReference.serverDataType['MensagemExcecaoInterna'] = 'S';
       ownerReference.serverDataType['MetodoHttp'] = 'S';
       ownerReference.serverDataType['NomeAcao'] = 'S';
       ownerReference.serverDataType['NomeControlador'] = 'S';
       ownerReference.serverDataType['NomeServidor'] = 'S';
       ownerReference.serverDataType['PilhaExcecao'] = 'S';
       ownerReference.serverDataType['UsuarioWindows'] = 'S';
       ownerReference.serverDataType['NomeAutenticacao'] = 'S';
       ownerReference.serverDataType['NomeUsuario'] = 'S';
       ownerReference.typeName = 'TcsLogErros';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdTcsLogErros' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           var parent = getAbsoluteValue(ownerReference.TcsLogErrosDash);
           if (ownerReference.ChangeState == 'I') {
               if (parent && (typeof parent.TcsLogErrosList === 'function')) { 
                   parent.TcsLogErrosList.remove(ownerReference); 
               }
               else {
                   vm.dataView.remove(ownerReference);
               }
               delete ownerReference.TcsLogErrosDash;
           }
           else {
               if (ownerReference.ChangeState == 'N') { ownerReference.createOriginal(); }
               ownerReference.ChangeState = 'D'; // mark for deletion
           }
           if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsLogErrosList === 'function') && parent.TcsLogErrosList().length == 0) parent.setCurrentDetails('TcsLogErros');
       };
       ownerReference.setParentAsModified = function() {
       var parent = getAbsoluteValue(ownerReference.TcsLogErrosDash);
       if (parent) {
           if (parent.isUnchanged()) {
               parent.setModified(); 
           }
           parent.setParentAsModified();
       }
       };
       ownerReference.getParent = function() {
           return getAbsoluteValue(ownerReference.TcsLogErrosDash);
       };
       ownerReference.getSelfList = function() {
           var parent = ownerReference.getParent();
           if (!isNullOrEmpty(parent)) {
               return getAbsoluteValue(parent.TcsLogErrosList);
           } else { return null; }
       };
       ownerReference.namespace = 'Linx.Framework.BV.TratamentoErros';
       ownerReference.myProperties = [ ,'DataErro','DescricaoAmbiente','DescricaoAplicacao','Empresa','EnderecoWeb','Gpecon','IdAplicacao','IdLinxEmpresa','IdLinxGpecon','IdTcsAmbiente','IdTcsLogErros','IdUsuario','MensagemExcecao','MensagemExcecaoInterna','MetodoHttp','NomeAcao','NomeControlador','NomeServidor','PilhaExcecao','UsuarioWindows','NomeAutenticacao','NomeUsuario' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("TcsLogErros", null, TcsLogErrosInitializer);
    //#endregion Classes Map
    //#region Context Definition
    
    //#region Get LookUps
    
    var getLookUpGpeconByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpGpeconByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getLookUpTcsAmbienteByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpTcsAmbienteByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getLookUpTcsAplicacaoByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpTcsAplicacaoByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getLookUpTcsEmpresaAutenticacaoByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpTcsEmpresaAutenticacaoByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getLookUpTcsUsuarioAutenticacaoByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpTcsUsuarioAutenticacaoByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    var lookUpExternalManagers = [];
    //#endregion
    //#region Get KPI Ranges
    //#endregion
    
    //#region Get Combo LookUp
    var getResultsCombo = function (lookupName, fieldName, current, callback) {
        if (typeof current.executeLookUp === 'function') {
           current.executeLookUp(lookupName, fieldName, null, vm, null, null, function (result) {
               if (callback) callback(result);
           });
        }
    };
    var clientFilterHasModified = function clientFilterHasModified(lookupName, current) {
        var lastFilter = dataContext.lastClientFilterExpressions[lookupName];
        if (lastFilter === 'Error') return true;
        var currentFilter = current.getLookUpClientFilterExpressions(lookupName, null);
        return lastFilter != currentFilter;
    };
    //#endregion Get Combo LookUp
    
    //#region Get Business Entities
    
    var getBmEntityProperties = function (entityName, parentDataPath) {
        return manager.executeQuery(EntityQuery.from('GetBmEntityProperties').withParameters({ entityName: entityName, parentDataPath: parentDataPath }).noTracking(true))
        .fail(queryFailed);
    };
    
    var clearTcsLogErrosDash = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsLogErrosDash');
        var refTcsLogErrosDash = manager.createEntity('TcsLogErrosDash', {}, breeze.EntityState.Unchanged);
        resetSequence('TcsLogErros');
        var refTcsLogErros = manager.createEntity('TcsLogErros', {}, breeze.EntityState.Unchanged);
        refTcsLogErrosDash.currentTcsLogErros(refTcsLogErros);
        if (complete) complete({ results: [ refTcsLogErrosDash ] });
        return true;
    };
    
    var getTcsLogErrosDash = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsLogErrosDash').noTracking(noTracking)
        .orderBy('DataErro asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsLogErrosDashByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkTratamentoErros/AddTcsLogErrosDashEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsLogErrosDashByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'DataErro asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsLogErrosDashByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'DataErro asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearLogFile = function (idBandeiraRede, complete) {
        clearAll();
        if (complete) complete({ results: [] });
        return true;
    };
    
    var getLogFile = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetLogFile').noTracking(noTracking)
        .orderBy('FileName asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getLogFileByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkTratamentoErros/AddLogFileEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetLogFileByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'FileName asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetLogFileByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'FileName asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsLogErros = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsLogErros');
        var refTcsLogErros = manager.createEntity('TcsLogErros', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsLogErros ] });
        return true;
    };
    
    var getTcsLogErros = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsLogErros').noTracking(noTracking)
        .orderBy('DataErro desc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsLogErrosByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkTratamentoErros/AddTcsLogErrosEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsLogErrosByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'DataErro desc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsLogErrosByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'DataErro desc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    //#endregion
    
    // Create the data update event.
    var dataUpdateEvent = document.createEvent('Event');
    // Define that the event name is 'TratamentoErrosContext_DataUpdate'.
    var contextUpdtEvt = 'TratamentoErrosContext_DataUpdate_' + getNewGuid();
    dataUpdateEvent.initEvent(contextUpdtEvt, true, true);
    
    //#region LookUps Finalizers
     var finalizeAllLookUpGpecon = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            if (propertyName === '' || propertyName === 'IdLinxGpecon') {
               if (selectedElement.hasOwnProperty('IdLinxGpecon') && replaceTo.hasOwnProperty('IdLinxGpecon'))
               {
                   setAbsoluteValue(replaceTo, 'IdLinxGpecon', getAbsoluteValue(selectedElement['IdLinxGpecon']));
               }
               else if (replaceTo.hasOwnProperty('IdLinxGpecon')) {
                   setAbsoluteValue(replaceTo, 'IdLinxGpecon', null);
               }
            }
            if (propertyName === '' || propertyName === 'Gpecon') {
               if (selectedElement.hasOwnProperty('Gpecon') && replaceTo.hasOwnProperty('Gpecon'))
               {
                   setAbsoluteValue(replaceTo, 'Gpecon', getAbsoluteValue(selectedElement['Gpecon']));
               }
               else if (replaceTo.hasOwnProperty('Gpecon')) {
                   setAbsoluteValue(replaceTo, 'Gpecon', null);
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpGpecon'))
                replaceTo.validatedlookupsArray.push('LookUpGpecon');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
    };
    
    function clearLookUpGpecon(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'IdLinxGpecon', null);
        setAbsoluteValue(replaceTo, 'Gpecon', null);
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
     var finalizeAllLookUpTcsAmbiente = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            if (propertyName === '' || propertyName === 'DescricaoAmbiente') {
               if (selectedElement.hasOwnProperty('DescricaoAmbiente') && replaceTo.hasOwnProperty('DescricaoAmbiente'))
               {
                   setAbsoluteValue(replaceTo, 'DescricaoAmbiente', getAbsoluteValue(selectedElement['DescricaoAmbiente']));
               }
               else if (replaceTo.hasOwnProperty('DescricaoAmbiente')) {
                   setAbsoluteValue(replaceTo, 'DescricaoAmbiente', null);
               }
            }
            if (propertyName === '' || propertyName === 'IdTcsAmbiente') {
               if (selectedElement.hasOwnProperty('IdTcsAmbiente') && replaceTo.hasOwnProperty('IdTcsAmbiente'))
               {
                   setAbsoluteValue(replaceTo, 'IdTcsAmbiente', getAbsoluteValue(selectedElement['IdTcsAmbiente']));
               }
               else if (replaceTo.hasOwnProperty('IdTcsAmbiente')) {
                   setAbsoluteValue(replaceTo, 'IdTcsAmbiente', null);
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpTcsAmbiente'))
                replaceTo.validatedlookupsArray.push('LookUpTcsAmbiente');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
    };
    
    function clearLookUpTcsAmbiente(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'DescricaoAmbiente', null);
        setAbsoluteValue(replaceTo, 'IdTcsAmbiente', null);
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
     var finalizeAllLookUpTcsAplicacao = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            if (propertyName === '' || propertyName === 'DescricaoAplicacao') {
               if (selectedElement.hasOwnProperty('DescricaoAplicacao') && replaceTo.hasOwnProperty('DescricaoAplicacao'))
               {
                   setAbsoluteValue(replaceTo, 'DescricaoAplicacao', getAbsoluteValue(selectedElement['DescricaoAplicacao']));
               }
               else if (replaceTo.hasOwnProperty('DescricaoAplicacao')) {
                   setAbsoluteValue(replaceTo, 'DescricaoAplicacao', null);
               }
            }
            if (propertyName === '' || propertyName === 'IdAplicacao') {
               if (selectedElement.hasOwnProperty('IdAplicacao') && replaceTo.hasOwnProperty('IdAplicacao'))
               {
                   setAbsoluteValue(replaceTo, 'IdAplicacao', getAbsoluteValue(selectedElement['IdAplicacao']));
               }
               else if (replaceTo.hasOwnProperty('IdAplicacao')) {
                   setAbsoluteValue(replaceTo, 'IdAplicacao', null);
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpTcsAplicacao'))
                replaceTo.validatedlookupsArray.push('LookUpTcsAplicacao');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
    };
    
    function clearLookUpTcsAplicacao(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'DescricaoAplicacao', null);
        setAbsoluteValue(replaceTo, 'IdAplicacao', null);
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
     var finalizeAllLookUpTcsEmpresaAutenticacao = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            if (propertyName === '' || propertyName === 'IdLinxEmpresa') {
               if (selectedElement.hasOwnProperty('IdLinxEmpresa') && replaceTo.hasOwnProperty('IdLinxEmpresa'))
               {
                   setAbsoluteValue(replaceTo, 'IdLinxEmpresa', getAbsoluteValue(selectedElement['IdLinxEmpresa']));
               }
               else if (replaceTo.hasOwnProperty('IdLinxEmpresa')) {
                   setAbsoluteValue(replaceTo, 'IdLinxEmpresa', null);
               }
            }
            if (propertyName === '' || propertyName === 'NomeEmpresa') {
               if (selectedElement.hasOwnProperty('NomeEmpresa') && replaceTo.hasOwnProperty('NomeEmpresa'))
               {
                   setAbsoluteValue(replaceTo, 'NomeEmpresa', getAbsoluteValue(selectedElement['NomeEmpresa']));
               }
               else if (replaceTo.hasOwnProperty('NomeEmpresa')) {
                   setAbsoluteValue(replaceTo, 'NomeEmpresa', null);
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpTcsEmpresaAutenticacao'))
                replaceTo.validatedlookupsArray.push('LookUpTcsEmpresaAutenticacao');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
    };
    
    function clearLookUpTcsEmpresaAutenticacao(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'IdLinxEmpresa', null);
        setAbsoluteValue(replaceTo, 'NomeEmpresa', null);
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
     var finalizeAllLookUpTcsUsuarioAutenticacao = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            if (propertyName === '' || propertyName === 'IdUsuario') {
               if (selectedElement.hasOwnProperty('IdUsuario') && replaceTo.hasOwnProperty('IdUsuario'))
               {
                   setAbsoluteValue(replaceTo, 'IdUsuario', getAbsoluteValue(selectedElement['IdUsuario']));
               }
               else if (replaceTo.hasOwnProperty('IdUsuario')) {
                   setAbsoluteValue(replaceTo, 'IdUsuario', null);
               }
            }
            if (propertyName === '' || propertyName === 'NomeAutenticacao') {
               if (selectedElement.hasOwnProperty('NomeAutenticacao') && replaceTo.hasOwnProperty('NomeAutenticacao'))
               {
                   setAbsoluteValue(replaceTo, 'NomeAutenticacao', getAbsoluteValue(selectedElement['NomeAutenticacao']));
               }
               else if (replaceTo.hasOwnProperty('NomeAutenticacao')) {
                   setAbsoluteValue(replaceTo, 'NomeAutenticacao', null);
               }
            }
            if (propertyName === '' || propertyName === 'NomeUsuario') {
               if (selectedElement.hasOwnProperty('NomeUsuario') && replaceTo.hasOwnProperty('NomeUsuario'))
               {
                   setAbsoluteValue(replaceTo, 'NomeUsuario', getAbsoluteValue(selectedElement['NomeUsuario']));
               }
               else if (replaceTo.hasOwnProperty('NomeUsuario')) {
                   setAbsoluteValue(replaceTo, 'NomeUsuario', null);
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpTcsUsuarioAutenticacao'))
                replaceTo.validatedlookupsArray.push('LookUpTcsUsuarioAutenticacao');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
    };
    
    function clearLookUpTcsUsuarioAutenticacao(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'IdUsuario', null);
        setAbsoluteValue(replaceTo, 'NomeAutenticacao', null);
        setAbsoluteValue(replaceTo, 'NomeUsuario', null);
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
    //#endregion
    
    var cancelChanges = function(dataForUndo) {
        if (dataForUndo && dataForUndo.length > 0) {
            dataForUndo.forEach(function(e) { e.restoreOriginal(); } ); 
        } else {
            manager.rejectChanges();
        }
    };
    
    var hasEmptyRequiredFilters = function() {
        return false;
    };
    
    var getEntityProperty = function (entityName, propertyName) {
        for (var i = 0; i < metadataInfo[entityName].length; i++) {
            if (metadataInfo[entityName][i].key === propertyName)
                return metadataInfo[entityName][i];
        }
        return null;
    };
    var getViewInfo = function (entityName) {
        var result = [];
        if (metadataInfo[entityName])
        {
            var viewInfoElements = metadataInfo[entityName];
            for (var i = 0; i < viewInfoElements.length; i++) {
                var selectedElement = viewInfoElements[i];
                if (!selectedElement.hidden && (selectedElement.dataType === 'string' || selectedElement.dataType === 'number'))
                {
                    if (strLeft(selectedElement.key, 3) === 'Cod' || strLeft(selectedElement.key, 2) === 'Id' || strLeft(selectedElement.key, 6) === 'Numero' || strLeft(selectedElement.key, 6) === 'Number') {
                        result.push(selectedElement);
                    }
                }
            }
            for (var i = 0; i < viewInfoElements.length; i++) {
                var selectedElement = viewInfoElements[i];
                if (!selectedElement.hidden && (selectedElement.dataType === 'string')) {
                    if (strLeft(selectedElement.key, 4) === 'Nome' || strLeft(selectedElement.key, 4) === 'Name' || strLeft(selectedElement.key, 4) === 'Desc' || strLeft(selectedElement.key, 6) === 'Titulo' || strLeft(selectedElement.key, 5) === 'Title') {
                        result.push(selectedElement);
                    }
                }
            }
            for (var i = 0; i < viewInfoElements.length; i++) {
                var selectedElement = viewInfoElements[i];
                if (!selectedElement.hidden && selectedElement.dataType === 'date')
                {                
                   result.push(selectedElement);
                }
            }
        }
        return result;
    };
    
    var getChanges = function() {
       return manager.getEntities(null, [breeze.EntityState.Added, breeze.EntityState.Modified, breeze.EntityState.Deleted]);
    };
    
    var hasValidationErrors = function(savingData) {
        if (savingData instanceof Array) {
           for (var idx = 0; idx < savingData.length; idx++) {
               var entity = savingData[idx];
               if (entity.ChangeState && entity.getValidationErrors && ['I', 'U'].indexOf(entity.ChangeState) >=0) {
                  var errors = entity.getValidationErrors();
                  if (errors.length > 0) {
                       showModalAlert('Campos obrigatórios não estão preenchidos.', errors);
                       return true;
                   }
               }
           }
        }
        else {
           var changes = manager.getEntities(null, [breeze.EntityState.Added, breeze.EntityState.Modified]);
           for (var idxChange = 0; idxChange < changes.length; idxChange++) {
              changes[idxChange].setParentAsModified();
           }
           changes = manager.getEntities(null, [breeze.EntityState.Added, breeze.EntityState.Modified]);
           for (var idxChange = 0; idxChange < changes.length; idxChange++) {
              var entity = changes[idxChange];
              var isOk = entity.entityAspect.validateEntity();
              if (!isOk) {
                  var errors = entity.entityAspect.getValidationErrors();
                  var strErrors = [];
                  for (var idx = 0; idx < errors.length; idx++) {
                      var errorMsg = errors[idx].errorMessage;
                      var propName = strExtract(errorMsg, "'", "'");
                      var propDisplay = entity.getDisplayName(propName);
                      errorMsg = errorMsg.replace("'" + propName + "'", "'" + propDisplay + "'" + (managerAuth.shellMode=='DEV' ? " (" + entity.typeName + "." + propName + ")": ""));
                      strErrors.push(translateError(errorMsg));
                  }
                  showModalAlert('Campos obrigatórios não estão preenchidos.', strErrors);
                  return true;
              }
           }
        }
        return false;
    };
    
    var saveChangesFake = function (transactionID, saveSucceeded) {
        var dataEntities = _.map(vm.getDataForSaving(), function (entity) { return entity.getPrimitiveDTO(entity.ChangeState != 'D'); });
        var dataForSaving = {
            TransactionID: transactionID,
            ComponentName: vm.__moduleId__,
            DataList: dataEntities,
            RelationInfo: vm.getViewMapInfo()
        };
        return $.ajax({
            type: 'POST',
            crossDomain: true,
            url: getServiceAddress('LinxFrameworkTratamentoErros/Save' + vm.rootDataTypeName + 'InCache'),
            globalError: false,
            contentType: 'application/json',
            async: true,
            cache: false,
            data: JSON.stringify(dataForSaving),
            success: function (response) {
                if (saveSucceeded)
                    saveSucceeded(response);
            },
            error: function (jqXHR, textStatus, errorThrown) {
                failed({ message: jqXHR.responseJSON.ExceptionMessage });
            }
        });
        function failed(error) {
            var msg = error.message.replace('Fail by saving data:', '');
            showModalAlert('Falha ao salvar informações.', [msg]);
            error.message = msg;
            throw error;
        }
    };
    
    var submitAllChanges = function (transactionId, saveSucceeded, failed, completed) {
        return $.ajax({
            type: 'GET',
            crossDomain: true,
            url: getServiceAddress('LinxFrameworkTratamentoErros/submitAllChanges?transactionID=' + transactionId),
            globalError: false,
            contentType: 'application/json',
            async: true,
            cache: false,
            success: function (response) { if (typeof saveSucceeded === 'function') saveSucceeded(response); },
            error: function (jqXHR, textStatus, errorThrown) { failed(jqXHR.responseJSON);}
        });
    }
    var cancelAllChanges = function (transactionId, saveSucceeded, failed) {
        return $.ajax({
            type: 'GET',
            crossDomain: true,
            url: getServiceAddress('LinxFrameworkTratamentoErros/CancelAllChanges?transactionID=' + transactionId),
            globalError: false,
            contentType: 'application/json',
            async: true,
            cache: false,
            success: function (response) { if (saveSucceeded) saveSucceeded(response); },
            error: function (jqXHR, textStatus, errorThrown) { failed(jqXHR.responseJSON); }
        });
    }
    
    var saveChanges = function(saveSucceeded, saveFailed, fin, saveNoTRack) {
        if (saveNoTRack === true) {
            var dataForSaving = JSON.stringify(_.map(vm.getDataForSaving(), function(entity){ return entity.getPrimitiveDTO(entity.ChangeState != 'D'); }));
            return $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkTratamentoErros/Save' + vm.rootDataTypeName),
               globalError: false,
               contentType: 'application/json',
               async: true,
               cache: false,
               data: dataForSaving,
               success: function (response) {
                      success(response);
               },
               error: function (jqXHR, textStatus, errorThrown) {
                      failed({ message: jqXHR.responseJSON.ExceptionMessage });
               }
            });
        }
        else {
            return manager.saveChanges()
                   .fail(failed).then(success);
        }
    
        function success(result) {
            if (fin) fin();
            if (saveNoTRack === true && result.length > 0) {
               for (var idx = 0; idx < result.length; idx++) { dataContext.initializePOCO(result[idx], vm.rootDataTypeName); }
            }
            else if (result != null && result.keyMappings != null && result.keyMappings.length > 0) {
                for (var idx = 0; idx < result.keyMappings.length; idx++) {
                    if (result.keyMappings[idx].realValue == null) {
                       var entity = manager.getEntityByKey(result.keyMappings[idx].entityTypeName, result.keyMappings[idx].tempValue);
                       if (entity) manager.detachEntity(entity);
                    }
                }
                manager.acceptChanges();
            }
            if (saveSucceeded)
                saveSucceeded(result);
        }
    
        function failed(error) {
            if (fin) fin();
            if (error.message.indexOf('Internal Error in key fixup - unable to locate entity') == -1 && error.message.indexOf('An entity with this key is already in the cache:') == -1) {
               if (saveFailed)
                   saveFailed(error);
               var msg = error.message.replace('Fail by saving data:', '');
               showModalAlert('Falha ao salvar informações.', [ msg ]);
               error.message = msg;
               throw error;
           } else {
               manager.acceptChanges();
           }
        }
    };
    
    var acceptChanges = function () {
        return manager.acceptChanges();
    };
    
    var getEntities = function (entityName) {
        return manager.getEntities(entityName);
    };
    
    var notifyPresentation = function(dataSourceName) {
          if (dataContext.dataForUpdate !== '') {
           setTimeout(function () { notifyPresentation(dataSourceName); }, 100);
           return;
          }
          dataContext.dataForUpdate = dataSourceName;
          document.dispatchEvent(dataUpdateEvent);
          dataContext.dataForUpdate = '';
    };
    
    var getEntityInCache = function (entityName, propertiesReference) {
        var keys = [];
        if (!isNullOrEmpty(propertiesReference)) {
            for (var i = 0; i < metadataInfo[entityName].length; i++) {
                if (metadataInfo[entityName][i].isPartOfKey && !isNullOrEmpty(propertiesReference[metadataInfo[entityName][i].key]))
                    keys.push(propertiesReference[metadataInfo[entityName][i].key]);
            }
            if (keys.length == 0)
                return null;
            else {
                return manager.getEntityByKey(entityName, (keys.length == 1 ? keys[0] : keys));
            }
        }
        else
            return null;
    }
    
    var createEntity = function(entityName, initialValues, unchanged) {
        var entity = getEntityInCache(entityName, initialValues);
        if (!isNullOrEmpty(entity))
            entity.entityAspect.entityState == (unchanged === true ? breeze.EntityState.Unchanged : breeze.EntityState.Added);
        else 
            entity = manager.createEntity(entityName, initialValues, (unchanged === true ? breeze.EntityState.Unchanged : breeze.EntityState.Added));
        return entity;
    };
    
    var createFreeEntity = function(entityName) {
       return manager.createEntity(entityName, {}, breeze.EntityState.Detached);
    }
    
    var createTcsLogErrosDash = function() {
        //Create entity instance
        enableChangeTrack = false;
        var defaultVals = { EntityUniqueKey: getNewGuid() };
        var entityType = manager.metadataStore.getEntityType('TcsLogErrosDash');
        var entity = {};
        for (var idx = 0; idx < entityType.dataProperties.length; idx++) { 
            var prop = entityType.dataProperties[idx]; 
            if ((typeof defaultVals[prop.name]) !== 'undefined') entity[prop.name] = defaultVals[prop.name];
            else  entity[prop.name] = prop.defaultValue;
        }
        dataContext.initializePOCO(entity, 'TcsLogErrosDash');
        entity.setDefaults();
        setAbsoluteValue(entity, 'ChangeState', 'I');
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createLogFile = function(parent, noCurrent) {
        //Create entity instance
        enableChangeTrack = false;
        var defaultVals = { TcsLogErrosDash: parent, FileName: (-1 * getSequence('LogFile')).toString() };
        var entityType = manager.metadataStore.getEntityType('LogFile');
        var entity = {};
        for (var idx = 0; idx < entityType.dataProperties.length; idx++) { 
            var prop = entityType.dataProperties[idx]; 
            if ((typeof defaultVals[prop.name]) !== 'undefined') entity[prop.name] = defaultVals[prop.name];
            else  entity[prop.name] = prop.defaultValue;
        }
        dataContext.initializePOCO(entity, 'LogFile');
        setAbsoluteValue(entity, 'TcsLogErrosDash', parent);
        setAbsoluteValue(entity, 'EntityParentUniqueKey', getAbsoluteValue(parent.EntityUniqueKey));
        entity.setDefaults();
        setAbsoluteValue(entity, 'ChangeState', 'I');
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        if (noCurrent !== true) parent.currentLogFile(entity);
        if (parent && (typeof parent.LogFileList === 'function')) parent.LogFileList().push(entity);
        if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.LogFileList === 'function') && parent.LogFileList().length == 0) parent.setCurrentDetails('LogFile');
        if (entity.setParentAsModified) entity.setParentAsModified();
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsLogErros = function(parent, noCurrent) {
        //Create entity instance
        enableChangeTrack = false;
        var defaultVals = { TcsLogErrosDash: parent, IdTcsLogErros: (-1 * getSequence('TcsLogErros')) };
        var entityType = manager.metadataStore.getEntityType('TcsLogErros');
        var entity = {};
        for (var idx = 0; idx < entityType.dataProperties.length; idx++) { 
            var prop = entityType.dataProperties[idx]; 
            if ((typeof defaultVals[prop.name]) !== 'undefined') entity[prop.name] = defaultVals[prop.name];
            else  entity[prop.name] = prop.defaultValue;
        }
        dataContext.initializePOCO(entity, 'TcsLogErros');
        setAbsoluteValue(entity, 'TcsLogErrosDash', parent);
        setAbsoluteValue(entity, 'EntityParentUniqueKey', getAbsoluteValue(parent.EntityUniqueKey));
        entity.setDefaults();
        setAbsoluteValue(entity, 'ChangeState', 'I');
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        if (noCurrent !== true) parent.currentTcsLogErros(entity);
        if (parent && (typeof parent.TcsLogErrosList === 'function')) parent.TcsLogErrosList().push(entity);
        if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsLogErrosList === 'function') && parent.TcsLogErrosList().length == 0) parent.setCurrentDetails('TcsLogErros');
        if (entity.setParentAsModified) entity.setParentAsModified();
        enableChangeTrack = true;
        return entity;
    };
    
    var deleteEntity = function (entity) {
        entity.delete();
    };
    
    var detachEntity = function (entity) {
        if (!entity.isDetached()) entity.entityAspect.setDetached();
    };
    
    var attachEntity = function (entity) {
        manager.attachEntity(entity);
    };
    
    var clearAll = function () {
        manager.rejectChanges();
        manager.clear();
    };
    
    var executeQuery = function (getMethod, jEntitySearch, order, skip, take, noTracking, callBack) {
        var query = EntityQuery.from(getMethod).noTracking(noTracking)
        .orderBy(order);
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed).then(function (data) {
            if (callBack) {
                callBack(data.results);
            }
        });
    };
    var exportToExcel = function(entityName, jEntitySearch, translatedJEntitySearch, complete, columnsVisible) {
        var info = jQuery.grep(dataExportInfo[vm.rootDataTypeName], function (item, i) { return (item.name === entityName);});
        if (info == null || info.length === 0) {
            app.showMessage('Erro na exportação', 'Alerta', ['Ok']);
            return;
        }
        $.ajax({
           type: 'POST',
           crossDomain: true,
           url: getServiceAddress(info[0].actionExport),
           globalError: true,
           headers: managerAuth.getHeaders(),
           contentType: 'application/json',
           async: true,
           cache: false,
           data: JSON.stringify([jEntitySearch, translatedJEntitySearch, columnsVisible]),
           success: function (response) {
               if (response.startsWith('~/FileDownload/')) {
                    saveURL(getBaseServiceAddress(info[0].actionExport) + response.substr(1), entityName + '.xlsx');
               } else {
                    saveExcelBlob(entityName + '.xlsx', response);
               }
           },
           complete: function (jqXHR, textStatus) {
               if(complete) complete();
           },
           error: function (jqXHR, textStatus, errorThrown) {
           }
        });
    };
    var exportReportDataSource = function(complete) {
        $.ajax({
           type: 'GET',
           crossDomain: true,
           headers: managerAuth.getHeaders(),
           url: getServiceAddress("LinxFrameworkTratamentoErros/GetReportDataSource"),
           success: function (response) {
                  saveExcelBlob('datasource.ldsx', response);
           },
           complete: function (jqXHR, textStatus) {
                  if(complete) complete();
           },
           error: function (jqXHR, textStatus, errorThrown) {
                  alert('Erro na exportação do data source');
           }
        });
    };
    var exportTemplateReport = function(reportPath, complete) {
        $.ajax({
           type: 'GET',
           crossDomain: true,
           headers: managerAuth.getHeaders(),
           url: getServiceAddress("LinxFrameworkTratamentoErros/GetTemplateReport"),
           data: { reportPath: reportPath },
           success: function (response) {
                  saveExcelBlob(reportPath + '.lrtx', response);
           },
           complete: function (jqXHR, textStatus) {
                  if(complete) complete();
           },
           error: function (jqXHR, textStatus, errorThrown) {
                  alert('Erro na exportação do data source');
           }
        });
    };
    var exportToReport = function(reportName, entityName, jEntitySearch, translatedJEntitySearch, complete, columnsVisible, exportMedia) {
        var info = jQuery.grep(dataExportInfo[vm.rootDataTypeName], function (item, i) { return (item.name === entityName);});
        if (info == null || info.length === 0) {
            app.showMessage('Erro na exportação', 'Alerta', ['Ok']);
            return;
        }
        $.ajax({
           type: 'POST',
           crossDomain: true,
           headers: managerAuth.getHeaders(),
           url: getServiceAddress(info[0].actionReport),
           globalError: true,
           contentType: 'application/json',
           async: true,
           cache: false,
           data: JSON.stringify([ reportName, jEntitySearch, translatedJEntitySearch, columnsVisible, getServiceAddress(''), exportMedia ]),
           success: function (response) {
                  saveExcelBlob(entityName + '.lrtx', response);
           },
           complete: function (jqXHR, textStatus) {
                  if(complete) complete();
           },
           error: function (jqXHR, textStatus, errorThrown) {
           }
        });
    };
    
    var hasChanges = ko.observable(false);
    
    manager.hasChangesChanged.subscribe(function(eventArgs) {
        hasChanges(eventArgs.hasChanges);
    });
    
    //#region Internal methods
    
    function queryFailed(error) {
    }
    
    function log(msg, data) {
        logger.log(msg, data, system.getModuleId(dataContext), true);
    }
    
    function logError(msg, error) {
         logger.logError(msg, error, system.getModuleId(dataContext), true);
    }
    
    function loadParameters() {
     dataParameters.isLoaded = true;
    }
    
    //#endregion Internal methods
    //#region getWithBinding
    var createDTOEmpty = function (entityName) {
        var entityType = manager.metadataStore.getEntityType(entityName);
        var entity = {};
        for (var idx = 0; idx < entityType.dataProperties.length; idx++) {
            var prop = entityType.dataProperties[idx];
            entity[prop.name] = prop.defaultValue;
        }
        for (var idx = 0; idx < entityType.navigationProperties.length; idx++) {
            var navigationName = entityType.navigationProperties[idx].name;
            if (navigationName.endsWith('List'))
                entity['current' + navigationName.replace('List', '')] = null;
        }
        entity.isEmptyEntity = true;
        return entity;
    };
    var getWithBinding = function (binding, entityName) {
        if (typeof binding === 'function' && binding() != null)
            return binding;
        else
            return createDTOEmpty(entityName);
    }
    //#endregion getWithBinding
    
        var vm = null;
        var dataContext = {
            dataForUpdate: '',
            getPivotLayouts: getPivotLayouts,
            getServiceAddress: getServiceAddress,
            getBaseServiceAddress: getBaseServiceAddress,
            getDataFeedUrl: getDataFeedUrl,
            getDataServiceUrl: getDataServiceUrl,
            setServiceBusUrl: setServiceBusUrl,
            initializePOCO: initializePOCO,
            getWithBinding: getWithBinding,
            managerAuth: managerAuth,
            hasDataFeed: true,
            getAccessGroup: getAccessGroup,
            getNewGuid: getNewGuid,
            metadataInfo: metadataInfo,
            dataExportInfo: dataExportInfo,
            entityNames: entityNames,
            lookUpNames: lookUpNames,
            lookUpProperties: lookUpProperties,
            metadataStore: metadataStore,
            cancelChanges: cancelChanges,
            saveChanges: saveChanges,
            saveChangesFake: saveChangesFake,
            submitAllChanges: submitAllChanges,
            cancelAllChanges: cancelAllChanges,
            getChanges: getChanges,
            hasValidationErrors: hasValidationErrors,
            getEntityProperty: getEntityProperty,
            getViewInfo: getViewInfo,
            createEntity: createEntity,
            notifyPresentation: notifyPresentation,
            createFreeEntity: createFreeEntity,
            createTcsLogErrosDash: createTcsLogErrosDash,
            createLogFile: createLogFile,
            createTcsLogErros: createTcsLogErros,
            deleteEntity: deleteEntity,
            acceptChanges: acceptChanges,
            getEntities: getEntities,
            detachEntity: detachEntity,
            attachEntity: attachEntity,
            executeQuery: executeQuery,
            sharedData: [],
            clearAll: clearAll,
            hasChanges: hasChanges,
            dataDomains: dataDomains,
            dataParameters: dataParameters,
            loadParameters: loadParameters,
            exportToExcel: exportToExcel,
            exportToReport: exportToReport,
            exportReportDataSource: exportReportDataSource,
            exportTemplateReport: exportTemplateReport,
            businessAssemblyName: businessAssemblyName,
            controllerName: controllerName,
            getResultsCombo: getResultsCombo,
            clientFilterHasModified: clientFilterHasModified,
            lastClientFilterExpressions: {},
            breeze: breeze,
            contextUpdtEvt: contextUpdtEvt,
            setCurrentViewModel: function(vModel) { vm = vModel; },
                getLookUpGpeconByEntitySearch: getLookUpGpeconByEntitySearch,
            getLookUpTcsAmbienteByEntitySearch: getLookUpTcsAmbienteByEntitySearch,
            getLookUpTcsAplicacaoByEntitySearch: getLookUpTcsAplicacaoByEntitySearch,
            getLookUpTcsEmpresaAutenticacaoByEntitySearch: getLookUpTcsEmpresaAutenticacaoByEntitySearch,
            getLookUpTcsUsuarioAutenticacaoByEntitySearch: getLookUpTcsUsuarioAutenticacaoByEntitySearch,
            getBmEntityProperties: getBmEntityProperties,
            clearTcsLogErrosDash: clearTcsLogErrosDash,
            getTcsLogErrosDash: getTcsLogErrosDash,
            getTcsLogErrosDashByEntitySearchNoAssociations: getTcsLogErrosDashByEntitySearchNoAssociations,
            clearLogFile: clearLogFile,
            getLogFile: getLogFile,
            getLogFileByEntitySearchNoAssociations: getLogFileByEntitySearchNoAssociations,
            clearTcsLogErros: clearTcsLogErros,
            getTcsLogErros: getTcsLogErros,
            getTcsLogErrosByEntitySearchNoAssociations: getTcsLogErrosByEntitySearchNoAssociations,
                finalizeAllLookUpGpecon: finalizeAllLookUpGpecon,
            clearLookUpGpecon: clearLookUpGpecon,
            finalizeAllLookUpTcsAmbiente: finalizeAllLookUpTcsAmbiente,
            clearLookUpTcsAmbiente: clearLookUpTcsAmbiente,
            finalizeAllLookUpTcsAplicacao: finalizeAllLookUpTcsAplicacao,
            clearLookUpTcsAplicacao: clearLookUpTcsAplicacao,
            finalizeAllLookUpTcsEmpresaAutenticacao: finalizeAllLookUpTcsEmpresaAutenticacao,
            clearLookUpTcsEmpresaAutenticacao: clearLookUpTcsEmpresaAutenticacao,
            finalizeAllLookUpTcsUsuarioAutenticacao: finalizeAllLookUpTcsUsuarioAutenticacao,
            clearLookUpTcsUsuarioAutenticacao: clearLookUpTcsUsuarioAutenticacao
        };
    loadParameters();
    return dataContext;
    //#endregion Context Definition
}
return result;
});
