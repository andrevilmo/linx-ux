define(['durandal/system', 'pkg_linx-framework-bv-spa/services/DataDomains', 'services/logger', 'breeze', 'durandal/app', 'managers/__auth', 'viewmodels/shared/modal', 'viewmodels/shared/modal2'],
function (system, dataDomains, logger, breeze, app, managerAuth, modal, modal2) {
var result = function () {
    var getPivotLayouts = function (params, success, error) {
        return $.ajax({
            messageUser: 'Busca dos layouts exportados',
            contentType: 'application/json; charset=UTF-8',
            headers: managerAuth.getHeaders(),
            url: getServiceAddress('linxframeworkobjeto') + '/GetPivotLayouts?' +
                                                                   'rootNameSpace=' + params.rootNamespace +
                                                                   '&viewName=' + params.viewName +
                                                                   '&pivotName=' + params.pivotName +
                                                                   '&pivotDataSource=' + params.pivotDataSource,
            async: true,
            cache: false,
            error: error,
            success: success
        });
    };
    
    var getServiceAddress = function(apiPart) {
       return managerAuth.getServiceAddress(apiPart, businessAssemblyName);
    };
    var getBaseServiceAddress = function(apiPart) {
       return managerAuth.getBaseServiceAddress(apiPart, businessAssemblyName);
    };
    var getAccessGroup = function() {
       return '00000000-0000-0000-0000-000000000000';
    };
    var getNewGuid = function() {
       return breeze.core.getUuid();
    };
    var getDataFeedUrl = function() {
       return getServiceAddress('LinxFrameworkMensagemOData');
    };
    var getDataServiceUrl = function () {
       return getServiceAddress(controllerName);
    };
    var setServiceBusUrl = function (url) {
       if (dataService) { dataService.serviceName = (isNullOrEmpty(url) ? getDataServiceUrl() : url + controllerName); }
    };
    var initializePOCO = function(ownerReference, entityName) {
       if (ownerReference && !ownerReference.RowDataId) { eval(entityName + 'Initializer(ownerReference, true);'); }
    };
    var businessAssemblyName = 'Linx.Framework.BV';
    var controllerName = 'LinxFrameworkMensagem';
    var dataService = new breeze.DataService({
        serviceName: getDataServiceUrl(),
        hasServerMetadata: false // don't ask the server for metadata
    });
    var manager = new breeze.EntityManager({ dataService: dataService });
    manager.entityChanged.subscribe(function(changeArgs) {
        if (changeArgs.entityAction === breeze.EntityAction.PropertyChange) {
            if ((typeof changeArgs.args.newValue) === 'number' && changeArgs.args.oldValue < 0 && changeArgs.args.newValue > 0 && changeArgs.entity.isPrimaryKey(changeArgs.args.propertyName)) vm.replaceInnerUIsKeys(changeArgs.entity, changeArgs.args.propertyName, changeArgs.args.oldValue, changeArgs.args.newValue);
            if (typeof changeArgs.entity.OnPropertyChanged == 'function')
                changeArgs.entity.OnPropertyChanged(changeArgs.args.propertyName, changeArgs.args.oldValue, changeArgs.args.newValue);
        }
    });
    var enableChangeTrack = true;
    var entityPropChanged = function(entity, propName, oldVal, newVal) {
        if (!enableChangeTrack) return true;
        var result = true;
        if ((typeof entity.OnPropertyChanged == 'function') && oldVal !== newVal)
            result = (entity.OnPropertyChanged(propName, oldVal, newVal) !== false);
        if (result && ['U', 'I', 'D'].indexOf(entity.ChangeState) < 0) { entity.createOriginal(propName, oldVal); entity.ChangeState = 'U'; if (entity.setParentAsModified) entity.setParentAsModified(); }
        if (result && (typeof newVal) === 'number' && oldVal < 0 && newVal > 0 && entity.isPrimaryKey(propName)) vm.replaceInnerUIsKeys(entity, propName, oldVal, newVal);
        return result;
    }
    var metadataStore = manager.metadataStore;
    var EntityQuery = breeze.EntityQuery;
    // Extract Breeze metadata definition types
    var DataType = breeze.DataType;
    var AutoGeneratedKeyType = breeze.AutoGeneratedKeyType;
    var Validator = breeze.Validator;
    Validator.hasValueValidator = new breeze.Validator('hasValueValidator', hasValueValidationFn, { messageTemplate: "'%displayName%' é requerido" });
    //#region Metadata Info
    var metadataInfo = [];
    var dataExportInfo = [];
    var entityNames = [];
    var lookUpNames = [];
    var entitylookUps = [];
    entityNames.push('TcsMensagem');
    metadataInfo['TcsMensagem'] = [
        { key: 'Corpo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Corpo', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Criacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: false, headerText: 'Criacao', width: '127px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'Envio', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: false, headerText: 'Envio', width: '120px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'Filtro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Filtro', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdLinx', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Linx', width: '151px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdTcsMensagem', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Tcs Mensagem', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Usuario', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxTipoMensagem', isQbeZero: false, isDomain: true, domainName: 'TipoMensagem', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: 'Lx Tipo Mensagem', width: '244px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxTipoMensagemName', isDomain: true, domainName: 'TipoMensagem', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Lx Tipo Mensagem (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'Titulo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 100, validateMaxLength: true, isPartOfKey: false, headerText: 'Titulo', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsMensagem'] = [ 
        { name: 'TcsMensagem', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkMensagem/GetTcsMensagemToExcel', actionReport: 'LinxFrameworkMensagem/GetTcsMensagemToReportXml', actionFeed: 'LinxFrameworkMensagemOData/TcsMensagem', actionName: 'LinxFrameworkMensagem/GetTcsMensagemByEntitySearchNoAssociations', display: 'TcsMensagem',  metaData: function() { return metadataInfo['TcsMensagem']; } }
        , { name: 'TcsMensagemLogDetail', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkMensagem/GetTcsMensagemLogDetailParentCompositionToExcel', actionReport: 'LinxFrameworkMensagem/GetTcsMensagemLogDetailParentCompositionToReportXml', actionFeed: 'LinxFrameworkMensagemOData/TcsMensagemLogDetailParentComposition', actionName: 'LinxFrameworkMensagem/GetTcsMensagemLogDetailParentCompositionByEntitySearchNoAssociations', display: 'TcsMensagemLogDetail',  metaData: function() { return metadataInfo['TcsMensagemLogDetailParentComposition']; } }
    ];
    entitylookUps.push('TcsMensagem');
    entitylookUps['TcsMensagem'] = [];
    entityNames.push('MensagemInfo');
    metadataInfo['MensagemInfo'] = [
        { key: 'IdTcsMensagemLog', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Tcs Mensagem Log', width: '283px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'Titulo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 100, validateMaxLength: true, isPartOfKey: false, headerText: 'Titulo', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Corpo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Corpo', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Lida', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Lida', width: '88px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'Entregue', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: false, headerText: 'Entregue', width: '140px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'TipoMensagem', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'TipoMensagem', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['MensagemInfo'] = [ 
        { name: 'MensagemInfo', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkMensagem/GetMensagemInfoToExcel', actionReport: 'LinxFrameworkMensagem/GetMensagemInfoToReportXml', actionFeed: 'LinxFrameworkMensagemOData/MensagemInfo', actionName: 'LinxFrameworkMensagem/GetMensagemInfoByEntitySearchNoAssociations', display: 'MensagemInfo',  metaData: function() { return metadataInfo['MensagemInfo']; } }
    ];
    entitylookUps.push('MensagemInfo');
    entitylookUps['MensagemInfo'] = [];
    entityNames.push('TcsMensagemUsuario');
    metadataInfo['TcsMensagemUsuario'] = [
        { key: 'Corpo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Corpo', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Dispensada', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Dispensada', width: '166px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'Entregue', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Entregue', width: '140px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'Envio', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Envio', width: '120px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'IdLinx', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Linx', width: '151px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdTcsMensagem', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Tcs Mensagem', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdTcsMensagemLog', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Tcs Mensagem Log', width: '283px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Usuario', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'Lida', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Lida', width: '120px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'LxTipoMensagem', isQbeZero: false, isDomain: true, domainName: 'TipoMensagem', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: 'Lx Tipo Mensagem', width: '244px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxTipoMensagemName', isDomain: true, domainName: 'TipoMensagem', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Lx Tipo Mensagem (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'Titulo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 100, validateMaxLength: true, isPartOfKey: false, headerText: 'Titulo', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsMensagemUsuario'] = [ 
        { name: 'TcsMensagemUsuario', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkMensagem/GetTcsMensagemUsuarioToExcel', actionReport: 'LinxFrameworkMensagem/GetTcsMensagemUsuarioToReportXml', actionFeed: 'LinxFrameworkMensagemOData/TcsMensagemUsuario', actionName: 'LinxFrameworkMensagem/GetTcsMensagemUsuarioByEntitySearchNoAssociations', display: 'TcsMensagemUsuario',  metaData: function() { return metadataInfo['TcsMensagemUsuario']; } }
    ];
    entitylookUps.push('TcsMensagemUsuario');
    entitylookUps['TcsMensagemUsuario'] = [];
    entityNames.push('TcsMensagemLog');
    metadataInfo['TcsMensagemLog'] = [
        { key: 'Dispensada', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Dispensada', width: '166px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'Entregue', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Entregue', width: '140px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'IdTcsMensagem', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Tcs Mensagem', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdTcsMensagemLog', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Tcs Mensagem Log', width: '283px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Usuario', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'Lida', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Lida', width: '120px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsMensagemLog'] = [ 
        { name: 'TcsMensagemLog', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkMensagem/GetTcsMensagemLogToExcel', actionReport: 'LinxFrameworkMensagem/GetTcsMensagemLogToReportXml', actionFeed: 'LinxFrameworkMensagemOData/TcsMensagemLog', actionName: 'LinxFrameworkMensagem/GetTcsMensagemLogByEntitySearchNoAssociations', display: 'TcsMensagemLog',  metaData: function() { return metadataInfo['TcsMensagemLog']; } }
    ];
    entitylookUps.push('TcsMensagemLog');
    entitylookUps['TcsMensagemLog'] = [];
    entityNames.push('TcsPerfil');
    metadataInfo['TcsPerfil'] = [
        { key: 'IdTcsUsuarioPerfil', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Tcs Usuario Perfil', width: '309px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Usuario', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'DescPerfil', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Descrição', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdPerfil', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id', width: '130px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsPerfil'] = [ 
        { name: 'TcsPerfil', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkMensagem/GetTcsPerfilToExcel', actionReport: 'LinxFrameworkMensagem/GetTcsPerfilToReportXml', actionFeed: 'LinxFrameworkMensagemOData/TcsPerfil', actionName: 'LinxFrameworkMensagem/GetTcsPerfilByEntitySearchNoAssociations', display: 'TcsPerfil',  metaData: function() { return metadataInfo['TcsPerfil']; } }
    ];
    entitylookUps.push('TcsPerfil');
    entitylookUps['TcsPerfil'] = [];
    entityNames.push('TcsUsuario');
    metadataInfo['TcsUsuario'] = [
        { key: 'AutenticacaoWindows', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Autenticacao Windows', width: '296px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'Bairro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Bairro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Cep', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, validateMaxLength: true, isPartOfKey: false, headerText: 'Cep', width: '100px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'CnpjCpf', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 20, validateMaxLength: true, isPartOfKey: false, headerText: 'Cnpj Cpf', width: '200px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Complemento', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Complemento', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DataAlteracao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Data Alteracao', width: '218px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'DataCadastro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Data Cadastro', width: '205px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'DataExpiracaoSenha', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: false, headerText: 'Data Expiracao Senha', width: '296px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'Email', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Email', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'FoneCelular', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 20, validateMaxLength: true, isPartOfKey: false, headerText: 'Fone Celular', width: '200px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'FoneFixo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 20, validateMaxLength: true, isPartOfKey: false, headerText: 'Fone Fixo', width: '200px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdLinx', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Linx', width: '151px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Usuario', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'Inativo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Inativo', width: '127px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'IndicaAcessoSuporte', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Indica Acesso Suporte', width: '309px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'InscrEstadualRg', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 20, validateMaxLength: true, isPartOfKey: false, headerText: 'Inscr Estadual Rg', width: '257px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Logradouro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Logradouro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'LxPfjFisicaJuridica', isQbeZero: false, isDomain: true, domainName: 'LX_PFJ_FISICA_JURIDICA', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: 'Lx Pfj Fisica Juridica', width: '322px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxPfjFisicaJuridicaName', isDomain: true, domainName: 'LX_PFJ_FISICA_JURIDICA', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Lx Pfj Fisica Juridica (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'LxTipoLogradouro', isQbeZero: false, isDomain: true, domainName: 'LxTipoLogradouro', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: 'Lx Tipo Logradouro', width: '270px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxTipoLogradouroName', isDomain: true, domainName: 'LxTipoLogradouro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Lx Tipo Logradouro (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'Municipio', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Municipio', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeAutenticacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Nome Autenticacao', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeCurtoUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Nome Curto Usuario', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Nome Usuario', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Numero', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Numero', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ObsEndereco', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Obs Endereco', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Ramal', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 6, validateMaxLength: true, isPartOfKey: false, headerText: 'Ramal', width: '101px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Uf', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 4, validateMaxLength: true, isPartOfKey: false, headerText: 'Uf', width: '62px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'UidUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 36, isPartOfKey: false, headerText: 'Uid Usuario', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'VigenciaFinal', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: false, headerText: 'Vigencia Final', width: '218px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'VigenciaInicial', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: false, headerText: 'Vigencia Inicial', width: '244px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsUsuario'] = [ 
        { name: 'TcsUsuario', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkMensagem/GetTcsUsuarioToExcel', actionReport: 'LinxFrameworkMensagem/GetTcsUsuarioToReportXml', actionFeed: 'LinxFrameworkMensagemOData/TcsUsuario', actionName: 'LinxFrameworkMensagem/GetTcsUsuarioByEntitySearchNoAssociations', display: 'TcsUsuario',  metaData: function() { return metadataInfo['TcsUsuario']; } }
    ];
    entitylookUps.push('TcsUsuario');
    entitylookUps['TcsUsuario'] = [];
    entityNames.push('NewMessageInfo');
    metadataInfo['NewMessageInfo'] = [
        { key: 'Titulo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 100, validateMaxLength: true, isPartOfKey: false, headerText: 'Titulo', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Corpo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Corpo', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Filtro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Filtro', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DataEnvio', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Envio', width: '120px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'IdLinx', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 12, isPartOfKey: true, headerText: 'Id Linx', width: '151px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxTipoMensagem', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'LxTipoMensagem', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['NewMessageInfo'] = [ 
        { name: 'NewMessageInfo', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkMensagem/GetNewMessageInfoToExcel', actionReport: 'LinxFrameworkMensagem/GetNewMessageInfoToReportXml', actionFeed: 'LinxFrameworkMensagemOData/NewMessageInfo', actionName: 'LinxFrameworkMensagem/GetNewMessageInfoByEntitySearchNoAssociations', display: 'NewMessageInfo',  metaData: function() { return metadataInfo['NewMessageInfo']; } }
    ];
    entitylookUps.push('NewMessageInfo');
    entitylookUps['NewMessageInfo'] = [];
    entityNames.push('TcsMensagemLogDetail');
    metadataInfo['TcsMensagemLogDetail'] = [
        { key: 'Dispensada', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Dispensada', width: '166px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'Entregue', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Entregue', width: '140px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'IdTcsMensagem', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Tcs Mensagem', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdTcsMensagemLog', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Tcs Mensagem Log', width: '283px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Usuario', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'Lida', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Lida', width: '120px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null }
    ];
    entityNames.push('TcsMensagemLogDetailParentComposition');
    metadataInfo['TcsMensagemLogDetailParentComposition'] = [
        { key: 'Dispensada', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Dispensada', width: '166px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'Entregue', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Entregue', width: '140px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'IdTcsMensagem', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Tcs Mensagem', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdTcsMensagemLog', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Tcs Mensagem Log', width: '283px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Usuario', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'Lida', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Lida', width: '120px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'Corpo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Corpo', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Criacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: false, headerText: 'Criacao', width: '127px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'Envio', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: false, headerText: 'Envio', width: '120px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'Filtro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Filtro', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdLinx', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Linx', width: '151px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxTipoMensagem', isQbeZero: false, isDomain: true, domainName: 'TipoMensagem', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: 'Lx Tipo Mensagem', width: '244px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxTipoMensagemName', isDomain: true, domainName: 'TipoMensagem', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Lx Tipo Mensagem (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'Titulo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 100, validateMaxLength: true, isPartOfKey: false, headerText: 'Titulo', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsMensagemLogDetail'] = [ 
        { name: 'TcsMensagemLogDetail', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkMensagem/GetTcsMensagemLogDetailToExcel', actionReport: 'LinxFrameworkMensagem/GetTcsMensagemLogDetailToReportXml', actionFeed: 'LinxFrameworkMensagemOData/TcsMensagemLogDetail', actionName: 'LinxFrameworkMensagem/GetTcsMensagemLogDetailByEntitySearchNoAssociations', display: 'TcsMensagemLogDetail',  metaData: function() { return metadataInfo['TcsMensagemLogDetail']; } }
    ];
    entitylookUps.push('TcsMensagemLogDetail');
    entitylookUps['TcsMensagemLogDetail'] = [];
    entityNames.push('TcsMensagemConsulta');
    metadataInfo['TcsMensagemConsulta'] = [
        { key: 'Corpo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Corpo', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Criacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: false, headerText: 'Criação', width: '135px', dataType: 'date', format: 'dd/MM/yyyy HH:mm', hidden: false, unbound: false, group: null },
        { key: 'Envio', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: false, headerText: 'Envio', width: '135px', dataType: 'date', format: 'dd/MM/yyyy HH:mm', hidden: false, unbound: false, group: null },
        { key: 'Filtro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Filtro', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdLinx', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdLinx', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Linx', width: '151px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdTcsMensagem', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Tcs Mensagem', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdUsuario', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Usuario', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxTipoMensagem', isQbeZero: false, isDomain: true, domainName: 'TipoMensagem', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: 'Tipo Mensagem', width: '205px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxTipoMensagemName', isDomain: true, domainName: 'TipoMensagem', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Tipo Mensagem (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'NomeEmpresa', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'NomeEmpresa', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Empresa', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Titulo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 100, validateMaxLength: true, isPartOfKey: false, headerText: 'Título', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeAutenticacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'NomeAutenticacao', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Usuário Autenticação', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'NomeUsuario', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Emitente', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsMensagemConsulta'] = [ 
        { name: 'TcsMensagemConsulta', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkMensagem/GetTcsMensagemConsultaToExcel', actionReport: 'LinxFrameworkMensagem/GetTcsMensagemConsultaToReportXml', actionFeed: 'LinxFrameworkMensagemOData/TcsMensagemConsulta', actionName: 'LinxFrameworkMensagem/GetTcsMensagemConsultaByEntitySearchNoAssociations', display: 'TcsMensagemConsulta',  metaData: function() { return metadataInfo['TcsMensagemConsulta']; } }
        , { name: 'TcsMensagemConsultaLog', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkMensagem/GetTcsMensagemConsultaLogParentCompositionToExcel', actionReport: 'LinxFrameworkMensagem/GetTcsMensagemConsultaLogParentCompositionToReportXml', actionFeed: 'LinxFrameworkMensagemOData/TcsMensagemConsultaLogParentComposition', actionName: 'LinxFrameworkMensagem/GetTcsMensagemConsultaLogParentCompositionByEntitySearchNoAssociations', display: 'TcsMensagemConsultaLog',  metaData: function() { return metadataInfo['TcsMensagemConsultaLogParentComposition']; } }
    ];
    entitylookUps.push('TcsMensagemConsulta');
    entitylookUps['TcsMensagemConsulta'] = [];
    entitylookUps['TcsMensagemConsulta'].push('LookUpTcsEmpresaAutenticacaoC');
    lookUpNames.push('LookUpTcsEmpresaAutenticacaoC');
    metadataInfo['LookUpTcsEmpresaAutenticacaoC'] = [
        { key: 'IdLinx', relatedKey: 'IdLinx', maxLength: 12, isPartOfKey: true, headerText: 'Id Linx', width: '130px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'NomeEmpresa', relatedKey: 'NomeEmpresa', maxLength: 250, isPartOfKey: false, headerText: 'Empresa', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    entitylookUps['TcsMensagemConsulta'].push('LookUpTcsUsuarioAutenticacaoC');
    lookUpNames.push('LookUpTcsUsuarioAutenticacaoC');
    metadataInfo['LookUpTcsUsuarioAutenticacaoC'] = [
        { key: 'IdUsuario', relatedKey: 'IdUsuario', maxLength: 24, isPartOfKey: true, headerText: 'Id Usuario', width: '250px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'NomeUsuario', relatedKey: 'NomeUsuario', maxLength: 250, isPartOfKey: false, headerText: 'Nome', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeAutenticacao', relatedKey: 'NomeAutenticacao', maxLength: 250, isPartOfKey: false, headerText: 'Usuário Autenticação', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    entityNames.push('TcsMensagemConsultaLog');
    metadataInfo['TcsMensagemConsultaLog'] = [
        { key: 'Dispensada', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Dispensada', width: '166px', dataType: 'date', format: 'dd/MM/yyyy HH:mm', hidden: false, unbound: false, group: null },
        { key: 'Entregue', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Entregue', width: '140px', dataType: 'date', format: 'dd/MM/yyyy HH:mm', hidden: false, unbound: false, group: null },
        { key: 'IdTcsMensagem', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Tcs Mensagem', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdTcsMensagemLog', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Tcs Mensagem Log', width: '283px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdUsuario', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Usuario', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'Lida', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Lida', width: '135px', dataType: 'date', format: 'dd/MM/yyyy HH:mm', hidden: false, unbound: false, group: null },
        { key: 'NomeAutenticacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'NomeAutenticacao', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Usuário Autenticação', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'NomeUsuario', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Emitente', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    entityNames.push('TcsMensagemConsultaLogParentComposition');
    metadataInfo['TcsMensagemConsultaLogParentComposition'] = [
        { key: 'Dispensada', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Dispensada', width: '166px', dataType: 'date', format: 'dd/MM/yyyy HH:mm', hidden: false, unbound: false, group: null },
        { key: 'Entregue', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Entregue', width: '140px', dataType: 'date', format: 'dd/MM/yyyy HH:mm', hidden: false, unbound: false, group: null },
        { key: 'IdTcsMensagem', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Tcs Mensagem', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdTcsMensagemLog', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Tcs Mensagem Log', width: '283px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdUsuario', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Usuario', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'Lida', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 10, isPartOfKey: false, headerText: 'Lida', width: '135px', dataType: 'date', format: 'dd/MM/yyyy HH:mm', hidden: false, unbound: false, group: null },
        { key: 'NomeAutenticacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'NomeAutenticacao', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Usuário Autenticação', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'NomeUsuario', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Emitente', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Corpo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Corpo', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Criacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: false, headerText: 'Criação', width: '135px', dataType: 'date', format: 'dd/MM/yyyy HH:mm', hidden: false, unbound: false, group: null },
        { key: 'Envio', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: false, headerText: 'Envio', width: '135px', dataType: 'date', format: 'dd/MM/yyyy HH:mm', hidden: false, unbound: false, group: null },
        { key: 'Filtro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Filtro', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdLinx', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Linx', width: '151px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxTipoMensagem', isQbeZero: false, isDomain: true, domainName: 'TipoMensagem', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: 'Tipo Mensagem', width: '205px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxTipoMensagemName', isDomain: true, domainName: 'TipoMensagem', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Tipo Mensagem (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'NomeEmpresa', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Empresa', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'Titulo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 100, validateMaxLength: true, isPartOfKey: false, headerText: 'Título', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsMensagemConsultaLog'] = [ 
        { name: 'TcsMensagemConsultaLog', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkMensagem/GetTcsMensagemConsultaLogToExcel', actionReport: 'LinxFrameworkMensagem/GetTcsMensagemConsultaLogToReportXml', actionFeed: 'LinxFrameworkMensagemOData/TcsMensagemConsultaLog', actionName: 'LinxFrameworkMensagem/GetTcsMensagemConsultaLogByEntitySearchNoAssociations', display: 'TcsMensagemConsultaLog',  metaData: function() { return metadataInfo['TcsMensagemConsultaLog']; } }
    ];
    entitylookUps.push('TcsMensagemConsultaLog');
    entitylookUps['TcsMensagemConsultaLog'] = [];
    entitylookUps['TcsMensagemConsultaLog'].push('LookUpTcsUsuarioAutenticacaoCL');
    lookUpNames.push('LookUpTcsUsuarioAutenticacaoCL');
    metadataInfo['LookUpTcsUsuarioAutenticacaoCL'] = [
        { key: 'IdUsuario', relatedKey: 'IdUsuario', maxLength: 24, isPartOfKey: true, headerText: 'Id', width: '250px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'NomeUsuario', relatedKey: 'NomeUsuario', maxLength: 250, isPartOfKey: false, headerText: 'Nome', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeAutenticacao', relatedKey: 'NomeAutenticacao', maxLength: 250, isPartOfKey: false, headerText: 'Usuário Autenticação', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    var lookUpProperties = [];
    //#endregion Metadata Info
    //#region dataParameters
    var dataParameters = {
        isLoaded: false,
        parameters: [],
        registerParameters: function (parameterList, complete) {
            if (parameterList !== '') {
                var variation = '{TBC_GRUPO_ECONOMICO|' + managerAuth.loginInfo.IdLinxGrupoEconomico.toString() + '|TCS_USUARIO|' + managerAuth.loginInfo.UidUsuario + (vm != null && vm.getBandeiraRede() > 0 ? '|TBC_BANDEIRA_REDE|' + vm.getBandeiraRede().toString() : '') + '}';
                $.ajax({
                    type: 'GET',
                    url: getServiceAddress('LinxFrameworkParametro') + '/GetParameterValue?serializedParameterList=' + stringReplace(parameterList, '{}', variation),
                    dataType: 'json',
                    cache: false,
                    headers: managerAuth.getHeaders(),
                    error: function (jqXHR, textStatus, errorThrown) {
                        var msg = 'Os seguintes parâmetros não foram pesquisados: [' + parameterList + ']';
                        app.showMessage(msg, 'Alerta', ['Ok']);
                        dataParameters.isLoaded = true;
                    },
                    success: function (data) {
                        var parametersName = '';
                        var parameters = data.split('#');
                        for (var idx in parameters) {
                            var values = parameters[idx].split('|');
                            var pName = values[0];
                            var pValue = values[1];
                            dataParameters.parameters[pName] = pValue;
                        }
                        dataParameters.isLoaded = true;
                        if (complete) complete();
                    }
                });
            }
        }
    };
    //#endregion dataParameters
    //#region Classes Map
    var sequences = [];
    var getNextSequence = function(entityName) {
        if (!sequences[entityName]) resetSequence(entityName);
        sequence = sequences[entityName];
        sequences[entityName]++;
        return sequence;
    };
    var resetSequence = function(entityName) {
        sequences[entityName] = 0;
    };
    var getSequence = function(entityName) {
        if (!sequences[entityName]) resetSequence(entityName);
        return sequences[entityName];
    };
    
    // Configure TcsMensagem data type
    metadataStore.addEntityType({
    shortName: "TcsMensagem",
    namespace: "Linx.Framework.BV.Mensagem",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    Corpo: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,Criacao: { dataType: DataType.DateTime, isNullable: false, isPartOfKey: false, defaultValue: '', validators: [ Validator.hasValueValidator]  }
    ,Envio: { dataType: DataType.DateTime, isNullable: false, isPartOfKey: false, defaultValue: '', validators: [ Validator.hasValueValidator]  }
    ,Filtro: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdLinx: { dataType: DataType.Int32, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdTcsMensagem: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,IdUsuario: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,LxTipoMensagem: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxTipoMensagemName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,Titulo: { dataType: DataType.String, maxLength: 100, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 100})]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
    TcsMensagemLogDetailList: { entityTypeName: "TcsMensagemLogDetail:#Linx.Framework.BV.Mensagem", isScalar: false, invForeignKeyNames: ["IdTcsMensagem"], associationName: "FK_TcsMensagem_TcsMensagemLogDetail" }
                          }
    });
    lookUpProperties['TcsMensagem'] = {};
    var TcsMensagemInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsMensagem') : ko.observable(getNextSequence('TcsMensagem')));
       ownerReference.currentTcsMensagemLogDetail = ko.observable(null);
       //Adjust details for a POCO reference
       if (isPOCO === true) {
           ownerReference.TcsMensagemLogDetailList = ko.observableArray(ownerReference.TcsMensagemLogDetailList);
       }
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           if (noDetails !== true && ownerReference.TcsMensagemLogDetailList && ownerReference.TcsMensagemLogDetailList().length > 0) {
             var detailExpr = ownerReference.TcsMensagemLogDetailList()[0].getJExpression(listFilterRange, ['IdTcsMensagem']);
             if (detailExpr === 'Error') return detailExpr;
             jExpression += detailExpr;
           }
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               if (isNullOrEmpty(propertyName)) {
                   for (var i = 0; i < ownerReference.TcsMensagemLogDetailList().length; i++) {
                       var detail = ownerReference.TcsMensagemLogDetailList()[i];
                       errors = errors.concat(detail.getValidationErrors());
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           if (loadDetails) {
               result.TcsMensagemLogDetailList = [];
               var sourceList = getAbsoluteValue(ownerReference.TcsMensagemLogDetailList);
               if (sourceList && sourceList.length > 0) {
                   for (var i = 0; i < sourceList.length; i++) {
                       if (['U', 'I', 'D'].indexOf(sourceList[i].ChangeState) >= 0) result.TcsMensagemLogDetailList.push(sourceList[i].getPrimitiveDTO(sourceList[i].ChangeState != 'D'));
                   }
               }
           }
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           var _TcsMensagemLogDetailList = getAbsoluteValue(ownerReference.TcsMensagemLogDetailList);
           if (_TcsMensagemLogDetailList && _TcsMensagemLogDetailList.length > 0) {
               for (var i = 0; i < _TcsMensagemLogDetailList.length; i++) {
                   var detail = _TcsMensagemLogDetailList[i];
                   if (['U', 'I', 'D'].indexOf(detail.ChangeState) >= 0) {
                       result.push(detail);
                       result = result.concat(detail.getAllDetailChanges());
                   }
               }
           }
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
           if (copyDetails) {
               if (ownerReference.TcsMensagemLogDetailList && originData.TcsMensagemLogDetailList) {
                   var toList = getAbsoluteValue(ownerReference.TcsMensagemLogDetailList);
                   var fromList = getAbsoluteValue(originData.TcsMensagemLogDetailList);
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                      if (toList[idxElem].ChangeState === 'D') toList.splice(idxElem, 1);
                   }
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                          if (toList[idxElem].ChangeState !== 'N') {
                               var fromObj = [];
                               if (toList[idxElem].ChangeState == 'I') {
                                   fromObj = _.where(fromList, { TemporaryIdTcsMensagemLog: toList[idxElem]['IdTcsMensagemLog'] });
                               } else {
                                   fromObj = _.where(fromList, { IdTcsMensagemLog: toList[idxElem]['IdTcsMensagemLog'] });
                               }
                               if (fromObj.length > 0) toList[idxElem].copyDataFrom(fromObj[0], true);
                          }
                   }
               }
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
              vm.dataBind('TcsMensagemLogDetailList', true);
              if (ownerReference.currentTcsMensagemLogDetail()) ownerReference.currentTcsMensagemLogDetail().commitDetailsVisualPendings();
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsMensagem{' + 'IdTcsMensagem#==#L' + getAbsoluteValue(ownerReference.IdTcsMensagem).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsMensagemByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              breezeReference.TcsMensagemLogDetailIsLoaded = ownerReference.TcsMensagemLogDetailIsLoaded;
              for (var idx = 0; idx < ownerReference.TcsMensagemLogDetailList().length; idx++) {
                  var entity = ownerReference.TcsMensagemLogDetailList()[idx];
                  if (entity.isPOCO)  {
                      var newReference = createEntity(entity.typeName, entity.getPrimitiveDTO(), true);
                      entity.enableDetailsDataTack(newReference);
                  }
              }
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['Corpo'] = 'S';
       ownerReference.serverDataType['Criacao'] = 'T';
       ownerReference.serverDataType['Envio'] = 'T';
       ownerReference.serverDataType['Filtro'] = 'S';
       ownerReference.serverDataType['IdLinx'] = 'I';
       ownerReference.serverDataType['IdTcsMensagem'] = 'L';
       ownerReference.serverDataType['IdUsuario'] = 'L';
       ownerReference.serverDataType['LxTipoMensagem'] = 'Y';
       ownerReference.serverDataType['Titulo'] = 'S';
       ownerReference.typeName = 'TcsMensagem';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdTcsMensagem' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsMensagem !== 'function') { return; }
               else  if (cacheElements[idxR].TcsMensagem() != ownerReference) { cacheElements[idxR].TcsMensagem(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (!isNullOrEmpty(ownerReference.TcsMensagemLogDetailList()) && ownerReference.TcsMensagemLogDetailList().length > 0) {
              var details = [].concat(ownerReference.TcsMensagemLogDetailList());
              for (var idx = 0; idx < details.length; idx++) {
                details[idx].delete();
              }
           }
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Mensagem';
       ownerReference.myProperties = [ 'Corpo','Criacao','Envio','Filtro','IdLinx','IdTcsMensagem','IdUsuario','LxTipoMensagem','Titulo' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
       if (!isNullOrEmpty(ownerReference.currentTcsMensagemLogDetail())) { result = result.concat(ownerReference.currentTcsMensagemLogDetail().getCurrentElements()); }
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.GetJsWhereDetailRelationForTcsMensagemLogDetail = function(customParentRelation) {
       return 'TcsMensagemLogDetail{' + (!isNullOrEmpty(customParentRelation) ? customParentRelation : 'IdTcsMensagem#==#' + ownerReference.serverDataType['IdTcsMensagem'] + getAbsoluteValue(ownerReference.IdTcsMensagem).toString()) + '}';    
       }
       ownerReference.TcsMensagemLogDetailIsLoaded = false;
       ownerReference.detailsLoaded = function() {
           return ownerReference.TcsMensagemLogDetailIsLoaded;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return ownerReference.TcsMensagemLogDetailIsLoaded;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
           ownerReference.TcsMensagemLogDetailIsLoaded = value;
           if (value === false && ownerReference.isPOCO)
               ownerReference.TcsMensagemLogDetailList([]);
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (ownerReference.isAdded()) {
            ownerReference.TcsMensagemLogDetailIsLoaded = true;
          }
          var _TcsMensagemLogDetailRemoteComplete = false;
          var detachList_TcsMensagemLogDetail = [];
          if (force) {
               if (isNullOrEmpty(detailName) || detailName == 'TcsMensagemLogDetail') ownerReference.TcsMensagemLogDetailIsLoaded = false;
               if ((isNullOrEmpty(detailName) || detailName == 'TcsMensagemLogDetail') && ownerReference.TcsMensagemLogDetailList && ownerReference.TcsMensagemLogDetailList().length > 0) {
                   if (ownerReference.isPOCO) {
                       ownerReference.TcsMensagemLogDetailList([]);
                   } else {
                       var detailList = ownerReference.TcsMensagemLogDetailList();
                       for (var idx = detailList.length - 1; idx >= 0; idx--) {
                           detachList_TcsMensagemLogDetail.push(detailList[idx]);
                       }
                   }
               }
          }
    
          if (!ownerReference.TcsMensagemLogDetailIsLoaded) {
            //Load TcsMensagemLogDetailList
            if (isNullOrEmpty(detailName) || detailName === 'TcsMensagemLogDetail') {
              ownerReference.TcsMensagemLogDetailIsLoaded = true;
              _TcsMensagemLogDetailRemoteComplete = (ownerReference.TcsMensagemLogDetailList && ownerReference.TcsMensagemLogDetailList().length > 0);
              if ((force || !ownerReference.TcsMensagemLogDetailList || ownerReference.TcsMensagemLogDetailList().length === 0) && (!isNullOrEmpty(getAbsoluteValue(ownerReference.IdTcsMensagem)))) {
                var navQuery = EntityQuery.from('GetTcsMensagemLogDetailByEntitySearchNoAssociations').noTracking(ownerReference.isPOCO === true)
                .orderBy('IdTcsMensagemLog asc')
                    .withParameters({ jEntitySearch: ownerReference.GetJsWhereDetailRelationForTcsMensagemLogDetail(customParentRelation) })    ;
                if (!vm.dataToolbar._noBusyLoading) vm.showProcessing('Pesquisando detalhes...');
                manager.executeQuery(navQuery).then(function (data) { if (ownerReference.isPOCO) { for (var idx = 0; idx < data.results.length; idx++) { initializePOCO(data.results[idx], 'TcsMensagemLogDetail'); data.results[idx].TcsMensagem = ko.observable(ownerReference); } ownerReference.TcsMensagemLogDetailList(data.results); } 
                   if (!ownerReference.isPOCO && detachList_TcsMensagemLogDetail.length > 0)
                   {
                       for (var idx = 0; idx < detachList_TcsMensagemLogDetail.length; idx++)
                       {
                           if (!data.results.contains(detachList_TcsMensagemLogDetail[idx]))
                               detachEntity(detachList_TcsMensagemLogDetail[idx]);
                           else {
                               if (force && detachList_TcsMensagemLogDetail[idx].atLeastOneDetailLoaded())
                                   detachList_TcsMensagemLogDetail[idx].fillDetails(force, '', false, noWait);
                           }
                       }
                   }
                   ownerReference.setCurrentDetails('TcsMensagemLogDetail'); notifyPresentation('TcsMensagemLogDetailList');
                   _TcsMensagemLogDetailRemoteComplete = true;
                   if (callback && (!isNullOrEmpty(detailName) || (_TcsMensagemLogDetailRemoteComplete))) { callback(); }
                }).fail(queryFailed).fin(function() { if (!vm.dataToolbar._noBusyLoading) vm.closeProcessing(); });
              } else { ownerReference.setCurrentDetails('TcsMensagemLogDetail'); notifyPresentation('TcsMensagemLogDetailList'); }
            } else { _TcsMensagemLogDetailRemoteComplete = true; if (!ownerReference.TcsMensagemLogDetailIsLoaded && ownerReference.TcsMensagemLogDetailList && ownerReference.TcsMensagemLogDetailList().length > 0) { ownerReference.TcsMensagemLogDetailIsLoaded = true; ownerReference.setCurrentDetails('TcsMensagemLogDetail'); } }
          } else { 
            if (isNullOrEmpty(detailName) || detailName == 'TcsMensagemLogDetail') {
               notifyPresentation('TcsMensagemLogDetailList');
               ownerReference.setCurrentDetails('TcsMensagemLogDetail');
            }
            _TcsMensagemLogDetailRemoteComplete = true;
          }
          if (callback && ((!isNullOrEmpty(detailName) && (eval('_' + detailName + 'RemoteComplete && ownerReference.' + detailName + 'IsLoaded') == true)) || (isNullOrEmpty(detailName) && (_TcsMensagemLogDetailRemoteComplete)))) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
          if ((isNullOrEmpty(detailName) || detailName === 'TcsMensagemLogDetail')) {
               if (ownerReference.TcsMensagemLogDetailList().length > 0) { ownerReference.currentTcsMensagemLogDetail(ownerReference.TcsMensagemLogDetailList()[0]); if (clearing == null || clearing === false) ownerReference.currentTcsMensagemLogDetail().fillDetails(); }
               else { ownerReference.currentTcsMensagemLogDetail(null); ownerReference.notifyEmptyDetails('TcsMensagemLogDetail'); }
          }
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
          if (detailName === 'TcsMensagemLogDetail') {
               notifyPresentation('TcsMensagemLogDetailList');
               vm.queryInnerUIs(null, 'TcsMensagemLogDetail');
          }
       };
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxTipoMensagemName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxTipoMensagemName(''); return; }
                   var value = (dataDomains.getId('TipoMensagem', newValue));
                   if (value != ownerReference.LxTipoMensagem()) {
                       ownerReference.LxTipoMensagem(value);
                   }
            });
    
           ownerReference.LxTipoMensagem.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxTipoMensagem(0); return; }
                   var value = dataDomains.getName('TipoMensagem', newValue);
                   if (value != ownerReference.LxTipoMensagemName()) {
                       ownerReference.LxTipoMensagemName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    //#region Adjust details already loaded for a POCO reference
       if (isPOCO === true) {
           if ((typeof ownerReference.TcsMensagemLogDetailList === 'function') && ownerReference.TcsMensagemLogDetailList().length > 0) {
                for(var idx = 0; idx < ownerReference.TcsMensagemLogDetailList().length; idx++) { TcsMensagemLogDetailInitializer(ownerReference.TcsMensagemLogDetailList()[idx], isPOCO); }
           }
       }
    //#endregion Adjust details already loaded for a POCO reference
    };
    metadataStore.registerEntityTypeCtor("TcsMensagem", null, TcsMensagemInitializer);
    
    // Configure TcsMensagemLogDetail data type
    metadataStore.addEntityType({
    shortName: "TcsMensagemLogDetail",
    namespace: "Linx.Framework.BV.Mensagem",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    Dispensada: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,Entregue: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IdTcsMensagem: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdTcsMensagemLog: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,IdUsuario: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,Lida: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns a single parent and associates with Details
    TcsMensagem: { entityTypeName: "TcsMensagem:#Linx.Framework.BV.Mensagem", isScalar: true, foreignKeyNames: ["IdTcsMensagem"], associationName: "FK_TcsMensagem_TcsMensagemLogDetail" }
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsMensagemLogDetail'] = {};
    var TcsMensagemLogDetailInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsMensagemLogDetail') : ko.observable(getNextSequence('TcsMensagemLogDetail')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsMensagemLogDetail{' + 'IdTcsMensagemLog#==#L' + getAbsoluteValue(ownerReference.IdTcsMensagemLog).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsMensagemLogDetailByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['Dispensada'] = 'T';
       ownerReference.serverDataType['Entregue'] = 'T';
       ownerReference.serverDataType['IdTcsMensagem'] = 'L';
       ownerReference.serverDataType['IdTcsMensagemLog'] = 'L';
       ownerReference.serverDataType['IdUsuario'] = 'L';
       ownerReference.serverDataType['Lida'] = 'T';
       ownerReference.typeName = 'TcsMensagemLogDetail';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdTcsMensagemLog' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsMensagemLogDetail !== 'function') { return; }
               else  if (cacheElements[idxR].TcsMensagemLogDetail() != ownerReference) { cacheElements[idxR].TcsMensagemLogDetail(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           var parent = getAbsoluteValue(ownerReference.TcsMensagem);
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
           if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsMensagemLogDetailList === 'function') && parent.TcsMensagemLogDetailList().length == 0) parent.setCurrentDetails('TcsMensagemLogDetail');
       };
       ownerReference.setParentAsModified = function() {
       var parent = getAbsoluteValue(ownerReference.TcsMensagem);
       if (parent) {
           if (parent.isUnchanged()) {
               parent.setModified(); 
           }
           parent.setParentAsModified();
       }
       };
       ownerReference.getParent = function() {
           return getAbsoluteValue(ownerReference.TcsMensagem);
       };
       ownerReference.getSelfList = function() {
           var parent = ownerReference.getParent();
           if (!isNullOrEmpty(parent)) {
               return getAbsoluteValue(parent.TcsMensagemLogDetailList);
           } else { return null; }
       };
       ownerReference.namespace = 'Linx.Framework.BV.Mensagem';
       ownerReference.myProperties = [ 'Dispensada','Entregue','IdTcsMensagem','IdTcsMensagemLog','IdUsuario','Lida' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("TcsMensagemLogDetail", null, TcsMensagemLogDetailInitializer);
    
    // Configure MensagemInfo data type
    metadataStore.addEntityType({
    shortName: "MensagemInfo",
    namespace: "Linx.Framework.BV.Mensagem",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    IdTcsMensagemLog: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,Titulo: { dataType: DataType.String, maxLength: 100, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 100})]  }
    ,Corpo: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,Lida: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,Entregue: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,TipoMensagem: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['MensagemInfo'] = {};
    var MensagemInfoInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('MensagemInfo') : ko.observable(getNextSequence('MensagemInfo')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'MensagemInfo{' + 'IdTcsMensagemLog#==#L' + getAbsoluteValue(ownerReference.IdTcsMensagemLog).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getMensagemInfoByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['IdTcsMensagemLog'] = 'L';
       ownerReference.serverDataType['Titulo'] = 'S';
       ownerReference.serverDataType['Corpo'] = 'S';
       ownerReference.serverDataType['Lida'] = 'B';
       ownerReference.serverDataType['Entregue'] = 'T';
       ownerReference.serverDataType['TipoMensagem'] = 'S';
       ownerReference.typeName = 'MensagemInfo';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdTcsMensagemLog' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].MensagemInfo !== 'function') { return; }
               else  if (cacheElements[idxR].MensagemInfo() != ownerReference) { cacheElements[idxR].MensagemInfo(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Mensagem';
       ownerReference.myProperties = [ 'IdTcsMensagemLog','Titulo','Corpo','Lida','Entregue','TipoMensagem' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("MensagemInfo", null, MensagemInfoInitializer);
    
    // Configure TcsMensagemUsuario data type
    metadataStore.addEntityType({
    shortName: "TcsMensagemUsuario",
    namespace: "Linx.Framework.BV.Mensagem",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    Corpo: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,Dispensada: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,Entregue: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,Envio: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IdLinx: { dataType: DataType.Int32, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdTcsMensagem: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdTcsMensagemLog: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,IdUsuario: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,Lida: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,LxTipoMensagem: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxTipoMensagemName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,Titulo: { dataType: DataType.String, maxLength: 100, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 100})]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsMensagemUsuario'] = {};
    var TcsMensagemUsuarioInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsMensagemUsuario') : ko.observable(getNextSequence('TcsMensagemUsuario')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsMensagemUsuario{' + 'IdTcsMensagemLog#==#L' + getAbsoluteValue(ownerReference.IdTcsMensagemLog).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsMensagemUsuarioByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['Corpo'] = 'S';
       ownerReference.serverDataType['Dispensada'] = 'T';
       ownerReference.serverDataType['Entregue'] = 'T';
       ownerReference.serverDataType['Envio'] = 'T';
       ownerReference.serverDataType['IdLinx'] = 'I';
       ownerReference.serverDataType['IdTcsMensagem'] = 'L';
       ownerReference.serverDataType['IdTcsMensagemLog'] = 'L';
       ownerReference.serverDataType['IdUsuario'] = 'L';
       ownerReference.serverDataType['Lida'] = 'T';
       ownerReference.serverDataType['LxTipoMensagem'] = 'Y';
       ownerReference.serverDataType['Titulo'] = 'S';
       ownerReference.typeName = 'TcsMensagemUsuario';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdTcsMensagemLog' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsMensagemUsuario !== 'function') { return; }
               else  if (cacheElements[idxR].TcsMensagemUsuario() != ownerReference) { cacheElements[idxR].TcsMensagemUsuario(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Mensagem';
       ownerReference.myProperties = [ 'Corpo','Dispensada','Entregue','Envio','IdLinx','IdTcsMensagem','IdTcsMensagemLog','IdUsuario','Lida','LxTipoMensagem','Titulo' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxTipoMensagemName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxTipoMensagemName(''); return; }
                   var value = (dataDomains.getId('TipoMensagem', newValue));
                   if (value != ownerReference.LxTipoMensagem()) {
                       ownerReference.LxTipoMensagem(value);
                   }
            });
    
           ownerReference.LxTipoMensagem.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxTipoMensagem(0); return; }
                   var value = dataDomains.getName('TipoMensagem', newValue);
                   if (value != ownerReference.LxTipoMensagemName()) {
                       ownerReference.LxTipoMensagemName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsMensagemUsuario", null, TcsMensagemUsuarioInitializer);
    
    // Configure TcsMensagemLog data type
    metadataStore.addEntityType({
    shortName: "TcsMensagemLog",
    namespace: "Linx.Framework.BV.Mensagem",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    Dispensada: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,Entregue: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IdTcsMensagem: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdTcsMensagemLog: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,IdUsuario: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,Lida: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsMensagemLog'] = {};
    var TcsMensagemLogInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsMensagemLog') : ko.observable(getNextSequence('TcsMensagemLog')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsMensagemLog{' + 'IdTcsMensagemLog#==#L' + getAbsoluteValue(ownerReference.IdTcsMensagemLog).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsMensagemLogByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['Dispensada'] = 'T';
       ownerReference.serverDataType['Entregue'] = 'T';
       ownerReference.serverDataType['IdTcsMensagem'] = 'L';
       ownerReference.serverDataType['IdTcsMensagemLog'] = 'L';
       ownerReference.serverDataType['IdUsuario'] = 'L';
       ownerReference.serverDataType['Lida'] = 'T';
       ownerReference.typeName = 'TcsMensagemLog';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdTcsMensagemLog' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsMensagemLog !== 'function') { return; }
               else  if (cacheElements[idxR].TcsMensagemLog() != ownerReference) { cacheElements[idxR].TcsMensagemLog(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Mensagem';
       ownerReference.myProperties = [ 'Dispensada','Entregue','IdTcsMensagem','IdTcsMensagemLog','IdUsuario','Lida' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("TcsMensagemLog", null, TcsMensagemLogInitializer);
    
    // Configure TcsPerfil data type
    metadataStore.addEntityType({
    shortName: "TcsPerfil",
    namespace: "Linx.Framework.BV.Mensagem",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    IdTcsUsuarioPerfil: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,IdUsuario: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,DescPerfil: { dataType: DataType.String, maxLength: 60, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 60})]  }
    ,IdPerfil: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsPerfil'] = {};
    var TcsPerfilInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsPerfil') : ko.observable(getNextSequence('TcsPerfil')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsPerfil{' + 'IdTcsUsuarioPerfil#==#L' + getAbsoluteValue(ownerReference.IdTcsUsuarioPerfil).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsPerfilByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['IdTcsUsuarioPerfil'] = 'L';
       ownerReference.serverDataType['IdUsuario'] = 'L';
       ownerReference.serverDataType['DescPerfil'] = 'S';
       ownerReference.serverDataType['IdPerfil'] = 'L';
       ownerReference.typeName = 'TcsPerfil';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdTcsUsuarioPerfil' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsPerfil !== 'function') { return; }
               else  if (cacheElements[idxR].TcsPerfil() != ownerReference) { cacheElements[idxR].TcsPerfil(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Mensagem';
       ownerReference.myProperties = [ 'IdTcsUsuarioPerfil','IdUsuario','DescPerfil','IdPerfil' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("TcsPerfil", null, TcsPerfilInitializer);
    
    // Configure TcsUsuario data type
    metadataStore.addEntityType({
    shortName: "TcsUsuario",
    namespace: "Linx.Framework.BV.Mensagem",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    AutenticacaoWindows: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,Bairro: { dataType: DataType.String, maxLength: 60, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 60})]  }
    ,Cep: { dataType: DataType.String, maxLength: 10, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 10})]  }
    ,CnpjCpf: { dataType: DataType.String, maxLength: 20, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 20})]  }
    ,Complemento: { dataType: DataType.String, maxLength: 60, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 60})]  }
    ,DataAlteracao: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,DataCadastro: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,DataExpiracaoSenha: { dataType: DataType.DateTime, isNullable: false, isPartOfKey: false, defaultValue: '', validators: [ Validator.hasValueValidator]  }
    ,Email: { dataType: DataType.String, maxLength: 250, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 250})]  }
    ,FoneCelular: { dataType: DataType.String, maxLength: 20, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 20})]  }
    ,FoneFixo: { dataType: DataType.String, maxLength: 20, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 20})]  }
    ,IdLinx: { dataType: DataType.Int32, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdUsuario: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,Inativo: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IndicaAcessoSuporte: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,InscrEstadualRg: { dataType: DataType.String, maxLength: 20, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 20})]  }
    ,Logradouro: { dataType: DataType.String, maxLength: 60, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 60})]  }
    ,LxPfjFisicaJuridica: { dataType: DataType.Byte, isNullable: true, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxPfjFisicaJuridicaName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,LxTipoLogradouro: { dataType: DataType.Byte, isNullable: true, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxTipoLogradouroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,Municipio: { dataType: DataType.String, maxLength: 60, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 60})]  }
    ,NomeAutenticacao: { dataType: DataType.String, maxLength: 250, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 250})]  }
    ,NomeCurtoUsuario: { dataType: DataType.String, maxLength: 60, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 60})]  }
    ,NomeUsuario: { dataType: DataType.String, maxLength: 250, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 250})]  }
    ,Numero: { dataType: DataType.String, maxLength: 60, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 60})]  }
    ,ObsEndereco: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,Ramal: { dataType: DataType.String, maxLength: 6, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 6})]  }
    ,Uf: { dataType: DataType.String, maxLength: 4, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 4})]  }
    ,UidUsuario: { dataType: DataType.Guid, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,VigenciaFinal: { dataType: DataType.DateTime, isNullable: false, isPartOfKey: false, defaultValue: '', validators: [ Validator.hasValueValidator]  }
    ,VigenciaInicial: { dataType: DataType.DateTime, isNullable: false, isPartOfKey: false, defaultValue: '', validators: [ Validator.hasValueValidator]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsUsuario'] = {};
    var TcsUsuarioInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsUsuario') : ko.observable(getNextSequence('TcsUsuario')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsUsuario{' + 'IdUsuario#==#L' + getAbsoluteValue(ownerReference.IdUsuario).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsUsuarioByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['AutenticacaoWindows'] = 'B';
       ownerReference.serverDataType['Bairro'] = 'S';
       ownerReference.serverDataType['Cep'] = 'S';
       ownerReference.serverDataType['CnpjCpf'] = 'S';
       ownerReference.serverDataType['Complemento'] = 'S';
       ownerReference.serverDataType['DataAlteracao'] = 'T';
       ownerReference.serverDataType['DataCadastro'] = 'T';
       ownerReference.serverDataType['DataExpiracaoSenha'] = 'T';
       ownerReference.serverDataType['Email'] = 'S';
       ownerReference.serverDataType['FoneCelular'] = 'S';
       ownerReference.serverDataType['FoneFixo'] = 'S';
       ownerReference.serverDataType['IdLinx'] = 'I';
       ownerReference.serverDataType['IdUsuario'] = 'L';
       ownerReference.serverDataType['Inativo'] = 'B';
       ownerReference.serverDataType['IndicaAcessoSuporte'] = 'B';
       ownerReference.serverDataType['InscrEstadualRg'] = 'S';
       ownerReference.serverDataType['Logradouro'] = 'S';
       ownerReference.serverDataType['LxPfjFisicaJuridica'] = 'Y';
       ownerReference.serverDataType['LxTipoLogradouro'] = 'Y';
       ownerReference.serverDataType['Municipio'] = 'S';
       ownerReference.serverDataType['NomeAutenticacao'] = 'S';
       ownerReference.serverDataType['NomeCurtoUsuario'] = 'S';
       ownerReference.serverDataType['NomeUsuario'] = 'S';
       ownerReference.serverDataType['Numero'] = 'S';
       ownerReference.serverDataType['ObsEndereco'] = 'S';
       ownerReference.serverDataType['Ramal'] = 'S';
       ownerReference.serverDataType['Uf'] = 'S';
       ownerReference.serverDataType['UidUsuario'] = 'G';
       ownerReference.serverDataType['VigenciaFinal'] = 'T';
       ownerReference.serverDataType['VigenciaInicial'] = 'T';
       ownerReference.typeName = 'TcsUsuario';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdUsuario' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsUsuario !== 'function') { return; }
               else  if (cacheElements[idxR].TcsUsuario() != ownerReference) { cacheElements[idxR].TcsUsuario(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Mensagem';
       ownerReference.myProperties = [ 'AutenticacaoWindows','Bairro','Cep','CnpjCpf','Complemento','DataAlteracao','DataCadastro','DataExpiracaoSenha','Email','FoneCelular','FoneFixo','IdLinx','IdUsuario','Inativo','IndicaAcessoSuporte','InscrEstadualRg','Logradouro','LxPfjFisicaJuridica','LxTipoLogradouro','Municipio','NomeAutenticacao','NomeCurtoUsuario','NomeUsuario','Numero','ObsEndereco','Ramal','Uf','UidUsuario','VigenciaFinal','VigenciaInicial' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxPfjFisicaJuridicaName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxPfjFisicaJuridicaName(''); return; }
                   var value = (dataDomains.getId('LX_PFJ_FISICA_JURIDICA', newValue));
                   if (value != ownerReference.LxPfjFisicaJuridica()) {
                       ownerReference.LxPfjFisicaJuridica(value);
                   }
            });
    
           ownerReference.LxPfjFisicaJuridica.subscribe(
           function (newValue) {
                   var value = (newValue == null) ? '' : dataDomains.getName('LX_PFJ_FISICA_JURIDICA', newValue);
                   if (value != ownerReference.LxPfjFisicaJuridicaName()) {
                       ownerReference.LxPfjFisicaJuridicaName(value);
               }
           });
       }
       if (isPOCO !== true) {
           ownerReference.LxTipoLogradouroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxTipoLogradouroName(''); return; }
                   var value = (dataDomains.getId('LxTipoLogradouro', newValue));
                   if (value != ownerReference.LxTipoLogradouro()) {
                       ownerReference.LxTipoLogradouro(value);
                   }
            });
    
           ownerReference.LxTipoLogradouro.subscribe(
           function (newValue) {
                   var value = (newValue == null) ? '' : dataDomains.getName('LxTipoLogradouro', newValue);
                   if (value != ownerReference.LxTipoLogradouroName()) {
                       ownerReference.LxTipoLogradouroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsUsuario", null, TcsUsuarioInitializer);
    
    // Configure NewMessageInfo data type
    metadataStore.addEntityType({
    shortName: "NewMessageInfo",
    namespace: "Linx.Framework.BV.Mensagem",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    Titulo: { dataType: DataType.String, maxLength: 100, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 100})]  }
    ,Corpo: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,Filtro: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,DataEnvio: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IdLinx: { dataType: DataType.Int32, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,LxTipoMensagem: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['NewMessageInfo'] = {};
    var NewMessageInfoInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('NewMessageInfo') : ko.observable(getNextSequence('NewMessageInfo')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'NewMessageInfo{' + 'IdLinx#==#I' + getAbsoluteValue(ownerReference.IdLinx).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getNewMessageInfoByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['Titulo'] = 'S';
       ownerReference.serverDataType['Corpo'] = 'S';
       ownerReference.serverDataType['Filtro'] = 'S';
       ownerReference.serverDataType['DataEnvio'] = 'T';
       ownerReference.serverDataType['IdLinx'] = 'I';
       ownerReference.serverDataType['LxTipoMensagem'] = 'Y';
       ownerReference.typeName = 'NewMessageInfo';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdLinx' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].NewMessageInfo !== 'function') { return; }
               else  if (cacheElements[idxR].NewMessageInfo() != ownerReference) { cacheElements[idxR].NewMessageInfo(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Mensagem';
       ownerReference.myProperties = [ 'Titulo','Corpo','Filtro','DataEnvio','IdLinx','LxTipoMensagem' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("NewMessageInfo", null, NewMessageInfoInitializer);
    
    // Configure TcsMensagemConsulta data type
    metadataStore.addEntityType({
    shortName: "TcsMensagemConsulta",
    namespace: "Linx.Framework.BV.Mensagem",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    Corpo: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,Criacao: { dataType: DataType.DateTime, isNullable: false, isPartOfKey: false, defaultValue: '', validators: [ Validator.hasValueValidator]  }
    ,Envio: { dataType: DataType.DateTime, isNullable: false, isPartOfKey: false, defaultValue: '', validators: [ Validator.hasValueValidator]  }
    ,Filtro: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdLinx: { dataType: DataType.Int32, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdTcsMensagem: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,IdUsuario: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,LxTipoMensagem: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxTipoMensagemName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,NomeEmpresa: { dataType: DataType.String, maxLength: 250, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 250})]  }
    ,Titulo: { dataType: DataType.String, maxLength: 100, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 100})]  }
    ,NomeAutenticacao: { dataType: DataType.String, maxLength: 250, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 250})]  }
    ,NomeUsuario: { dataType: DataType.String, maxLength: 250, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 250})]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
    TcsMensagemConsultaLogList: { entityTypeName: "TcsMensagemConsultaLog:#Linx.Framework.BV.Mensagem", isScalar: false, invForeignKeyNames: ["IdTcsMensagem"], associationName: "FK_TcsMensagemConsulta_TcsMensagemConsultaLog" }
                          }
    });
    lookUpProperties['TcsMensagemConsulta'] = {IdLinx: 'LookUpTcsEmpresaAutenticacaoC', IdUsuario: 'LookUpTcsUsuarioAutenticacaoC', NomeEmpresa: 'LookUpTcsEmpresaAutenticacaoC', NomeAutenticacao: 'LookUpTcsUsuarioAutenticacaoC', NomeUsuario: 'LookUpTcsUsuarioAutenticacaoC'};
    var TcsMensagemConsultaInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsMensagemConsulta') : ko.observable(getNextSequence('TcsMensagemConsulta')));
       ownerReference.currentTcsMensagemConsultaLog = ko.observable(null);
       //Adjust details for a POCO reference
       if (isPOCO === true) {
           ownerReference.TcsMensagemConsultaLogList = ko.observableArray(ownerReference.TcsMensagemConsultaLogList);
       }
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           if (noDetails !== true && ownerReference.TcsMensagemConsultaLogList && ownerReference.TcsMensagemConsultaLogList().length > 0) {
             var detailExpr = ownerReference.TcsMensagemConsultaLogList()[0].getJExpression(listFilterRange, ['IdTcsMensagem']);
             if (detailExpr === 'Error') return detailExpr;
             jExpression += detailExpr;
           }
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               if (isNullOrEmpty(propertyName)) {
                   for (var i = 0; i < ownerReference.TcsMensagemConsultaLogList().length; i++) {
                       var detail = ownerReference.TcsMensagemConsultaLogList()[i];
                       errors = errors.concat(detail.getValidationErrors());
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           if (loadDetails) {
               result.TcsMensagemConsultaLogList = [];
               var sourceList = getAbsoluteValue(ownerReference.TcsMensagemConsultaLogList);
               if (sourceList && sourceList.length > 0) {
                   for (var i = 0; i < sourceList.length; i++) {
                       if (['U', 'I', 'D'].indexOf(sourceList[i].ChangeState) >= 0) result.TcsMensagemConsultaLogList.push(sourceList[i].getPrimitiveDTO(sourceList[i].ChangeState != 'D'));
                   }
               }
           }
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           var _TcsMensagemConsultaLogList = getAbsoluteValue(ownerReference.TcsMensagemConsultaLogList);
           if (_TcsMensagemConsultaLogList && _TcsMensagemConsultaLogList.length > 0) {
               for (var i = 0; i < _TcsMensagemConsultaLogList.length; i++) {
                   var detail = _TcsMensagemConsultaLogList[i];
                   if (['U', 'I', 'D'].indexOf(detail.ChangeState) >= 0) {
                       result.push(detail);
                       result = result.concat(detail.getAllDetailChanges());
                   }
               }
           }
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
           if (copyDetails) {
               if (ownerReference.TcsMensagemConsultaLogList && originData.TcsMensagemConsultaLogList) {
                   var toList = getAbsoluteValue(ownerReference.TcsMensagemConsultaLogList);
                   var fromList = getAbsoluteValue(originData.TcsMensagemConsultaLogList);
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                      if (toList[idxElem].ChangeState === 'D') toList.splice(idxElem, 1);
                   }
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                          if (toList[idxElem].ChangeState !== 'N') {
                               var fromObj = [];
                               if (toList[idxElem].ChangeState == 'I') {
                                   fromObj = _.where(fromList, { TemporaryIdTcsMensagemLog: toList[idxElem]['IdTcsMensagemLog'] });
                               } else {
                                   fromObj = _.where(fromList, { IdTcsMensagemLog: toList[idxElem]['IdTcsMensagemLog'] });
                               }
                               if (fromObj.length > 0) toList[idxElem].copyDataFrom(fromObj[0], true);
                          }
                   }
               }
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
              vm.dataBind('TcsMensagemConsultaLogList', true);
              if (ownerReference.currentTcsMensagemConsultaLog()) ownerReference.currentTcsMensagemConsultaLog().commitDetailsVisualPendings();
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsMensagemConsulta{' + 'IdTcsMensagem#==#L' + getAbsoluteValue(ownerReference.IdTcsMensagem).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsMensagemConsultaByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              breezeReference.TcsMensagemConsultaLogIsLoaded = ownerReference.TcsMensagemConsultaLogIsLoaded;
              for (var idx = 0; idx < ownerReference.TcsMensagemConsultaLogList().length; idx++) {
                  var entity = ownerReference.TcsMensagemConsultaLogList()[idx];
                  if (entity.isPOCO)  {
                      var newReference = createEntity(entity.typeName, entity.getPrimitiveDTO(), true);
                      entity.enableDetailsDataTack(newReference);
                  }
              }
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['Corpo'] = 'S';
       ownerReference.serverDataType['Criacao'] = 'T';
       ownerReference.serverDataType['Envio'] = 'T';
       ownerReference.serverDataType['Filtro'] = 'S';
       ownerReference.serverDataType['IdLinx'] = 'I';
       ownerReference.serverDataType['IdTcsMensagem'] = 'L';
       ownerReference.serverDataType['IdUsuario'] = 'L';
       ownerReference.serverDataType['LxTipoMensagem'] = 'Y';
       ownerReference.serverDataType['NomeEmpresa'] = 'S';
       ownerReference.serverDataType['Titulo'] = 'S';
       ownerReference.serverDataType['NomeAutenticacao'] = 'S';
       ownerReference.serverDataType['NomeUsuario'] = 'S';
       ownerReference.typeName = 'TcsMensagemConsulta';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdTcsMensagem' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsMensagemConsulta !== 'function') { return; }
               else  if (cacheElements[idxR].TcsMensagemConsulta() != ownerReference) { cacheElements[idxR].TcsMensagemConsulta(ownerReference); }
           }
       }
       //#region Lookup Extended Methods
       if (isPOCO !== true) {
           ownerReference.getLookupPropertyName = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null && !isNullOrEmpty(property.lookupPropertyName) ? property.lookupPropertyName : propertyName);
           }
           ownerReference.getLookupVisibleColumns = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null ? property.lookupVisibleColumns : '');
           }
           ownerReference.getLookUpClientFilterExpressions = function (lookupName, lookupInfo) {
               return '';
           };
        
           ownerReference.getLookupDisplay = function (lookupName) {
               var displayName = '';
               if (lookupName === 'LookUpTcsEmpresaAutenticacaoC') {
                   displayName = ' de Tcs_Empresa_Autenticacao';
               }
               if (lookupName === 'LookUpTcsUsuarioAutenticacaoC') {
                   displayName = ' de Tcs_Usuario_Autenticacao';
               }
               return 'Seleção' + displayName;
           };
        
           ownerReference.getSpecializedLookup = function (lookupName, lookupInfo, fieldToSearch, valueToSearch, ownerReference, allowMultiSelectionInSearch) {
               var specializedLookup = '';
               return specializedLookup;
           };
        
           ownerReference.getSubQueryFilterFromLookUpTcsEmpresaAutenticacaoC = function (propertyName) {
               var filter = '';
               return filter;
           }
           ownerReference.getSubQueryFilterFromLookUpTcsUsuarioAutenticacaoC = function (propertyName) {
               var filter = '';
               return filter;
           }
           ownerReference.canGetClientFilter = function (lookupName) {
               return true;
           }
           ownerReference.validatedlookupsArray = [];
           ownerReference.internalLookupSearch = function (lookupName, fieldToSearch, operation, querySucceeded, lookupInfo, valueToSearch, beforeGettingLookup, referencefield) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); querySucceeded(null); return lookupInfo; }
               if (isNullOrEmpty(lookupInfo.lastJEntityExpression)) {
                   if (isNullOrEmpty(referencefield)) referencefield = fieldToSearch;
                   if ((typeof valueToSearch) === 'undefined')
                       valueToSearch = getAbsoluteValue(ownerReference[referencefield]);
                   var extraFilters = '';
                   if (ownerReference.canGetClientFilter(lookupName)) {
                       extraFilters = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
                       dataContext.lastClientFilterExpressions[lookupName] = extraFilters;
                       if (extraFilters === 'Error') { querySucceeded(null); return lookupInfo; }
                       if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                           var customFilter = ownerReference['BeforeGet' + lookupName + 'Query'](fieldToSearch, lookupInfo);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                       if (typeof ownerReference['getSubQueryFilterFrom' + lookupName] == 'function') {
                           var customFilter = ownerReference['getSubQueryFilterFrom' + lookupName](referencefield);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                   }
                   var completeExpression = getLookUpJEntityExpression(lookupName, ownerReference, fieldToSearch, valueToSearch, extraFilters, referencefield, app, lookupInfo.vm.useLikeCommandAsDefault);
                   if (completeExpression === 'Error') { querySucceeded(null); return lookupInfo; }
                   lookupInfo.lastJEntityExpression = completeExpression;
               }
               switch (operation) {
                   case 'F':
                       lookupInfo.pageSkip = 0;
                       break;
                   case 'B':
                       lookupInfo.pageSkip = lookupInfo.pageSkip - 1;
                       break;
                   case 'N':
                       lookupInfo.pageSkip = lookupInfo.pageSkip + 1;
                       break;
                   case 'L':
                       lookupInfo.pageSkip = lookupInfo.totalPages();
                       break;
                   default:
               }
        
               var e = { cancel: false, lookupName: lookupName, jEntitySearch: lookupInfo.lastJEntityExpression, entity: ownerReference, viewModel: lookupInfo.vm };
               if (beforeGettingLookup) beforeGettingLookup(e);
               if (e.cancel) { querySucceeded(null); return lookupInfo; }
               if(lookupInfo.lastJEntityExpression !== e.jEntitySearch)
                   lookupInfo.lastJEntityExpression = e.jEntitySearch;
               if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(true);
               var returnQueryResult = function (data) { lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount); querySucceeded(data); };
               eval('dataContext.get' + lookupName + 'ByEntitySearch(lookupInfo.lastJEntityExpression, (isNullOrEmpty(lookupInfo.fieldToSort) ? fieldToSearch : lookupInfo.fieldToSort), lookupInfo.pageSize*lookupInfo.pageSkip, lookupInfo.pageSize, lookupInfo.sortDirection, fieldToSearch).then(returnQueryResult).fail(queryFailed)').fin(function(){ if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(false); });
               return lookupInfo;
           };
        
           ownerReference.hasValidClientFilter = function (lookupName, lookupInfo) {
               var checkClientFilter = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
               if (checkClientFilter === 'Error') { return false; }
               if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                   checkClientFilter = ownerReference['BeforeGet' + lookupName + 'Query']('', lookupInfo);
                   if (checkClientFilter === 'Error') { return false; }
               }
               return true;
           }
        
           ownerReference.executeLookUp = function (lookupName, fieldToSearch, beforeGettingLookup, vm, valueToSearch, finished, comboCallBack, allowMultiSelectionInSearch) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); if (finished) finished(false, null); return; }
               var lookupFieldName = ownerReference.getLookupPropertyName(fieldToSearch);
               vm.dataBind('', true);
               var lookupInfo = new lookupInformation();
               lookupInfo.visibleColumns = ownerReference.getLookupVisibleColumns(fieldToSearch);
               lookupInfo.vm = vm;
               lookupInfo.isMultiSelection = lookupName.in([]);
               var specializedLookup = ownerReference.getSpecializedLookup(lookupName, lookupInfo, lookupFieldName, valueToSearch, ownerReference, allowMultiSelectionInSearch);
               if (isNullOrEmpty(specializedLookup)) {
                       ownerReference.internalLookupSearch(lookupName, lookupFieldName, 'F',
                           function querySucceeded(data) {
                               if (typeof ownerReference['OnLoading' + lookupName + 'Query'] == 'function') {
                                   ownerReference['OnLoading' + lookupName + 'Query'](data);
                               }
                               if ((typeof comboCallBack) === 'function') {
                                   return comboCallBack(data ? data.results : null);
                               }
                               else if (data == null || data.results == null || data.results.length == 0) {
                                   if (finished) finished(false, null);
                                   ownerReference.clearLookUp(lookupName);
                                   if (data != null) app.showMessage('A informação de Lookup [' + ownerReference.getDisplayName(fieldToSearch) + '] não foi encontrada!', 'Informação', ['Ok']);
                                   return;
                               }
                               lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount);
                               showLookUp(dataContext, ownerReference, ownerReference.getLookupDisplay(lookupName), lookupName, lookupFieldName, ownerReference.internalLookupSearch, lookupInfo, 
                                   function (confirm, values) {
                                       var results = '';
                                       if (values != null && values.length > 1) {
                                           $.each(values, function (index, item) { results += (index == 0 ? '' : ',') + item[lookupFieldName].toString().trim() });
                                           results = '[' + results + ']';
                                           ownerReference[fieldToSearch](results);
                                           if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                               vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                           else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                           document.dispatchEvent(dataUpdateEvent);
                                       }
                                       if (finished) finished(confirm, results);
                                   }, data.results, allowMultiSelectionInSearch);
                       }, lookupInfo, valueToSearch, beforeGettingLookup, fieldToSearch);
               }
               else {
                       var currentModal = (modal.inUse ? modal2 : modal);
                       if (currentModal.inUse === false) {
                           //Check Client Validations
                           if (!ownerReference.hasValidClientFilter(lookupName, lookupInfo)) { if (finished) finished(false); return; }
                           //Show External Lookup
                           currentModal.showModal(specializedLookup.moduleName, specializedLookup.uiSettings, ownerReference.getLookupDisplay(lookupName), ['Ok', 'Cancelar'], 'large').then(function (r, data) {
                           if (r == 'Ok') {
                               if (!currentModal.internalUIs || currentModal.internalUIs.length != 1) { if (finished) finished(false); return; }
                               var lookupVM = currentModal[currentModal.internalUIs[0]]; 
                               if (!lookupVM) return; 
                               if (typeof lookupVM == 'function') lookupVM = lookupVM(); 
                               currentModal[currentModal.internalUIs[0]] = null; 
                               var selectedItems = lookupVM.getSpecializedLookupItems(); 
                               if (vm.status() == 'C' && selectedItems != null && selectedItems.length > 1) { 
                                   var results = '';
                                   $.each(selectedItems, function (index, item) { results += (index == 0 ? '' : ',') + (typeof item[lookupFieldName] == 'function' ? item[lookupFieldName]() : item[lookupFieldName]).toString().trim() });
                                   results = '[' + results + ']'
                                   ownerReference[fieldToSearch](results);
                                   if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                       vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                   else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                   document.dispatchEvent(dataUpdateEvent);
                                   if (finished) finished(true, results);
                               }
                               else if (selectedItems.length > 0) { dataContext['finalizeAll' + lookupName](ownerReference, selectedItems, '', lookupInfo); }
                           }
                           if (finished) finished(r === 'Ok');
                       });
                   }
                   if (finished) finished(false);
               }
           };
           ownerReference.clearLookUp = function (lookupName) {
               return eval('dataContext.clear' + lookupName + '(ownerReference)');
           };
       }
       //#endregion Lookup Extended Methods
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (!isNullOrEmpty(ownerReference.TcsMensagemConsultaLogList()) && ownerReference.TcsMensagemConsultaLogList().length > 0) {
              var details = [].concat(ownerReference.TcsMensagemConsultaLogList());
              for (var idx = 0; idx < details.length; idx++) {
                details[idx].delete();
              }
           }
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Mensagem';
       ownerReference.myProperties = [ 'Corpo','Criacao','Envio','Filtro','IdLinx','IdTcsMensagem','IdUsuario','LxTipoMensagem','NomeEmpresa','Titulo','NomeAutenticacao','NomeUsuario' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
       if (!isNullOrEmpty(ownerReference.currentTcsMensagemConsultaLog())) { result = result.concat(ownerReference.currentTcsMensagemConsultaLog().getCurrentElements()); }
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.GetJsWhereDetailRelationForTcsMensagemConsultaLog = function(customParentRelation) {
       return 'TcsMensagemConsultaLog{' + (!isNullOrEmpty(customParentRelation) ? customParentRelation : 'IdTcsMensagem#==#' + ownerReference.serverDataType['IdTcsMensagem'] + getAbsoluteValue(ownerReference.IdTcsMensagem).toString()) + '}';    
       }
       ownerReference.TcsMensagemConsultaLogIsLoaded = false;
       ownerReference.detailsLoaded = function() {
           return ownerReference.TcsMensagemConsultaLogIsLoaded;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return ownerReference.TcsMensagemConsultaLogIsLoaded;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
           ownerReference.TcsMensagemConsultaLogIsLoaded = value;
           if (value === false && ownerReference.isPOCO)
               ownerReference.TcsMensagemConsultaLogList([]);
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (ownerReference.isAdded()) {
            ownerReference.TcsMensagemConsultaLogIsLoaded = true;
          }
          var _TcsMensagemConsultaLogRemoteComplete = false;
          var detachList_TcsMensagemConsultaLog = [];
          if (force) {
               if (isNullOrEmpty(detailName) || detailName == 'TcsMensagemConsultaLog') ownerReference.TcsMensagemConsultaLogIsLoaded = false;
               if ((isNullOrEmpty(detailName) || detailName == 'TcsMensagemConsultaLog') && ownerReference.TcsMensagemConsultaLogList && ownerReference.TcsMensagemConsultaLogList().length > 0) {
                   if (ownerReference.isPOCO) {
                       ownerReference.TcsMensagemConsultaLogList([]);
                   } else {
                       var detailList = ownerReference.TcsMensagemConsultaLogList();
                       for (var idx = detailList.length - 1; idx >= 0; idx--) {
                           detachList_TcsMensagemConsultaLog.push(detailList[idx]);
                       }
                   }
               }
          }
    
          if (!ownerReference.TcsMensagemConsultaLogIsLoaded) {
            //Load TcsMensagemConsultaLogList
            if (isNullOrEmpty(detailName) || detailName === 'TcsMensagemConsultaLog') {
              ownerReference.TcsMensagemConsultaLogIsLoaded = true;
              _TcsMensagemConsultaLogRemoteComplete = (ownerReference.TcsMensagemConsultaLogList && ownerReference.TcsMensagemConsultaLogList().length > 0);
              if ((force || !ownerReference.TcsMensagemConsultaLogList || ownerReference.TcsMensagemConsultaLogList().length === 0) && (!isNullOrEmpty(getAbsoluteValue(ownerReference.IdTcsMensagem)))) {
                var navQuery = EntityQuery.from('GetTcsMensagemConsultaLogByEntitySearchNoAssociations').noTracking(ownerReference.isPOCO === true)
                .orderBy('IdTcsMensagemLog asc')
                    .withParameters({ jEntitySearch: ownerReference.GetJsWhereDetailRelationForTcsMensagemConsultaLog(customParentRelation) })    ;
                if (!vm.dataToolbar._noBusyLoading) vm.showProcessing('Pesquisando detalhes...');
                manager.executeQuery(navQuery).then(function (data) { if (ownerReference.isPOCO) { for (var idx = 0; idx < data.results.length; idx++) { initializePOCO(data.results[idx], 'TcsMensagemConsultaLog'); data.results[idx].TcsMensagemConsulta = ko.observable(ownerReference); } ownerReference.TcsMensagemConsultaLogList(data.results); } 
                   if (!ownerReference.isPOCO && detachList_TcsMensagemConsultaLog.length > 0)
                   {
                       for (var idx = 0; idx < detachList_TcsMensagemConsultaLog.length; idx++)
                       {
                           if (!data.results.contains(detachList_TcsMensagemConsultaLog[idx]))
                               detachEntity(detachList_TcsMensagemConsultaLog[idx]);
                           else {
                               if (force && detachList_TcsMensagemConsultaLog[idx].atLeastOneDetailLoaded())
                                   detachList_TcsMensagemConsultaLog[idx].fillDetails(force, '', false, noWait);
                           }
                       }
                   }
                   ownerReference.setCurrentDetails('TcsMensagemConsultaLog'); notifyPresentation('TcsMensagemConsultaLogList');
                   _TcsMensagemConsultaLogRemoteComplete = true;
                   if (callback && (!isNullOrEmpty(detailName) || (_TcsMensagemConsultaLogRemoteComplete))) { callback(); }
                }).fail(queryFailed).fin(function() { if (!vm.dataToolbar._noBusyLoading) vm.closeProcessing(); });
              } else { ownerReference.setCurrentDetails('TcsMensagemConsultaLog'); notifyPresentation('TcsMensagemConsultaLogList'); }
            } else { _TcsMensagemConsultaLogRemoteComplete = true; if (!ownerReference.TcsMensagemConsultaLogIsLoaded && ownerReference.TcsMensagemConsultaLogList && ownerReference.TcsMensagemConsultaLogList().length > 0) { ownerReference.TcsMensagemConsultaLogIsLoaded = true; ownerReference.setCurrentDetails('TcsMensagemConsultaLog'); } }
          } else { 
            if (isNullOrEmpty(detailName) || detailName == 'TcsMensagemConsultaLog') {
               notifyPresentation('TcsMensagemConsultaLogList');
               ownerReference.setCurrentDetails('TcsMensagemConsultaLog');
            }
            _TcsMensagemConsultaLogRemoteComplete = true;
          }
          if (callback && ((!isNullOrEmpty(detailName) && (eval('_' + detailName + 'RemoteComplete && ownerReference.' + detailName + 'IsLoaded') == true)) || (isNullOrEmpty(detailName) && (_TcsMensagemConsultaLogRemoteComplete)))) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
          if ((isNullOrEmpty(detailName) || detailName === 'TcsMensagemConsultaLog')) {
               if (ownerReference.TcsMensagemConsultaLogList().length > 0) { ownerReference.currentTcsMensagemConsultaLog(ownerReference.TcsMensagemConsultaLogList()[0]); if (clearing == null || clearing === false) ownerReference.currentTcsMensagemConsultaLog().fillDetails(); }
               else { ownerReference.currentTcsMensagemConsultaLog(null); ownerReference.notifyEmptyDetails('TcsMensagemConsultaLog'); }
          }
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
          if (detailName === 'TcsMensagemConsultaLog') {
               notifyPresentation('TcsMensagemConsultaLogList');
               vm.queryInnerUIs(null, 'TcsMensagemConsultaLog');
          }
       };
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxTipoMensagemName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxTipoMensagemName(''); return; }
                   var value = (dataDomains.getId('TipoMensagem', newValue));
                   if (value != ownerReference.LxTipoMensagem()) {
                       ownerReference.LxTipoMensagem(value);
                   }
            });
    
           ownerReference.LxTipoMensagem.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxTipoMensagem(0); return; }
                   var value = dataDomains.getName('TipoMensagem', newValue);
                   if (value != ownerReference.LxTipoMensagemName()) {
                       ownerReference.LxTipoMensagemName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    //#region Adjust details already loaded for a POCO reference
       if (isPOCO === true) {
           if ((typeof ownerReference.TcsMensagemConsultaLogList === 'function') && ownerReference.TcsMensagemConsultaLogList().length > 0) {
                for(var idx = 0; idx < ownerReference.TcsMensagemConsultaLogList().length; idx++) { TcsMensagemConsultaLogInitializer(ownerReference.TcsMensagemConsultaLogList()[idx], isPOCO); }
           }
       }
    //#endregion Adjust details already loaded for a POCO reference
    };
    metadataStore.registerEntityTypeCtor("TcsMensagemConsulta", null, TcsMensagemConsultaInitializer);
    
    // Configure TcsMensagemConsultaLog data type
    metadataStore.addEntityType({
    shortName: "TcsMensagemConsultaLog",
    namespace: "Linx.Framework.BV.Mensagem",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    Dispensada: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,Entregue: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IdTcsMensagem: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdTcsMensagemLog: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,IdUsuario: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,Lida: { dataType: DataType.DateTime, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,NomeAutenticacao: { dataType: DataType.String, maxLength: 250, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 250})]  }
    ,NomeUsuario: { dataType: DataType.String, maxLength: 250, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 250})]  }
                    },
    navigationProperties: {
    // Returns a single parent and associates with Details
    TcsMensagemConsulta: { entityTypeName: "TcsMensagemConsulta:#Linx.Framework.BV.Mensagem", isScalar: true, foreignKeyNames: ["IdTcsMensagem"], associationName: "FK_TcsMensagemConsulta_TcsMensagemConsultaLog" }
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsMensagemConsultaLog'] = {IdUsuario: 'LookUpTcsUsuarioAutenticacaoCL', NomeAutenticacao: 'LookUpTcsUsuarioAutenticacaoCL', NomeUsuario: 'LookUpTcsUsuarioAutenticacaoCL'};
    var TcsMensagemConsultaLogInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsMensagemConsultaLog') : ko.observable(getNextSequence('TcsMensagemConsultaLog')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsMensagemConsultaLog{' + 'IdTcsMensagemLog#==#L' + getAbsoluteValue(ownerReference.IdTcsMensagemLog).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsMensagemConsultaLogByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['Dispensada'] = 'T';
       ownerReference.serverDataType['Entregue'] = 'T';
       ownerReference.serverDataType['IdTcsMensagem'] = 'L';
       ownerReference.serverDataType['IdTcsMensagemLog'] = 'L';
       ownerReference.serverDataType['IdUsuario'] = 'L';
       ownerReference.serverDataType['Lida'] = 'T';
       ownerReference.serverDataType['NomeAutenticacao'] = 'S';
       ownerReference.serverDataType['NomeUsuario'] = 'S';
       ownerReference.typeName = 'TcsMensagemConsultaLog';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdTcsMensagemLog' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsMensagemConsultaLog !== 'function') { return; }
               else  if (cacheElements[idxR].TcsMensagemConsultaLog() != ownerReference) { cacheElements[idxR].TcsMensagemConsultaLog(ownerReference); }
           }
       }
       //#region Lookup Extended Methods
       if (isPOCO !== true) {
           ownerReference.getLookupPropertyName = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null && !isNullOrEmpty(property.lookupPropertyName) ? property.lookupPropertyName : propertyName);
           }
           ownerReference.getLookupVisibleColumns = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null ? property.lookupVisibleColumns : '');
           }
           ownerReference.getLookUpClientFilterExpressions = function (lookupName, lookupInfo) {
               return '';
           };
        
           ownerReference.getLookupDisplay = function (lookupName) {
               var displayName = '';
               if (lookupName === 'LookUpTcsUsuarioAutenticacaoCL') {
                   displayName = ' de Tcs_Usuario_Autenticacao';
               }
               return 'Seleção' + displayName;
           };
        
           ownerReference.getSpecializedLookup = function (lookupName, lookupInfo, fieldToSearch, valueToSearch, ownerReference, allowMultiSelectionInSearch) {
               var specializedLookup = '';
               return specializedLookup;
           };
        
           ownerReference.getSubQueryFilterFromLookUpTcsUsuarioAutenticacaoCL = function (propertyName) {
               var filter = '';
               return filter;
           }
           ownerReference.canGetClientFilter = function (lookupName) {
               return true;
           }
           ownerReference.validatedlookupsArray = [];
           ownerReference.internalLookupSearch = function (lookupName, fieldToSearch, operation, querySucceeded, lookupInfo, valueToSearch, beforeGettingLookup, referencefield) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); querySucceeded(null); return lookupInfo; }
               if (isNullOrEmpty(lookupInfo.lastJEntityExpression)) {
                   if (isNullOrEmpty(referencefield)) referencefield = fieldToSearch;
                   if ((typeof valueToSearch) === 'undefined')
                       valueToSearch = getAbsoluteValue(ownerReference[referencefield]);
                   var extraFilters = '';
                   if (ownerReference.canGetClientFilter(lookupName)) {
                       extraFilters = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
                       dataContext.lastClientFilterExpressions[lookupName] = extraFilters;
                       if (extraFilters === 'Error') { querySucceeded(null); return lookupInfo; }
                       if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                           var customFilter = ownerReference['BeforeGet' + lookupName + 'Query'](fieldToSearch, lookupInfo);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                       if (typeof ownerReference['getSubQueryFilterFrom' + lookupName] == 'function') {
                           var customFilter = ownerReference['getSubQueryFilterFrom' + lookupName](referencefield);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                   }
                   var completeExpression = getLookUpJEntityExpression(lookupName, ownerReference, fieldToSearch, valueToSearch, extraFilters, referencefield, app, lookupInfo.vm.useLikeCommandAsDefault);
                   if (completeExpression === 'Error') { querySucceeded(null); return lookupInfo; }
                   lookupInfo.lastJEntityExpression = completeExpression;
               }
               switch (operation) {
                   case 'F':
                       lookupInfo.pageSkip = 0;
                       break;
                   case 'B':
                       lookupInfo.pageSkip = lookupInfo.pageSkip - 1;
                       break;
                   case 'N':
                       lookupInfo.pageSkip = lookupInfo.pageSkip + 1;
                       break;
                   case 'L':
                       lookupInfo.pageSkip = lookupInfo.totalPages();
                       break;
                   default:
               }
        
               var e = { cancel: false, lookupName: lookupName, jEntitySearch: lookupInfo.lastJEntityExpression, entity: ownerReference, viewModel: lookupInfo.vm };
               if (beforeGettingLookup) beforeGettingLookup(e);
               if (e.cancel) { querySucceeded(null); return lookupInfo; }
               if(lookupInfo.lastJEntityExpression !== e.jEntitySearch)
                   lookupInfo.lastJEntityExpression = e.jEntitySearch;
               if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(true);
               var returnQueryResult = function (data) { lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount); querySucceeded(data); };
               eval('dataContext.get' + lookupName + 'ByEntitySearch(lookupInfo.lastJEntityExpression, (isNullOrEmpty(lookupInfo.fieldToSort) ? fieldToSearch : lookupInfo.fieldToSort), lookupInfo.pageSize*lookupInfo.pageSkip, lookupInfo.pageSize, lookupInfo.sortDirection, fieldToSearch).then(returnQueryResult).fail(queryFailed)').fin(function(){ if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(false); });
               return lookupInfo;
           };
        
           ownerReference.hasValidClientFilter = function (lookupName, lookupInfo) {
               var checkClientFilter = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
               if (checkClientFilter === 'Error') { return false; }
               if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                   checkClientFilter = ownerReference['BeforeGet' + lookupName + 'Query']('', lookupInfo);
                   if (checkClientFilter === 'Error') { return false; }
               }
               return true;
           }
        
           ownerReference.executeLookUp = function (lookupName, fieldToSearch, beforeGettingLookup, vm, valueToSearch, finished, comboCallBack, allowMultiSelectionInSearch) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); if (finished) finished(false, null); return; }
               var lookupFieldName = ownerReference.getLookupPropertyName(fieldToSearch);
               vm.dataBind('', true);
               var lookupInfo = new lookupInformation();
               lookupInfo.visibleColumns = ownerReference.getLookupVisibleColumns(fieldToSearch);
               lookupInfo.vm = vm;
               lookupInfo.isMultiSelection = lookupName.in([]);
               var specializedLookup = ownerReference.getSpecializedLookup(lookupName, lookupInfo, lookupFieldName, valueToSearch, ownerReference, allowMultiSelectionInSearch);
               if (isNullOrEmpty(specializedLookup)) {
                       ownerReference.internalLookupSearch(lookupName, lookupFieldName, 'F',
                           function querySucceeded(data) {
                               if (typeof ownerReference['OnLoading' + lookupName + 'Query'] == 'function') {
                                   ownerReference['OnLoading' + lookupName + 'Query'](data);
                               }
                               if ((typeof comboCallBack) === 'function') {
                                   return comboCallBack(data ? data.results : null);
                               }
                               else if (data == null || data.results == null || data.results.length == 0) {
                                   if (finished) finished(false, null);
                                   ownerReference.clearLookUp(lookupName);
                                   if (data != null) app.showMessage('A informação de Lookup [' + ownerReference.getDisplayName(fieldToSearch) + '] não foi encontrada!', 'Informação', ['Ok']);
                                   return;
                               }
                               lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount);
                               showLookUp(dataContext, ownerReference, ownerReference.getLookupDisplay(lookupName), lookupName, lookupFieldName, ownerReference.internalLookupSearch, lookupInfo, 
                                   function (confirm, values) {
                                       var results = '';
                                       if (values != null && values.length > 1) {
                                           $.each(values, function (index, item) { results += (index == 0 ? '' : ',') + item[lookupFieldName].toString().trim() });
                                           results = '[' + results + ']';
                                           ownerReference[fieldToSearch](results);
                                           if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                               vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                           else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                           document.dispatchEvent(dataUpdateEvent);
                                       }
                                       if (finished) finished(confirm, results);
                                   }, data.results, allowMultiSelectionInSearch);
                       }, lookupInfo, valueToSearch, beforeGettingLookup, fieldToSearch);
               }
               else {
                       var currentModal = (modal.inUse ? modal2 : modal);
                       if (currentModal.inUse === false) {
                           //Check Client Validations
                           if (!ownerReference.hasValidClientFilter(lookupName, lookupInfo)) { if (finished) finished(false); return; }
                           //Show External Lookup
                           currentModal.showModal(specializedLookup.moduleName, specializedLookup.uiSettings, ownerReference.getLookupDisplay(lookupName), ['Ok', 'Cancelar'], 'large').then(function (r, data) {
                           if (r == 'Ok') {
                               if (!currentModal.internalUIs || currentModal.internalUIs.length != 1) { if (finished) finished(false); return; }
                               var lookupVM = currentModal[currentModal.internalUIs[0]]; 
                               if (!lookupVM) return; 
                               if (typeof lookupVM == 'function') lookupVM = lookupVM(); 
                               currentModal[currentModal.internalUIs[0]] = null; 
                               var selectedItems = lookupVM.getSpecializedLookupItems(); 
                               if (vm.status() == 'C' && selectedItems != null && selectedItems.length > 1) { 
                                   var results = '';
                                   $.each(selectedItems, function (index, item) { results += (index == 0 ? '' : ',') + (typeof item[lookupFieldName] == 'function' ? item[lookupFieldName]() : item[lookupFieldName]).toString().trim() });
                                   results = '[' + results + ']'
                                   ownerReference[fieldToSearch](results);
                                   if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                       vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                   else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                   document.dispatchEvent(dataUpdateEvent);
                                   if (finished) finished(true, results);
                               }
                               else if (selectedItems.length > 0) { dataContext['finalizeAll' + lookupName](ownerReference, selectedItems, '', lookupInfo); }
                           }
                           if (finished) finished(r === 'Ok');
                       });
                   }
                   if (finished) finished(false);
               }
           };
           ownerReference.clearLookUp = function (lookupName) {
               return eval('dataContext.clear' + lookupName + '(ownerReference)');
           };
       }
       //#endregion Lookup Extended Methods
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           var parent = getAbsoluteValue(ownerReference.TcsMensagemConsulta);
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
           if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsMensagemConsultaLogList === 'function') && parent.TcsMensagemConsultaLogList().length == 0) parent.setCurrentDetails('TcsMensagemConsultaLog');
       };
       ownerReference.setParentAsModified = function() {
       var parent = getAbsoluteValue(ownerReference.TcsMensagemConsulta);
       if (parent) {
           if (parent.isUnchanged()) {
               parent.setModified(); 
           }
           parent.setParentAsModified();
       }
       };
       ownerReference.getParent = function() {
           return getAbsoluteValue(ownerReference.TcsMensagemConsulta);
       };
       ownerReference.getSelfList = function() {
           var parent = ownerReference.getParent();
           if (!isNullOrEmpty(parent)) {
               return getAbsoluteValue(parent.TcsMensagemConsultaLogList);
           } else { return null; }
       };
       ownerReference.namespace = 'Linx.Framework.BV.Mensagem';
       ownerReference.myProperties = [ 'Dispensada','Entregue','IdTcsMensagem','IdTcsMensagemLog','IdUsuario','Lida','NomeAutenticacao','NomeUsuario' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("TcsMensagemConsultaLog", null, TcsMensagemConsultaLogInitializer);
    //#endregion Classes Map
    //#region Context Definition
    
    //#region Get LookUps
    
    var getLookUpTcsUsuarioAutenticacaoCLByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpTcsUsuarioAutenticacaoCLByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getLookUpTcsEmpresaAutenticacaoCByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpTcsEmpresaAutenticacaoCByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getLookUpTcsUsuarioAutenticacaoCByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpTcsUsuarioAutenticacaoCByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    var lookUpExternalManagers = [];
    //#endregion
    //#region Get KPI Ranges
    //#endregion
    
    //#region Get Combo LookUp
    var getResultsCombo = function (lookupName, fieldName, current, callback) {
        if (typeof current.executeLookUp === 'function') {
           current.executeLookUp(lookupName, fieldName, null, vm, null, null, function (result) {
               if (callback) callback(result);
           });
        }
    };
    var clientFilterHasModified = function clientFilterHasModified(lookupName, current) {
        var lastFilter = dataContext.lastClientFilterExpressions[lookupName];
        if (lastFilter === 'Error') return true;
        var currentFilter = current.getLookUpClientFilterExpressions(lookupName, null);
        return lastFilter != currentFilter;
    };
    //#endregion Get Combo LookUp
    
    //#region Get Business Entities
    
    var getBmEntityProperties = function (entityName, parentDataPath) {
        return manager.executeQuery(EntityQuery.from('GetBmEntityProperties').withParameters({ entityName: entityName, parentDataPath: parentDataPath }).noTracking(true))
        .fail(queryFailed);
    };
    
    var clearTcsMensagem = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsMensagem');
        var refTcsMensagem = manager.createEntity('TcsMensagem', {}, breeze.EntityState.Unchanged);
        resetSequence('TcsMensagemLogDetail');
        var refTcsMensagemLogDetail = manager.createEntity('TcsMensagemLogDetail', {}, breeze.EntityState.Unchanged);
        refTcsMensagem.currentTcsMensagemLogDetail(refTcsMensagemLogDetail);
        if (complete) complete({ results: [ refTcsMensagem ] });
        return true;
    };
    
    var getTcsMensagem = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsMensagem').noTracking(noTracking)
        .orderBy('IdTcsMensagem asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsMensagemByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkMensagem/AddTcsMensagemEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsMensagemByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsMensagem asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsMensagemByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsMensagem asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearMensagemInfo = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('MensagemInfo');
        var refMensagemInfo = manager.createEntity('MensagemInfo', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refMensagemInfo ] });
        return true;
    };
    
    var getMensagemInfo = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetMensagemInfo').noTracking(noTracking)
        .orderBy('IdTcsMensagemLog asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getMensagemInfoByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkMensagem/AddMensagemInfoEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetMensagemInfoByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsMensagemLog asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetMensagemInfoByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsMensagemLog asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsMensagemUsuario = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsMensagemUsuario');
        var refTcsMensagemUsuario = manager.createEntity('TcsMensagemUsuario', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsMensagemUsuario ] });
        return true;
    };
    
    var getTcsMensagemUsuario = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsMensagemUsuario').noTracking(noTracking)
        .orderBy('IdTcsMensagemLog asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsMensagemUsuarioByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkMensagem/AddTcsMensagemUsuarioEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsMensagemUsuarioByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsMensagemLog asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsMensagemUsuarioByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsMensagemLog asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsMensagemLog = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsMensagemLog');
        var refTcsMensagemLog = manager.createEntity('TcsMensagemLog', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsMensagemLog ] });
        return true;
    };
    
    var getTcsMensagemLog = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsMensagemLog').noTracking(noTracking)
        .orderBy('IdTcsMensagemLog asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsMensagemLogByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkMensagem/AddTcsMensagemLogEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsMensagemLogByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsMensagemLog asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsMensagemLogByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsMensagemLog asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsPerfil = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsPerfil');
        var refTcsPerfil = manager.createEntity('TcsPerfil', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsPerfil ] });
        return true;
    };
    
    var getTcsPerfil = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsPerfil').noTracking(noTracking)
        .orderBy('IdTcsUsuarioPerfil asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsPerfilByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkMensagem/AddTcsPerfilEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsPerfilByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsUsuarioPerfil asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsPerfilByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsUsuarioPerfil asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsUsuario = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsUsuario');
        var refTcsUsuario = manager.createEntity('TcsUsuario', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsUsuario ] });
        return true;
    };
    
    var getTcsUsuario = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsUsuario').noTracking(noTracking)
        .orderBy('IdUsuario asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsUsuarioByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkMensagem/AddTcsUsuarioEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsUsuarioByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdUsuario asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsUsuarioByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdUsuario asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearNewMessageInfo = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('NewMessageInfo');
        var refNewMessageInfo = manager.createEntity('NewMessageInfo', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refNewMessageInfo ] });
        return true;
    };
    
    var getNewMessageInfo = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetNewMessageInfo').noTracking(noTracking)
        .orderBy('IdLinx asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getNewMessageInfoByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkMensagem/AddNewMessageInfoEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetNewMessageInfoByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdLinx asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetNewMessageInfoByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdLinx asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsMensagemLogDetail = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsMensagemLogDetail');
        var refTcsMensagemLogDetail = manager.createEntity('TcsMensagemLogDetail', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsMensagemLogDetail ] });
        return true;
    };
    
    var getTcsMensagemLogDetail = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsMensagemLogDetail').noTracking(noTracking)
        .orderBy('IdTcsMensagemLog asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsMensagemLogDetailByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkMensagem/AddTcsMensagemLogDetailEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsMensagemLogDetailByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsMensagemLog asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsMensagemLogDetailByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsMensagemLog asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsMensagemConsulta = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsMensagemConsulta');
        var refTcsMensagemConsulta = manager.createEntity('TcsMensagemConsulta', {}, breeze.EntityState.Unchanged);
        resetSequence('TcsMensagemConsultaLog');
        var refTcsMensagemConsultaLog = manager.createEntity('TcsMensagemConsultaLog', {}, breeze.EntityState.Unchanged);
        refTcsMensagemConsulta.currentTcsMensagemConsultaLog(refTcsMensagemConsultaLog);
        if (complete) complete({ results: [ refTcsMensagemConsulta ] });
        return true;
    };
    
    var getTcsMensagemConsulta = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsMensagemConsulta').noTracking(noTracking)
        .orderBy('IdTcsMensagem asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsMensagemConsultaByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkMensagem/AddTcsMensagemConsultaEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsMensagemConsultaByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsMensagem asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsMensagemConsultaByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsMensagem asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsMensagemConsultaLog = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsMensagemConsultaLog');
        var refTcsMensagemConsultaLog = manager.createEntity('TcsMensagemConsultaLog', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsMensagemConsultaLog ] });
        return true;
    };
    
    var getTcsMensagemConsultaLog = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsMensagemConsultaLog').noTracking(noTracking)
        .orderBy('IdTcsMensagemLog asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsMensagemConsultaLogByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkMensagem/AddTcsMensagemConsultaLogEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsMensagemConsultaLogByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsMensagemLog asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsMensagemConsultaLogByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsMensagemLog asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    //#endregion
    
    // Create the data update event.
    var dataUpdateEvent = document.createEvent('Event');
    // Define that the event name is 'MensagemContext_DataUpdate'.
    var contextUpdtEvt = 'MensagemContext_DataUpdate_' + getNewGuid();
    dataUpdateEvent.initEvent(contextUpdtEvt, true, true);
    
    //#region LookUps Finalizers
     var finalizeAllLookUpTcsEmpresaAutenticacaoC = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            if (propertyName === '' || propertyName === 'IdLinx') {
               if (selectedElement.hasOwnProperty('IdLinx') && replaceTo.hasOwnProperty('IdLinx'))
               {
                   setAbsoluteValue(replaceTo, 'IdLinx', getAbsoluteValue(selectedElement['IdLinx']));
               }
               else if (replaceTo.hasOwnProperty('IdLinx')) {
                   setAbsoluteValue(replaceTo, 'IdLinx', 0);
               }
            }
            if (propertyName === '' || propertyName === 'NomeEmpresa') {
               if (selectedElement.hasOwnProperty('NomeEmpresa') && replaceTo.hasOwnProperty('NomeEmpresa'))
               {
                   setAbsoluteValue(replaceTo, 'NomeEmpresa', getAbsoluteValue(selectedElement['NomeEmpresa']));
               }
               else if (replaceTo.hasOwnProperty('NomeEmpresa')) {
                   setAbsoluteValue(replaceTo, 'NomeEmpresa', '');
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpTcsEmpresaAutenticacaoC'))
                replaceTo.validatedlookupsArray.push('LookUpTcsEmpresaAutenticacaoC');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
    };
    
    function clearLookUpTcsEmpresaAutenticacaoC(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'IdLinx', 0);
        setAbsoluteValue(replaceTo, 'NomeEmpresa', '');
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
     var finalizeAllLookUpTcsUsuarioAutenticacaoC = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            if (propertyName === '' || propertyName === 'IdUsuario') {
               if (selectedElement.hasOwnProperty('IdUsuario') && replaceTo.hasOwnProperty('IdUsuario'))
               {
                   setAbsoluteValue(replaceTo, 'IdUsuario', getAbsoluteValue(selectedElement['IdUsuario']));
               }
               else if (replaceTo.hasOwnProperty('IdUsuario')) {
                   setAbsoluteValue(replaceTo, 'IdUsuario', 0);
               }
            }
            if (propertyName === '' || propertyName === 'NomeUsuario') {
               if (selectedElement.hasOwnProperty('NomeUsuario') && replaceTo.hasOwnProperty('NomeUsuario'))
               {
                   setAbsoluteValue(replaceTo, 'NomeUsuario', getAbsoluteValue(selectedElement['NomeUsuario']));
               }
               else if (replaceTo.hasOwnProperty('NomeUsuario')) {
                   setAbsoluteValue(replaceTo, 'NomeUsuario', '');
               }
            }
            if (propertyName === '' || propertyName === 'NomeAutenticacao') {
               if (selectedElement.hasOwnProperty('NomeAutenticacao') && replaceTo.hasOwnProperty('NomeAutenticacao'))
               {
                   setAbsoluteValue(replaceTo, 'NomeAutenticacao', getAbsoluteValue(selectedElement['NomeAutenticacao']));
               }
               else if (replaceTo.hasOwnProperty('NomeAutenticacao')) {
                   setAbsoluteValue(replaceTo, 'NomeAutenticacao', '');
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpTcsUsuarioAutenticacaoC'))
                replaceTo.validatedlookupsArray.push('LookUpTcsUsuarioAutenticacaoC');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
    };
    
    function clearLookUpTcsUsuarioAutenticacaoC(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'IdUsuario', 0);
        setAbsoluteValue(replaceTo, 'NomeUsuario', '');
        setAbsoluteValue(replaceTo, 'NomeAutenticacao', '');
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
     var finalizeAllLookUpTcsUsuarioAutenticacaoCL = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            if (propertyName === '' || propertyName === 'IdUsuario') {
               if (selectedElement.hasOwnProperty('IdUsuario') && replaceTo.hasOwnProperty('IdUsuario'))
               {
                   setAbsoluteValue(replaceTo, 'IdUsuario', getAbsoluteValue(selectedElement['IdUsuario']));
               }
               else if (replaceTo.hasOwnProperty('IdUsuario')) {
                   setAbsoluteValue(replaceTo, 'IdUsuario', 0);
               }
            }
            if (propertyName === '' || propertyName === 'NomeUsuario') {
               if (selectedElement.hasOwnProperty('NomeUsuario') && replaceTo.hasOwnProperty('NomeUsuario'))
               {
                   setAbsoluteValue(replaceTo, 'NomeUsuario', getAbsoluteValue(selectedElement['NomeUsuario']));
               }
               else if (replaceTo.hasOwnProperty('NomeUsuario')) {
                   setAbsoluteValue(replaceTo, 'NomeUsuario', '');
               }
            }
            if (propertyName === '' || propertyName === 'NomeAutenticacao') {
               if (selectedElement.hasOwnProperty('NomeAutenticacao') && replaceTo.hasOwnProperty('NomeAutenticacao'))
               {
                   setAbsoluteValue(replaceTo, 'NomeAutenticacao', getAbsoluteValue(selectedElement['NomeAutenticacao']));
               }
               else if (replaceTo.hasOwnProperty('NomeAutenticacao')) {
                   setAbsoluteValue(replaceTo, 'NomeAutenticacao', '');
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpTcsUsuarioAutenticacaoCL'))
                replaceTo.validatedlookupsArray.push('LookUpTcsUsuarioAutenticacaoCL');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
    };
    
    function clearLookUpTcsUsuarioAutenticacaoCL(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'IdUsuario', 0);
        setAbsoluteValue(replaceTo, 'NomeUsuario', '');
        setAbsoluteValue(replaceTo, 'NomeAutenticacao', '');
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
    //#endregion
    
    var cancelChanges = function(dataForUndo) {
        if (dataForUndo && dataForUndo.length > 0) {
            dataForUndo.forEach(function(e) { e.restoreOriginal(); } ); 
        } else {
            manager.rejectChanges();
        }
    };
    
    var hasEmptyRequiredFilters = function() {
        return false;
    };
    
    var getEntityProperty = function (entityName, propertyName) {
        for (var i = 0; i < metadataInfo[entityName].length; i++) {
            if (metadataInfo[entityName][i].key === propertyName)
                return metadataInfo[entityName][i];
        }
        return null;
    };
    var getViewInfo = function (entityName) {
        var result = [];
        if (metadataInfo[entityName])
        {
            var viewInfoElements = metadataInfo[entityName];
            for (var i = 0; i < viewInfoElements.length; i++) {
                var selectedElement = viewInfoElements[i];
                if (!selectedElement.hidden && (selectedElement.dataType === 'string' || selectedElement.dataType === 'number'))
                {
                    if (strLeft(selectedElement.key, 3) === 'Cod' || strLeft(selectedElement.key, 2) === 'Id' || strLeft(selectedElement.key, 6) === 'Numero' || strLeft(selectedElement.key, 6) === 'Number') {
                        result.push(selectedElement);
                    }
                }
            }
            for (var i = 0; i < viewInfoElements.length; i++) {
                var selectedElement = viewInfoElements[i];
                if (!selectedElement.hidden && (selectedElement.dataType === 'string')) {
                    if (strLeft(selectedElement.key, 4) === 'Nome' || strLeft(selectedElement.key, 4) === 'Name' || strLeft(selectedElement.key, 4) === 'Desc' || strLeft(selectedElement.key, 6) === 'Titulo' || strLeft(selectedElement.key, 5) === 'Title') {
                        result.push(selectedElement);
                    }
                }
            }
            for (var i = 0; i < viewInfoElements.length; i++) {
                var selectedElement = viewInfoElements[i];
                if (!selectedElement.hidden && selectedElement.dataType === 'date')
                {                
                   result.push(selectedElement);
                }
            }
        }
        return result;
    };
    
    var getChanges = function() {
       return manager.getEntities(null, [breeze.EntityState.Added, breeze.EntityState.Modified, breeze.EntityState.Deleted]);
    };
    
    var hasValidationErrors = function(savingData) {
        if (savingData instanceof Array) {
           for (var idx = 0; idx < savingData.length; idx++) {
               var entity = savingData[idx];
               if (entity.ChangeState && entity.getValidationErrors && ['I', 'U'].indexOf(entity.ChangeState) >=0) {
                  var errors = entity.getValidationErrors();
                  if (errors.length > 0) {
                       showModalAlert('Campos obrigatórios não estão preenchidos.', errors);
                       return true;
                   }
               }
           }
        }
        else {
           var changes = manager.getEntities(null, [breeze.EntityState.Added, breeze.EntityState.Modified]);
           for (var idxChange = 0; idxChange < changes.length; idxChange++) {
              changes[idxChange].setParentAsModified();
           }
           changes = manager.getEntities(null, [breeze.EntityState.Added, breeze.EntityState.Modified]);
           for (var idxChange = 0; idxChange < changes.length; idxChange++) {
              var entity = changes[idxChange];
              var isOk = entity.entityAspect.validateEntity();
              if (!isOk) {
                  var errors = entity.entityAspect.getValidationErrors();
                  var strErrors = [];
                  for (var idx = 0; idx < errors.length; idx++) {
                      var errorMsg = errors[idx].errorMessage;
                      var propName = strExtract(errorMsg, "'", "'");
                      var propDisplay = entity.getDisplayName(propName);
                      errorMsg = errorMsg.replace("'" + propName + "'", "'" + propDisplay + "'" + (managerAuth.shellMode=='DEV' ? " (" + entity.typeName + "." + propName + ")": ""));
                      strErrors.push(translateError(errorMsg));
                  }
                  showModalAlert('Campos obrigatórios não estão preenchidos.', strErrors);
                  return true;
              }
           }
        }
        return false;
    };
    
    var saveChanges = function(saveSucceeded, saveFailed, fin, saveNoTRack) {
        if (saveNoTRack === true) {
            var dataForSaving = JSON.stringify(_.map(vm.getDataForSaving(), function(entity){ return entity.getPrimitiveDTO(entity.ChangeState != 'D'); }));
            return $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkMensagem/Save' + vm.rootDataTypeName),
               globalError: false,
               contentType: 'application/json',
               async: true,
               cache: false,
               data: dataForSaving,
               success: function (response) {
                      success(response);
               },
               error: function (jqXHR, textStatus, errorThrown) {
                      failed({ message: jqXHR.responseJSON.ExceptionMessage });
               }
            });
        }
        else {
            return manager.saveChanges()
                   .fail(failed).then(success);
        }
    
        function success(result) {
            if (fin) fin();
            if (saveNoTRack === true && result.length > 0) {
               for (var idx = 0; idx < result.length; idx++) { dataContext.initializePOCO(result[idx], vm.rootDataTypeName); }
            }
            else if (result != null && result.keyMappings != null && result.keyMappings.length > 0) {
                for (var idx = 0; idx < result.keyMappings.length; idx++) {
                    if (result.keyMappings[idx].realValue == null) {
                       var entity = manager.getEntityByKey(result.keyMappings[idx].entityTypeName, result.keyMappings[idx].tempValue);
                       if (entity) manager.detachEntity(entity);
                    }
                }
                manager.acceptChanges();
            }
            if (saveSucceeded)
                saveSucceeded(result);
        }
    
        function failed(error) {
            if (fin) fin();
            if (error.message.indexOf('Internal Error in key fixup - unable to locate entity') == -1 && error.message.indexOf('An entity with this key is already in the cache:') == -1) {
               if (saveFailed)
                   saveFailed(error);
               var msg = error.message.replace('Fail by saving data:', '');
               showModalAlert('Falha ao salvar informações.', [ msg ]);
               error.message = msg;
               throw error;
           } else {
               manager.acceptChanges();
           }
        }
    };
    
    var acceptChanges = function () {
        return manager.acceptChanges();
    };
    
    var getEntities = function (entityName) {
        return manager.getEntities(entityName);
    };
    
    var notifyPresentation = function(dataSourceName) {
          if (dataContext.dataForUpdate !== '') {
           setTimeout(function () { notifyPresentation(dataSourceName); }, 100);
           return;
          }
          dataContext.dataForUpdate = dataSourceName;
          document.dispatchEvent(dataUpdateEvent);
          dataContext.dataForUpdate = '';
    };
    
    var getEntityInCache = function (entityName, propertiesReference) {
        var keys = [];
        if (!isNullOrEmpty(propertiesReference)) {
            for (var i = 0; i < metadataInfo[entityName].length; i++) {
                if (metadataInfo[entityName][i].isPartOfKey && !isNullOrEmpty(propertiesReference[metadataInfo[entityName][i].key]))
                    keys.push(propertiesReference[metadataInfo[entityName][i].key]);
            }
            if (keys.length == 0)
                return null;
            else {
                return manager.getEntityByKey(entityName, (keys.length == 1 ? keys[0] : keys));
            }
        }
        else
            return null;
    }
    
    var createEntity = function(entityName, initialValues, unchanged) {
        var entity = getEntityInCache(entityName, initialValues);
        if (!isNullOrEmpty(entity))
            entity.entityAspect.entityState == (unchanged === true ? breeze.EntityState.Unchanged : breeze.EntityState.Added);
        else 
            entity = manager.createEntity(entityName, initialValues, (unchanged === true ? breeze.EntityState.Unchanged : breeze.EntityState.Added));
        return entity;
    };
    
    var createFreeEntity = function(entityName) {
       return manager.createEntity(entityName, {}, breeze.EntityState.Detached);
    }
    
    var createTcsMensagem = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsMensagem');
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createMensagemInfo = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('MensagemInfo', { Lida: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsMensagemUsuario = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsMensagemUsuario');
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsMensagemLog = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsMensagemLog');
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsPerfil = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsPerfil');
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsUsuario = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsUsuario', { AutenticacaoWindows: false, Inativo: false, IndicaAcessoSuporte: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createNewMessageInfo = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('NewMessageInfo');
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsMensagemLogDetail = function(parent, noCurrent) {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsMensagemLogDetail', { TcsMensagem: parent });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        if (noCurrent !== true) parent.currentTcsMensagemLogDetail(entity);
        if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsMensagemLogDetailList === 'function') && parent.TcsMensagemLogDetailList().length == 0) parent.setCurrentDetails('TcsMensagemLogDetail');
        if (entity.setParentAsModified) entity.setParentAsModified();
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsMensagemConsulta = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsMensagemConsulta');
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsMensagemConsultaLog = function(parent, noCurrent) {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsMensagemConsultaLog', { TcsMensagemConsulta: parent });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        if (noCurrent !== true) parent.currentTcsMensagemConsultaLog(entity);
        if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsMensagemConsultaLogList === 'function') && parent.TcsMensagemConsultaLogList().length == 0) parent.setCurrentDetails('TcsMensagemConsultaLog');
        if (entity.setParentAsModified) entity.setParentAsModified();
        enableChangeTrack = true;
        return entity;
    };
    
    var deleteEntity = function (entity) {
        entity.delete();
    };
    
    var detachEntity = function (entity) {
        if (!entity.isDetached()) entity.entityAspect.setDetached();
    };
    
    var attachEntity = function (entity) {
        manager.attachEntity(entity);
    };
    
    var clearAll = function () {
        manager.rejectChanges();
        manager.clear();
    };
    
    var executeQuery = function (getMethod, jEntitySearch, order, skip, take, noTracking, callBack) {
        var query = EntityQuery.from(getMethod).noTracking(noTracking)
        .orderBy(order);
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed).then(function (data) {
            if (callBack) {
                callBack(data.results);
            }
        });
    };
    var exportToExcel = function(entityName, jEntitySearch, translatedJEntitySearch, complete, columnsVisible) {
        var info = jQuery.grep(dataExportInfo[vm.rootDataTypeName], function (item, i) { return (item.name === entityName);});
        if (info == null || info.length === 0) {
            app.showMessage('Erro na exportação', 'Alerta', ['Ok']);
            return;
        }
        $.ajax({
           type: 'POST',
           crossDomain: true,
           url: getServiceAddress(info[0].actionExport),
           globalError: true,
           headers: managerAuth.getHeaders(),
           contentType: 'application/json',
           async: true,
           cache: false,
           data: JSON.stringify([jEntitySearch, translatedJEntitySearch, columnsVisible]),
           success: function (response) {
               if (response.startsWith('~/FileDownload/')) {
                    saveURL(getBaseServiceAddress(info[0].actionExport) + response.substr(1), entityName + '.xlsx');
               } else {
                    saveExcelBlob(entityName + '.xlsx', response);
               }
           },
           complete: function (jqXHR, textStatus) {
               if(complete) complete();
           },
           error: function (jqXHR, textStatus, errorThrown) {
           }
        });
    };
    var exportReportDataSource = function(complete) {
        $.ajax({
           type: 'GET',
           crossDomain: true,
           headers: managerAuth.getHeaders(),
           url: getServiceAddress("LinxFrameworkMensagem/GetReportDataSource"),
           success: function (response) {
                  saveExcelBlob('datasource.ldsx', response);
           },
           complete: function (jqXHR, textStatus) {
                  if(complete) complete();
           },
           error: function (jqXHR, textStatus, errorThrown) {
                  alert('Erro na exportação do data source');
           }
        });
    };
    var exportTemplateReport = function(reportPath, complete) {
        $.ajax({
           type: 'GET',
           crossDomain: true,
           headers: managerAuth.getHeaders(),
           url: getServiceAddress("LinxFrameworkMensagem/GetTemplateReport"),
           data: { reportPath: reportPath },
           success: function (response) {
                  saveExcelBlob(reportPath + '.lrtx', response);
           },
           complete: function (jqXHR, textStatus) {
                  if(complete) complete();
           },
           error: function (jqXHR, textStatus, errorThrown) {
                  alert('Erro na exportação do data source');
           }
        });
    };
    var exportToReport = function(reportName, entityName, jEntitySearch, translatedJEntitySearch, complete, columnsVisible, exportMedia) {
        var info = jQuery.grep(dataExportInfo[vm.rootDataTypeName], function (item, i) { return (item.name === entityName);});
        if (info == null || info.length === 0) {
            app.showMessage('Erro na exportação', 'Alerta', ['Ok']);
            return;
        }
        $.ajax({
           type: 'POST',
           crossDomain: true,
           headers: managerAuth.getHeaders(),
           url: getServiceAddress(info[0].actionReport),
           globalError: true,
           contentType: 'application/json',
           async: true,
           cache: false,
           data: JSON.stringify([ reportName, jEntitySearch, translatedJEntitySearch, columnsVisible, getServiceAddress(''), exportMedia ]),
           success: function (response) {
                  saveExcelBlob(entityName + '.lrtx', response);
           },
           complete: function (jqXHR, textStatus) {
                  if(complete) complete();
           },
           error: function (jqXHR, textStatus, errorThrown) {
           }
        });
    };
    
    var hasChanges = ko.observable(false);
    
    manager.hasChangesChanged.subscribe(function(eventArgs) {
        hasChanges(eventArgs.hasChanges);
    });
    
    //#region Internal methods
    
    function queryFailed(error) {
    }
    
    function log(msg, data) {
        logger.log(msg, data, system.getModuleId(dataContext), true);
    }
    
    function logError(msg, error) {
         logger.logError(msg, error, system.getModuleId(dataContext), true);
    }
    
    function loadParameters() {
     dataParameters.isLoaded = true;
    }
    
    //#endregion Internal methods
    //#region getWithBinding
    var createDTOEmpty = function (entityName) {
        var entityType = manager.metadataStore.getEntityType(entityName);
        var entity = {};
        for (var idx = 0; idx < entityType.dataProperties.length; idx++) {
            var prop = entityType.dataProperties[idx];
            entity[prop.name] = prop.defaultValue;
        }
        for (var idx = 0; idx < entityType.navigationProperties.length; idx++) {
            var navigationName = entityType.navigationProperties[idx].name;
            if (navigationName.endsWith('List'))
                entity['current' + navigationName.replace('List', '')] = null;
        }
        entity.isEmptyEntity = true;
        return entity;
    };
    var getWithBinding = function (binding, entityName) {
        if (typeof binding === 'function' && binding() != null)
            return binding;
        else
            return createDTOEmpty(entityName);
    }
    //#endregion getWithBinding
    
        var vm = null;
        var dataContext = {
            dataForUpdate: '',
            getPivotLayouts: getPivotLayouts,
            getServiceAddress: getServiceAddress,
            getBaseServiceAddress: getBaseServiceAddress,
            getDataFeedUrl: getDataFeedUrl,
            getDataServiceUrl: getDataServiceUrl,
            setServiceBusUrl: setServiceBusUrl,
            initializePOCO: initializePOCO,
            getWithBinding: getWithBinding,
            managerAuth: managerAuth,
            hasDataFeed: true,
            getAccessGroup: getAccessGroup,
            getNewGuid: getNewGuid,
            metadataInfo: metadataInfo,
            dataExportInfo: dataExportInfo,
            entityNames: entityNames,
            lookUpNames: lookUpNames,
            lookUpProperties: lookUpProperties,
            metadataStore: metadataStore,
            cancelChanges: cancelChanges,
            saveChanges: saveChanges,
            getChanges: getChanges,
            hasValidationErrors: hasValidationErrors,
            getEntityProperty: getEntityProperty,
            getViewInfo: getViewInfo,
            createEntity: createEntity,
            notifyPresentation: notifyPresentation,
            createFreeEntity: createFreeEntity,
            createTcsMensagem: createTcsMensagem,
            createMensagemInfo: createMensagemInfo,
            createTcsMensagemUsuario: createTcsMensagemUsuario,
            createTcsMensagemLog: createTcsMensagemLog,
            createTcsPerfil: createTcsPerfil,
            createTcsUsuario: createTcsUsuario,
            createNewMessageInfo: createNewMessageInfo,
            createTcsMensagemLogDetail: createTcsMensagemLogDetail,
            createTcsMensagemConsulta: createTcsMensagemConsulta,
            createTcsMensagemConsultaLog: createTcsMensagemConsultaLog,
            deleteEntity: deleteEntity,
            acceptChanges: acceptChanges,
            getEntities: getEntities,
            detachEntity: detachEntity,
            attachEntity: attachEntity,
            executeQuery: executeQuery,
            sharedData: [],
            clearAll: clearAll,
            hasChanges: hasChanges,
            dataDomains: dataDomains,
            dataParameters: dataParameters,
            loadParameters: loadParameters,
            exportToExcel: exportToExcel,
            exportToReport: exportToReport,
            exportReportDataSource: exportReportDataSource,
            exportTemplateReport: exportTemplateReport,
            businessAssemblyName: businessAssemblyName,
            controllerName: controllerName,
            getResultsCombo: getResultsCombo,
            clientFilterHasModified: clientFilterHasModified,
            lastClientFilterExpressions: {},
            breeze: breeze,
            contextUpdtEvt: contextUpdtEvt,
            setCurrentViewModel: function(vModel) { vm = vModel; },
                getLookUpTcsUsuarioAutenticacaoCLByEntitySearch: getLookUpTcsUsuarioAutenticacaoCLByEntitySearch,
            getLookUpTcsEmpresaAutenticacaoCByEntitySearch: getLookUpTcsEmpresaAutenticacaoCByEntitySearch,
            getLookUpTcsUsuarioAutenticacaoCByEntitySearch: getLookUpTcsUsuarioAutenticacaoCByEntitySearch,
            getBmEntityProperties: getBmEntityProperties,
            clearTcsMensagem: clearTcsMensagem,
            getTcsMensagem: getTcsMensagem,
            getTcsMensagemByEntitySearchNoAssociations: getTcsMensagemByEntitySearchNoAssociations,
            clearMensagemInfo: clearMensagemInfo,
            getMensagemInfo: getMensagemInfo,
            getMensagemInfoByEntitySearchNoAssociations: getMensagemInfoByEntitySearchNoAssociations,
            clearTcsMensagemUsuario: clearTcsMensagemUsuario,
            getTcsMensagemUsuario: getTcsMensagemUsuario,
            getTcsMensagemUsuarioByEntitySearchNoAssociations: getTcsMensagemUsuarioByEntitySearchNoAssociations,
            clearTcsMensagemLog: clearTcsMensagemLog,
            getTcsMensagemLog: getTcsMensagemLog,
            getTcsMensagemLogByEntitySearchNoAssociations: getTcsMensagemLogByEntitySearchNoAssociations,
            clearTcsPerfil: clearTcsPerfil,
            getTcsPerfil: getTcsPerfil,
            getTcsPerfilByEntitySearchNoAssociations: getTcsPerfilByEntitySearchNoAssociations,
            clearTcsUsuario: clearTcsUsuario,
            getTcsUsuario: getTcsUsuario,
            getTcsUsuarioByEntitySearchNoAssociations: getTcsUsuarioByEntitySearchNoAssociations,
            clearNewMessageInfo: clearNewMessageInfo,
            getNewMessageInfo: getNewMessageInfo,
            getNewMessageInfoByEntitySearchNoAssociations: getNewMessageInfoByEntitySearchNoAssociations,
            clearTcsMensagemLogDetail: clearTcsMensagemLogDetail,
            getTcsMensagemLogDetail: getTcsMensagemLogDetail,
            getTcsMensagemLogDetailByEntitySearchNoAssociations: getTcsMensagemLogDetailByEntitySearchNoAssociations,
            clearTcsMensagemConsulta: clearTcsMensagemConsulta,
            getTcsMensagemConsulta: getTcsMensagemConsulta,
            getTcsMensagemConsultaByEntitySearchNoAssociations: getTcsMensagemConsultaByEntitySearchNoAssociations,
            clearTcsMensagemConsultaLog: clearTcsMensagemConsultaLog,
            getTcsMensagemConsultaLog: getTcsMensagemConsultaLog,
            getTcsMensagemConsultaLogByEntitySearchNoAssociations: getTcsMensagemConsultaLogByEntitySearchNoAssociations,
                finalizeAllLookUpTcsEmpresaAutenticacaoC: finalizeAllLookUpTcsEmpresaAutenticacaoC,
            clearLookUpTcsEmpresaAutenticacaoC: clearLookUpTcsEmpresaAutenticacaoC,
            finalizeAllLookUpTcsUsuarioAutenticacaoC: finalizeAllLookUpTcsUsuarioAutenticacaoC,
            clearLookUpTcsUsuarioAutenticacaoC: clearLookUpTcsUsuarioAutenticacaoC,
            finalizeAllLookUpTcsUsuarioAutenticacaoCL: finalizeAllLookUpTcsUsuarioAutenticacaoCL,
            clearLookUpTcsUsuarioAutenticacaoCL: clearLookUpTcsUsuarioAutenticacaoCL
        };
    loadParameters();
    return dataContext;
    //#endregion Context Definition
}
return result;
});
