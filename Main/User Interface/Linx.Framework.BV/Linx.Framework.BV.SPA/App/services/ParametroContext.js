define(['durandal/system', 'pkg_linx-framework-bv-spa/services/DataDomains', 'services/logger', 'breeze', 'durandal/app', 'managers/__auth', 'viewmodels/shared/modal', 'viewmodels/shared/modal2'],
function (system, dataDomains, logger, breeze, app, managerAuth, modal, modal2) {
var result = function () {
    var getPivotLayouts = function (params, success, error) {
        return $.ajax({
            messageUser: 'Busca dos layouts exportados',
            contentType: 'application/json; charset=UTF-8',
            headers: managerAuth.getHeaders(),
            url: getServiceAddress('linxframeworkobjeto') + '/GetPivotLayouts?' +
                                                                   'rootNameSpace=' + params.rootNamespace +
                                                                   '&viewName=' + params.viewName +
                                                                   '&pivotName=' + params.pivotName +
                                                                   '&pivotDataSource=' + params.pivotDataSource,
            async: true,
            cache: false,
            error: error,
            success: success
        });
    };
    
    var getServiceAddress = function(apiPart) {
       return managerAuth.getServiceAddress(apiPart, businessAssemblyName);
    };
    var getBaseServiceAddress = function(apiPart) {
       return managerAuth.getBaseServiceAddress(apiPart, businessAssemblyName);
    };
    var getAccessGroup = function() {
       return '00000000-0000-0000-0000-000000000000';
    };
    var getNewGuid = function() {
       return breeze.core.getUuid();
    };
    var getDataFeedUrl = function() {
       return getServiceAddress('LinxFrameworkParametroOData');
    };
    var getDataServiceUrl = function () {
       return getServiceAddress(controllerName);
    };
    var setServiceBusUrl = function (url) {
       if (dataService) { dataService.serviceName = (isNullOrEmpty(url) ? getDataServiceUrl() : url + controllerName); }
    };
    var initializePOCO = function(ownerReference, entityName) {
       if (ownerReference && !ownerReference.RowDataId) { eval(entityName + 'Initializer(ownerReference, true);'); }
    };
    var businessAssemblyName = 'Linx.Framework.BV';
    var controllerName = 'LinxFrameworkParametro';
    var dataService = new breeze.DataService({
        serviceName: getDataServiceUrl(),
        hasServerMetadata: false // don't ask the server for metadata
    });
    var manager = new breeze.EntityManager({ dataService: dataService });
    manager.entityChanged.subscribe(function(changeArgs) {
        if (changeArgs.entityAction === breeze.EntityAction.PropertyChange) {
            if ((typeof changeArgs.args.newValue) === 'number' && changeArgs.args.oldValue < 0 && changeArgs.args.newValue > 0 && changeArgs.entity.isPrimaryKey(changeArgs.args.propertyName)) vm.replaceInnerUIsKeys(changeArgs.entity, changeArgs.args.propertyName, changeArgs.args.oldValue, changeArgs.args.newValue);
            if (typeof changeArgs.entity.OnPropertyChanged == 'function')
                changeArgs.entity.OnPropertyChanged(changeArgs.args.propertyName, changeArgs.args.oldValue, changeArgs.args.newValue);
        }
    });
    var enableChangeTrack = true;
    var entityPropChanged = function(entity, propName, oldVal, newVal) {
        if (!enableChangeTrack) return true;
        var result = true;
        if ((typeof entity.OnPropertyChanged == 'function') && oldVal !== newVal)
            result = (entity.OnPropertyChanged(propName, oldVal, newVal) !== false);
        if (result && ['U', 'I', 'D'].indexOf(entity.ChangeState) < 0) { entity.createOriginal(propName, oldVal); entity.ChangeState = 'U'; if (entity.setParentAsModified) entity.setParentAsModified(); }
        if (result && (typeof newVal) === 'number' && oldVal < 0 && newVal > 0 && entity.isPrimaryKey(propName)) vm.replaceInnerUIsKeys(entity, propName, oldVal, newVal);
        return result;
    }
    var metadataStore = manager.metadataStore;
    var EntityQuery = breeze.EntityQuery;
    // Extract Breeze metadata definition types
    var DataType = breeze.DataType;
    var AutoGeneratedKeyType = breeze.AutoGeneratedKeyType;
    var Validator = breeze.Validator;
    Validator.hasValueValidator = new breeze.Validator('hasValueValidator', hasValueValidationFn, { messageTemplate: "'%displayName%' é requerido" });
    //#region Metadata Info
    var metadataInfo = [];
    var dataExportInfo = [];
    var entityNames = [];
    var lookUpNames = [];
    var entitylookUps = [];
    entityNames.push('TcsParametro');
    metadataInfo['TcsParametro'] = [
        { key: 'ColunaCodValida', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Coluna Cod Valida', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ColunaDescValida', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Coluna Desc Valida', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DescGrupoParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'DescGrupoParametro', lookupVisibleColumns: '', isRequired: true, maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Grupo', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DescParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Descrição', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DescricaoAplicativo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'DescricaoAplicativo', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Descrição', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'FaixaFinal', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 70, validateMaxLength: true, isPartOfKey: false, headerText: 'Faixa Final', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'FaixaInicial', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 70, validateMaxLength: true, isPartOfKey: false, headerText: 'Faixa Inicial', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdGrupoParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdGrupoParametro', lookupVisibleColumns: '', maxLength: 6, isPartOfKey: false, headerText: 'Id Grupo Parametro', width: '270px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Parametro', width: '271px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'IdTcsAplicativo', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdTcsAplicativo', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Aplicativo', width: '166px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IndicaEnviaPdv', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Envia PDV', width: '153px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'IndicaParametroLinx', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Parâmetro Linx', width: '218px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: 'Tipo do Dado', width: '200px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametroName', isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Tipo do Dado (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'LxTipoValidacaoParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValidacaoParametro', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: 'Tipo Validação', width: '218px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxTipoValidacaoParametroName', isDomain: true, domainName: 'TipoValidacaoParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Tipo Validação (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'NivelAcesso', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 3, isPartOfKey: false, headerText: 'Nível Acesso Visualização', width: '361px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'NivelAcessoEdicao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 3, isPartOfKey: false, headerText: 'Nível Acesso Edição', width: '283px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'ObsParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Obs', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'PermiteVariacaoPorEntidade', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Permite Variação por Entidade', width: '413px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'TituloParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Título', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametro'] = [ 
        { name: 'TcsParametro', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametro', actionName: 'LinxFrameworkParametro/GetTcsParametroByEntitySearchNoAssociations', display: 'Parâmetros',  metaData: function() { return metadataInfo['TcsParametro']; } }
        , { name: 'TcsParametroTabelaSelecao', canExportMedia: false , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroTabelaSelecaoToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroTabelaSelecaoToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroTabelaSelecao', actionName: 'LinxFrameworkParametro/GetTcsParametroTabelaSelecaoByEntitySearchNoAssociations', display: 'Variação',  metaData: function() { return metadataInfo['TcsParametroTabelaSelecao']; } }
        , { name: 'TcsParametroValor', canExportMedia: false , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValor', actionName: 'LinxFrameworkParametro/GetTcsParametroValorByEntitySearchNoAssociations', display: 'Valor padrão',  metaData: function() { return metadataInfo['TcsParametroValor']; } }
        , { name: 'TcsParametroValorUsuario', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorUsuarioToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorUsuarioToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorUsuario', actionName: 'LinxFrameworkParametro/GetTcsParametroValorUsuarioByEntitySearchNoAssociations', display: 'Usuário',  metaData: function() { return metadataInfo['TcsParametroValorUsuario']; } }
        , { name: 'TcsParametroValorRede', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorRedeToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorRedeToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorRede', actionName: 'LinxFrameworkParametro/GetTcsParametroValorRedeByEntitySearchNoAssociations', display: 'Rede',  metaData: function() { return metadataInfo['TcsParametroValorRede']; } }
        , { name: 'TcsParametroValorGpecon', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorGpeconToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorGpeconToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorGpecon', actionName: 'LinxFrameworkParametro/GetTcsParametroValorGpeconByEntitySearchNoAssociations', display: 'Grupo Econômico',  metaData: function() { return metadataInfo['TcsParametroValorGpecon']; } }
        , { name: 'TcsParametroValorFilial', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorFilialToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorFilialToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorFilial', actionName: 'LinxFrameworkParametro/GetTcsParametroValorFilialByEntitySearchNoAssociations', display: 'Filial',  metaData: function() { return metadataInfo['TcsParametroValorFilial']; } }
        , { name: 'TcsParametroValorLjvLoja', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorLjvLojaToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorLjvLojaToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorLjvLoja', actionName: 'LinxFrameworkParametro/GetTcsParametroValorLjvLojaByEntitySearchNoAssociations', display: 'Loja',  metaData: function() { return metadataInfo['TcsParametroValorLjvLoja']; } }
        , { name: 'TcsParametroValorVariacaoGenerica', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoGenericaToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoGenericaToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorVariacaoGenerica', actionName: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoGenericaByEntitySearchNoAssociations', display: 'Demais Variações',  metaData: function() { return metadataInfo['TcsParametroValorVariacaoGenerica']; } }
    ];
    entitylookUps.push('TcsParametro');
    entitylookUps['TcsParametro'] = [];
    entitylookUps['TcsParametro'].push('LookUpTcsAplicativo');
    lookUpNames.push('LookUpTcsAplicativo');
    metadataInfo['LookUpTcsAplicativo'] = [
        { key: 'IdTcsAplicativo', relatedKey: 'IdTcsAplicativo', maxLength: 12, isPartOfKey: true, headerText: 'Aplicativo', width: '166px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'IdTcsAplicativo', relatedKey: 'IdTcsAplicativo', maxLength: 12, isPartOfKey: true, headerText: 'Aplicativo', width: '166px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'DescricaoAplicativo', relatedKey: 'DescricaoAplicativo', maxLength: 250, isPartOfKey: false, headerText: 'Descrição', width: '400px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'DescricaoAplicativo', relatedKey: 'DescricaoAplicativo', maxLength: 250, isPartOfKey: false, headerText: 'Descrição', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    entitylookUps['TcsParametro'].push('LookUpTcsParametroGrupoAutorizacao');
    lookUpNames.push('LookUpTcsParametroGrupoAutorizacao');
    metadataInfo['LookUpTcsParametroGrupoAutorizacao'] = [
        { key: 'DescGrupoParametro', relatedKey: 'DescGrupoParametro', maxLength: 60, isPartOfKey: false, headerText: 'Descrição Grupo', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdGrupoParametro', relatedKey: 'IdGrupoParametro', maxLength: 6, isPartOfKey: true, headerText: 'Id Grupo', width: '140px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null }
    ];
    entitylookUps['TcsParametro'].push('LookUpTcsAplicativo');
    lookUpNames.push('LookUpTcsAplicativo');
    metadataInfo['LookUpTcsAplicativo'] = [
        { key: 'IdTcsAplicativo', relatedKey: 'IdTcsAplicativo', maxLength: 12, isPartOfKey: true, headerText: 'Aplicativo', width: '166px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'IdTcsAplicativo', relatedKey: 'IdTcsAplicativo', maxLength: 12, isPartOfKey: true, headerText: 'Aplicativo', width: '166px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'DescricaoAplicativo', relatedKey: 'DescricaoAplicativo', maxLength: 250, isPartOfKey: false, headerText: 'Descrição', width: '400px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'DescricaoAplicativo', relatedKey: 'DescricaoAplicativo', maxLength: 250, isPartOfKey: false, headerText: 'Descrição', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    entityNames.push('TcsParametroValorP');
    metadataInfo['TcsParametroValorP'] = [
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Parametro', width: '192px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 12, isPartOfKey: true, headerText: 'Id Parametro Valor', width: '270px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Possui Variacao', width: '231px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Valor Padrão', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: 'Tipo do Dado', width: '200px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametroName', isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Tipo do Dado (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'ValorParametroData', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'ValorParametroData', width: '120px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null },
        { key: 'ValorParametroBool', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'ValorParametroBool', width: '250px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorP'] = [ 
        { name: 'TcsParametroValorP', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorPToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorPToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorP', actionName: 'LinxFrameworkParametro/GetTcsParametroValorPByEntitySearchNoAssociations', display: 'Valor padrão',  metaData: function() { return metadataInfo['TcsParametroValorP']; } }
    ];
    entitylookUps.push('TcsParametroValorP');
    entitylookUps['TcsParametroValorP'] = [];
    entityNames.push('ParametroValor');
    metadataInfo['ParametroValor'] = [
        { key: 'TituloParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: true, headerText: 'TituloParametro', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'ValorParametro', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'TipoValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'TipoValorParametro', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['ParametroValor'] = [ 
        { name: 'ParametroValor', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetParametroValorToExcel', actionReport: 'LinxFrameworkParametro/GetParametroValorToReportXml', actionFeed: 'LinxFrameworkParametroOData/ParametroValor', actionName: 'LinxFrameworkParametro/GetParametroValorByEntitySearchNoAssociations', display: 'ParametroValor',  metaData: function() { return metadataInfo['ParametroValor']; } }
    ];
    entitylookUps.push('ParametroValor');
    entitylookUps['ParametroValor'] = [];
    entityNames.push('TcsParametroValorVariacaoP');
    metadataInfo['TcsParametroValorVariacaoP'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Chave Selecao', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Parametro Valor', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Possui Variacao', width: '231px', dataType: 'bool', format: 'checkbox', hidden: true, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Valor Padrão', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorVariacaoP'] = [ 
        { name: 'TcsParametroValorVariacaoP', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoPToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoPToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorVariacaoP', actionName: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoPByEntitySearchNoAssociations', display: 'Valor Variação',  metaData: function() { return metadataInfo['TcsParametroValorVariacaoP']; } }
    ];
    entitylookUps.push('TcsParametroValorVariacaoP');
    entitylookUps['TcsParametroValorVariacaoP'] = [];
    entityNames.push('TcsParametroTabelaSelecao');
    metadataInfo['TcsParametroTabelaSelecao'] = [
        { key: 'EntityUniqueKey', maxLength: 0, isPartOfKey: true, headerText: 'EntityUniqueKey', width: '50px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'DescTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'DescTabela', lookupVisibleColumns: '', isRequired: true, maxLength: 80, validateMaxLength: true, isPartOfKey: false, headerText: 'Descrição', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Parametro', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdTabelaSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Tabela Selecao', width: '257px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxParametroHierarquia', isQbeZero: false, isDomain: true, domainName: 'ParametroHierarquia', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: 'Hierarquia', width: '200px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxParametroHierarquiaName', isDomain: true, domainName: 'ParametroHierarquia', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Hierarquia (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'NomeTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'NomeTabela', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Nome Tabela', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'UidTabela', lookupVisibleColumns: '', isRequired: true, maxLength: 12, isPartOfKey: false, headerText: 'Uid Tabela', width: '271px', dataType: 'string', format: '', hidden: true, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroTabelaSelecao'] = [ 
        { name: 'TcsParametroTabelaSelecao', canExportMedia: false , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroTabelaSelecaoToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroTabelaSelecaoToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroTabelaSelecao', actionName: 'LinxFrameworkParametro/GetTcsParametroTabelaSelecaoByEntitySearchNoAssociations', display: 'Variação',  metaData: function() { return metadataInfo['TcsParametroTabelaSelecao']; } }
    ];
    entitylookUps.push('TcsParametroTabelaSelecao');
    entitylookUps['TcsParametroTabelaSelecao'] = [];
    entitylookUps['TcsParametroTabelaSelecao'].push('LookUpTcsTabelaAutorizacaoSelecao');
    lookUpNames.push('LookUpTcsTabelaAutorizacaoSelecao');
    metadataInfo['LookUpTcsTabelaAutorizacaoSelecao'] = [
        { key: 'NomeTabela', relatedKey: 'NomeTabela', maxLength: 250, isPartOfKey: false, headerText: 'Nome Tabela', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DescTabela', relatedKey: 'DescTabela', maxLength: 80, isPartOfKey: false, headerText: 'Descrição', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'UidTabela', relatedKey: 'UidTabela', maxLength: 12, isPartOfKey: true, headerText: 'Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null }
    ];
    entityNames.push('TcsParametroValor');
    metadataInfo['TcsParametroValor'] = [
        { key: 'EntityUniqueKey', maxLength: 0, isPartOfKey: true, headerText: 'EntityUniqueKey', width: '50px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Parametro', width: '192px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Id Parametro Valor', width: '270px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Possui Variacao', width: '231px', dataType: 'bool', format: 'checkbox', hidden: true, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'ValorParametro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametroBool', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 25, isPartOfKey: false, headerText: 'Verdadeiro', width: '166px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'ValorParametroData', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 25, isPartOfKey: false, headerText: 'ValorParametroData', width: '120px', dataType: 'date', format: 'dd/MM/yyyy', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValor'] = [ 
        { name: 'TcsParametroValor', canExportMedia: false , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValor', actionName: 'LinxFrameworkParametro/GetTcsParametroValorByEntitySearchNoAssociations', display: 'Valor padrão',  metaData: function() { return metadataInfo['TcsParametroValor']; } }
    ];
    entitylookUps.push('TcsParametroValor');
    entitylookUps['TcsParametroValor'] = [];
    entityNames.push('ParametroValorVariacao');
    metadataInfo['ParametroValorVariacao'] = [
        { key: 'TituloParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: true, headerText: 'TituloParametro', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'NomeTabela', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ChaveVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'ChaveVariacao', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'ValorVariacao', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['ParametroValorVariacao'] = [ 
        { name: 'ParametroValorVariacao', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetParametroValorVariacaoToExcel', actionReport: 'LinxFrameworkParametro/GetParametroValorVariacaoToReportXml', actionFeed: 'LinxFrameworkParametroOData/ParametroValorVariacao', actionName: 'LinxFrameworkParametro/GetParametroValorVariacaoByEntitySearchNoAssociations', display: 'ParametroValorVariacao',  metaData: function() { return metadataInfo['ParametroValorVariacao']; } }
    ];
    entitylookUps.push('ParametroValorVariacao');
    entitylookUps['ParametroValorVariacao'] = [];
    entityNames.push('TcsUsuarioParametro');
    metadataInfo['TcsUsuarioParametro'] = [
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Usuario', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'NomeUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Nome Usuario', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'UidUsuarioString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'UidUsuarioString', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsUsuarioParametro'] = [ 
        { name: 'TcsUsuarioParametro', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsUsuarioParametroToExcel', actionReport: 'LinxFrameworkParametro/GetTcsUsuarioParametroToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsUsuarioParametro', actionName: 'LinxFrameworkParametro/GetTcsUsuarioParametroByEntitySearchNoAssociations', display: 'TcsUsuarioParametro',  metaData: function() { return metadataInfo['TcsUsuarioParametro']; } }
    ];
    entitylookUps.push('TcsUsuarioParametro');
    entitylookUps['TcsUsuarioParametro'] = [];
    entityNames.push('TcsParametroValorUsuario');
    metadataInfo['TcsParametroValorUsuario'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'ChaveSelecao', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Chave', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Id Parametro', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: true, headerText: 'Id Parametro Valor', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdUsuario', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: '  Id Usuario', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', isPartOfKey: false, headerText: 'Tipo do Dado', width: '200px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'LxDatatypeParametroName', isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Tipo do Dado (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'NomeUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'NomeUsuario', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Nome Usuário', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Possui Variacao', width: '231px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 36, isPartOfKey: false, headerText: 'Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'UidUsuarioString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'UidUsuarioString', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: '  ', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'ValorParametro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametroBool', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 25, isPartOfKey: false, headerText: 'Verdadeiro', width: '166px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorUsuario'] = [ 
        { name: 'TcsParametroValorUsuario', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorUsuarioToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorUsuarioToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorUsuario', actionName: 'LinxFrameworkParametro/GetTcsParametroValorUsuarioByEntitySearchNoAssociations', display: 'Usuário',  metaData: function() { return metadataInfo['TcsParametroValorUsuario']; } }
    ];
    entitylookUps.push('TcsParametroValorUsuario');
    entitylookUps['TcsParametroValorUsuario'] = [];
    entitylookUps['TcsParametroValorUsuario'].push('LookTcsParametroUsuario');
    lookUpNames.push('LookTcsParametroUsuario');
    metadataInfo['LookTcsParametroUsuario'] = [
        { key: 'IdUsuario', relatedKey: 'IdUsuario', maxLength: 1, isPartOfKey: true, headerText: 'IdUsuario', width: '40px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'NomeUsuario', relatedKey: 'NomeUsuario', maxLength: 25, isPartOfKey: false, headerText: 'NomeUsuario', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ChaveSelecao', relatedKey: 'ChaveSelecao', maxLength: 25, isPartOfKey: false, headerText: 'ChaveSelecao', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'UidUsuarioString', relatedKey: 'UidUsuarioString', maxLength: 25, isPartOfKey: false, headerText: 'UidUsuarioString', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null }
    ];
    entityNames.push('TbcBandeiraRedeParametro');
    metadataInfo['TbcBandeiraRedeParametro'] = [
        { key: 'CodBandeiraRede', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 25, validateMaxLength: true, isPartOfKey: false, headerText: 'Cod Bandeira Rede', width: '257px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DescBandeiraRede', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Desc Bandeira Rede', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdBandeiraRedeParam', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 12, isPartOfKey: true, headerText: 'Id Bandeira Rede', width: '244px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdBandeiraRedeString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'IdBandeiraRedeString', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TbcBandeiraRedeParametro'] = [ 
        { name: 'TbcBandeiraRedeParametro', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTbcBandeiraRedeParametroToExcel', actionReport: 'LinxFrameworkParametro/GetTbcBandeiraRedeParametroToReportXml', actionFeed: 'LinxFrameworkParametroOData/TbcBandeiraRedeParametro', actionName: 'LinxFrameworkParametro/GetTbcBandeiraRedeParametroByEntitySearchNoAssociations', display: 'TbcBandeiraRedeParametro',  metaData: function() { return metadataInfo['TbcBandeiraRedeParametro']; } }
    ];
    entitylookUps.push('TbcBandeiraRedeParametro');
    entitylookUps['TbcBandeiraRedeParametro'] = [];
    entityNames.push('TcsParametroValorRede');
    metadataInfo['TcsParametroValorRede'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'ChaveSelecao', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Chave', width: '400px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'CodBandeiraRede', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'CodBandeiraRede', lookupVisibleColumns: '', isRequired: true, maxLength: 25, validateMaxLength: true, isPartOfKey: false, headerText: 'Código Bandeira / Rede', width: '322px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DescBandeiraRede', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'DescBandeiraRede', lookupVisibleColumns: '', isRequired: true, maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Bandeira / Rede', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdBandeiraRedeParam', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdBandeiraRedeParam', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: '  Id Bandeira Rede', width: '270px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'IdBandeiraRedeString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdBandeiraRedeString', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: '  ', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: '  Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: true, headerText: '  Id Parametro Valor', width: '296px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'LxDatatypeParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', isPartOfKey: false, headerText: 'Tipo do Dado', width: '200px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'LxDatatypeParametroName', isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Tipo do Dado (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: '  Possui Variacao', width: '257px', dataType: 'bool', format: 'checkbox', hidden: true, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 36, isPartOfKey: false, headerText: '  Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'ValorParametro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametroBool', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 25, isPartOfKey: false, headerText: 'Verdadeiro', width: '166px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorRede'] = [ 
        { name: 'TcsParametroValorRede', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorRedeToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorRedeToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorRede', actionName: 'LinxFrameworkParametro/GetTcsParametroValorRedeByEntitySearchNoAssociations', display: 'Rede',  metaData: function() { return metadataInfo['TcsParametroValorRede']; } }
    ];
    entitylookUps.push('TcsParametroValorRede');
    entitylookUps['TcsParametroValorRede'] = [];
    entitylookUps['TcsParametroValorRede'].push('LookUpParametroRede');
    lookUpNames.push('LookUpParametroRede');
    metadataInfo['LookUpParametroRede'] = [
        { key: 'IdBandeiraRedeParam', relatedKey: 'IdBandeiraRedeParam', maxLength: 1, isPartOfKey: true, headerText: 'IdBandeiraRedeParam', width: '250px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'CodBandeiraRede', relatedKey: 'CodBandeiraRede', maxLength: 2, isPartOfKey: false, headerText: 'Código Bandeira / Rede', width: '322px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DescBandeiraRede', relatedKey: 'DescBandeiraRede', maxLength: 6, isPartOfKey: false, headerText: 'Bandeira / Rede', width: '231px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ChaveSelecao', relatedKey: 'ChaveSelecao', maxLength: 25, isPartOfKey: false, headerText: 'ChaveSelecao', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'IdBandeiraRedeString', relatedKey: 'IdBandeiraRedeString', maxLength: 25, isPartOfKey: false, headerText: 'IdBandeiraRedeString', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null }
    ];
    entityNames.push('TbcGrupoEconomicoParametro');
    metadataInfo['TbcGrupoEconomicoParametro'] = [
        { key: 'DescGrupoEconomico', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Desc Grupo Economico', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdGpecon', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 12, isPartOfKey: true, headerText: 'Id Gpecon', width: '153px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdGpeconString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'IdGpeconString', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TbcGrupoEconomicoParametro'] = [ 
        { name: 'TbcGrupoEconomicoParametro', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTbcGrupoEconomicoParametroToExcel', actionReport: 'LinxFrameworkParametro/GetTbcGrupoEconomicoParametroToReportXml', actionFeed: 'LinxFrameworkParametroOData/TbcGrupoEconomicoParametro', actionName: 'LinxFrameworkParametro/GetTbcGrupoEconomicoParametroByEntitySearchNoAssociations', display: 'TbcGrupoEconomicoParametro',  metaData: function() { return metadataInfo['TbcGrupoEconomicoParametro']; } }
    ];
    entitylookUps.push('TbcGrupoEconomicoParametro');
    entitylookUps['TbcGrupoEconomicoParametro'] = [];
    entityNames.push('TcsParametroValorGpecon');
    metadataInfo['TcsParametroValorGpecon'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'ChaveSelecao', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Chave', width: '400px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'DescGrupoEconomico', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'DescGrupoEconomico', lookupVisibleColumns: '', isRequired: true, maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Grupo Econômico', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdGpecon', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdGpecon', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Código Grupo Econômico', width: '322px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdGpeconString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdGpeconString', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: '  ', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: '  Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: true, headerText: '  Id Parametro Valor', width: '296px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'LxDatatypeParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', isPartOfKey: false, headerText: 'Tipo do Dado', width: '200px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'LxDatatypeParametroName', isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Tipo do Dado (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: '  Possui Variacao', width: '257px', dataType: 'bool', format: 'checkbox', hidden: true, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 36, isPartOfKey: false, headerText: '  Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'ValorParametro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametroBool', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 25, isPartOfKey: false, headerText: 'Verdadeiro', width: '166px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorGpecon'] = [ 
        { name: 'TcsParametroValorGpecon', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorGpeconToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorGpeconToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorGpecon', actionName: 'LinxFrameworkParametro/GetTcsParametroValorGpeconByEntitySearchNoAssociations', display: 'Grupo Econômico',  metaData: function() { return metadataInfo['TcsParametroValorGpecon']; } }
    ];
    entitylookUps.push('TcsParametroValorGpecon');
    entitylookUps['TcsParametroValorGpecon'] = [];
    entitylookUps['TcsParametroValorGpecon'].push('LookUpParametroGpecon');
    lookUpNames.push('LookUpParametroGpecon');
    metadataInfo['LookUpParametroGpecon'] = [
        { key: 'IdGpecon', relatedKey: 'IdGpecon', maxLength: 1, isPartOfKey: true, headerText: ' Código Grupo Econômico', width: '335px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'DescGrupoEconomico', relatedKey: 'DescGrupoEconomico', maxLength: 6, isPartOfKey: false, headerText: 'Grupo Econômico', width: '231px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdGpeconString', relatedKey: 'IdGpeconString', maxLength: 1, isPartOfKey: false, headerText: 'IdGpeconString', width: '40px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'ChaveSelecao', relatedKey: 'ChaveSelecao', maxLength: 25, isPartOfKey: false, headerText: 'ChaveSelecao', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null }
    ];
    entityNames.push('TbcFilialParametro');
    metadataInfo['TbcFilialParametro'] = [
        { key: 'CodigoFilial', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 18, validateMaxLength: true, isPartOfKey: false, headerText: 'Codigo Filial', width: '205px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdFilialPfj', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 12, isPartOfKey: true, headerText: 'Id Filial Pfj', width: '205px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdFilialString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'IdFilialString', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeFilial', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Nome Filial', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TbcFilialParametro'] = [ 
        { name: 'TbcFilialParametro', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTbcFilialParametroToExcel', actionReport: 'LinxFrameworkParametro/GetTbcFilialParametroToReportXml', actionFeed: 'LinxFrameworkParametroOData/TbcFilialParametro', actionName: 'LinxFrameworkParametro/GetTbcFilialParametroByEntitySearchNoAssociations', display: 'TbcFilialParametro',  metaData: function() { return metadataInfo['TbcFilialParametro']; } }
    ];
    entitylookUps.push('TbcFilialParametro');
    entitylookUps['TbcFilialParametro'] = [];
    entityNames.push('TcsParametroValorFilial');
    metadataInfo['TcsParametroValorFilial'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'ChaveSelecao', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Chave', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'CodigoFilial', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'CodigoFilial', lookupVisibleColumns: '', maxLength: 18, validateMaxLength: true, isPartOfKey: false, headerText: 'Código Filial', width: '205px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdFilialPfj', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdFilialPfj', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: '  Id Filial Pfj', width: '231px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'IdFilialString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdFilialString', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: '  ', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: '  Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: true, headerText: '  Id Parametro Valor', width: '296px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'LxDatatypeParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', isPartOfKey: false, headerText: 'Tipo do Dado', width: '200px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'LxDatatypeParametroName', isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Tipo do Dado (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'NomeFilial', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'NomeFilial', lookupVisibleColumns: '', maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Filial', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: '  Possui Variacao', width: '257px', dataType: 'bool', format: 'checkbox', hidden: true, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 36, isPartOfKey: false, headerText: '  Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'ValorParametro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametroBool', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 25, isPartOfKey: false, headerText: 'Verdadeiro', width: '166px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorFilial'] = [ 
        { name: 'TcsParametroValorFilial', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorFilialToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorFilialToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorFilial', actionName: 'LinxFrameworkParametro/GetTcsParametroValorFilialByEntitySearchNoAssociations', display: 'Filial',  metaData: function() { return metadataInfo['TcsParametroValorFilial']; } }
    ];
    entitylookUps.push('TcsParametroValorFilial');
    entitylookUps['TcsParametroValorFilial'] = [];
    entitylookUps['TcsParametroValorFilial'].push('LookUpParametroFilial');
    lookUpNames.push('LookUpParametroFilial');
    metadataInfo['LookUpParametroFilial'] = [
        { key: 'CodigoFilial', relatedKey: 'CodigoFilial', maxLength: 1, isPartOfKey: false, headerText: 'Código Filial', width: '205px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdFilialPfj', relatedKey: 'IdFilialPfj', maxLength: 1, isPartOfKey: true, headerText: 'IdFilialPfj', width: '40px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'IdFilialString', relatedKey: 'IdFilialString', maxLength: 2, isPartOfKey: false, headerText: 'IdFilialString', width: '40px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'NomeFilial', relatedKey: 'NomeFilial', maxLength: 6, isPartOfKey: false, headerText: 'Filial', width: '114px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ChaveSelecao', relatedKey: 'ChaveSelecao', maxLength: 25, isPartOfKey: false, headerText: 'ChaveSelecao', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null }
    ];
    entityNames.push('TcsParametroValorLjvLoja');
    metadataInfo['TcsParametroValorLjvLoja'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'ChaveSelecao', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Chave', width: '400px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'CodLoja', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'CodLoja', lookupVisibleColumns: '', isRequired: true, maxLength: 20, validateMaxLength: true, isPartOfKey: false, headerText: 'Código Loja', width: '221px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'DescLoja', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'DescLoja', lookupVisibleColumns: '', isRequired: true, maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Loja', width: '421px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'IdLoja', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdLoja', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: '  Id Loja', width: '250px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'IdLojaString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'IdLojaString', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: '  ', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: '  Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: true, headerText: '  Id Parametro Valor', width: '296px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'LxDatatypeParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', isPartOfKey: false, headerText: 'Tipo do Dado', width: '200px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'LxDatatypeParametroName', isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Tipo do Dado (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: '  Possui Variacao', width: '257px', dataType: 'bool', format: 'checkbox', hidden: true, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 36, isPartOfKey: false, headerText: '  Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'ValorParametro', width: '400px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'ValorParametroBool', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 25, isPartOfKey: false, headerText: 'Verdadeiro', width: '166px', dataType: 'bool', format: 'checkbox', hidden: true, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorLjvLoja'] = [ 
        { name: 'TcsParametroValorLjvLoja', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorLjvLojaToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorLjvLojaToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorLjvLoja', actionName: 'LinxFrameworkParametro/GetTcsParametroValorLjvLojaByEntitySearchNoAssociations', display: 'Loja',  metaData: function() { return metadataInfo['TcsParametroValorLjvLoja']; } }
    ];
    entitylookUps.push('TcsParametroValorLjvLoja');
    entitylookUps['TcsParametroValorLjvLoja'] = [];
    entitylookUps['TcsParametroValorLjvLoja'].push('LookUpParametroLoja');
    lookUpNames.push('LookUpParametroLoja');
    metadataInfo['LookUpParametroLoja'] = [
        { key: 'CodLoja', relatedKey: 'CodLoja', maxLength: 2, isPartOfKey: false, headerText: 'Código Loja', width: '179px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DescLoja', relatedKey: 'DescLoja', maxLength: 6, isPartOfKey: false, headerText: 'Loja', width: '88px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdLoja', relatedKey: 'IdLoja', maxLength: 1, isPartOfKey: true, headerText: 'IdLoja', width: '40px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'IdLojaString', relatedKey: 'IdLojaString', maxLength: 2, isPartOfKey: false, headerText: 'IdLojaString', width: '40px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'ChaveSelecao', relatedKey: 'ChaveSelecao', maxLength: 25, isPartOfKey: false, headerText: 'ChaveSelecao', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null }
    ];
    entityNames.push('TcsParametroValorVariacaoGenericaP');
    metadataInfo['TcsParametroValorVariacaoGenericaP'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Chave Selecao', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Parametro Valor', width: '270px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Possui Variacao', width: '231px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: 'Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Valor Parametro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorVariacaoGenericaP'] = [ 
        { name: 'TcsParametroValorVariacaoGenericaP', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoGenericaPToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoGenericaPToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorVariacaoGenericaP', actionName: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoGenericaPByEntitySearchNoAssociations', display: 'TcsParametroValorVariacaoGenericaP',  metaData: function() { return metadataInfo['TcsParametroValorVariacaoGenericaP']; } }
    ];
    entitylookUps.push('TcsParametroValorVariacaoGenericaP');
    entitylookUps['TcsParametroValorVariacaoGenericaP'] = [];
    entityNames.push('TcsParametroValorVariacaoGenerica');
    metadataInfo['TcsParametroValorVariacaoGenerica'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Chave', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: '  Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: true, headerText: '  Id Parametro Valor', width: '296px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'LxDatatypeParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', isPartOfKey: false, headerText: 'Tipo do Dado', width: '200px', dataType: 'number', format: 'int', hidden: true, unbound: false, group: null },
        { key: 'LxDatatypeParametroName', isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Tipo do Dado (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'NomeTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'NomeTabela', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Nome Tabela', width: '421px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: '  Possui Variacao', width: '257px', dataType: 'bool', format: 'checkbox', hidden: true, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: 'UidTabela', lookupVisibleColumns: '', maxLength: 36, isPartOfKey: false, headerText: '  Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'ValorParametro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametroBool', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 25, isPartOfKey: false, headerText: 'Verdadeiro', width: '166px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorVariacaoGenerica'] = [ 
        { name: 'TcsParametroValorVariacaoGenerica', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoGenericaToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoGenericaToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorVariacaoGenerica', actionName: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoGenericaByEntitySearchNoAssociations', display: 'Demais Variações',  metaData: function() { return metadataInfo['TcsParametroValorVariacaoGenerica']; } }
    ];
    entitylookUps.push('TcsParametroValorVariacaoGenerica');
    entitylookUps['TcsParametroValorVariacaoGenerica'] = [];
    entitylookUps['TcsParametroValorVariacaoGenerica'].push('LookUpTcsTabelaAutorizacaoC');
    lookUpNames.push('LookUpTcsTabelaAutorizacaoC');
    metadataInfo['LookUpTcsTabelaAutorizacaoC'] = [
        { key: 'UidTabela', relatedKey: 'UidTabela', maxLength: 1, isPartOfKey: true, headerText: 'UidTabela', width: '250px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'DescTabela', relatedKey: 'DescTabela', maxLength: 8, isPartOfKey: false, headerText: 'Descrição', width: '153px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeTabela', relatedKey: 'NomeTabela', maxLength: 25, isPartOfKey: false, headerText: 'Nome Tabela', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ClasseNome', relatedKey: 'ClasseNome', maxLength: 4, isPartOfKey: false, headerText: 'Código Transação', width: '244px', dataType: 'string', format: '', hidden: true, unbound: false, group: null }
    ];
    entityNames.push('TcsParametroValorP1');
    metadataInfo['TcsParametroValorP1'] = [
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: 'Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 24, isPartOfKey: true, headerText: 'Id Parametro Valor', width: '271px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Possui Variacao', width: '231px', dataType: 'bool', format: 'checkbox', hidden: true, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Valor Padrão', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorP1'] = [ 
        { name: 'TcsParametroValorP1', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorP1ToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorP1ToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorP1', actionName: 'LinxFrameworkParametro/GetTcsParametroValorP1ByEntitySearchNoAssociations', display: 'Valor padrão',  metaData: function() { return metadataInfo['TcsParametroValorP1']; } }
    ];
    entitylookUps.push('TcsParametroValorP1');
    entitylookUps['TcsParametroValorP1'] = [];
    entityNames.push('ParametroInfo');
    metadataInfo['ParametroInfo'] = [
        { key: 'IdTcsAmbiente', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: true, headerText: 'IdTcsAmbiente', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'TituloParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: true, headerText: 'TituloParametro', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'ValorParametro', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['ParametroInfo'] = [ 
        { name: 'ParametroInfo', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetParametroInfoToExcel', actionReport: 'LinxFrameworkParametro/GetParametroInfoToReportXml', actionFeed: 'LinxFrameworkParametroOData/ParametroInfo', actionName: 'LinxFrameworkParametro/GetParametroInfoByEntitySearchNoAssociations', display: 'ParametroInfo',  metaData: function() { return metadataInfo['ParametroInfo']; } }
    ];
    entitylookUps.push('ParametroInfo');
    entitylookUps['ParametroInfo'] = [];
    entityNames.push('TcsParametroValorP2');
    metadataInfo['TcsParametroValorP2'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Chave Selecao', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: true, headerText: 'Id Parametro Valor', width: '270px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'Possui Variacao', width: '250px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 36, isPartOfKey: false, headerText: 'Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: 'Valor Parametro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorP2'] = [ 
        { name: 'TcsParametroValorP2', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorP2ToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorP2ToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorP2', actionName: 'LinxFrameworkParametro/GetTcsParametroValorP2ByEntitySearchNoAssociations', display: 'TcsParametroValorP2',  metaData: function() { return metadataInfo['TcsParametroValorP2']; } }
    ];
    entitylookUps.push('TcsParametroValorP2');
    entitylookUps['TcsParametroValorP2'] = [];
    entityNames.push('TcsParametroValorLojaP');
    metadataInfo['TcsParametroValorLojaP'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: ' Chave Selecao', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: ' Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: true, headerText: ' Id Parametro Valor', width: '283px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: ' Possui Variacao', width: '250px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 36, isPartOfKey: false, headerText: ' Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: ' Valor Parametro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: ' Tipo do Dado', width: '205px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametroName', isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: ' Tipo do Dado (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'CodLoja', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 20, validateMaxLength: true, isPartOfKey: false, headerText: ' Cod Loja', width: '200px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DescLoja', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: ' Desc Loja', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdLoja', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: ' Id Loja', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdLojaString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: ' ', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametroBool', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'ValorParametroBool', width: '250px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorLojaP'] = [ 
        { name: 'TcsParametroValorLojaP', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorLojaPToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorLojaPToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorLojaP', actionName: 'LinxFrameworkParametro/GetTcsParametroValorLojaPByEntitySearchNoAssociations', display: 'TcsParametroValorLojaP',  metaData: function() { return metadataInfo['TcsParametroValorLojaP']; } }
    ];
    entitylookUps.push('TcsParametroValorLojaP');
    entitylookUps['TcsParametroValorLojaP'] = [];
    entityNames.push('LjvLojaParametro');
    metadataInfo['LjvLojaParametro'] = [
        { key: 'CodLoja', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 20, validateMaxLength: true, isPartOfKey: false, headerText: 'Cod Loja', width: '200px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DescLoja', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: 'Desc Loja', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdLoja', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: true, headerText: 'Id Loja', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdLojaString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: 'IdLojaString', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['LjvLojaParametro'] = [ 
        { name: 'LjvLojaParametro', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetLjvLojaParametroToExcel', actionReport: 'LinxFrameworkParametro/GetLjvLojaParametroToReportXml', actionFeed: 'LinxFrameworkParametroOData/LjvLojaParametro', actionName: 'LinxFrameworkParametro/GetLjvLojaParametroByEntitySearchNoAssociations', display: 'LjvLojaParametro',  metaData: function() { return metadataInfo['LjvLojaParametro']; } }
    ];
    entitylookUps.push('LjvLojaParametro');
    entitylookUps['LjvLojaParametro'] = [];
    entityNames.push('TcsParametroValorUsuarioP');
    metadataInfo['TcsParametroValorUsuarioP'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: ' Chave Selecao', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: ' Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: true, headerText: ' Id Parametro Valor', width: '283px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: ' Possui Variacao', width: '250px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 36, isPartOfKey: false, headerText: ' Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: ' Valor Parametro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: ' Tipo do Dado', width: '205px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametroName', isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: ' Tipo do Dado (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'IdUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 24, isPartOfKey: false, headerText: ' Id Usuario', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'NomeUsuario', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: ' Nome Usuario', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'UidUsuarioString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: ' ', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametroBool', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'ValorParametroBool', width: '250px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorUsuarioP'] = [ 
        { name: 'TcsParametroValorUsuarioP', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorUsuarioPToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorUsuarioPToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorUsuarioP', actionName: 'LinxFrameworkParametro/GetTcsParametroValorUsuarioPByEntitySearchNoAssociations', display: 'TcsParametroValorUsuarioP',  metaData: function() { return metadataInfo['TcsParametroValorUsuarioP']; } }
    ];
    entitylookUps.push('TcsParametroValorUsuarioP');
    entitylookUps['TcsParametroValorUsuarioP'] = [];
    entityNames.push('TcsParametroValorRedeP');
    metadataInfo['TcsParametroValorRedeP'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: ' Chave Selecao', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: ' Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: true, headerText: ' Id Parametro Valor', width: '283px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: ' Possui Variacao', width: '250px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 36, isPartOfKey: false, headerText: ' Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: ' Valor Parametro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: ' Tipo do Dado', width: '205px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametroName', isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: ' Tipo do Dado (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'CodBandeiraRede', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 25, validateMaxLength: true, isPartOfKey: false, headerText: ' Cod Bandeira Rede', width: '270px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'DescBandeiraRede', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: ' Desc Bandeira Rede', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdBandeiraRedeParam', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: ' Id Bandeira Rede', width: '257px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdBandeiraRedeString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: ' ', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametroBool', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'ValorParametroBool', width: '250px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorRedeP'] = [ 
        { name: 'TcsParametroValorRedeP', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorRedePToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorRedePToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorRedeP', actionName: 'LinxFrameworkParametro/GetTcsParametroValorRedePByEntitySearchNoAssociations', display: 'TcsParametroValorRedeP',  metaData: function() { return metadataInfo['TcsParametroValorRedeP']; } }
    ];
    entitylookUps.push('TcsParametroValorRedeP');
    entitylookUps['TcsParametroValorRedeP'] = [];
    entityNames.push('TcsParametroValorGpeconP');
    metadataInfo['TcsParametroValorGpeconP'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: ' Chave Selecao', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: ' Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: true, headerText: ' Id Parametro Valor', width: '283px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: ' Possui Variacao', width: '250px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 36, isPartOfKey: false, headerText: ' Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: ' Valor Parametro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: ' Tipo do Dado', width: '205px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametroName', isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: ' Tipo do Dado (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'DescGrupoEconomico', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: ' Desc Grupo Economico', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdGpecon', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: ' Id Gpecon', width: '166px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdGpeconString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: ' ', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametroBool', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'ValorParametroBool', width: '250px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorGpeconP'] = [ 
        { name: 'TcsParametroValorGpeconP', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorGpeconPToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorGpeconPToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorGpeconP', actionName: 'LinxFrameworkParametro/GetTcsParametroValorGpeconPByEntitySearchNoAssociations', display: 'TcsParametroValorGpeconP',  metaData: function() { return metadataInfo['TcsParametroValorGpeconP']; } }
    ];
    entitylookUps.push('TcsParametroValorGpeconP');
    entitylookUps['TcsParametroValorGpeconP'] = [];
    entityNames.push('TcsParametroValorFilialP');
    metadataInfo['TcsParametroValorFilialP'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: ' Chave Selecao', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: ' Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: true, headerText: ' Id Parametro Valor', width: '283px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: ' Possui Variacao', width: '250px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 36, isPartOfKey: false, headerText: ' Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: ' Valor Parametro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: ' Tipo do Dado', width: '205px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametroName', isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: ' Tipo do Dado (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'CodigoFilial', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 18, validateMaxLength: true, isPartOfKey: false, headerText: ' Codigo Filial', width: '218px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdFilialPfj', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 12, isPartOfKey: false, headerText: ' Id Filial Pfj', width: '218px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdFilialString', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: ' ', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'NomeFilial', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 60, validateMaxLength: true, isPartOfKey: false, headerText: ' Nome Filial', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametroBool', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'ValorParametroBool', width: '250px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorFilialP'] = [ 
        { name: 'TcsParametroValorFilialP', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorFilialPToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorFilialPToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorFilialP', actionName: 'LinxFrameworkParametro/GetTcsParametroValorFilialPByEntitySearchNoAssociations', display: 'TcsParametroValorFilialP',  metaData: function() { return metadataInfo['TcsParametroValorFilialP']; } }
    ];
    entitylookUps.push('TcsParametroValorFilialP');
    entitylookUps['TcsParametroValorFilialP'] = [];
    entityNames.push('TcsParametroValorVariacaoGenericaP1');
    metadataInfo['TcsParametroValorVariacaoGenericaP1'] = [
        { key: 'ChaveSelecao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: ' Chave Selecao', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'IdParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 0, isPartOfKey: false, headerText: ' Id Parametro', width: '250px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'IdParametroValor', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 10, isPartOfKey: true, headerText: ' Id Parametro Valor', width: '283px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'PossuiVariacao', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: ' Possui Variacao', width: '250px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null },
        { key: 'UidTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 36, isPartOfKey: false, headerText: ' Uid Tabela', width: '250px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametro', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: ' Valor Parametro', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametro', isQbeZero: false, isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 3, isPartOfKey: false, headerText: ' Tipo do Dado', width: '205px', dataType: 'number', format: 'int', hidden: false, unbound: false, group: null },
        { key: 'LxDatatypeParametroName', isDomain: true, domainName: 'TipoValorParametro', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: ' Tipo do Dado (Name)', width: '0px', dataType: 'string', format: '', hidden: true, unbound: false, group: null },
        { key: 'NomeTabela', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', isRequired: true, maxLength: 250, validateMaxLength: true, isPartOfKey: false, headerText: ' Nome Tabela', width: '400px', dataType: 'string', format: '', hidden: false, unbound: false, group: null },
        { key: 'ValorParametroBool', isQbeZero: false, isDomain: false, domainName: '', lookupPropertyName: '', lookupVisibleColumns: '', maxLength: 0, isPartOfKey: false, headerText: 'ValorParametroBool', width: '250px', dataType: 'bool', format: 'checkbox', hidden: false, unbound: false, group: null }
    ];
    dataExportInfo['TcsParametroValorVariacaoGenericaP1'] = [ 
        { name: 'TcsParametroValorVariacaoGenericaP1', canExportMedia: true , canExportReport: true, actionExport: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoGenericaP1ToExcel', actionReport: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoGenericaP1ToReportXml', actionFeed: 'LinxFrameworkParametroOData/TcsParametroValorVariacaoGenericaP1', actionName: 'LinxFrameworkParametro/GetTcsParametroValorVariacaoGenericaP1ByEntitySearchNoAssociations', display: 'TcsParametroValorVariacaoGenericaP1',  metaData: function() { return metadataInfo['TcsParametroValorVariacaoGenericaP1']; } }
    ];
    entitylookUps.push('TcsParametroValorVariacaoGenericaP1');
    entitylookUps['TcsParametroValorVariacaoGenericaP1'] = [];
    var lookUpProperties = [];
    //#endregion Metadata Info
    //#region dataParameters
    var dataParameters = {
        isLoaded: false,
        parameters: [],
        registerParameters: function (parameterList, complete) {
            if (parameterList !== '') {
                var variation = '{TBC_GRUPO_ECONOMICO|' + managerAuth.loginInfo.IdLinxGrupoEconomico.toString() + '|TCS_USUARIO|' + managerAuth.loginInfo.UidUsuario + (vm != null && vm.getBandeiraRede() > 0 ? '|TBC_BANDEIRA_REDE|' + vm.getBandeiraRede().toString() : '') + '}';
                $.ajax({
                    type: 'GET',
                    url: getServiceAddress('LinxFrameworkParametro') + '/GetParameterValue?serializedParameterList=' + stringReplace(parameterList, '{}', variation),
                    dataType: 'json',
                    cache: false,
                    headers: managerAuth.getHeaders(),
                    error: function (jqXHR, textStatus, errorThrown) {
                        var msg = 'Os seguintes parâmetros não foram pesquisados: [' + parameterList + ']';
                        app.showMessage(msg, 'Alerta', ['Ok']);
                        dataParameters.isLoaded = true;
                    },
                    success: function (data) {
                        var parametersName = '';
                        var parameters = data.split('#');
                        for (var idx in parameters) {
                            var values = parameters[idx].split('|');
                            var pName = values[0];
                            var pValue = values[1];
                            dataParameters.parameters[pName] = pValue;
                        }
                        dataParameters.isLoaded = true;
                        if (complete) complete();
                    }
                });
            }
        }
    };
    //#endregion dataParameters
    //#region Classes Map
    var sequences = [];
    var getNextSequence = function(entityName) {
        if (!sequences[entityName]) resetSequence(entityName);
        sequence = sequences[entityName];
        sequences[entityName]++;
        return sequence;
    };
    var resetSequence = function(entityName) {
        sequences[entityName] = 0;
    };
    var getSequence = function(entityName) {
        if (!sequences[entityName]) resetSequence(entityName);
        return sequences[entityName];
    };
    
    // Configure TcsParametro data type
    metadataStore.addEntityType({
    shortName: "TcsParametro",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ColunaCodValida: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,ColunaDescValida: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,DescGrupoParametro: { dataType: DataType.String, maxLength: 60, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 60})]  }
    ,DescParametro: { dataType: DataType.String, maxLength: 60, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 60})]  }
    ,DescricaoAplicativo: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,FaixaFinal: { dataType: DataType.String, maxLength: 70, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 70})]  }
    ,FaixaInicial: { dataType: DataType.String, maxLength: 70, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 70})]  }
    ,IdGrupoParametro: { dataType: DataType.Int16, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,IdTcsAplicativo: { dataType: DataType.Int32, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IndicaEnviaPdv: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,IndicaParametroLinx: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,LxDatatypeParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxDatatypeParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,LxTipoValidacaoParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxTipoValidacaoParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,NivelAcesso: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,NivelAcessoEdicao: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,ObsParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,PermiteVariacaoPorEntidade: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,TituloParametro: { dataType: DataType.String, maxLength: 60, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 60})]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
    TcsParametroTabelaSelecaoList: { entityTypeName: "TcsParametroTabelaSelecao:#Linx.Framework.BV.Parametro", isScalar: false, invForeignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroTabelaSelecao" }
    ,TcsParametroValorList: { entityTypeName: "TcsParametroValor:#Linx.Framework.BV.Parametro", isScalar: false, invForeignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroValor" }
    ,TcsParametroValorUsuarioList: { entityTypeName: "TcsParametroValorUsuario:#Linx.Framework.BV.Parametro", isScalar: false, invForeignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroValorUsuario" }
    ,TcsParametroValorRedeList: { entityTypeName: "TcsParametroValorRede:#Linx.Framework.BV.Parametro", isScalar: false, invForeignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroValorRede" }
    ,TcsParametroValorGpeconList: { entityTypeName: "TcsParametroValorGpecon:#Linx.Framework.BV.Parametro", isScalar: false, invForeignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroValorGpecon" }
    ,TcsParametroValorFilialList: { entityTypeName: "TcsParametroValorFilial:#Linx.Framework.BV.Parametro", isScalar: false, invForeignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroValorFilial" }
    ,TcsParametroValorLjvLojaList: { entityTypeName: "TcsParametroValorLjvLoja:#Linx.Framework.BV.Parametro", isScalar: false, invForeignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroValorLjvLoja" }
    ,TcsParametroValorVariacaoGenericaList: { entityTypeName: "TcsParametroValorVariacaoGenerica:#Linx.Framework.BV.Parametro", isScalar: false, invForeignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroValorVariacaoGenerica" }
                          }
    });
    lookUpProperties['TcsParametro'] = {DescGrupoParametro: 'LookUpTcsParametroGrupoAutorizacao', DescricaoAplicativo: 'LookUpTcsAplicativo', IdGrupoParametro: 'LookUpTcsParametroGrupoAutorizacao', IdTcsAplicativo: 'LookUpTcsAplicativo'};
    var TcsParametroInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametro') : ko.observable(getNextSequence('TcsParametro')));
       ownerReference.currentTcsParametroTabelaSelecao = ko.observable(null);
       ownerReference.currentTcsParametroValor = ko.observable(null);
       ownerReference.currentTcsParametroValorUsuario = ko.observable(null);
       ownerReference.currentTcsParametroValorRede = ko.observable(null);
       ownerReference.currentTcsParametroValorGpecon = ko.observable(null);
       ownerReference.currentTcsParametroValorFilial = ko.observable(null);
       ownerReference.currentTcsParametroValorLjvLoja = ko.observable(null);
       ownerReference.currentTcsParametroValorVariacaoGenerica = ko.observable(null);
       //Adjust details for a POCO reference
       if (isPOCO === true) {
           ownerReference.TcsParametroTabelaSelecaoList = ko.observableArray(ownerReference.TcsParametroTabelaSelecaoList);
           ownerReference.TcsParametroValorList = ko.observableArray(ownerReference.TcsParametroValorList);
           ownerReference.TcsParametroValorUsuarioList = ko.observableArray(ownerReference.TcsParametroValorUsuarioList);
           ownerReference.TcsParametroValorRedeList = ko.observableArray(ownerReference.TcsParametroValorRedeList);
           ownerReference.TcsParametroValorGpeconList = ko.observableArray(ownerReference.TcsParametroValorGpeconList);
           ownerReference.TcsParametroValorFilialList = ko.observableArray(ownerReference.TcsParametroValorFilialList);
           ownerReference.TcsParametroValorLjvLojaList = ko.observableArray(ownerReference.TcsParametroValorLjvLojaList);
           ownerReference.TcsParametroValorVariacaoGenericaList = ko.observableArray(ownerReference.TcsParametroValorVariacaoGenericaList);
       }
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               if (isNullOrEmpty(propertyName)) {
                   for (var i = 0; i < ownerReference.TcsParametroTabelaSelecaoList().length; i++) {
                       var detail = ownerReference.TcsParametroTabelaSelecaoList()[i];
                       errors = errors.concat(detail.getValidationErrors());
                   }
               }
               if (isNullOrEmpty(propertyName)) {
                   for (var i = 0; i < ownerReference.TcsParametroValorList().length; i++) {
                       var detail = ownerReference.TcsParametroValorList()[i];
                       errors = errors.concat(detail.getValidationErrors());
                   }
               }
               if (isNullOrEmpty(propertyName)) {
                   for (var i = 0; i < ownerReference.TcsParametroValorUsuarioList().length; i++) {
                       var detail = ownerReference.TcsParametroValorUsuarioList()[i];
                       errors = errors.concat(detail.getValidationErrors());
                   }
               }
               if (isNullOrEmpty(propertyName)) {
                   for (var i = 0; i < ownerReference.TcsParametroValorRedeList().length; i++) {
                       var detail = ownerReference.TcsParametroValorRedeList()[i];
                       errors = errors.concat(detail.getValidationErrors());
                   }
               }
               if (isNullOrEmpty(propertyName)) {
                   for (var i = 0; i < ownerReference.TcsParametroValorGpeconList().length; i++) {
                       var detail = ownerReference.TcsParametroValorGpeconList()[i];
                       errors = errors.concat(detail.getValidationErrors());
                   }
               }
               if (isNullOrEmpty(propertyName)) {
                   for (var i = 0; i < ownerReference.TcsParametroValorFilialList().length; i++) {
                       var detail = ownerReference.TcsParametroValorFilialList()[i];
                       errors = errors.concat(detail.getValidationErrors());
                   }
               }
               if (isNullOrEmpty(propertyName)) {
                   for (var i = 0; i < ownerReference.TcsParametroValorLjvLojaList().length; i++) {
                       var detail = ownerReference.TcsParametroValorLjvLojaList()[i];
                       errors = errors.concat(detail.getValidationErrors());
                   }
               }
               if (isNullOrEmpty(propertyName)) {
                   for (var i = 0; i < ownerReference.TcsParametroValorVariacaoGenericaList().length; i++) {
                       var detail = ownerReference.TcsParametroValorVariacaoGenericaList()[i];
                       errors = errors.concat(detail.getValidationErrors());
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           if (loadDetails) {
               result.TcsParametroTabelaSelecaoList = [];
               var sourceList = getAbsoluteValue(ownerReference.TcsParametroTabelaSelecaoList);
               if (sourceList && sourceList.length > 0) {
                   for (var i = 0; i < sourceList.length; i++) {
                       if (['U', 'I', 'D'].indexOf(sourceList[i].ChangeState) >= 0) result.TcsParametroTabelaSelecaoList.push(sourceList[i].getPrimitiveDTO(sourceList[i].ChangeState != 'D'));
                   }
               }
               result.TcsParametroValorList = [];
               var sourceList = getAbsoluteValue(ownerReference.TcsParametroValorList);
               if (sourceList && sourceList.length > 0) {
                   for (var i = 0; i < sourceList.length; i++) {
                       if (['U', 'I', 'D'].indexOf(sourceList[i].ChangeState) >= 0) result.TcsParametroValorList.push(sourceList[i].getPrimitiveDTO(sourceList[i].ChangeState != 'D'));
                   }
               }
               result.TcsParametroValorUsuarioList = [];
               var sourceList = getAbsoluteValue(ownerReference.TcsParametroValorUsuarioList);
               if (sourceList && sourceList.length > 0) {
                   for (var i = 0; i < sourceList.length; i++) {
                       if (['U', 'I', 'D'].indexOf(sourceList[i].ChangeState) >= 0) result.TcsParametroValorUsuarioList.push(sourceList[i].getPrimitiveDTO(sourceList[i].ChangeState != 'D'));
                   }
               }
               result.TcsParametroValorRedeList = [];
               var sourceList = getAbsoluteValue(ownerReference.TcsParametroValorRedeList);
               if (sourceList && sourceList.length > 0) {
                   for (var i = 0; i < sourceList.length; i++) {
                       if (['U', 'I', 'D'].indexOf(sourceList[i].ChangeState) >= 0) result.TcsParametroValorRedeList.push(sourceList[i].getPrimitiveDTO(sourceList[i].ChangeState != 'D'));
                   }
               }
               result.TcsParametroValorGpeconList = [];
               var sourceList = getAbsoluteValue(ownerReference.TcsParametroValorGpeconList);
               if (sourceList && sourceList.length > 0) {
                   for (var i = 0; i < sourceList.length; i++) {
                       if (['U', 'I', 'D'].indexOf(sourceList[i].ChangeState) >= 0) result.TcsParametroValorGpeconList.push(sourceList[i].getPrimitiveDTO(sourceList[i].ChangeState != 'D'));
                   }
               }
               result.TcsParametroValorFilialList = [];
               var sourceList = getAbsoluteValue(ownerReference.TcsParametroValorFilialList);
               if (sourceList && sourceList.length > 0) {
                   for (var i = 0; i < sourceList.length; i++) {
                       if (['U', 'I', 'D'].indexOf(sourceList[i].ChangeState) >= 0) result.TcsParametroValorFilialList.push(sourceList[i].getPrimitiveDTO(sourceList[i].ChangeState != 'D'));
                   }
               }
               result.TcsParametroValorLjvLojaList = [];
               var sourceList = getAbsoluteValue(ownerReference.TcsParametroValorLjvLojaList);
               if (sourceList && sourceList.length > 0) {
                   for (var i = 0; i < sourceList.length; i++) {
                       if (['U', 'I', 'D'].indexOf(sourceList[i].ChangeState) >= 0) result.TcsParametroValorLjvLojaList.push(sourceList[i].getPrimitiveDTO(sourceList[i].ChangeState != 'D'));
                   }
               }
               result.TcsParametroValorVariacaoGenericaList = [];
               var sourceList = getAbsoluteValue(ownerReference.TcsParametroValorVariacaoGenericaList);
               if (sourceList && sourceList.length > 0) {
                   for (var i = 0; i < sourceList.length; i++) {
                       if (['U', 'I', 'D'].indexOf(sourceList[i].ChangeState) >= 0) result.TcsParametroValorVariacaoGenericaList.push(sourceList[i].getPrimitiveDTO(sourceList[i].ChangeState != 'D'));
                   }
               }
           }
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           var _TcsParametroTabelaSelecaoList = getAbsoluteValue(ownerReference.TcsParametroTabelaSelecaoList);
           if (_TcsParametroTabelaSelecaoList && _TcsParametroTabelaSelecaoList.length > 0) {
               for (var i = 0; i < _TcsParametroTabelaSelecaoList.length; i++) {
                   var detail = _TcsParametroTabelaSelecaoList[i];
                   if (['U', 'I', 'D'].indexOf(detail.ChangeState) >= 0) {
                       result.push(detail);
                       result = result.concat(detail.getAllDetailChanges());
                   }
               }
           }
           var _TcsParametroValorList = getAbsoluteValue(ownerReference.TcsParametroValorList);
           if (_TcsParametroValorList && _TcsParametroValorList.length > 0) {
               for (var i = 0; i < _TcsParametroValorList.length; i++) {
                   var detail = _TcsParametroValorList[i];
                   if (['U', 'I', 'D'].indexOf(detail.ChangeState) >= 0) {
                       result.push(detail);
                       result = result.concat(detail.getAllDetailChanges());
                   }
               }
           }
           var _TcsParametroValorUsuarioList = getAbsoluteValue(ownerReference.TcsParametroValorUsuarioList);
           if (_TcsParametroValorUsuarioList && _TcsParametroValorUsuarioList.length > 0) {
               for (var i = 0; i < _TcsParametroValorUsuarioList.length; i++) {
                   var detail = _TcsParametroValorUsuarioList[i];
                   if (['U', 'I', 'D'].indexOf(detail.ChangeState) >= 0) {
                       result.push(detail);
                       result = result.concat(detail.getAllDetailChanges());
                   }
               }
           }
           var _TcsParametroValorRedeList = getAbsoluteValue(ownerReference.TcsParametroValorRedeList);
           if (_TcsParametroValorRedeList && _TcsParametroValorRedeList.length > 0) {
               for (var i = 0; i < _TcsParametroValorRedeList.length; i++) {
                   var detail = _TcsParametroValorRedeList[i];
                   if (['U', 'I', 'D'].indexOf(detail.ChangeState) >= 0) {
                       result.push(detail);
                       result = result.concat(detail.getAllDetailChanges());
                   }
               }
           }
           var _TcsParametroValorGpeconList = getAbsoluteValue(ownerReference.TcsParametroValorGpeconList);
           if (_TcsParametroValorGpeconList && _TcsParametroValorGpeconList.length > 0) {
               for (var i = 0; i < _TcsParametroValorGpeconList.length; i++) {
                   var detail = _TcsParametroValorGpeconList[i];
                   if (['U', 'I', 'D'].indexOf(detail.ChangeState) >= 0) {
                       result.push(detail);
                       result = result.concat(detail.getAllDetailChanges());
                   }
               }
           }
           var _TcsParametroValorFilialList = getAbsoluteValue(ownerReference.TcsParametroValorFilialList);
           if (_TcsParametroValorFilialList && _TcsParametroValorFilialList.length > 0) {
               for (var i = 0; i < _TcsParametroValorFilialList.length; i++) {
                   var detail = _TcsParametroValorFilialList[i];
                   if (['U', 'I', 'D'].indexOf(detail.ChangeState) >= 0) {
                       result.push(detail);
                       result = result.concat(detail.getAllDetailChanges());
                   }
               }
           }
           var _TcsParametroValorLjvLojaList = getAbsoluteValue(ownerReference.TcsParametroValorLjvLojaList);
           if (_TcsParametroValorLjvLojaList && _TcsParametroValorLjvLojaList.length > 0) {
               for (var i = 0; i < _TcsParametroValorLjvLojaList.length; i++) {
                   var detail = _TcsParametroValorLjvLojaList[i];
                   if (['U', 'I', 'D'].indexOf(detail.ChangeState) >= 0) {
                       result.push(detail);
                       result = result.concat(detail.getAllDetailChanges());
                   }
               }
           }
           var _TcsParametroValorVariacaoGenericaList = getAbsoluteValue(ownerReference.TcsParametroValorVariacaoGenericaList);
           if (_TcsParametroValorVariacaoGenericaList && _TcsParametroValorVariacaoGenericaList.length > 0) {
               for (var i = 0; i < _TcsParametroValorVariacaoGenericaList.length; i++) {
                   var detail = _TcsParametroValorVariacaoGenericaList[i];
                   if (['U', 'I', 'D'].indexOf(detail.ChangeState) >= 0) {
                       result.push(detail);
                       result = result.concat(detail.getAllDetailChanges());
                   }
               }
           }
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
           if (copyDetails) {
               if (ownerReference.TcsParametroTabelaSelecaoList && originData.TcsParametroTabelaSelecaoList) {
                   var toList = getAbsoluteValue(ownerReference.TcsParametroTabelaSelecaoList);
                   var fromList = getAbsoluteValue(originData.TcsParametroTabelaSelecaoList);
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                      if (toList[idxElem].ChangeState === 'D') toList.splice(idxElem, 1);
                   }
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                          if (toList[idxElem].ChangeState !== 'N') {
                               var fromObj = _.where(fromList, { EntityUniqueKey: toList[idxElem]['EntityUniqueKey'] });
                               if (fromObj.length > 0) toList[idxElem].copyDataFrom(fromObj[0], true);
                          }
                   }
               }
               if (ownerReference.TcsParametroValorList && originData.TcsParametroValorList) {
                   var toList = getAbsoluteValue(ownerReference.TcsParametroValorList);
                   var fromList = getAbsoluteValue(originData.TcsParametroValorList);
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                      if (toList[idxElem].ChangeState === 'D') toList.splice(idxElem, 1);
                   }
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                          if (toList[idxElem].ChangeState !== 'N') {
                               var fromObj = _.where(fromList, { EntityUniqueKey: toList[idxElem]['EntityUniqueKey'] });
                               if (fromObj.length > 0) toList[idxElem].copyDataFrom(fromObj[0], true);
                          }
                   }
               }
               if (ownerReference.TcsParametroValorUsuarioList && originData.TcsParametroValorUsuarioList) {
                   var toList = getAbsoluteValue(ownerReference.TcsParametroValorUsuarioList);
                   var fromList = getAbsoluteValue(originData.TcsParametroValorUsuarioList);
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                      if (toList[idxElem].ChangeState === 'D') toList.splice(idxElem, 1);
                   }
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                          if (toList[idxElem].ChangeState !== 'N') {
                               var fromObj = _.where(fromList, { IdParametroValor: toList[idxElem]['IdParametroValor'] });
                               if (fromObj.length > 0) toList[idxElem].copyDataFrom(fromObj[0], true);
                          }
                   }
               }
               if (ownerReference.TcsParametroValorRedeList && originData.TcsParametroValorRedeList) {
                   var toList = getAbsoluteValue(ownerReference.TcsParametroValorRedeList);
                   var fromList = getAbsoluteValue(originData.TcsParametroValorRedeList);
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                      if (toList[idxElem].ChangeState === 'D') toList.splice(idxElem, 1);
                   }
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                          if (toList[idxElem].ChangeState !== 'N') {
                               var fromObj = _.where(fromList, { IdParametroValor: toList[idxElem]['IdParametroValor'] });
                               if (fromObj.length > 0) toList[idxElem].copyDataFrom(fromObj[0], true);
                          }
                   }
               }
               if (ownerReference.TcsParametroValorGpeconList && originData.TcsParametroValorGpeconList) {
                   var toList = getAbsoluteValue(ownerReference.TcsParametroValorGpeconList);
                   var fromList = getAbsoluteValue(originData.TcsParametroValorGpeconList);
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                      if (toList[idxElem].ChangeState === 'D') toList.splice(idxElem, 1);
                   }
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                          if (toList[idxElem].ChangeState !== 'N') {
                               var fromObj = _.where(fromList, { IdParametroValor: toList[idxElem]['IdParametroValor'] });
                               if (fromObj.length > 0) toList[idxElem].copyDataFrom(fromObj[0], true);
                          }
                   }
               }
               if (ownerReference.TcsParametroValorFilialList && originData.TcsParametroValorFilialList) {
                   var toList = getAbsoluteValue(ownerReference.TcsParametroValorFilialList);
                   var fromList = getAbsoluteValue(originData.TcsParametroValorFilialList);
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                      if (toList[idxElem].ChangeState === 'D') toList.splice(idxElem, 1);
                   }
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                          if (toList[idxElem].ChangeState !== 'N') {
                               var fromObj = _.where(fromList, { IdParametroValor: toList[idxElem]['IdParametroValor'] });
                               if (fromObj.length > 0) toList[idxElem].copyDataFrom(fromObj[0], true);
                          }
                   }
               }
               if (ownerReference.TcsParametroValorLjvLojaList && originData.TcsParametroValorLjvLojaList) {
                   var toList = getAbsoluteValue(ownerReference.TcsParametroValorLjvLojaList);
                   var fromList = getAbsoluteValue(originData.TcsParametroValorLjvLojaList);
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                      if (toList[idxElem].ChangeState === 'D') toList.splice(idxElem, 1);
                   }
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                          if (toList[idxElem].ChangeState !== 'N') {
                               var fromObj = _.where(fromList, { IdParametroValor: toList[idxElem]['IdParametroValor'] });
                               if (fromObj.length > 0) toList[idxElem].copyDataFrom(fromObj[0], true);
                          }
                   }
               }
               if (ownerReference.TcsParametroValorVariacaoGenericaList && originData.TcsParametroValorVariacaoGenericaList) {
                   var toList = getAbsoluteValue(ownerReference.TcsParametroValorVariacaoGenericaList);
                   var fromList = getAbsoluteValue(originData.TcsParametroValorVariacaoGenericaList);
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                      if (toList[idxElem].ChangeState === 'D') toList.splice(idxElem, 1);
                   }
                   for (var idxElem = toList.length - 1; idxElem >= 0; idxElem--) {
                          if (toList[idxElem].ChangeState !== 'N') {
                               var fromObj = _.where(fromList, { IdParametroValor: toList[idxElem]['IdParametroValor'] });
                               if (fromObj.length > 0) toList[idxElem].copyDataFrom(fromObj[0], true);
                          }
                   }
               }
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
              vm.dataBind('TcsParametroTabelaSelecaoList', true);
              if (ownerReference.currentTcsParametroTabelaSelecao()) ownerReference.currentTcsParametroTabelaSelecao().commitDetailsVisualPendings();
              vm.dataBind('TcsParametroValorList', true);
              if (ownerReference.currentTcsParametroValor()) ownerReference.currentTcsParametroValor().commitDetailsVisualPendings();
              vm.dataBind('TcsParametroValorUsuarioList', true);
              if (ownerReference.currentTcsParametroValorUsuario()) ownerReference.currentTcsParametroValorUsuario().commitDetailsVisualPendings();
              vm.dataBind('TcsParametroValorRedeList', true);
              if (ownerReference.currentTcsParametroValorRede()) ownerReference.currentTcsParametroValorRede().commitDetailsVisualPendings();
              vm.dataBind('TcsParametroValorGpeconList', true);
              if (ownerReference.currentTcsParametroValorGpecon()) ownerReference.currentTcsParametroValorGpecon().commitDetailsVisualPendings();
              vm.dataBind('TcsParametroValorFilialList', true);
              if (ownerReference.currentTcsParametroValorFilial()) ownerReference.currentTcsParametroValorFilial().commitDetailsVisualPendings();
              vm.dataBind('TcsParametroValorLjvLojaList', true);
              if (ownerReference.currentTcsParametroValorLjvLoja()) ownerReference.currentTcsParametroValorLjvLoja().commitDetailsVisualPendings();
              vm.dataBind('TcsParametroValorVariacaoGenericaList', true);
              if (ownerReference.currentTcsParametroValorVariacaoGenerica()) ownerReference.currentTcsParametroValorVariacaoGenerica().commitDetailsVisualPendings();
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametro{' + 'IdParametro#==#L' + getAbsoluteValue(ownerReference.IdParametro).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              breezeReference.TcsParametroTabelaSelecaoIsLoaded = ownerReference.TcsParametroTabelaSelecaoIsLoaded;
              for (var idx = 0; idx < ownerReference.TcsParametroTabelaSelecaoList().length; idx++) {
                  var entity = ownerReference.TcsParametroTabelaSelecaoList()[idx];
                  if (entity.isPOCO)  {
                      var newReference = createEntity(entity.typeName, entity.getPrimitiveDTO(), true);
                      entity.enableDetailsDataTack(newReference);
                  }
              }
              breezeReference.TcsParametroValorIsLoaded = ownerReference.TcsParametroValorIsLoaded;
              for (var idx = 0; idx < ownerReference.TcsParametroValorList().length; idx++) {
                  var entity = ownerReference.TcsParametroValorList()[idx];
                  if (entity.isPOCO)  {
                      var newReference = createEntity(entity.typeName, entity.getPrimitiveDTO(), true);
                      entity.enableDetailsDataTack(newReference);
                  }
              }
              breezeReference.TcsParametroValorUsuarioIsLoaded = ownerReference.TcsParametroValorUsuarioIsLoaded;
              for (var idx = 0; idx < ownerReference.TcsParametroValorUsuarioList().length; idx++) {
                  var entity = ownerReference.TcsParametroValorUsuarioList()[idx];
                  if (entity.isPOCO)  {
                      var newReference = createEntity(entity.typeName, entity.getPrimitiveDTO(), true);
                      entity.enableDetailsDataTack(newReference);
                  }
              }
              breezeReference.TcsParametroValorRedeIsLoaded = ownerReference.TcsParametroValorRedeIsLoaded;
              for (var idx = 0; idx < ownerReference.TcsParametroValorRedeList().length; idx++) {
                  var entity = ownerReference.TcsParametroValorRedeList()[idx];
                  if (entity.isPOCO)  {
                      var newReference = createEntity(entity.typeName, entity.getPrimitiveDTO(), true);
                      entity.enableDetailsDataTack(newReference);
                  }
              }
              breezeReference.TcsParametroValorGpeconIsLoaded = ownerReference.TcsParametroValorGpeconIsLoaded;
              for (var idx = 0; idx < ownerReference.TcsParametroValorGpeconList().length; idx++) {
                  var entity = ownerReference.TcsParametroValorGpeconList()[idx];
                  if (entity.isPOCO)  {
                      var newReference = createEntity(entity.typeName, entity.getPrimitiveDTO(), true);
                      entity.enableDetailsDataTack(newReference);
                  }
              }
              breezeReference.TcsParametroValorFilialIsLoaded = ownerReference.TcsParametroValorFilialIsLoaded;
              for (var idx = 0; idx < ownerReference.TcsParametroValorFilialList().length; idx++) {
                  var entity = ownerReference.TcsParametroValorFilialList()[idx];
                  if (entity.isPOCO)  {
                      var newReference = createEntity(entity.typeName, entity.getPrimitiveDTO(), true);
                      entity.enableDetailsDataTack(newReference);
                  }
              }
              breezeReference.TcsParametroValorLjvLojaIsLoaded = ownerReference.TcsParametroValorLjvLojaIsLoaded;
              for (var idx = 0; idx < ownerReference.TcsParametroValorLjvLojaList().length; idx++) {
                  var entity = ownerReference.TcsParametroValorLjvLojaList()[idx];
                  if (entity.isPOCO)  {
                      var newReference = createEntity(entity.typeName, entity.getPrimitiveDTO(), true);
                      entity.enableDetailsDataTack(newReference);
                  }
              }
              breezeReference.TcsParametroValorVariacaoGenericaIsLoaded = ownerReference.TcsParametroValorVariacaoGenericaIsLoaded;
              for (var idx = 0; idx < ownerReference.TcsParametroValorVariacaoGenericaList().length; idx++) {
                  var entity = ownerReference.TcsParametroValorVariacaoGenericaList()[idx];
                  if (entity.isPOCO)  {
                      var newReference = createEntity(entity.typeName, entity.getPrimitiveDTO(), true);
                      entity.enableDetailsDataTack(newReference);
                  }
              }
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ColunaCodValida'] = 'S';
       ownerReference.serverDataType['ColunaDescValida'] = 'S';
       ownerReference.serverDataType['DescGrupoParametro'] = 'S';
       ownerReference.serverDataType['DescParametro'] = 'S';
       ownerReference.serverDataType['DescricaoAplicativo'] = 'S';
       ownerReference.serverDataType['FaixaFinal'] = 'S';
       ownerReference.serverDataType['FaixaInicial'] = 'S';
       ownerReference.serverDataType['IdGrupoParametro'] = 'H';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdTcsAplicativo'] = 'I';
       ownerReference.serverDataType['IndicaEnviaPdv'] = 'B';
       ownerReference.serverDataType['IndicaParametroLinx'] = 'B';
       ownerReference.serverDataType['LxDatatypeParametro'] = 'Y';
       ownerReference.serverDataType['LxTipoValidacaoParametro'] = 'Y';
       ownerReference.serverDataType['NivelAcesso'] = 'Y';
       ownerReference.serverDataType['NivelAcessoEdicao'] = 'Y';
       ownerReference.serverDataType['ObsParametro'] = 'S';
       ownerReference.serverDataType['PermiteVariacaoPorEntidade'] = 'B';
       ownerReference.serverDataType['TituloParametro'] = 'S';
       ownerReference.typeName = 'TcsParametro';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametro' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametro !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametro() != ownerReference) { cacheElements[idxR].TcsParametro(ownerReference); }
           }
       }
       //#region Lookup Extended Methods
       if (isPOCO !== true) {
           ownerReference.getLookupPropertyName = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null && !isNullOrEmpty(property.lookupPropertyName) ? property.lookupPropertyName : propertyName);
           }
           ownerReference.getLookupVisibleColumns = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null ? property.lookupVisibleColumns : '');
           }
           ownerReference.getLookUpClientFilterExpressions = function (lookupName, lookupInfo) {
               return '';
           };
        
           ownerReference.getLookupDisplay = function (lookupName) {
               var displayName = '';
               if (lookupName === 'LookUpTcsAplicativo') {
                   displayName = ' de Tcs_Aplicativo';
               }
               return 'Seleção' + displayName;
           };
        
           ownerReference.getSpecializedLookup = function (lookupName, lookupInfo, fieldToSearch, valueToSearch, ownerReference, allowMultiSelectionInSearch) {
               var specializedLookup = '';
               return specializedLookup;
           };
        
           ownerReference.getSubQueryFilterFromLookUpTcsAplicativo = function (propertyName) {
               var filter = '';
               return filter;
           }
           ownerReference.getSubQueryFilterFromLookUpTcsParametroGrupoAutorizacao = function (propertyName) {
               var filter = '';
               return filter;
           }
           ownerReference.canGetClientFilter = function (lookupName) {
               return true;
           }
           ownerReference.validatedlookupsArray = [];
           ownerReference.internalLookupSearch = function (lookupName, fieldToSearch, operation, querySucceeded, lookupInfo, valueToSearch, beforeGettingLookup, referencefield) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); querySucceeded(null); return lookupInfo; }
               if (isNullOrEmpty(lookupInfo.lastJEntityExpression)) {
                   if (isNullOrEmpty(referencefield)) referencefield = fieldToSearch;
                   if ((typeof valueToSearch) === 'undefined')
                       valueToSearch = getAbsoluteValue(ownerReference[referencefield]);
                   var extraFilters = '';
                   if (ownerReference.canGetClientFilter(lookupName)) {
                       extraFilters = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
                       dataContext.lastClientFilterExpressions[lookupName] = extraFilters;
                       if (extraFilters === 'Error') { querySucceeded(null); return lookupInfo; }
                       if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                           var customFilter = ownerReference['BeforeGet' + lookupName + 'Query'](fieldToSearch, lookupInfo);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                       if (typeof ownerReference['getSubQueryFilterFrom' + lookupName] == 'function') {
                           var customFilter = ownerReference['getSubQueryFilterFrom' + lookupName](referencefield);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                   }
                   var completeExpression = getLookUpJEntityExpression(lookupName, ownerReference, fieldToSearch, valueToSearch, extraFilters, referencefield, app, lookupInfo.vm.useLikeCommandAsDefault);
                   if (completeExpression === 'Error') { querySucceeded(null); return lookupInfo; }
                   lookupInfo.lastJEntityExpression = completeExpression;
               }
               switch (operation) {
                   case 'F':
                       lookupInfo.pageSkip = 0;
                       break;
                   case 'B':
                       lookupInfo.pageSkip = lookupInfo.pageSkip - 1;
                       break;
                   case 'N':
                       lookupInfo.pageSkip = lookupInfo.pageSkip + 1;
                       break;
                   case 'L':
                       lookupInfo.pageSkip = lookupInfo.totalPages();
                       break;
                   default:
               }
        
               var e = { cancel: false, lookupName: lookupName, jEntitySearch: lookupInfo.lastJEntityExpression, entity: ownerReference, viewModel: lookupInfo.vm };
               if (beforeGettingLookup) beforeGettingLookup(e);
               if (e.cancel) { querySucceeded(null); return lookupInfo; }
               if(lookupInfo.lastJEntityExpression !== e.jEntitySearch)
                   lookupInfo.lastJEntityExpression = e.jEntitySearch;
               if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(true);
               var returnQueryResult = function (data) { lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount); querySucceeded(data); };
               eval('dataContext.get' + lookupName + 'ByEntitySearch(lookupInfo.lastJEntityExpression, (isNullOrEmpty(lookupInfo.fieldToSort) ? fieldToSearch : lookupInfo.fieldToSort), lookupInfo.pageSize*lookupInfo.pageSkip, lookupInfo.pageSize, lookupInfo.sortDirection, fieldToSearch).then(returnQueryResult).fail(queryFailed)').fin(function(){ if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(false); });
               return lookupInfo;
           };
        
           ownerReference.hasValidClientFilter = function (lookupName, lookupInfo) {
               var checkClientFilter = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
               if (checkClientFilter === 'Error') { return false; }
               if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                   checkClientFilter = ownerReference['BeforeGet' + lookupName + 'Query']('', lookupInfo);
                   if (checkClientFilter === 'Error') { return false; }
               }
               return true;
           }
        
           ownerReference.executeLookUp = function (lookupName, fieldToSearch, beforeGettingLookup, vm, valueToSearch, finished, comboCallBack, allowMultiSelectionInSearch) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); if (finished) finished(false, null); return; }
               var lookupFieldName = ownerReference.getLookupPropertyName(fieldToSearch);
               vm.dataBind('', true);
               var lookupInfo = new lookupInformation();
               lookupInfo.visibleColumns = ownerReference.getLookupVisibleColumns(fieldToSearch);
               lookupInfo.vm = vm;
               lookupInfo.isMultiSelection = lookupName.in(['LookTcsParametroUsuario', 'LookUpParametroRede', 'LookUpParametroGpecon', 'LookUpParametroFilial', 'LookUpParametroLoja', 'LookUpTcsTabelaAutorizacaoC']);
               var specializedLookup = ownerReference.getSpecializedLookup(lookupName, lookupInfo, lookupFieldName, valueToSearch, ownerReference, allowMultiSelectionInSearch);
               if (isNullOrEmpty(specializedLookup)) {
                       ownerReference.internalLookupSearch(lookupName, lookupFieldName, 'F',
                           function querySucceeded(data) {
                               if (typeof ownerReference['OnLoading' + lookupName + 'Query'] == 'function') {
                                   ownerReference['OnLoading' + lookupName + 'Query'](data);
                               }
                               if ((typeof comboCallBack) === 'function') {
                                   return comboCallBack(data ? data.results : null);
                               }
                               else if (data == null || data.results == null || data.results.length == 0) {
                                   if (finished) finished(false, null);
                                   ownerReference.clearLookUp(lookupName);
                                   if (data != null) app.showMessage('A informação de Lookup [' + ownerReference.getDisplayName(fieldToSearch) + '] não foi encontrada!', 'Informação', ['Ok']);
                                   return;
                               }
                               lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount);
                               showLookUp(dataContext, ownerReference, ownerReference.getLookupDisplay(lookupName), lookupName, lookupFieldName, ownerReference.internalLookupSearch, lookupInfo, 
                                   function (confirm, values) {
                                       var results = '';
                                       if (values != null && values.length > 1) {
                                           $.each(values, function (index, item) { results += (index == 0 ? '' : ',') + item[lookupFieldName].toString().trim() });
                                           results = '[' + results + ']';
                                           ownerReference[fieldToSearch](results);
                                           if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                               vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                           else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                           document.dispatchEvent(dataUpdateEvent);
                                       }
                                       if (finished) finished(confirm, results);
                                   }, data.results, allowMultiSelectionInSearch);
                       }, lookupInfo, valueToSearch, beforeGettingLookup, fieldToSearch);
               }
               else {
                       var currentModal = (modal.inUse ? modal2 : modal);
                       if (currentModal.inUse === false) {
                           //Check Client Validations
                           if (!ownerReference.hasValidClientFilter(lookupName, lookupInfo)) { if (finished) finished(false); return; }
                           //Show External Lookup
                           currentModal.showModal(specializedLookup.moduleName, specializedLookup.uiSettings, ownerReference.getLookupDisplay(lookupName), ['Ok', 'Cancelar'], 'large').then(function (r, data) {
                           if (r == 'Ok') {
                               if (!currentModal.internalUIs || currentModal.internalUIs.length != 1) { if (finished) finished(false); return; }
                               var lookupVM = currentModal[currentModal.internalUIs[0]]; 
                               if (!lookupVM) return; 
                               if (typeof lookupVM == 'function') lookupVM = lookupVM(); 
                               currentModal[currentModal.internalUIs[0]] = null; 
                               var selectedItems = lookupVM.getSpecializedLookupItems(); 
                               if (vm.status() == 'C' && selectedItems != null && selectedItems.length > 1) { 
                                   var results = '';
                                   $.each(selectedItems, function (index, item) { results += (index == 0 ? '' : ',') + (typeof item[lookupFieldName] == 'function' ? item[lookupFieldName]() : item[lookupFieldName]).toString().trim() });
                                   results = '[' + results + ']'
                                   ownerReference[fieldToSearch](results);
                                   if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                       vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                   else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                   document.dispatchEvent(dataUpdateEvent);
                                   if (finished) finished(true, results);
                               }
                               else if (selectedItems.length > 0) { dataContext['finalizeAll' + lookupName](ownerReference, selectedItems, '', lookupInfo); }
                           }
                           if (finished) finished(r === 'Ok');
                       });
                   }
                   if (finished) finished(false);
               }
           };
           ownerReference.clearLookUp = function (lookupName) {
               return eval('dataContext.clear' + lookupName + '(ownerReference)');
           };
       }
       //#endregion Lookup Extended Methods
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
           setAbsoluteValue(ownerReference, 'IndicaParametroLinx', true);
           setAbsoluteValue(ownerReference, 'LxTipoValidacaoParametro', 8);
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (!isNullOrEmpty(ownerReference.TcsParametroTabelaSelecaoList()) && ownerReference.TcsParametroTabelaSelecaoList().length > 0) {
              var details = [].concat(ownerReference.TcsParametroTabelaSelecaoList());
              for (var idx = 0; idx < details.length; idx++) {
                details[idx].delete();
              }
           }
           if (!isNullOrEmpty(ownerReference.TcsParametroValorList()) && ownerReference.TcsParametroValorList().length > 0) {
              var details = [].concat(ownerReference.TcsParametroValorList());
              for (var idx = 0; idx < details.length; idx++) {
                details[idx].delete();
              }
           }
           if (!isNullOrEmpty(ownerReference.TcsParametroValorUsuarioList()) && ownerReference.TcsParametroValorUsuarioList().length > 0) {
              var details = [].concat(ownerReference.TcsParametroValorUsuarioList());
              for (var idx = 0; idx < details.length; idx++) {
                details[idx].delete();
              }
           }
           if (!isNullOrEmpty(ownerReference.TcsParametroValorRedeList()) && ownerReference.TcsParametroValorRedeList().length > 0) {
              var details = [].concat(ownerReference.TcsParametroValorRedeList());
              for (var idx = 0; idx < details.length; idx++) {
                details[idx].delete();
              }
           }
           if (!isNullOrEmpty(ownerReference.TcsParametroValorGpeconList()) && ownerReference.TcsParametroValorGpeconList().length > 0) {
              var details = [].concat(ownerReference.TcsParametroValorGpeconList());
              for (var idx = 0; idx < details.length; idx++) {
                details[idx].delete();
              }
           }
           if (!isNullOrEmpty(ownerReference.TcsParametroValorFilialList()) && ownerReference.TcsParametroValorFilialList().length > 0) {
              var details = [].concat(ownerReference.TcsParametroValorFilialList());
              for (var idx = 0; idx < details.length; idx++) {
                details[idx].delete();
              }
           }
           if (!isNullOrEmpty(ownerReference.TcsParametroValorLjvLojaList()) && ownerReference.TcsParametroValorLjvLojaList().length > 0) {
              var details = [].concat(ownerReference.TcsParametroValorLjvLojaList());
              for (var idx = 0; idx < details.length; idx++) {
                details[idx].delete();
              }
           }
           if (!isNullOrEmpty(ownerReference.TcsParametroValorVariacaoGenericaList()) && ownerReference.TcsParametroValorVariacaoGenericaList().length > 0) {
              var details = [].concat(ownerReference.TcsParametroValorVariacaoGenericaList());
              for (var idx = 0; idx < details.length; idx++) {
                details[idx].delete();
              }
           }
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ColunaCodValida','ColunaDescValida','DescGrupoParametro','DescParametro','DescricaoAplicativo','FaixaFinal','FaixaInicial','IdGrupoParametro','IdParametro','IdTcsAplicativo','IndicaEnviaPdv','IndicaParametroLinx','LxDatatypeParametro','LxTipoValidacaoParametro','NivelAcesso','NivelAcessoEdicao','ObsParametro','PermiteVariacaoPorEntidade','TituloParametro' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
       if (!isNullOrEmpty(ownerReference.currentTcsParametroTabelaSelecao())) { result = result.concat(ownerReference.currentTcsParametroTabelaSelecao().getCurrentElements()); }
       if (!isNullOrEmpty(ownerReference.currentTcsParametroValor())) { result = result.concat(ownerReference.currentTcsParametroValor().getCurrentElements()); }
       if (!isNullOrEmpty(ownerReference.currentTcsParametroValorUsuario())) { result = result.concat(ownerReference.currentTcsParametroValorUsuario().getCurrentElements()); }
       if (!isNullOrEmpty(ownerReference.currentTcsParametroValorRede())) { result = result.concat(ownerReference.currentTcsParametroValorRede().getCurrentElements()); }
       if (!isNullOrEmpty(ownerReference.currentTcsParametroValorGpecon())) { result = result.concat(ownerReference.currentTcsParametroValorGpecon().getCurrentElements()); }
       if (!isNullOrEmpty(ownerReference.currentTcsParametroValorFilial())) { result = result.concat(ownerReference.currentTcsParametroValorFilial().getCurrentElements()); }
       if (!isNullOrEmpty(ownerReference.currentTcsParametroValorLjvLoja())) { result = result.concat(ownerReference.currentTcsParametroValorLjvLoja().getCurrentElements()); }
       if (!isNullOrEmpty(ownerReference.currentTcsParametroValorVariacaoGenerica())) { result = result.concat(ownerReference.currentTcsParametroValorVariacaoGenerica().getCurrentElements()); }
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.GetJsWhereDetailRelationForTcsParametroTabelaSelecao = function(customParentRelation) {
       return 'TcsParametroTabelaSelecao{' + (!isNullOrEmpty(customParentRelation) ? customParentRelation : 'IdParametro#==#' + ownerReference.serverDataType['IdParametro'] + getAbsoluteValue(ownerReference.IdParametro).toString()) + '}';    
       }
       ownerReference.GetJsWhereDetailRelationForTcsParametroValor = function(customParentRelation) {
       return 'TcsParametroValor{' + (!isNullOrEmpty(customParentRelation) ? customParentRelation : 'IdParametro#==#' + ownerReference.serverDataType['IdParametro'] + getAbsoluteValue(ownerReference.IdParametro).toString()) + '}';    
       }
       ownerReference.GetJsWhereDetailRelationForTcsParametroValorUsuario = function(customParentRelation) {
       return 'TcsParametroValorUsuario{' + (!isNullOrEmpty(customParentRelation) ? customParentRelation : 'IdParametro#==#' + ownerReference.serverDataType['IdParametro'] + getAbsoluteValue(ownerReference.IdParametro).toString()) + '}';    
       }
       ownerReference.GetJsWhereDetailRelationForTcsParametroValorRede = function(customParentRelation) {
       return 'TcsParametroValorRede{' + (!isNullOrEmpty(customParentRelation) ? customParentRelation : 'IdParametro#==#' + ownerReference.serverDataType['IdParametro'] + getAbsoluteValue(ownerReference.IdParametro).toString()) + '}';    
       }
       ownerReference.GetJsWhereDetailRelationForTcsParametroValorGpecon = function(customParentRelation) {
       return 'TcsParametroValorGpecon{' + (!isNullOrEmpty(customParentRelation) ? customParentRelation : 'IdParametro#==#' + ownerReference.serverDataType['IdParametro'] + getAbsoluteValue(ownerReference.IdParametro).toString()) + '}';    
       }
       ownerReference.GetJsWhereDetailRelationForTcsParametroValorFilial = function(customParentRelation) {
       return 'TcsParametroValorFilial{' + (!isNullOrEmpty(customParentRelation) ? customParentRelation : 'IdParametro#==#' + ownerReference.serverDataType['IdParametro'] + getAbsoluteValue(ownerReference.IdParametro).toString()) + '}';    
       }
       ownerReference.GetJsWhereDetailRelationForTcsParametroValorLjvLoja = function(customParentRelation) {
       return 'TcsParametroValorLjvLoja{' + (!isNullOrEmpty(customParentRelation) ? customParentRelation : 'IdParametro#==#' + ownerReference.serverDataType['IdParametro'] + getAbsoluteValue(ownerReference.IdParametro).toString()) + '}';    
       }
       ownerReference.GetJsWhereDetailRelationForTcsParametroValorVariacaoGenerica = function(customParentRelation) {
       return 'TcsParametroValorVariacaoGenerica{' + (!isNullOrEmpty(customParentRelation) ? customParentRelation : 'IdParametro#==#' + ownerReference.serverDataType['IdParametro'] + getAbsoluteValue(ownerReference.IdParametro).toString()) + '}';    
       }
       ownerReference.TcsParametroTabelaSelecaoIsLoaded = false;
       ownerReference.TcsParametroValorIsLoaded = false;
       ownerReference.TcsParametroValorUsuarioIsLoaded = false;
       ownerReference.TcsParametroValorRedeIsLoaded = false;
       ownerReference.TcsParametroValorGpeconIsLoaded = false;
       ownerReference.TcsParametroValorFilialIsLoaded = false;
       ownerReference.TcsParametroValorLjvLojaIsLoaded = false;
       ownerReference.TcsParametroValorVariacaoGenericaIsLoaded = false;
       ownerReference.detailsLoaded = function() {
           return ownerReference.TcsParametroTabelaSelecaoIsLoaded && ownerReference.TcsParametroValorIsLoaded && ownerReference.TcsParametroValorUsuarioIsLoaded && ownerReference.TcsParametroValorRedeIsLoaded && ownerReference.TcsParametroValorGpeconIsLoaded && ownerReference.TcsParametroValorFilialIsLoaded && ownerReference.TcsParametroValorLjvLojaIsLoaded && ownerReference.TcsParametroValorVariacaoGenericaIsLoaded;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return ownerReference.TcsParametroTabelaSelecaoIsLoaded || ownerReference.TcsParametroValorIsLoaded || ownerReference.TcsParametroValorUsuarioIsLoaded || ownerReference.TcsParametroValorRedeIsLoaded || ownerReference.TcsParametroValorGpeconIsLoaded || ownerReference.TcsParametroValorFilialIsLoaded || ownerReference.TcsParametroValorLjvLojaIsLoaded || ownerReference.TcsParametroValorVariacaoGenericaIsLoaded;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
           ownerReference.TcsParametroTabelaSelecaoIsLoaded = value;
           if (value === false && ownerReference.isPOCO)
               ownerReference.TcsParametroTabelaSelecaoList([]);
           ownerReference.TcsParametroValorIsLoaded = value;
           if (value === false && ownerReference.isPOCO)
               ownerReference.TcsParametroValorList([]);
           ownerReference.TcsParametroValorUsuarioIsLoaded = value;
           if (value === false && ownerReference.isPOCO)
               ownerReference.TcsParametroValorUsuarioList([]);
           ownerReference.TcsParametroValorRedeIsLoaded = value;
           if (value === false && ownerReference.isPOCO)
               ownerReference.TcsParametroValorRedeList([]);
           ownerReference.TcsParametroValorGpeconIsLoaded = value;
           if (value === false && ownerReference.isPOCO)
               ownerReference.TcsParametroValorGpeconList([]);
           ownerReference.TcsParametroValorFilialIsLoaded = value;
           if (value === false && ownerReference.isPOCO)
               ownerReference.TcsParametroValorFilialList([]);
           ownerReference.TcsParametroValorLjvLojaIsLoaded = value;
           if (value === false && ownerReference.isPOCO)
               ownerReference.TcsParametroValorLjvLojaList([]);
           ownerReference.TcsParametroValorVariacaoGenericaIsLoaded = value;
           if (value === false && ownerReference.isPOCO)
               ownerReference.TcsParametroValorVariacaoGenericaList([]);
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (ownerReference.isAdded()) {
            ownerReference.TcsParametroTabelaSelecaoIsLoaded = true;
            ownerReference.TcsParametroValorIsLoaded = true;
            ownerReference.TcsParametroValorUsuarioIsLoaded = true;
            ownerReference.TcsParametroValorRedeIsLoaded = true;
            ownerReference.TcsParametroValorGpeconIsLoaded = true;
            ownerReference.TcsParametroValorFilialIsLoaded = true;
            ownerReference.TcsParametroValorLjvLojaIsLoaded = true;
            ownerReference.TcsParametroValorVariacaoGenericaIsLoaded = true;
          }
          var _TcsParametroTabelaSelecaoRemoteComplete = false;
          var _TcsParametroValorRemoteComplete = false;
          var _TcsParametroValorUsuarioRemoteComplete = false;
          var _TcsParametroValorRedeRemoteComplete = false;
          var _TcsParametroValorGpeconRemoteComplete = false;
          var _TcsParametroValorFilialRemoteComplete = false;
          var _TcsParametroValorLjvLojaRemoteComplete = false;
          var _TcsParametroValorVariacaoGenericaRemoteComplete = false;
          var detachList_TcsParametroTabelaSelecao = [];
          if (force) {
               if (isNullOrEmpty(detailName) || detailName == 'TcsParametroTabelaSelecao') ownerReference.TcsParametroTabelaSelecaoIsLoaded = false;
               if ((isNullOrEmpty(detailName) || detailName == 'TcsParametroTabelaSelecao') && ownerReference.TcsParametroTabelaSelecaoList && ownerReference.TcsParametroTabelaSelecaoList().length > 0) {
                   if (ownerReference.isPOCO) {
                       ownerReference.TcsParametroTabelaSelecaoList([]);
                   } else {
                       var detailList = ownerReference.TcsParametroTabelaSelecaoList();
                       for (var idx = detailList.length - 1; idx >= 0; idx--) {
                           detachList_TcsParametroTabelaSelecao.push(detailList[idx]);
                       }
                   }
               }
          }
    
          if (!ownerReference.TcsParametroTabelaSelecaoIsLoaded) {
            //Load TcsParametroTabelaSelecaoList
            if (isNullOrEmpty(detailName) || detailName === 'TcsParametroTabelaSelecao') {
              ownerReference.TcsParametroTabelaSelecaoIsLoaded = true;
              _TcsParametroTabelaSelecaoRemoteComplete = (ownerReference.TcsParametroTabelaSelecaoList && ownerReference.TcsParametroTabelaSelecaoList().length > 0);
              if ((force || !ownerReference.TcsParametroTabelaSelecaoList || ownerReference.TcsParametroTabelaSelecaoList().length === 0) && (!isNullOrEmpty(getAbsoluteValue(ownerReference.IdParametro)))) {
                var navQuery = EntityQuery.from('GetTcsParametroTabelaSelecaoByEntitySearchNoAssociations').noTracking(ownerReference.isPOCO === true)
                .orderBy('DescTabela asc')
                    .withParameters({ jEntitySearch: ownerReference.GetJsWhereDetailRelationForTcsParametroTabelaSelecao(customParentRelation) })    ;
                if (!vm.dataToolbar._noBusyLoading) vm.showProcessing('Pesquisando detalhes...');
                manager.executeQuery(navQuery).then(function (data) { if (ownerReference.isPOCO) { for (var idx = 0; idx < data.results.length; idx++) { initializePOCO(data.results[idx], 'TcsParametroTabelaSelecao'); data.results[idx].TcsParametro = ko.observable(ownerReference); } ownerReference.TcsParametroTabelaSelecaoList(data.results); } 
                   if (!ownerReference.isPOCO && detachList_TcsParametroTabelaSelecao.length > 0)
                   {
                       for (var idx = 0; idx < detachList_TcsParametroTabelaSelecao.length; idx++)
                       {
                           if (!data.results.contains(detachList_TcsParametroTabelaSelecao[idx]))
                               detachEntity(detachList_TcsParametroTabelaSelecao[idx]);
                           else {
                               if (force && detachList_TcsParametroTabelaSelecao[idx].atLeastOneDetailLoaded())
                                   detachList_TcsParametroTabelaSelecao[idx].fillDetails(force, '', false, noWait);
                           }
                       }
                   }
                   ownerReference.setCurrentDetails('TcsParametroTabelaSelecao'); ownerReference.OnDetailSearched('TcsParametroTabelaSelecao'); notifyPresentation('TcsParametroTabelaSelecaoList');
                   _TcsParametroTabelaSelecaoRemoteComplete = true;
                   if (callback && (!isNullOrEmpty(detailName) || (_TcsParametroTabelaSelecaoRemoteComplete && _TcsParametroValorRemoteComplete && _TcsParametroValorUsuarioRemoteComplete && _TcsParametroValorRedeRemoteComplete && _TcsParametroValorGpeconRemoteComplete && _TcsParametroValorFilialRemoteComplete && _TcsParametroValorLjvLojaRemoteComplete && _TcsParametroValorVariacaoGenericaRemoteComplete))) { callback(); }
                }).fail(queryFailed).fin(function() { if (!vm.dataToolbar._noBusyLoading) vm.closeProcessing(); });
              } else { ownerReference.setCurrentDetails('TcsParametroTabelaSelecao'); notifyPresentation('TcsParametroTabelaSelecaoList'); }
            } else { _TcsParametroTabelaSelecaoRemoteComplete = true; if (!ownerReference.TcsParametroTabelaSelecaoIsLoaded && ownerReference.TcsParametroTabelaSelecaoList && ownerReference.TcsParametroTabelaSelecaoList().length > 0) { ownerReference.TcsParametroTabelaSelecaoIsLoaded = true; ownerReference.setCurrentDetails('TcsParametroTabelaSelecao'); } }
          } else { 
            if (isNullOrEmpty(detailName) || detailName == 'TcsParametroTabelaSelecao') {
               notifyPresentation('TcsParametroTabelaSelecaoList');
               ownerReference.setCurrentDetails('TcsParametroTabelaSelecao');
            }
            _TcsParametroTabelaSelecaoRemoteComplete = true;
          }
          var detachList_TcsParametroValor = [];
          if (force) {
               if (isNullOrEmpty(detailName) || detailName == 'TcsParametroValor') ownerReference.TcsParametroValorIsLoaded = false;
               if ((isNullOrEmpty(detailName) || detailName == 'TcsParametroValor') && ownerReference.TcsParametroValorList && ownerReference.TcsParametroValorList().length > 0) {
                   if (ownerReference.isPOCO) {
                       ownerReference.TcsParametroValorList([]);
                   } else {
                       var detailList = ownerReference.TcsParametroValorList();
                       for (var idx = detailList.length - 1; idx >= 0; idx--) {
                           detachList_TcsParametroValor.push(detailList[idx]);
                       }
                   }
               }
          }
    
          if (!ownerReference.TcsParametroValorIsLoaded) {
            //Load TcsParametroValorList
            if (isNullOrEmpty(detailName) || detailName === 'TcsParametroValor') {
              ownerReference.TcsParametroValorIsLoaded = true;
              _TcsParametroValorRemoteComplete = (ownerReference.TcsParametroValorList && ownerReference.TcsParametroValorList().length > 0);
              if ((force || !ownerReference.TcsParametroValorList || ownerReference.TcsParametroValorList().length === 0) && (!isNullOrEmpty(getAbsoluteValue(ownerReference.IdParametro)))) {
                var navQuery = EntityQuery.from('GetTcsParametroValorByEntitySearchNoAssociations').noTracking(ownerReference.isPOCO === true)
                .orderBy('IdParametro asc')
                    .withParameters({ jEntitySearch: ownerReference.GetJsWhereDetailRelationForTcsParametroValor(customParentRelation) })    ;
                if (!vm.dataToolbar._noBusyLoading) vm.showProcessing('Pesquisando detalhes...');
                manager.executeQuery(navQuery).then(function (data) { if (ownerReference.isPOCO) { for (var idx = 0; idx < data.results.length; idx++) { initializePOCO(data.results[idx], 'TcsParametroValor'); data.results[idx].TcsParametro = ko.observable(ownerReference); } ownerReference.TcsParametroValorList(data.results); } 
                   if (!ownerReference.isPOCO && detachList_TcsParametroValor.length > 0)
                   {
                       for (var idx = 0; idx < detachList_TcsParametroValor.length; idx++)
                       {
                           if (!data.results.contains(detachList_TcsParametroValor[idx]))
                               detachEntity(detachList_TcsParametroValor[idx]);
                           else {
                               if (force && detachList_TcsParametroValor[idx].atLeastOneDetailLoaded())
                                   detachList_TcsParametroValor[idx].fillDetails(force, '', false, noWait);
                           }
                       }
                   }
                   ownerReference.setCurrentDetails('TcsParametroValor'); ownerReference.OnDetailSearched('TcsParametroValor'); notifyPresentation('TcsParametroValorList');
                   _TcsParametroValorRemoteComplete = true;
                   if (callback && (!isNullOrEmpty(detailName) || (_TcsParametroTabelaSelecaoRemoteComplete && _TcsParametroValorRemoteComplete && _TcsParametroValorUsuarioRemoteComplete && _TcsParametroValorRedeRemoteComplete && _TcsParametroValorGpeconRemoteComplete && _TcsParametroValorFilialRemoteComplete && _TcsParametroValorLjvLojaRemoteComplete && _TcsParametroValorVariacaoGenericaRemoteComplete))) { callback(); }
                }).fail(queryFailed).fin(function() { if (!vm.dataToolbar._noBusyLoading) vm.closeProcessing(); });
              } else { ownerReference.setCurrentDetails('TcsParametroValor'); notifyPresentation('TcsParametroValorList'); }
            } else { _TcsParametroValorRemoteComplete = true; if (!ownerReference.TcsParametroValorIsLoaded && ownerReference.TcsParametroValorList && ownerReference.TcsParametroValorList().length > 0) { ownerReference.TcsParametroValorIsLoaded = true; ownerReference.setCurrentDetails('TcsParametroValor'); } }
          } else { 
            if (isNullOrEmpty(detailName) || detailName == 'TcsParametroValor') {
               notifyPresentation('TcsParametroValorList');
               ownerReference.setCurrentDetails('TcsParametroValor');
            }
            _TcsParametroValorRemoteComplete = true;
          }
          var detachList_TcsParametroValorUsuario = [];
          if (force) {
               if (isNullOrEmpty(detailName) || detailName == 'TcsParametroValorUsuario') ownerReference.TcsParametroValorUsuarioIsLoaded = false;
               if ((isNullOrEmpty(detailName) || detailName == 'TcsParametroValorUsuario') && ownerReference.TcsParametroValorUsuarioList && ownerReference.TcsParametroValorUsuarioList().length > 0) {
                   if (ownerReference.isPOCO) {
                       ownerReference.TcsParametroValorUsuarioList([]);
                   } else {
                       var detailList = ownerReference.TcsParametroValorUsuarioList();
                       for (var idx = detailList.length - 1; idx >= 0; idx--) {
                           detachList_TcsParametroValorUsuario.push(detailList[idx]);
                       }
                   }
               }
          }
    
          if (!ownerReference.TcsParametroValorUsuarioIsLoaded) {
            //Load TcsParametroValorUsuarioList
            if (isNullOrEmpty(detailName) || detailName === 'TcsParametroValorUsuario') {
              ownerReference.TcsParametroValorUsuarioIsLoaded = true;
              _TcsParametroValorUsuarioRemoteComplete = (ownerReference.TcsParametroValorUsuarioList && ownerReference.TcsParametroValorUsuarioList().length > 0);
              if ((force || !ownerReference.TcsParametroValorUsuarioList || ownerReference.TcsParametroValorUsuarioList().length === 0) && (!isNullOrEmpty(getAbsoluteValue(ownerReference.IdParametro)))) {
                var navQuery = EntityQuery.from('GetTcsParametroValorUsuarioByEntitySearchNoAssociations').noTracking(ownerReference.isPOCO === true)
                .orderBy('IdParametroValor asc')
                    .withParameters({ jEntitySearch: ownerReference.GetJsWhereDetailRelationForTcsParametroValorUsuario(customParentRelation) })    ;
                if (!vm.dataToolbar._noBusyLoading) vm.showProcessing('Pesquisando detalhes...');
                manager.executeQuery(navQuery).then(function (data) { if (ownerReference.isPOCO) { for (var idx = 0; idx < data.results.length; idx++) { initializePOCO(data.results[idx], 'TcsParametroValorUsuario'); data.results[idx].TcsParametro = ko.observable(ownerReference); } ownerReference.TcsParametroValorUsuarioList(data.results); } 
                   if (!ownerReference.isPOCO && detachList_TcsParametroValorUsuario.length > 0)
                   {
                       for (var idx = 0; idx < detachList_TcsParametroValorUsuario.length; idx++)
                       {
                           if (!data.results.contains(detachList_TcsParametroValorUsuario[idx]))
                               detachEntity(detachList_TcsParametroValorUsuario[idx]);
                           else {
                               if (force && detachList_TcsParametroValorUsuario[idx].atLeastOneDetailLoaded())
                                   detachList_TcsParametroValorUsuario[idx].fillDetails(force, '', false, noWait);
                           }
                       }
                   }
                   ownerReference.setCurrentDetails('TcsParametroValorUsuario'); ownerReference.OnDetailSearched('TcsParametroValorUsuario'); notifyPresentation('TcsParametroValorUsuarioList');
                   _TcsParametroValorUsuarioRemoteComplete = true;
                   if (callback && (!isNullOrEmpty(detailName) || (_TcsParametroTabelaSelecaoRemoteComplete && _TcsParametroValorRemoteComplete && _TcsParametroValorUsuarioRemoteComplete && _TcsParametroValorRedeRemoteComplete && _TcsParametroValorGpeconRemoteComplete && _TcsParametroValorFilialRemoteComplete && _TcsParametroValorLjvLojaRemoteComplete && _TcsParametroValorVariacaoGenericaRemoteComplete))) { callback(); }
                }).fail(queryFailed).fin(function() { if (!vm.dataToolbar._noBusyLoading) vm.closeProcessing(); });
              } else { ownerReference.setCurrentDetails('TcsParametroValorUsuario'); notifyPresentation('TcsParametroValorUsuarioList'); }
            } else { _TcsParametroValorUsuarioRemoteComplete = true; if (!ownerReference.TcsParametroValorUsuarioIsLoaded && ownerReference.TcsParametroValorUsuarioList && ownerReference.TcsParametroValorUsuarioList().length > 0) { ownerReference.TcsParametroValorUsuarioIsLoaded = true; ownerReference.setCurrentDetails('TcsParametroValorUsuario'); } }
          } else { 
            if (isNullOrEmpty(detailName) || detailName == 'TcsParametroValorUsuario') {
               notifyPresentation('TcsParametroValorUsuarioList');
               ownerReference.setCurrentDetails('TcsParametroValorUsuario');
            }
            _TcsParametroValorUsuarioRemoteComplete = true;
          }
          var detachList_TcsParametroValorRede = [];
          if (force) {
               if (isNullOrEmpty(detailName) || detailName == 'TcsParametroValorRede') ownerReference.TcsParametroValorRedeIsLoaded = false;
               if ((isNullOrEmpty(detailName) || detailName == 'TcsParametroValorRede') && ownerReference.TcsParametroValorRedeList && ownerReference.TcsParametroValorRedeList().length > 0) {
                   if (ownerReference.isPOCO) {
                       ownerReference.TcsParametroValorRedeList([]);
                   } else {
                       var detailList = ownerReference.TcsParametroValorRedeList();
                       for (var idx = detailList.length - 1; idx >= 0; idx--) {
                           detachList_TcsParametroValorRede.push(detailList[idx]);
                       }
                   }
               }
          }
    
          if (!ownerReference.TcsParametroValorRedeIsLoaded) {
            //Load TcsParametroValorRedeList
            if (isNullOrEmpty(detailName) || detailName === 'TcsParametroValorRede') {
              ownerReference.TcsParametroValorRedeIsLoaded = true;
              _TcsParametroValorRedeRemoteComplete = (ownerReference.TcsParametroValorRedeList && ownerReference.TcsParametroValorRedeList().length > 0);
              if ((force || !ownerReference.TcsParametroValorRedeList || ownerReference.TcsParametroValorRedeList().length === 0) && (!isNullOrEmpty(getAbsoluteValue(ownerReference.IdParametro)))) {
                var navQuery = EntityQuery.from('GetTcsParametroValorRedeByEntitySearchNoAssociations').noTracking(ownerReference.isPOCO === true)
                .orderBy('IdParametroValor asc')
                    .withParameters({ jEntitySearch: ownerReference.GetJsWhereDetailRelationForTcsParametroValorRede(customParentRelation) })    ;
                if (!vm.dataToolbar._noBusyLoading) vm.showProcessing('Pesquisando detalhes...');
                manager.executeQuery(navQuery).then(function (data) { if (ownerReference.isPOCO) { for (var idx = 0; idx < data.results.length; idx++) { initializePOCO(data.results[idx], 'TcsParametroValorRede'); data.results[idx].TcsParametro = ko.observable(ownerReference); } ownerReference.TcsParametroValorRedeList(data.results); } 
                   if (!ownerReference.isPOCO && detachList_TcsParametroValorRede.length > 0)
                   {
                       for (var idx = 0; idx < detachList_TcsParametroValorRede.length; idx++)
                       {
                           if (!data.results.contains(detachList_TcsParametroValorRede[idx]))
                               detachEntity(detachList_TcsParametroValorRede[idx]);
                           else {
                               if (force && detachList_TcsParametroValorRede[idx].atLeastOneDetailLoaded())
                                   detachList_TcsParametroValorRede[idx].fillDetails(force, '', false, noWait);
                           }
                       }
                   }
                   ownerReference.setCurrentDetails('TcsParametroValorRede'); ownerReference.OnDetailSearched('TcsParametroValorRede'); notifyPresentation('TcsParametroValorRedeList');
                   _TcsParametroValorRedeRemoteComplete = true;
                   if (callback && (!isNullOrEmpty(detailName) || (_TcsParametroTabelaSelecaoRemoteComplete && _TcsParametroValorRemoteComplete && _TcsParametroValorUsuarioRemoteComplete && _TcsParametroValorRedeRemoteComplete && _TcsParametroValorGpeconRemoteComplete && _TcsParametroValorFilialRemoteComplete && _TcsParametroValorLjvLojaRemoteComplete && _TcsParametroValorVariacaoGenericaRemoteComplete))) { callback(); }
                }).fail(queryFailed).fin(function() { if (!vm.dataToolbar._noBusyLoading) vm.closeProcessing(); });
              } else { ownerReference.setCurrentDetails('TcsParametroValorRede'); notifyPresentation('TcsParametroValorRedeList'); }
            } else { _TcsParametroValorRedeRemoteComplete = true; if (!ownerReference.TcsParametroValorRedeIsLoaded && ownerReference.TcsParametroValorRedeList && ownerReference.TcsParametroValorRedeList().length > 0) { ownerReference.TcsParametroValorRedeIsLoaded = true; ownerReference.setCurrentDetails('TcsParametroValorRede'); } }
          } else { 
            if (isNullOrEmpty(detailName) || detailName == 'TcsParametroValorRede') {
               notifyPresentation('TcsParametroValorRedeList');
               ownerReference.setCurrentDetails('TcsParametroValorRede');
            }
            _TcsParametroValorRedeRemoteComplete = true;
          }
          var detachList_TcsParametroValorGpecon = [];
          if (force) {
               if (isNullOrEmpty(detailName) || detailName == 'TcsParametroValorGpecon') ownerReference.TcsParametroValorGpeconIsLoaded = false;
               if ((isNullOrEmpty(detailName) || detailName == 'TcsParametroValorGpecon') && ownerReference.TcsParametroValorGpeconList && ownerReference.TcsParametroValorGpeconList().length > 0) {
                   if (ownerReference.isPOCO) {
                       ownerReference.TcsParametroValorGpeconList([]);
                   } else {
                       var detailList = ownerReference.TcsParametroValorGpeconList();
                       for (var idx = detailList.length - 1; idx >= 0; idx--) {
                           detachList_TcsParametroValorGpecon.push(detailList[idx]);
                       }
                   }
               }
          }
    
          if (!ownerReference.TcsParametroValorGpeconIsLoaded) {
            //Load TcsParametroValorGpeconList
            if (isNullOrEmpty(detailName) || detailName === 'TcsParametroValorGpecon') {
              ownerReference.TcsParametroValorGpeconIsLoaded = true;
              _TcsParametroValorGpeconRemoteComplete = (ownerReference.TcsParametroValorGpeconList && ownerReference.TcsParametroValorGpeconList().length > 0);
              if ((force || !ownerReference.TcsParametroValorGpeconList || ownerReference.TcsParametroValorGpeconList().length === 0) && (!isNullOrEmpty(getAbsoluteValue(ownerReference.IdParametro)))) {
                var navQuery = EntityQuery.from('GetTcsParametroValorGpeconByEntitySearchNoAssociations').noTracking(ownerReference.isPOCO === true)
                .orderBy('IdParametroValor asc')
                    .withParameters({ jEntitySearch: ownerReference.GetJsWhereDetailRelationForTcsParametroValorGpecon(customParentRelation) })    ;
                if (!vm.dataToolbar._noBusyLoading) vm.showProcessing('Pesquisando detalhes...');
                manager.executeQuery(navQuery).then(function (data) { if (ownerReference.isPOCO) { for (var idx = 0; idx < data.results.length; idx++) { initializePOCO(data.results[idx], 'TcsParametroValorGpecon'); data.results[idx].TcsParametro = ko.observable(ownerReference); } ownerReference.TcsParametroValorGpeconList(data.results); } 
                   if (!ownerReference.isPOCO && detachList_TcsParametroValorGpecon.length > 0)
                   {
                       for (var idx = 0; idx < detachList_TcsParametroValorGpecon.length; idx++)
                       {
                           if (!data.results.contains(detachList_TcsParametroValorGpecon[idx]))
                               detachEntity(detachList_TcsParametroValorGpecon[idx]);
                           else {
                               if (force && detachList_TcsParametroValorGpecon[idx].atLeastOneDetailLoaded())
                                   detachList_TcsParametroValorGpecon[idx].fillDetails(force, '', false, noWait);
                           }
                       }
                   }
                   ownerReference.setCurrentDetails('TcsParametroValorGpecon'); ownerReference.OnDetailSearched('TcsParametroValorGpecon'); notifyPresentation('TcsParametroValorGpeconList');
                   _TcsParametroValorGpeconRemoteComplete = true;
                   if (callback && (!isNullOrEmpty(detailName) || (_TcsParametroTabelaSelecaoRemoteComplete && _TcsParametroValorRemoteComplete && _TcsParametroValorUsuarioRemoteComplete && _TcsParametroValorRedeRemoteComplete && _TcsParametroValorGpeconRemoteComplete && _TcsParametroValorFilialRemoteComplete && _TcsParametroValorLjvLojaRemoteComplete && _TcsParametroValorVariacaoGenericaRemoteComplete))) { callback(); }
                }).fail(queryFailed).fin(function() { if (!vm.dataToolbar._noBusyLoading) vm.closeProcessing(); });
              } else { ownerReference.setCurrentDetails('TcsParametroValorGpecon'); notifyPresentation('TcsParametroValorGpeconList'); }
            } else { _TcsParametroValorGpeconRemoteComplete = true; if (!ownerReference.TcsParametroValorGpeconIsLoaded && ownerReference.TcsParametroValorGpeconList && ownerReference.TcsParametroValorGpeconList().length > 0) { ownerReference.TcsParametroValorGpeconIsLoaded = true; ownerReference.setCurrentDetails('TcsParametroValorGpecon'); } }
          } else { 
            if (isNullOrEmpty(detailName) || detailName == 'TcsParametroValorGpecon') {
               notifyPresentation('TcsParametroValorGpeconList');
               ownerReference.setCurrentDetails('TcsParametroValorGpecon');
            }
            _TcsParametroValorGpeconRemoteComplete = true;
          }
          var detachList_TcsParametroValorFilial = [];
          if (force) {
               if (isNullOrEmpty(detailName) || detailName == 'TcsParametroValorFilial') ownerReference.TcsParametroValorFilialIsLoaded = false;
               if ((isNullOrEmpty(detailName) || detailName == 'TcsParametroValorFilial') && ownerReference.TcsParametroValorFilialList && ownerReference.TcsParametroValorFilialList().length > 0) {
                   if (ownerReference.isPOCO) {
                       ownerReference.TcsParametroValorFilialList([]);
                   } else {
                       var detailList = ownerReference.TcsParametroValorFilialList();
                       for (var idx = detailList.length - 1; idx >= 0; idx--) {
                           detachList_TcsParametroValorFilial.push(detailList[idx]);
                       }
                   }
               }
          }
    
          if (!ownerReference.TcsParametroValorFilialIsLoaded) {
            //Load TcsParametroValorFilialList
            if (isNullOrEmpty(detailName) || detailName === 'TcsParametroValorFilial') {
              ownerReference.TcsParametroValorFilialIsLoaded = true;
              _TcsParametroValorFilialRemoteComplete = (ownerReference.TcsParametroValorFilialList && ownerReference.TcsParametroValorFilialList().length > 0);
              if ((force || !ownerReference.TcsParametroValorFilialList || ownerReference.TcsParametroValorFilialList().length === 0) && (!isNullOrEmpty(getAbsoluteValue(ownerReference.IdParametro)))) {
                var navQuery = EntityQuery.from('GetTcsParametroValorFilialByEntitySearchNoAssociations').noTracking(ownerReference.isPOCO === true)
                .orderBy('IdParametroValor asc')
                    .withParameters({ jEntitySearch: ownerReference.GetJsWhereDetailRelationForTcsParametroValorFilial(customParentRelation) })    ;
                if (!vm.dataToolbar._noBusyLoading) vm.showProcessing('Pesquisando detalhes...');
                manager.executeQuery(navQuery).then(function (data) { if (ownerReference.isPOCO) { for (var idx = 0; idx < data.results.length; idx++) { initializePOCO(data.results[idx], 'TcsParametroValorFilial'); data.results[idx].TcsParametro = ko.observable(ownerReference); } ownerReference.TcsParametroValorFilialList(data.results); } 
                   if (!ownerReference.isPOCO && detachList_TcsParametroValorFilial.length > 0)
                   {
                       for (var idx = 0; idx < detachList_TcsParametroValorFilial.length; idx++)
                       {
                           if (!data.results.contains(detachList_TcsParametroValorFilial[idx]))
                               detachEntity(detachList_TcsParametroValorFilial[idx]);
                           else {
                               if (force && detachList_TcsParametroValorFilial[idx].atLeastOneDetailLoaded())
                                   detachList_TcsParametroValorFilial[idx].fillDetails(force, '', false, noWait);
                           }
                       }
                   }
                   ownerReference.setCurrentDetails('TcsParametroValorFilial'); ownerReference.OnDetailSearched('TcsParametroValorFilial'); notifyPresentation('TcsParametroValorFilialList');
                   _TcsParametroValorFilialRemoteComplete = true;
                   if (callback && (!isNullOrEmpty(detailName) || (_TcsParametroTabelaSelecaoRemoteComplete && _TcsParametroValorRemoteComplete && _TcsParametroValorUsuarioRemoteComplete && _TcsParametroValorRedeRemoteComplete && _TcsParametroValorGpeconRemoteComplete && _TcsParametroValorFilialRemoteComplete && _TcsParametroValorLjvLojaRemoteComplete && _TcsParametroValorVariacaoGenericaRemoteComplete))) { callback(); }
                }).fail(queryFailed).fin(function() { if (!vm.dataToolbar._noBusyLoading) vm.closeProcessing(); });
              } else { ownerReference.setCurrentDetails('TcsParametroValorFilial'); notifyPresentation('TcsParametroValorFilialList'); }
            } else { _TcsParametroValorFilialRemoteComplete = true; if (!ownerReference.TcsParametroValorFilialIsLoaded && ownerReference.TcsParametroValorFilialList && ownerReference.TcsParametroValorFilialList().length > 0) { ownerReference.TcsParametroValorFilialIsLoaded = true; ownerReference.setCurrentDetails('TcsParametroValorFilial'); } }
          } else { 
            if (isNullOrEmpty(detailName) || detailName == 'TcsParametroValorFilial') {
               notifyPresentation('TcsParametroValorFilialList');
               ownerReference.setCurrentDetails('TcsParametroValorFilial');
            }
            _TcsParametroValorFilialRemoteComplete = true;
          }
          var detachList_TcsParametroValorLjvLoja = [];
          if (force) {
               if (isNullOrEmpty(detailName) || detailName == 'TcsParametroValorLjvLoja') ownerReference.TcsParametroValorLjvLojaIsLoaded = false;
               if ((isNullOrEmpty(detailName) || detailName == 'TcsParametroValorLjvLoja') && ownerReference.TcsParametroValorLjvLojaList && ownerReference.TcsParametroValorLjvLojaList().length > 0) {
                   if (ownerReference.isPOCO) {
                       ownerReference.TcsParametroValorLjvLojaList([]);
                   } else {
                       var detailList = ownerReference.TcsParametroValorLjvLojaList();
                       for (var idx = detailList.length - 1; idx >= 0; idx--) {
                           detachList_TcsParametroValorLjvLoja.push(detailList[idx]);
                       }
                   }
               }
          }
    
          if (!ownerReference.TcsParametroValorLjvLojaIsLoaded) {
            //Load TcsParametroValorLjvLojaList
            if (isNullOrEmpty(detailName) || detailName === 'TcsParametroValorLjvLoja') {
              ownerReference.TcsParametroValorLjvLojaIsLoaded = true;
              _TcsParametroValorLjvLojaRemoteComplete = (ownerReference.TcsParametroValorLjvLojaList && ownerReference.TcsParametroValorLjvLojaList().length > 0);
              if ((force || !ownerReference.TcsParametroValorLjvLojaList || ownerReference.TcsParametroValorLjvLojaList().length === 0) && (!isNullOrEmpty(getAbsoluteValue(ownerReference.IdParametro)))) {
                var navQuery = EntityQuery.from('GetTcsParametroValorLjvLojaByEntitySearchNoAssociations').noTracking(ownerReference.isPOCO === true)
                .orderBy('IdParametroValor asc')
                    .withParameters({ jEntitySearch: ownerReference.GetJsWhereDetailRelationForTcsParametroValorLjvLoja(customParentRelation) })    ;
                if (!vm.dataToolbar._noBusyLoading) vm.showProcessing('Pesquisando detalhes...');
                manager.executeQuery(navQuery).then(function (data) { if (ownerReference.isPOCO) { for (var idx = 0; idx < data.results.length; idx++) { initializePOCO(data.results[idx], 'TcsParametroValorLjvLoja'); data.results[idx].TcsParametro = ko.observable(ownerReference); } ownerReference.TcsParametroValorLjvLojaList(data.results); } 
                   if (!ownerReference.isPOCO && detachList_TcsParametroValorLjvLoja.length > 0)
                   {
                       for (var idx = 0; idx < detachList_TcsParametroValorLjvLoja.length; idx++)
                       {
                           if (!data.results.contains(detachList_TcsParametroValorLjvLoja[idx]))
                               detachEntity(detachList_TcsParametroValorLjvLoja[idx]);
                           else {
                               if (force && detachList_TcsParametroValorLjvLoja[idx].atLeastOneDetailLoaded())
                                   detachList_TcsParametroValorLjvLoja[idx].fillDetails(force, '', false, noWait);
                           }
                       }
                   }
                   ownerReference.setCurrentDetails('TcsParametroValorLjvLoja'); ownerReference.OnDetailSearched('TcsParametroValorLjvLoja'); notifyPresentation('TcsParametroValorLjvLojaList');
                   _TcsParametroValorLjvLojaRemoteComplete = true;
                   if (callback && (!isNullOrEmpty(detailName) || (_TcsParametroTabelaSelecaoRemoteComplete && _TcsParametroValorRemoteComplete && _TcsParametroValorUsuarioRemoteComplete && _TcsParametroValorRedeRemoteComplete && _TcsParametroValorGpeconRemoteComplete && _TcsParametroValorFilialRemoteComplete && _TcsParametroValorLjvLojaRemoteComplete && _TcsParametroValorVariacaoGenericaRemoteComplete))) { callback(); }
                }).fail(queryFailed).fin(function() { if (!vm.dataToolbar._noBusyLoading) vm.closeProcessing(); });
              } else { ownerReference.setCurrentDetails('TcsParametroValorLjvLoja'); notifyPresentation('TcsParametroValorLjvLojaList'); }
            } else { _TcsParametroValorLjvLojaRemoteComplete = true; if (!ownerReference.TcsParametroValorLjvLojaIsLoaded && ownerReference.TcsParametroValorLjvLojaList && ownerReference.TcsParametroValorLjvLojaList().length > 0) { ownerReference.TcsParametroValorLjvLojaIsLoaded = true; ownerReference.setCurrentDetails('TcsParametroValorLjvLoja'); } }
          } else { 
            if (isNullOrEmpty(detailName) || detailName == 'TcsParametroValorLjvLoja') {
               notifyPresentation('TcsParametroValorLjvLojaList');
               ownerReference.setCurrentDetails('TcsParametroValorLjvLoja');
            }
            _TcsParametroValorLjvLojaRemoteComplete = true;
          }
          var detachList_TcsParametroValorVariacaoGenerica = [];
          if (force) {
               if (isNullOrEmpty(detailName) || detailName == 'TcsParametroValorVariacaoGenerica') ownerReference.TcsParametroValorVariacaoGenericaIsLoaded = false;
               if ((isNullOrEmpty(detailName) || detailName == 'TcsParametroValorVariacaoGenerica') && ownerReference.TcsParametroValorVariacaoGenericaList && ownerReference.TcsParametroValorVariacaoGenericaList().length > 0) {
                   if (ownerReference.isPOCO) {
                       ownerReference.TcsParametroValorVariacaoGenericaList([]);
                   } else {
                       var detailList = ownerReference.TcsParametroValorVariacaoGenericaList();
                       for (var idx = detailList.length - 1; idx >= 0; idx--) {
                           detachList_TcsParametroValorVariacaoGenerica.push(detailList[idx]);
                       }
                   }
               }
          }
    
          if (!ownerReference.TcsParametroValorVariacaoGenericaIsLoaded) {
            //Load TcsParametroValorVariacaoGenericaList
            if (isNullOrEmpty(detailName) || detailName === 'TcsParametroValorVariacaoGenerica') {
              ownerReference.TcsParametroValorVariacaoGenericaIsLoaded = true;
              _TcsParametroValorVariacaoGenericaRemoteComplete = (ownerReference.TcsParametroValorVariacaoGenericaList && ownerReference.TcsParametroValorVariacaoGenericaList().length > 0);
              if ((force || !ownerReference.TcsParametroValorVariacaoGenericaList || ownerReference.TcsParametroValorVariacaoGenericaList().length === 0) && (!isNullOrEmpty(getAbsoluteValue(ownerReference.IdParametro)))) {
                var navQuery = EntityQuery.from('GetTcsParametroValorVariacaoGenericaByEntitySearchNoAssociations').noTracking(ownerReference.isPOCO === true)
                .orderBy('IdParametroValor asc')
                    .withParameters({ jEntitySearch: ownerReference.GetJsWhereDetailRelationForTcsParametroValorVariacaoGenerica(customParentRelation) })    ;
                if (!vm.dataToolbar._noBusyLoading) vm.showProcessing('Pesquisando detalhes...');
                manager.executeQuery(navQuery).then(function (data) { if (ownerReference.isPOCO) { for (var idx = 0; idx < data.results.length; idx++) { initializePOCO(data.results[idx], 'TcsParametroValorVariacaoGenerica'); data.results[idx].TcsParametro = ko.observable(ownerReference); } ownerReference.TcsParametroValorVariacaoGenericaList(data.results); } 
                   if (!ownerReference.isPOCO && detachList_TcsParametroValorVariacaoGenerica.length > 0)
                   {
                       for (var idx = 0; idx < detachList_TcsParametroValorVariacaoGenerica.length; idx++)
                       {
                           if (!data.results.contains(detachList_TcsParametroValorVariacaoGenerica[idx]))
                               detachEntity(detachList_TcsParametroValorVariacaoGenerica[idx]);
                           else {
                               if (force && detachList_TcsParametroValorVariacaoGenerica[idx].atLeastOneDetailLoaded())
                                   detachList_TcsParametroValorVariacaoGenerica[idx].fillDetails(force, '', false, noWait);
                           }
                       }
                   }
                   ownerReference.setCurrentDetails('TcsParametroValorVariacaoGenerica'); ownerReference.OnDetailSearched('TcsParametroValorVariacaoGenerica'); notifyPresentation('TcsParametroValorVariacaoGenericaList');
                   _TcsParametroValorVariacaoGenericaRemoteComplete = true;
                   if (callback && (!isNullOrEmpty(detailName) || (_TcsParametroTabelaSelecaoRemoteComplete && _TcsParametroValorRemoteComplete && _TcsParametroValorUsuarioRemoteComplete && _TcsParametroValorRedeRemoteComplete && _TcsParametroValorGpeconRemoteComplete && _TcsParametroValorFilialRemoteComplete && _TcsParametroValorLjvLojaRemoteComplete && _TcsParametroValorVariacaoGenericaRemoteComplete))) { callback(); }
                }).fail(queryFailed).fin(function() { if (!vm.dataToolbar._noBusyLoading) vm.closeProcessing(); });
              } else { ownerReference.setCurrentDetails('TcsParametroValorVariacaoGenerica'); notifyPresentation('TcsParametroValorVariacaoGenericaList'); }
            } else { _TcsParametroValorVariacaoGenericaRemoteComplete = true; if (!ownerReference.TcsParametroValorVariacaoGenericaIsLoaded && ownerReference.TcsParametroValorVariacaoGenericaList && ownerReference.TcsParametroValorVariacaoGenericaList().length > 0) { ownerReference.TcsParametroValorVariacaoGenericaIsLoaded = true; ownerReference.setCurrentDetails('TcsParametroValorVariacaoGenerica'); } }
          } else { 
            if (isNullOrEmpty(detailName) || detailName == 'TcsParametroValorVariacaoGenerica') {
               notifyPresentation('TcsParametroValorVariacaoGenericaList');
               ownerReference.setCurrentDetails('TcsParametroValorVariacaoGenerica');
            }
            _TcsParametroValorVariacaoGenericaRemoteComplete = true;
          }
          if (callback && ((!isNullOrEmpty(detailName) && (eval('_' + detailName + 'RemoteComplete && ownerReference.' + detailName + 'IsLoaded') == true)) || (isNullOrEmpty(detailName) && (_TcsParametroTabelaSelecaoRemoteComplete && _TcsParametroValorRemoteComplete && _TcsParametroValorUsuarioRemoteComplete && _TcsParametroValorRedeRemoteComplete && _TcsParametroValorGpeconRemoteComplete && _TcsParametroValorFilialRemoteComplete && _TcsParametroValorLjvLojaRemoteComplete && _TcsParametroValorVariacaoGenericaRemoteComplete)))) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
          if ((isNullOrEmpty(detailName) || detailName === 'TcsParametroTabelaSelecao')) {
               if (ownerReference.TcsParametroTabelaSelecaoList().length > 0) { ownerReference.currentTcsParametroTabelaSelecao(ownerReference.TcsParametroTabelaSelecaoList()[0]); if (clearing == null || clearing === false) ownerReference.currentTcsParametroTabelaSelecao().fillDetails(); }
               else { ownerReference.currentTcsParametroTabelaSelecao(null); ownerReference.notifyEmptyDetails('TcsParametroTabelaSelecao'); }
          }
          if ((isNullOrEmpty(detailName) || detailName === 'TcsParametroValor')) {
               if (ownerReference.TcsParametroValorList().length > 0) { ownerReference.currentTcsParametroValor(ownerReference.TcsParametroValorList()[0]); if (clearing == null || clearing === false) ownerReference.currentTcsParametroValor().fillDetails(); }
               else { ownerReference.currentTcsParametroValor(null); ownerReference.notifyEmptyDetails('TcsParametroValor'); }
          }
          if ((isNullOrEmpty(detailName) || detailName === 'TcsParametroValorUsuario')) {
               if (ownerReference.TcsParametroValorUsuarioList().length > 0) { ownerReference.currentTcsParametroValorUsuario(ownerReference.TcsParametroValorUsuarioList()[0]); if (clearing == null || clearing === false) ownerReference.currentTcsParametroValorUsuario().fillDetails(); }
               else { ownerReference.currentTcsParametroValorUsuario(null); ownerReference.notifyEmptyDetails('TcsParametroValorUsuario'); }
          }
          if ((isNullOrEmpty(detailName) || detailName === 'TcsParametroValorRede')) {
               if (ownerReference.TcsParametroValorRedeList().length > 0) { ownerReference.currentTcsParametroValorRede(ownerReference.TcsParametroValorRedeList()[0]); if (clearing == null || clearing === false) ownerReference.currentTcsParametroValorRede().fillDetails(); }
               else { ownerReference.currentTcsParametroValorRede(null); ownerReference.notifyEmptyDetails('TcsParametroValorRede'); }
          }
          if ((isNullOrEmpty(detailName) || detailName === 'TcsParametroValorGpecon')) {
               if (ownerReference.TcsParametroValorGpeconList().length > 0) { ownerReference.currentTcsParametroValorGpecon(ownerReference.TcsParametroValorGpeconList()[0]); if (clearing == null || clearing === false) ownerReference.currentTcsParametroValorGpecon().fillDetails(); }
               else { ownerReference.currentTcsParametroValorGpecon(null); ownerReference.notifyEmptyDetails('TcsParametroValorGpecon'); }
          }
          if ((isNullOrEmpty(detailName) || detailName === 'TcsParametroValorFilial')) {
               if (ownerReference.TcsParametroValorFilialList().length > 0) { ownerReference.currentTcsParametroValorFilial(ownerReference.TcsParametroValorFilialList()[0]); if (clearing == null || clearing === false) ownerReference.currentTcsParametroValorFilial().fillDetails(); }
               else { ownerReference.currentTcsParametroValorFilial(null); ownerReference.notifyEmptyDetails('TcsParametroValorFilial'); }
          }
          if ((isNullOrEmpty(detailName) || detailName === 'TcsParametroValorLjvLoja')) {
               if (ownerReference.TcsParametroValorLjvLojaList().length > 0) { ownerReference.currentTcsParametroValorLjvLoja(ownerReference.TcsParametroValorLjvLojaList()[0]); if (clearing == null || clearing === false) ownerReference.currentTcsParametroValorLjvLoja().fillDetails(); }
               else { ownerReference.currentTcsParametroValorLjvLoja(null); ownerReference.notifyEmptyDetails('TcsParametroValorLjvLoja'); }
          }
          if ((isNullOrEmpty(detailName) || detailName === 'TcsParametroValorVariacaoGenerica')) {
               if (ownerReference.TcsParametroValorVariacaoGenericaList().length > 0) { ownerReference.currentTcsParametroValorVariacaoGenerica(ownerReference.TcsParametroValorVariacaoGenericaList()[0]); if (clearing == null || clearing === false) ownerReference.currentTcsParametroValorVariacaoGenerica().fillDetails(); }
               else { ownerReference.currentTcsParametroValorVariacaoGenerica(null); ownerReference.notifyEmptyDetails('TcsParametroValorVariacaoGenerica'); }
          }
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
          if (detailName === 'TcsParametroTabelaSelecao') {
               notifyPresentation('TcsParametroTabelaSelecaoList');
               vm.queryInnerUIs(null, 'TcsParametroTabelaSelecao');
          }
          if (detailName === 'TcsParametroValor') {
               notifyPresentation('TcsParametroValorList');
               vm.queryInnerUIs(null, 'TcsParametroValor');
          }
          if (detailName === 'TcsParametroValorUsuario') {
               notifyPresentation('TcsParametroValorUsuarioList');
               vm.queryInnerUIs(null, 'TcsParametroValorUsuario');
          }
          if (detailName === 'TcsParametroValorRede') {
               notifyPresentation('TcsParametroValorRedeList');
               vm.queryInnerUIs(null, 'TcsParametroValorRede');
          }
          if (detailName === 'TcsParametroValorGpecon') {
               notifyPresentation('TcsParametroValorGpeconList');
               vm.queryInnerUIs(null, 'TcsParametroValorGpecon');
          }
          if (detailName === 'TcsParametroValorFilial') {
               notifyPresentation('TcsParametroValorFilialList');
               vm.queryInnerUIs(null, 'TcsParametroValorFilial');
          }
          if (detailName === 'TcsParametroValorLjvLoja') {
               notifyPresentation('TcsParametroValorLjvLojaList');
               vm.queryInnerUIs(null, 'TcsParametroValorLjvLoja');
          }
          if (detailName === 'TcsParametroValorVariacaoGenerica') {
               notifyPresentation('TcsParametroValorVariacaoGenericaList');
               vm.queryInnerUIs(null, 'TcsParametroValorVariacaoGenerica');
          }
       };
    //#region Client Events
       ownerReference.OnDetailSearched = function (detailName) {
    if (jQuery.inArray(detailName, ["TcsParametroTabelaSelecao", "TcsParametroValor"]) >= 0){
	vm.AdjustControls();
}
       }
    //#endregion Client Events
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxDatatypeParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValorParametro', newValue));
                   if (value != ownerReference.LxDatatypeParametro()) {
                       ownerReference.LxDatatypeParametro(value);
                   }
            });
    
           ownerReference.LxDatatypeParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametro(0); return; }
                   var value = dataDomains.getName('TipoValorParametro', newValue);
                   if (value != ownerReference.LxDatatypeParametroName()) {
                       ownerReference.LxDatatypeParametroName(value);
               }
           });
       }
       if (isPOCO !== true) {
           ownerReference.LxTipoValidacaoParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxTipoValidacaoParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValidacaoParametro', newValue));
                   if (value != ownerReference.LxTipoValidacaoParametro()) {
                       ownerReference.LxTipoValidacaoParametro(value);
                   }
            });
    
           ownerReference.LxTipoValidacaoParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxTipoValidacaoParametro(0); return; }
                   var value = dataDomains.getName('TipoValidacaoParametro', newValue);
                   if (value != ownerReference.LxTipoValidacaoParametroName()) {
                       ownerReference.LxTipoValidacaoParametroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    //#region Adjust details already loaded for a POCO reference
       if (isPOCO === true) {
           if ((typeof ownerReference.TcsParametroTabelaSelecaoList === 'function') && ownerReference.TcsParametroTabelaSelecaoList().length > 0) {
                for(var idx = 0; idx < ownerReference.TcsParametroTabelaSelecaoList().length; idx++) { TcsParametroTabelaSelecaoInitializer(ownerReference.TcsParametroTabelaSelecaoList()[idx], isPOCO); }
           }
           if ((typeof ownerReference.TcsParametroValorList === 'function') && ownerReference.TcsParametroValorList().length > 0) {
                for(var idx = 0; idx < ownerReference.TcsParametroValorList().length; idx++) { TcsParametroValorInitializer(ownerReference.TcsParametroValorList()[idx], isPOCO); }
           }
           if ((typeof ownerReference.TcsParametroValorUsuarioList === 'function') && ownerReference.TcsParametroValorUsuarioList().length > 0) {
                for(var idx = 0; idx < ownerReference.TcsParametroValorUsuarioList().length; idx++) { TcsParametroValorUsuarioInitializer(ownerReference.TcsParametroValorUsuarioList()[idx], isPOCO); }
           }
           if ((typeof ownerReference.TcsParametroValorRedeList === 'function') && ownerReference.TcsParametroValorRedeList().length > 0) {
                for(var idx = 0; idx < ownerReference.TcsParametroValorRedeList().length; idx++) { TcsParametroValorRedeInitializer(ownerReference.TcsParametroValorRedeList()[idx], isPOCO); }
           }
           if ((typeof ownerReference.TcsParametroValorGpeconList === 'function') && ownerReference.TcsParametroValorGpeconList().length > 0) {
                for(var idx = 0; idx < ownerReference.TcsParametroValorGpeconList().length; idx++) { TcsParametroValorGpeconInitializer(ownerReference.TcsParametroValorGpeconList()[idx], isPOCO); }
           }
           if ((typeof ownerReference.TcsParametroValorFilialList === 'function') && ownerReference.TcsParametroValorFilialList().length > 0) {
                for(var idx = 0; idx < ownerReference.TcsParametroValorFilialList().length; idx++) { TcsParametroValorFilialInitializer(ownerReference.TcsParametroValorFilialList()[idx], isPOCO); }
           }
           if ((typeof ownerReference.TcsParametroValorLjvLojaList === 'function') && ownerReference.TcsParametroValorLjvLojaList().length > 0) {
                for(var idx = 0; idx < ownerReference.TcsParametroValorLjvLojaList().length; idx++) { TcsParametroValorLjvLojaInitializer(ownerReference.TcsParametroValorLjvLojaList()[idx], isPOCO); }
           }
           if ((typeof ownerReference.TcsParametroValorVariacaoGenericaList === 'function') && ownerReference.TcsParametroValorVariacaoGenericaList().length > 0) {
                for(var idx = 0; idx < ownerReference.TcsParametroValorVariacaoGenericaList().length; idx++) { TcsParametroValorVariacaoGenericaInitializer(ownerReference.TcsParametroValorVariacaoGenericaList()[idx], isPOCO); }
           }
       }
    //#endregion Adjust details already loaded for a POCO reference
    };
    metadataStore.registerEntityTypeCtor("TcsParametro", null, TcsParametroInitializer);
    
    // Configure TcsParametroTabelaSelecao data type
    metadataStore.addEntityType({
    shortName: "TcsParametroTabelaSelecao",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.None,
    dataProperties: {
    EntityUniqueKey: { dataType: DataType.Guid, isNullable: false, isPartOfKey: true, validators: [ ]  }
    ,DescTabela: { dataType: DataType.String, maxLength: 80, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 80})]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdTabelaSelecao: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,LxParametroHierarquia: { dataType: DataType.Byte, isNullable: true, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxParametroHierarquiaName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,NomeTabela: { dataType: DataType.String, maxLength: 250, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 250})]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
                    },
    navigationProperties: {
    // Returns a single parent and associates with Details
    TcsParametro: { entityTypeName: "TcsParametro:#Linx.Framework.BV.Parametro", isScalar: true, foreignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroTabelaSelecao" }
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroTabelaSelecao'] = {DescTabela: 'LookUpTcsTabelaAutorizacaoSelecao', NomeTabela: 'LookUpTcsTabelaAutorizacaoSelecao', UidTabela: 'LookUpTcsTabelaAutorizacaoSelecao'};
    var TcsParametroTabelaSelecaoInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroTabelaSelecao') : ko.observable(getNextSequence('TcsParametroTabelaSelecao')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
           if (succeeded) { succeeded({results: [ownerReference]}); }
           return { then: function(thenMethod) { if (thenMethod) thenMethod(); }, fin: function(finMethod) { if (finMethod) finMethod(); } }; 
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['DescTabela'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdTabelaSelecao'] = 'L';
       ownerReference.serverDataType['LxParametroHierarquia'] = 'Y';
       ownerReference.serverDataType['NomeTabela'] = 'S';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.typeName = 'TcsParametroTabelaSelecao';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [  ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroTabelaSelecao !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroTabelaSelecao() != ownerReference) { cacheElements[idxR].TcsParametroTabelaSelecao(ownerReference); }
           }
       }
       //#region Lookup Extended Methods
       if (isPOCO !== true) {
           ownerReference.getLookupPropertyName = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null && !isNullOrEmpty(property.lookupPropertyName) ? property.lookupPropertyName : propertyName);
           }
           ownerReference.getLookupVisibleColumns = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null ? property.lookupVisibleColumns : '');
           }
           ownerReference.getLookUpClientFilterExpressions = function (lookupName, lookupInfo) {
               return '';
           };
        
           ownerReference.getLookupDisplay = function (lookupName) {
               var displayName = '';
               return 'Seleção' + displayName;
           };
        
           ownerReference.getSpecializedLookup = function (lookupName, lookupInfo, fieldToSearch, valueToSearch, ownerReference, allowMultiSelectionInSearch) {
               var specializedLookup = '';
               return specializedLookup;
           };
        
           ownerReference.getSubQueryFilterFromLookUpTcsTabelaAutorizacaoSelecao = function (propertyName) {
               var filter = '';
               return filter;
           }
           ownerReference.canGetClientFilter = function (lookupName) {
               return true;
           }
           ownerReference.validatedlookupsArray = [];
           ownerReference.internalLookupSearch = function (lookupName, fieldToSearch, operation, querySucceeded, lookupInfo, valueToSearch, beforeGettingLookup, referencefield) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); querySucceeded(null); return lookupInfo; }
               if (isNullOrEmpty(lookupInfo.lastJEntityExpression)) {
                   if (isNullOrEmpty(referencefield)) referencefield = fieldToSearch;
                   if ((typeof valueToSearch) === 'undefined')
                       valueToSearch = getAbsoluteValue(ownerReference[referencefield]);
                   var extraFilters = '';
                   if (ownerReference.canGetClientFilter(lookupName)) {
                       extraFilters = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
                       dataContext.lastClientFilterExpressions[lookupName] = extraFilters;
                       if (extraFilters === 'Error') { querySucceeded(null); return lookupInfo; }
                       if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                           var customFilter = ownerReference['BeforeGet' + lookupName + 'Query'](fieldToSearch, lookupInfo);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                       if (typeof ownerReference['getSubQueryFilterFrom' + lookupName] == 'function') {
                           var customFilter = ownerReference['getSubQueryFilterFrom' + lookupName](referencefield);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                   }
                   var completeExpression = getLookUpJEntityExpression(lookupName, ownerReference, fieldToSearch, valueToSearch, extraFilters, referencefield, app, lookupInfo.vm.useLikeCommandAsDefault);
                   if (completeExpression === 'Error') { querySucceeded(null); return lookupInfo; }
                   lookupInfo.lastJEntityExpression = completeExpression;
               }
               switch (operation) {
                   case 'F':
                       lookupInfo.pageSkip = 0;
                       break;
                   case 'B':
                       lookupInfo.pageSkip = lookupInfo.pageSkip - 1;
                       break;
                   case 'N':
                       lookupInfo.pageSkip = lookupInfo.pageSkip + 1;
                       break;
                   case 'L':
                       lookupInfo.pageSkip = lookupInfo.totalPages();
                       break;
                   default:
               }
        
               var e = { cancel: false, lookupName: lookupName, jEntitySearch: lookupInfo.lastJEntityExpression, entity: ownerReference, viewModel: lookupInfo.vm };
               if (beforeGettingLookup) beforeGettingLookup(e);
               if (e.cancel) { querySucceeded(null); return lookupInfo; }
               if(lookupInfo.lastJEntityExpression !== e.jEntitySearch)
                   lookupInfo.lastJEntityExpression = e.jEntitySearch;
               if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(true);
               var returnQueryResult = function (data) { lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount); querySucceeded(data); };
               eval('dataContext.get' + lookupName + 'ByEntitySearch(lookupInfo.lastJEntityExpression, (isNullOrEmpty(lookupInfo.fieldToSort) ? fieldToSearch : lookupInfo.fieldToSort), lookupInfo.pageSize*lookupInfo.pageSkip, lookupInfo.pageSize, lookupInfo.sortDirection, fieldToSearch).then(returnQueryResult).fail(queryFailed)').fin(function(){ if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(false); });
               return lookupInfo;
           };
        
           ownerReference.hasValidClientFilter = function (lookupName, lookupInfo) {
               var checkClientFilter = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
               if (checkClientFilter === 'Error') { return false; }
               if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                   checkClientFilter = ownerReference['BeforeGet' + lookupName + 'Query']('', lookupInfo);
                   if (checkClientFilter === 'Error') { return false; }
               }
               return true;
           }
        
           ownerReference.executeLookUp = function (lookupName, fieldToSearch, beforeGettingLookup, vm, valueToSearch, finished, comboCallBack, allowMultiSelectionInSearch) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); if (finished) finished(false, null); return; }
               var lookupFieldName = ownerReference.getLookupPropertyName(fieldToSearch);
               vm.dataBind('', true);
               var lookupInfo = new lookupInformation();
               lookupInfo.visibleColumns = ownerReference.getLookupVisibleColumns(fieldToSearch);
               lookupInfo.vm = vm;
               lookupInfo.isMultiSelection = lookupName.in(['LookTcsParametroUsuario', 'LookUpParametroRede', 'LookUpParametroGpecon', 'LookUpParametroFilial', 'LookUpParametroLoja', 'LookUpTcsTabelaAutorizacaoC']);
               var specializedLookup = ownerReference.getSpecializedLookup(lookupName, lookupInfo, lookupFieldName, valueToSearch, ownerReference, allowMultiSelectionInSearch);
               if (isNullOrEmpty(specializedLookup)) {
                       ownerReference.internalLookupSearch(lookupName, lookupFieldName, 'F',
                           function querySucceeded(data) {
                               if (typeof ownerReference['OnLoading' + lookupName + 'Query'] == 'function') {
                                   ownerReference['OnLoading' + lookupName + 'Query'](data);
                               }
                               if ((typeof comboCallBack) === 'function') {
                                   return comboCallBack(data ? data.results : null);
                               }
                               else if (data == null || data.results == null || data.results.length == 0) {
                                   if (finished) finished(false, null);
                                   ownerReference.clearLookUp(lookupName);
                                   if (data != null) app.showMessage('A informação de Lookup [' + ownerReference.getDisplayName(fieldToSearch) + '] não foi encontrada!', 'Informação', ['Ok']);
                                   return;
                               }
                               lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount);
                               showLookUp(dataContext, ownerReference, ownerReference.getLookupDisplay(lookupName), lookupName, lookupFieldName, ownerReference.internalLookupSearch, lookupInfo, 
                                   function (confirm, values) {
                                       var results = '';
                                       if (values != null && values.length > 1) {
                                           $.each(values, function (index, item) { results += (index == 0 ? '' : ',') + item[lookupFieldName].toString().trim() });
                                           results = '[' + results + ']';
                                           ownerReference[fieldToSearch](results);
                                           if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                               vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                           else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                           document.dispatchEvent(dataUpdateEvent);
                                       }
                                       if (finished) finished(confirm, results);
                                   }, data.results, allowMultiSelectionInSearch);
                       }, lookupInfo, valueToSearch, beforeGettingLookup, fieldToSearch);
               }
               else {
                       var currentModal = (modal.inUse ? modal2 : modal);
                       if (currentModal.inUse === false) {
                           //Check Client Validations
                           if (!ownerReference.hasValidClientFilter(lookupName, lookupInfo)) { if (finished) finished(false); return; }
                           //Show External Lookup
                           currentModal.showModal(specializedLookup.moduleName, specializedLookup.uiSettings, ownerReference.getLookupDisplay(lookupName), ['Ok', 'Cancelar'], 'large').then(function (r, data) {
                           if (r == 'Ok') {
                               if (!currentModal.internalUIs || currentModal.internalUIs.length != 1) { if (finished) finished(false); return; }
                               var lookupVM = currentModal[currentModal.internalUIs[0]]; 
                               if (!lookupVM) return; 
                               if (typeof lookupVM == 'function') lookupVM = lookupVM(); 
                               currentModal[currentModal.internalUIs[0]] = null; 
                               var selectedItems = lookupVM.getSpecializedLookupItems(); 
                               if (vm.status() == 'C' && selectedItems != null && selectedItems.length > 1) { 
                                   var results = '';
                                   $.each(selectedItems, function (index, item) { results += (index == 0 ? '' : ',') + (typeof item[lookupFieldName] == 'function' ? item[lookupFieldName]() : item[lookupFieldName]).toString().trim() });
                                   results = '[' + results + ']'
                                   ownerReference[fieldToSearch](results);
                                   if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                       vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                   else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                   document.dispatchEvent(dataUpdateEvent);
                                   if (finished) finished(true, results);
                               }
                               else if (selectedItems.length > 0) { dataContext['finalizeAll' + lookupName](ownerReference, selectedItems, '', lookupInfo); }
                           }
                           if (finished) finished(r === 'Ok');
                       });
                   }
                   if (finished) finished(false);
               }
           };
           ownerReference.clearLookUp = function (lookupName) {
               return eval('dataContext.clear' + lookupName + '(ownerReference)');
           };
       }
       //#endregion Lookup Extended Methods
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           var parent = getAbsoluteValue(ownerReference.TcsParametro);
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
           if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroTabelaSelecaoList === 'function') && parent.TcsParametroTabelaSelecaoList().length == 0) parent.setCurrentDetails('TcsParametroTabelaSelecao');
       };
       ownerReference.setParentAsModified = function() {
       var parent = getAbsoluteValue(ownerReference.TcsParametro);
       if (parent) {
           if (parent.isUnchanged()) {
               parent.setModified(); 
           }
           parent.setParentAsModified();
       }
       };
       ownerReference.getParent = function() {
           return getAbsoluteValue(ownerReference.TcsParametro);
       };
       ownerReference.getSelfList = function() {
           var parent = ownerReference.getParent();
           if (!isNullOrEmpty(parent)) {
               return getAbsoluteValue(parent.TcsParametroTabelaSelecaoList);
           } else { return null; }
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ ,'DescTabela','IdParametro','IdTabelaSelecao','LxParametroHierarquia','NomeTabela','UidTabela' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxParametroHierarquiaName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxParametroHierarquiaName(''); return; }
                   var value = (dataDomains.getId('ParametroHierarquia', newValue));
                   if (value != ownerReference.LxParametroHierarquia()) {
                       ownerReference.LxParametroHierarquia(value);
                   }
            });
    
           ownerReference.LxParametroHierarquia.subscribe(
           function (newValue) {
                   var value = (newValue == null) ? '' : dataDomains.getName('ParametroHierarquia', newValue);
                   if (value != ownerReference.LxParametroHierarquiaName()) {
                       ownerReference.LxParametroHierarquiaName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsParametroTabelaSelecao", null, TcsParametroTabelaSelecaoInitializer);
    
    // Configure TcsParametroValor data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValor",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.None,
    dataProperties: {
    EntityUniqueKey: { dataType: DataType.Guid, isNullable: false, isPartOfKey: true, validators: [ ]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,ValorParametroBool: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametroData: { dataType: DataType.DateTime, isNullable: false, isPartOfKey: false, defaultValue: '', validators: [ Validator.hasValueValidator]  }
                    },
    navigationProperties: {
    // Returns a single parent and associates with Details
    TcsParametro: { entityTypeName: "TcsParametro:#Linx.Framework.BV.Parametro", isScalar: true, foreignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroValor" }
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValor'] = {};
    var TcsParametroValorInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValor') : ko.observable(getNextSequence('TcsParametroValor')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
           if (succeeded) { succeeded({results: [ownerReference]}); }
           return { then: function(thenMethod) { if (thenMethod) thenMethod(); }, fin: function(finMethod) { if (finMethod) finMethod(); } }; 
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['ValorParametroBool'] = 'B';
       ownerReference.serverDataType['ValorParametroData'] = 'T';
       ownerReference.typeName = 'TcsParametroValor';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [  ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValor !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValor() != ownerReference) { cacheElements[idxR].TcsParametroValor(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
           setAbsoluteValue(ownerReference, 'PossuiVariacao', false);
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           var parent = getAbsoluteValue(ownerReference.TcsParametro);
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
           if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroValorList === 'function') && parent.TcsParametroValorList().length == 0) parent.setCurrentDetails('TcsParametroValor');
       };
       ownerReference.setParentAsModified = function() {
       var parent = getAbsoluteValue(ownerReference.TcsParametro);
       if (parent) {
           if (parent.isUnchanged()) {
               parent.setModified(); 
           }
           parent.setParentAsModified();
       }
       };
       ownerReference.getParent = function() {
           return getAbsoluteValue(ownerReference.TcsParametro);
       };
       ownerReference.getSelfList = function() {
           var parent = ownerReference.getParent();
           if (!isNullOrEmpty(parent)) {
               return getAbsoluteValue(parent.TcsParametroValorList);
           } else { return null; }
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ ,'IdParametro','IdParametroValor','PossuiVariacao','ValorParametro','ValorParametroBool','ValorParametroData' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Client Events
       ownerReference.OnPropertyChanged = function (propertyName, oldValue, newValue) {
    if (propertyName == 'ValorParametro' && getAbsoluteValue(vm.currentDataItem().LxDatatypeParametro) == 1){
			if (isNaN(parseInt(newValue))){
				app.showMessage('Valor inválido.', 'Atenção', ['Ok']).then(function (){
					var value = (isNaN(parseInt(oldValue)) ? 0 : oldValue);
					setAbsoluteValue(ownerReference, 'ValorParametro', value);
					notifyPresentation('')
			});
	}
}

       }
    //#endregion Client Events
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValor", null, TcsParametroValorInitializer);
    
    // Configure TcsParametroValorUsuario data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorUsuario",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,IdUsuario: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,LxDatatypeParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,LxDatatypeParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,NomeUsuario: { dataType: DataType.String, maxLength: 250, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 250})]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidUsuarioString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,ValorParametroBool: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns a single parent and associates with Details
    TcsParametro: { entityTypeName: "TcsParametro:#Linx.Framework.BV.Parametro", isScalar: true, foreignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroValorUsuario" }
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorUsuario'] = {ChaveSelecao: 'LookTcsParametroUsuario', IdUsuario: 'LookTcsParametroUsuario', NomeUsuario: 'LookTcsParametroUsuario', UidUsuarioString: 'LookTcsParametroUsuario'};
    var TcsParametroValorUsuarioInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorUsuario') : ko.observable(getNextSequence('TcsParametroValorUsuario')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorUsuario{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorUsuarioByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['IdUsuario'] = 'L';
       ownerReference.serverDataType['LxDatatypeParametro'] = 'Y';
       ownerReference.serverDataType['NomeUsuario'] = 'S';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['UidUsuarioString'] = 'S';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['ValorParametroBool'] = 'B';
       ownerReference.typeName = 'TcsParametroValorUsuario';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorUsuario !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorUsuario() != ownerReference) { cacheElements[idxR].TcsParametroValorUsuario(ownerReference); }
           }
       }
       //#region Lookup Extended Methods
       if (isPOCO !== true) {
           ownerReference.getLookupPropertyName = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null && !isNullOrEmpty(property.lookupPropertyName) ? property.lookupPropertyName : propertyName);
           }
           ownerReference.getLookupVisibleColumns = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null ? property.lookupVisibleColumns : '');
           }
           ownerReference.getLookUpClientFilterExpressions = function (lookupName, lookupInfo) {
               return '';
           };
        
           ownerReference.getLookupDisplay = function (lookupName) {
               var displayName = '';
               return 'Seleção' + displayName;
           };
        
           ownerReference.getSpecializedLookup = function (lookupName, lookupInfo, fieldToSearch, valueToSearch, ownerReference, allowMultiSelectionInSearch) {
               var specializedLookup = '';
               return specializedLookup;
           };
        
           ownerReference.getSubQueryFilterFromLookTcsParametroUsuario = function (propertyName) {
               var filter = '';
               return filter;
           }
           ownerReference.canGetClientFilter = function (lookupName) {
               return true;
           }
           ownerReference.validatedlookupsArray = [];
           ownerReference.internalLookupSearch = function (lookupName, fieldToSearch, operation, querySucceeded, lookupInfo, valueToSearch, beforeGettingLookup, referencefield) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); querySucceeded(null); return lookupInfo; }
               if (isNullOrEmpty(lookupInfo.lastJEntityExpression)) {
                   if (isNullOrEmpty(referencefield)) referencefield = fieldToSearch;
                   if ((typeof valueToSearch) === 'undefined')
                       valueToSearch = getAbsoluteValue(ownerReference[referencefield]);
                   var extraFilters = '';
                   if (ownerReference.canGetClientFilter(lookupName)) {
                       extraFilters = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
                       dataContext.lastClientFilterExpressions[lookupName] = extraFilters;
                       if (extraFilters === 'Error') { querySucceeded(null); return lookupInfo; }
                       if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                           var customFilter = ownerReference['BeforeGet' + lookupName + 'Query'](fieldToSearch, lookupInfo);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                       if (typeof ownerReference['getSubQueryFilterFrom' + lookupName] == 'function') {
                           var customFilter = ownerReference['getSubQueryFilterFrom' + lookupName](referencefield);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                   }
                   var completeExpression = getLookUpJEntityExpression(lookupName, ownerReference, fieldToSearch, valueToSearch, extraFilters, referencefield, app, lookupInfo.vm.useLikeCommandAsDefault);
                   if (completeExpression === 'Error') { querySucceeded(null); return lookupInfo; }
                   lookupInfo.lastJEntityExpression = completeExpression;
               }
               switch (operation) {
                   case 'F':
                       lookupInfo.pageSkip = 0;
                       break;
                   case 'B':
                       lookupInfo.pageSkip = lookupInfo.pageSkip - 1;
                       break;
                   case 'N':
                       lookupInfo.pageSkip = lookupInfo.pageSkip + 1;
                       break;
                   case 'L':
                       lookupInfo.pageSkip = lookupInfo.totalPages();
                       break;
                   default:
               }
        
               var e = { cancel: false, lookupName: lookupName, jEntitySearch: lookupInfo.lastJEntityExpression, entity: ownerReference, viewModel: lookupInfo.vm };
               if (beforeGettingLookup) beforeGettingLookup(e);
               if (e.cancel) { querySucceeded(null); return lookupInfo; }
               if(lookupInfo.lastJEntityExpression !== e.jEntitySearch)
                   lookupInfo.lastJEntityExpression = e.jEntitySearch;
               if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(true);
               var returnQueryResult = function (data) { lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount); querySucceeded(data); };
               eval('dataContext.get' + lookupName + 'ByEntitySearch(lookupInfo.lastJEntityExpression, (isNullOrEmpty(lookupInfo.fieldToSort) ? fieldToSearch : lookupInfo.fieldToSort), lookupInfo.pageSize*lookupInfo.pageSkip, lookupInfo.pageSize, lookupInfo.sortDirection, fieldToSearch).then(returnQueryResult).fail(queryFailed)').fin(function(){ if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(false); });
               return lookupInfo;
           };
        
           ownerReference.hasValidClientFilter = function (lookupName, lookupInfo) {
               var checkClientFilter = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
               if (checkClientFilter === 'Error') { return false; }
               if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                   checkClientFilter = ownerReference['BeforeGet' + lookupName + 'Query']('', lookupInfo);
                   if (checkClientFilter === 'Error') { return false; }
               }
               return true;
           }
        
           ownerReference.executeLookUp = function (lookupName, fieldToSearch, beforeGettingLookup, vm, valueToSearch, finished, comboCallBack, allowMultiSelectionInSearch) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); if (finished) finished(false, null); return; }
               var lookupFieldName = ownerReference.getLookupPropertyName(fieldToSearch);
               vm.dataBind('', true);
               var lookupInfo = new lookupInformation();
               lookupInfo.visibleColumns = ownerReference.getLookupVisibleColumns(fieldToSearch);
               lookupInfo.vm = vm;
               lookupInfo.isMultiSelection = lookupName.in(['LookTcsParametroUsuario', 'LookUpParametroRede', 'LookUpParametroGpecon', 'LookUpParametroFilial', 'LookUpParametroLoja', 'LookUpTcsTabelaAutorizacaoC']);
               var specializedLookup = ownerReference.getSpecializedLookup(lookupName, lookupInfo, lookupFieldName, valueToSearch, ownerReference, allowMultiSelectionInSearch);
               if (isNullOrEmpty(specializedLookup)) {
                       ownerReference.internalLookupSearch(lookupName, lookupFieldName, 'F',
                           function querySucceeded(data) {
                               if (typeof ownerReference['OnLoading' + lookupName + 'Query'] == 'function') {
                                   ownerReference['OnLoading' + lookupName + 'Query'](data);
                               }
                               if ((typeof comboCallBack) === 'function') {
                                   return comboCallBack(data ? data.results : null);
                               }
                               else if (data == null || data.results == null || data.results.length == 0) {
                                   if (finished) finished(false, null);
                                   ownerReference.clearLookUp(lookupName);
                                   if (data != null) app.showMessage('A informação de Lookup [' + ownerReference.getDisplayName(fieldToSearch) + '] não foi encontrada!', 'Informação', ['Ok']);
                                   return;
                               }
                               lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount);
                               showLookUp(dataContext, ownerReference, ownerReference.getLookupDisplay(lookupName), lookupName, lookupFieldName, ownerReference.internalLookupSearch, lookupInfo, 
                                   function (confirm, values) {
                                       var results = '';
                                       if (values != null && values.length > 1) {
                                           $.each(values, function (index, item) { results += (index == 0 ? '' : ',') + item[lookupFieldName].toString().trim() });
                                           results = '[' + results + ']';
                                           ownerReference[fieldToSearch](results);
                                           if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                               vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                           else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                           document.dispatchEvent(dataUpdateEvent);
                                       }
                                       if (finished) finished(confirm, results);
                                   }, data.results, allowMultiSelectionInSearch);
                       }, lookupInfo, valueToSearch, beforeGettingLookup, fieldToSearch);
               }
               else {
                       var currentModal = (modal.inUse ? modal2 : modal);
                       if (currentModal.inUse === false) {
                           //Check Client Validations
                           if (!ownerReference.hasValidClientFilter(lookupName, lookupInfo)) { if (finished) finished(false); return; }
                           //Show External Lookup
                           currentModal.showModal(specializedLookup.moduleName, specializedLookup.uiSettings, ownerReference.getLookupDisplay(lookupName), ['Ok', 'Cancelar'], 'large').then(function (r, data) {
                           if (r == 'Ok') {
                               if (!currentModal.internalUIs || currentModal.internalUIs.length != 1) { if (finished) finished(false); return; }
                               var lookupVM = currentModal[currentModal.internalUIs[0]]; 
                               if (!lookupVM) return; 
                               if (typeof lookupVM == 'function') lookupVM = lookupVM(); 
                               currentModal[currentModal.internalUIs[0]] = null; 
                               var selectedItems = lookupVM.getSpecializedLookupItems(); 
                               if (vm.status() == 'C' && selectedItems != null && selectedItems.length > 1) { 
                                   var results = '';
                                   $.each(selectedItems, function (index, item) { results += (index == 0 ? '' : ',') + (typeof item[lookupFieldName] == 'function' ? item[lookupFieldName]() : item[lookupFieldName]).toString().trim() });
                                   results = '[' + results + ']'
                                   ownerReference[fieldToSearch](results);
                                   if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                       vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                   else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                   document.dispatchEvent(dataUpdateEvent);
                                   if (finished) finished(true, results);
                               }
                               else if (selectedItems.length > 0) { dataContext['finalizeAll' + lookupName](ownerReference, selectedItems, '', lookupInfo); }
                           }
                           if (finished) finished(r === 'Ok');
                       });
                   }
                   if (finished) finished(false);
               }
           };
           ownerReference.clearLookUp = function (lookupName) {
               return eval('dataContext.clear' + lookupName + '(ownerReference)');
           };
       }
       //#endregion Lookup Extended Methods
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
           setAbsoluteValue(ownerReference, 'PossuiVariacao', true);
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           var parent = getAbsoluteValue(ownerReference.TcsParametro);
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
           if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroValorUsuarioList === 'function') && parent.TcsParametroValorUsuarioList().length == 0) parent.setCurrentDetails('TcsParametroValorUsuario');
       };
       ownerReference.setParentAsModified = function() {
       var parent = getAbsoluteValue(ownerReference.TcsParametro);
       if (parent) {
           if (parent.isUnchanged()) {
               parent.setModified(); 
           }
           parent.setParentAsModified();
       }
       };
       ownerReference.getParent = function() {
           return getAbsoluteValue(ownerReference.TcsParametro);
       };
       ownerReference.getSelfList = function() {
           var parent = ownerReference.getParent();
           if (!isNullOrEmpty(parent)) {
               return getAbsoluteValue(parent.TcsParametroValorUsuarioList);
           } else { return null; }
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','IdParametro','IdParametroValor','IdUsuario','LxDatatypeParametro','NomeUsuario','PossuiVariacao','UidTabela','UidUsuarioString','ValorParametro','ValorParametroBool' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Client Events
       ownerReference.OnAdded = function () {
    var entity = ownerReference;
setAbsoluteValue(entity, 'UidTabela', '18E96E62-AB34-41E9-99FA-B6F755CAD3D2')
setAbsoluteValue(entity, 'PossuiVariacao', true)

if (getAbsoluteValue(vm.currentDataItem().LxDatatypeParametro)){
	setAbsoluteValue(entity, 'ValorParametroBool', false)
	setAbsoluteValue(entity, 'ValorParametro', false)
}
vm.notifyPresentation("TcsParametroValorUsuarioList");
       }
       ownerReference.OnPropertyChanged = function (propertyName, oldValue, newValue) {
    if (propertyName == 'ValorParametro' && getAbsoluteValue(vm.currentDataItem().LxDatatypeParametro) == 1){
			if (isNaN(parseInt(newValue))){
				app.showMessage('Valor inválido.', 'Atenção', ['Ok']).then(function (){
					var value = (isNaN(parseInt(oldValue)) ? 0 : oldValue);
					setAbsoluteValue(ownerReference, 'ValorParametro', value);
					notifyPresentation('')
			});
	}
}

if (propertyName == 'ValorParametroBool'){
	setAbsoluteValue(ownerReference, 'ValorParametro', newValue);
}
       }
    //#endregion Client Events
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxDatatypeParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValorParametro', newValue));
                   if (value != ownerReference.LxDatatypeParametro()) {
                       ownerReference.LxDatatypeParametro(value);
                   }
            });
    
           ownerReference.LxDatatypeParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametro(0); return; }
                   var value = dataDomains.getName('TipoValorParametro', newValue);
                   if (value != ownerReference.LxDatatypeParametroName()) {
                       ownerReference.LxDatatypeParametroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorUsuario", null, TcsParametroValorUsuarioInitializer);
    
    // Configure TcsParametroValorRede data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorRede",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,CodBandeiraRede: { dataType: DataType.String, maxLength: 25, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 25})]  }
    ,DescBandeiraRede: { dataType: DataType.String, maxLength: 60, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 60})]  }
    ,IdBandeiraRedeParam: { dataType: DataType.Int32, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdBandeiraRedeString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,LxDatatypeParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,LxDatatypeParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,ValorParametroBool: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns a single parent and associates with Details
    TcsParametro: { entityTypeName: "TcsParametro:#Linx.Framework.BV.Parametro", isScalar: true, foreignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroValorRede" }
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorRede'] = {ChaveSelecao: 'LookUpParametroRede', CodBandeiraRede: 'LookUpParametroRede', DescBandeiraRede: 'LookUpParametroRede', IdBandeiraRedeParam: 'LookUpParametroRede', IdBandeiraRedeString: 'LookUpParametroRede'};
    var TcsParametroValorRedeInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorRede') : ko.observable(getNextSequence('TcsParametroValorRede')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorRede{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorRedeByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['CodBandeiraRede'] = 'S';
       ownerReference.serverDataType['DescBandeiraRede'] = 'S';
       ownerReference.serverDataType['IdBandeiraRedeParam'] = 'I';
       ownerReference.serverDataType['IdBandeiraRedeString'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['LxDatatypeParametro'] = 'Y';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['ValorParametroBool'] = 'B';
       ownerReference.typeName = 'TcsParametroValorRede';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorRede !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorRede() != ownerReference) { cacheElements[idxR].TcsParametroValorRede(ownerReference); }
           }
       }
       //#region Lookup Extended Methods
       if (isPOCO !== true) {
           ownerReference.getLookupPropertyName = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null && !isNullOrEmpty(property.lookupPropertyName) ? property.lookupPropertyName : propertyName);
           }
           ownerReference.getLookupVisibleColumns = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null ? property.lookupVisibleColumns : '');
           }
           ownerReference.getLookUpClientFilterExpressions = function (lookupName, lookupInfo) {
               return '';
           };
        
           ownerReference.getLookupDisplay = function (lookupName) {
               var displayName = '';
               return 'Seleção' + displayName;
           };
        
           ownerReference.getSpecializedLookup = function (lookupName, lookupInfo, fieldToSearch, valueToSearch, ownerReference, allowMultiSelectionInSearch) {
               var specializedLookup = '';
               return specializedLookup;
           };
        
           ownerReference.getSubQueryFilterFromLookUpParametroRede = function (propertyName) {
               var filter = '';
               return filter;
           }
           ownerReference.canGetClientFilter = function (lookupName) {
               return true;
           }
           ownerReference.validatedlookupsArray = [];
           ownerReference.internalLookupSearch = function (lookupName, fieldToSearch, operation, querySucceeded, lookupInfo, valueToSearch, beforeGettingLookup, referencefield) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); querySucceeded(null); return lookupInfo; }
               if (isNullOrEmpty(lookupInfo.lastJEntityExpression)) {
                   if (isNullOrEmpty(referencefield)) referencefield = fieldToSearch;
                   if ((typeof valueToSearch) === 'undefined')
                       valueToSearch = getAbsoluteValue(ownerReference[referencefield]);
                   var extraFilters = '';
                   if (ownerReference.canGetClientFilter(lookupName)) {
                       extraFilters = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
                       dataContext.lastClientFilterExpressions[lookupName] = extraFilters;
                       if (extraFilters === 'Error') { querySucceeded(null); return lookupInfo; }
                       if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                           var customFilter = ownerReference['BeforeGet' + lookupName + 'Query'](fieldToSearch, lookupInfo);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                       if (typeof ownerReference['getSubQueryFilterFrom' + lookupName] == 'function') {
                           var customFilter = ownerReference['getSubQueryFilterFrom' + lookupName](referencefield);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                   }
                   var completeExpression = getLookUpJEntityExpression(lookupName, ownerReference, fieldToSearch, valueToSearch, extraFilters, referencefield, app, lookupInfo.vm.useLikeCommandAsDefault);
                   if (completeExpression === 'Error') { querySucceeded(null); return lookupInfo; }
                   lookupInfo.lastJEntityExpression = completeExpression;
               }
               switch (operation) {
                   case 'F':
                       lookupInfo.pageSkip = 0;
                       break;
                   case 'B':
                       lookupInfo.pageSkip = lookupInfo.pageSkip - 1;
                       break;
                   case 'N':
                       lookupInfo.pageSkip = lookupInfo.pageSkip + 1;
                       break;
                   case 'L':
                       lookupInfo.pageSkip = lookupInfo.totalPages();
                       break;
                   default:
               }
        
               var e = { cancel: false, lookupName: lookupName, jEntitySearch: lookupInfo.lastJEntityExpression, entity: ownerReference, viewModel: lookupInfo.vm };
               if (beforeGettingLookup) beforeGettingLookup(e);
               if (e.cancel) { querySucceeded(null); return lookupInfo; }
               if(lookupInfo.lastJEntityExpression !== e.jEntitySearch)
                   lookupInfo.lastJEntityExpression = e.jEntitySearch;
               if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(true);
               var returnQueryResult = function (data) { lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount); querySucceeded(data); };
               eval('dataContext.get' + lookupName + 'ByEntitySearch(lookupInfo.lastJEntityExpression, (isNullOrEmpty(lookupInfo.fieldToSort) ? fieldToSearch : lookupInfo.fieldToSort), lookupInfo.pageSize*lookupInfo.pageSkip, lookupInfo.pageSize, lookupInfo.sortDirection, fieldToSearch).then(returnQueryResult).fail(queryFailed)').fin(function(){ if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(false); });
               return lookupInfo;
           };
        
           ownerReference.hasValidClientFilter = function (lookupName, lookupInfo) {
               var checkClientFilter = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
               if (checkClientFilter === 'Error') { return false; }
               if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                   checkClientFilter = ownerReference['BeforeGet' + lookupName + 'Query']('', lookupInfo);
                   if (checkClientFilter === 'Error') { return false; }
               }
               return true;
           }
        
           ownerReference.executeLookUp = function (lookupName, fieldToSearch, beforeGettingLookup, vm, valueToSearch, finished, comboCallBack, allowMultiSelectionInSearch) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); if (finished) finished(false, null); return; }
               var lookupFieldName = ownerReference.getLookupPropertyName(fieldToSearch);
               vm.dataBind('', true);
               var lookupInfo = new lookupInformation();
               lookupInfo.visibleColumns = ownerReference.getLookupVisibleColumns(fieldToSearch);
               lookupInfo.vm = vm;
               lookupInfo.isMultiSelection = lookupName.in(['LookTcsParametroUsuario', 'LookUpParametroRede', 'LookUpParametroGpecon', 'LookUpParametroFilial', 'LookUpParametroLoja', 'LookUpTcsTabelaAutorizacaoC']);
               var specializedLookup = ownerReference.getSpecializedLookup(lookupName, lookupInfo, lookupFieldName, valueToSearch, ownerReference, allowMultiSelectionInSearch);
               if (isNullOrEmpty(specializedLookup)) {
                       ownerReference.internalLookupSearch(lookupName, lookupFieldName, 'F',
                           function querySucceeded(data) {
                               if (typeof ownerReference['OnLoading' + lookupName + 'Query'] == 'function') {
                                   ownerReference['OnLoading' + lookupName + 'Query'](data);
                               }
                               if ((typeof comboCallBack) === 'function') {
                                   return comboCallBack(data ? data.results : null);
                               }
                               else if (data == null || data.results == null || data.results.length == 0) {
                                   if (finished) finished(false, null);
                                   ownerReference.clearLookUp(lookupName);
                                   if (data != null) app.showMessage('A informação de Lookup [' + ownerReference.getDisplayName(fieldToSearch) + '] não foi encontrada!', 'Informação', ['Ok']);
                                   return;
                               }
                               lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount);
                               showLookUp(dataContext, ownerReference, ownerReference.getLookupDisplay(lookupName), lookupName, lookupFieldName, ownerReference.internalLookupSearch, lookupInfo, 
                                   function (confirm, values) {
                                       var results = '';
                                       if (values != null && values.length > 1) {
                                           $.each(values, function (index, item) { results += (index == 0 ? '' : ',') + item[lookupFieldName].toString().trim() });
                                           results = '[' + results + ']';
                                           ownerReference[fieldToSearch](results);
                                           if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                               vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                           else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                           document.dispatchEvent(dataUpdateEvent);
                                       }
                                       if (finished) finished(confirm, results);
                                   }, data.results, allowMultiSelectionInSearch);
                       }, lookupInfo, valueToSearch, beforeGettingLookup, fieldToSearch);
               }
               else {
                       var currentModal = (modal.inUse ? modal2 : modal);
                       if (currentModal.inUse === false) {
                           //Check Client Validations
                           if (!ownerReference.hasValidClientFilter(lookupName, lookupInfo)) { if (finished) finished(false); return; }
                           //Show External Lookup
                           currentModal.showModal(specializedLookup.moduleName, specializedLookup.uiSettings, ownerReference.getLookupDisplay(lookupName), ['Ok', 'Cancelar'], 'large').then(function (r, data) {
                           if (r == 'Ok') {
                               if (!currentModal.internalUIs || currentModal.internalUIs.length != 1) { if (finished) finished(false); return; }
                               var lookupVM = currentModal[currentModal.internalUIs[0]]; 
                               if (!lookupVM) return; 
                               if (typeof lookupVM == 'function') lookupVM = lookupVM(); 
                               currentModal[currentModal.internalUIs[0]] = null; 
                               var selectedItems = lookupVM.getSpecializedLookupItems(); 
                               if (vm.status() == 'C' && selectedItems != null && selectedItems.length > 1) { 
                                   var results = '';
                                   $.each(selectedItems, function (index, item) { results += (index == 0 ? '' : ',') + (typeof item[lookupFieldName] == 'function' ? item[lookupFieldName]() : item[lookupFieldName]).toString().trim() });
                                   results = '[' + results + ']'
                                   ownerReference[fieldToSearch](results);
                                   if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                       vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                   else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                   document.dispatchEvent(dataUpdateEvent);
                                   if (finished) finished(true, results);
                               }
                               else if (selectedItems.length > 0) { dataContext['finalizeAll' + lookupName](ownerReference, selectedItems, '', lookupInfo); }
                           }
                           if (finished) finished(r === 'Ok');
                       });
                   }
                   if (finished) finished(false);
               }
           };
           ownerReference.clearLookUp = function (lookupName) {
               return eval('dataContext.clear' + lookupName + '(ownerReference)');
           };
       }
       //#endregion Lookup Extended Methods
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           var parent = getAbsoluteValue(ownerReference.TcsParametro);
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
           if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroValorRedeList === 'function') && parent.TcsParametroValorRedeList().length == 0) parent.setCurrentDetails('TcsParametroValorRede');
       };
       ownerReference.setParentAsModified = function() {
       var parent = getAbsoluteValue(ownerReference.TcsParametro);
       if (parent) {
           if (parent.isUnchanged()) {
               parent.setModified(); 
           }
           parent.setParentAsModified();
       }
       };
       ownerReference.getParent = function() {
           return getAbsoluteValue(ownerReference.TcsParametro);
       };
       ownerReference.getSelfList = function() {
           var parent = ownerReference.getParent();
           if (!isNullOrEmpty(parent)) {
               return getAbsoluteValue(parent.TcsParametroValorRedeList);
           } else { return null; }
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','CodBandeiraRede','DescBandeiraRede','IdBandeiraRedeParam','IdBandeiraRedeString','IdParametro','IdParametroValor','LxDatatypeParametro','PossuiVariacao','UidTabela','ValorParametro','ValorParametroBool' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Client Events
       ownerReference.OnAdded = function () {
    var entity = ownerReference;
setAbsoluteValue(entity, 'UidTabela', 'B24D57B5-BE89-4335-8017-9E581F44E06D')
setAbsoluteValue(entity, 'PossuiVariacao', true)

if (getAbsoluteValue(vm.currentDataItem().LxDatatypeParametro)){
	setAbsoluteValue(entity, 'ValorParametroBool', false)
	setAbsoluteValue(entity, 'ValorParametro', false)
}
vm.notifyPresentation("TcsParametroValorRedeList");
       }
       ownerReference.OnPropertyChanged = function (propertyName, oldValue, newValue) {
    if (propertyName == 'ValorParametro' && getAbsoluteValue(vm.currentDataItem().LxDatatypeParametro) == 1){
			if (isNaN(parseInt(newValue))){
				app.showMessage('Valor inválido.', 'Atenção', ['Ok']).then(function (){
					var value = (isNaN(parseInt(oldValue)) ? 0 : oldValue);
					setAbsoluteValue(ownerReference, 'ValorParametro', value);
					notifyPresentation('')
			});
	}
}

if (propertyName == 'ValorParametroBool'){
	setAbsoluteValue(ownerReference, 'ValorParametro', newValue);
}
       }
    //#endregion Client Events
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxDatatypeParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValorParametro', newValue));
                   if (value != ownerReference.LxDatatypeParametro()) {
                       ownerReference.LxDatatypeParametro(value);
                   }
            });
    
           ownerReference.LxDatatypeParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametro(0); return; }
                   var value = dataDomains.getName('TipoValorParametro', newValue);
                   if (value != ownerReference.LxDatatypeParametroName()) {
                       ownerReference.LxDatatypeParametroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorRede", null, TcsParametroValorRedeInitializer);
    
    // Configure TcsParametroValorGpecon data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorGpecon",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,DescGrupoEconomico: { dataType: DataType.String, maxLength: 60, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 60})]  }
    ,IdGpecon: { dataType: DataType.Int32, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdGpeconString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,LxDatatypeParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,LxDatatypeParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,ValorParametroBool: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns a single parent and associates with Details
    TcsParametro: { entityTypeName: "TcsParametro:#Linx.Framework.BV.Parametro", isScalar: true, foreignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroValorGpecon" }
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorGpecon'] = {ChaveSelecao: 'LookUpParametroGpecon', DescGrupoEconomico: 'LookUpParametroGpecon', IdGpecon: 'LookUpParametroGpecon', IdGpeconString: 'LookUpParametroGpecon'};
    var TcsParametroValorGpeconInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorGpecon') : ko.observable(getNextSequence('TcsParametroValorGpecon')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorGpecon{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorGpeconByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['DescGrupoEconomico'] = 'S';
       ownerReference.serverDataType['IdGpecon'] = 'I';
       ownerReference.serverDataType['IdGpeconString'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['LxDatatypeParametro'] = 'Y';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['ValorParametroBool'] = 'B';
       ownerReference.typeName = 'TcsParametroValorGpecon';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
           if (idGpecon > 0) setAbsoluteValue(ownerReference, 'IdGpecon', idGpecon);
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorGpecon !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorGpecon() != ownerReference) { cacheElements[idxR].TcsParametroValorGpecon(ownerReference); }
           }
       }
       //#region Lookup Extended Methods
       if (isPOCO !== true) {
           ownerReference.getLookupPropertyName = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null && !isNullOrEmpty(property.lookupPropertyName) ? property.lookupPropertyName : propertyName);
           }
           ownerReference.getLookupVisibleColumns = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null ? property.lookupVisibleColumns : '');
           }
           ownerReference.getLookUpClientFilterExpressions = function (lookupName, lookupInfo) {
               return '';
           };
        
           ownerReference.getLookupDisplay = function (lookupName) {
               var displayName = '';
               return 'Seleção' + displayName;
           };
        
           ownerReference.getSpecializedLookup = function (lookupName, lookupInfo, fieldToSearch, valueToSearch, ownerReference, allowMultiSelectionInSearch) {
               var specializedLookup = '';
               return specializedLookup;
           };
        
           ownerReference.getSubQueryFilterFromLookUpParametroGpecon = function (propertyName) {
               var filter = '';
               return filter;
           }
           ownerReference.canGetClientFilter = function (lookupName) {
               return true;
           }
           ownerReference.validatedlookupsArray = [];
           ownerReference.internalLookupSearch = function (lookupName, fieldToSearch, operation, querySucceeded, lookupInfo, valueToSearch, beforeGettingLookup, referencefield) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); querySucceeded(null); return lookupInfo; }
               if (isNullOrEmpty(lookupInfo.lastJEntityExpression)) {
                   if (isNullOrEmpty(referencefield)) referencefield = fieldToSearch;
                   if ((typeof valueToSearch) === 'undefined')
                       valueToSearch = getAbsoluteValue(ownerReference[referencefield]);
                   var extraFilters = '';
                   if (ownerReference.canGetClientFilter(lookupName)) {
                       extraFilters = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
                       dataContext.lastClientFilterExpressions[lookupName] = extraFilters;
                       if (extraFilters === 'Error') { querySucceeded(null); return lookupInfo; }
                       if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                           var customFilter = ownerReference['BeforeGet' + lookupName + 'Query'](fieldToSearch, lookupInfo);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                       if (typeof ownerReference['getSubQueryFilterFrom' + lookupName] == 'function') {
                           var customFilter = ownerReference['getSubQueryFilterFrom' + lookupName](referencefield);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                   }
                   var completeExpression = getLookUpJEntityExpression(lookupName, ownerReference, fieldToSearch, valueToSearch, extraFilters, referencefield, app, lookupInfo.vm.useLikeCommandAsDefault);
                   if (completeExpression === 'Error') { querySucceeded(null); return lookupInfo; }
                   lookupInfo.lastJEntityExpression = completeExpression;
               }
               switch (operation) {
                   case 'F':
                       lookupInfo.pageSkip = 0;
                       break;
                   case 'B':
                       lookupInfo.pageSkip = lookupInfo.pageSkip - 1;
                       break;
                   case 'N':
                       lookupInfo.pageSkip = lookupInfo.pageSkip + 1;
                       break;
                   case 'L':
                       lookupInfo.pageSkip = lookupInfo.totalPages();
                       break;
                   default:
               }
        
               var e = { cancel: false, lookupName: lookupName, jEntitySearch: lookupInfo.lastJEntityExpression, entity: ownerReference, viewModel: lookupInfo.vm };
               if (beforeGettingLookup) beforeGettingLookup(e);
               if (e.cancel) { querySucceeded(null); return lookupInfo; }
               if(lookupInfo.lastJEntityExpression !== e.jEntitySearch)
                   lookupInfo.lastJEntityExpression = e.jEntitySearch;
               if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(true);
               var returnQueryResult = function (data) { lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount); querySucceeded(data); };
               eval('dataContext.get' + lookupName + 'ByEntitySearch(lookupInfo.lastJEntityExpression, (isNullOrEmpty(lookupInfo.fieldToSort) ? fieldToSearch : lookupInfo.fieldToSort), lookupInfo.pageSize*lookupInfo.pageSkip, lookupInfo.pageSize, lookupInfo.sortDirection, fieldToSearch).then(returnQueryResult).fail(queryFailed)').fin(function(){ if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(false); });
               return lookupInfo;
           };
        
           ownerReference.hasValidClientFilter = function (lookupName, lookupInfo) {
               var checkClientFilter = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
               if (checkClientFilter === 'Error') { return false; }
               if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                   checkClientFilter = ownerReference['BeforeGet' + lookupName + 'Query']('', lookupInfo);
                   if (checkClientFilter === 'Error') { return false; }
               }
               return true;
           }
        
           ownerReference.executeLookUp = function (lookupName, fieldToSearch, beforeGettingLookup, vm, valueToSearch, finished, comboCallBack, allowMultiSelectionInSearch) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); if (finished) finished(false, null); return; }
               var lookupFieldName = ownerReference.getLookupPropertyName(fieldToSearch);
               vm.dataBind('', true);
               var lookupInfo = new lookupInformation();
               lookupInfo.visibleColumns = ownerReference.getLookupVisibleColumns(fieldToSearch);
               lookupInfo.vm = vm;
               lookupInfo.isMultiSelection = lookupName.in(['LookTcsParametroUsuario', 'LookUpParametroRede', 'LookUpParametroGpecon', 'LookUpParametroFilial', 'LookUpParametroLoja', 'LookUpTcsTabelaAutorizacaoC']);
               var specializedLookup = ownerReference.getSpecializedLookup(lookupName, lookupInfo, lookupFieldName, valueToSearch, ownerReference, allowMultiSelectionInSearch);
               if (isNullOrEmpty(specializedLookup)) {
                       ownerReference.internalLookupSearch(lookupName, lookupFieldName, 'F',
                           function querySucceeded(data) {
                               if (typeof ownerReference['OnLoading' + lookupName + 'Query'] == 'function') {
                                   ownerReference['OnLoading' + lookupName + 'Query'](data);
                               }
                               if ((typeof comboCallBack) === 'function') {
                                   return comboCallBack(data ? data.results : null);
                               }
                               else if (data == null || data.results == null || data.results.length == 0) {
                                   if (finished) finished(false, null);
                                   ownerReference.clearLookUp(lookupName);
                                   if (data != null) app.showMessage('A informação de Lookup [' + ownerReference.getDisplayName(fieldToSearch) + '] não foi encontrada!', 'Informação', ['Ok']);
                                   return;
                               }
                               lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount);
                               showLookUp(dataContext, ownerReference, ownerReference.getLookupDisplay(lookupName), lookupName, lookupFieldName, ownerReference.internalLookupSearch, lookupInfo, 
                                   function (confirm, values) {
                                       var results = '';
                                       if (values != null && values.length > 1) {
                                           $.each(values, function (index, item) { results += (index == 0 ? '' : ',') + item[lookupFieldName].toString().trim() });
                                           results = '[' + results + ']';
                                           ownerReference[fieldToSearch](results);
                                           if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                               vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                           else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                           document.dispatchEvent(dataUpdateEvent);
                                       }
                                       if (finished) finished(confirm, results);
                                   }, data.results, allowMultiSelectionInSearch);
                       }, lookupInfo, valueToSearch, beforeGettingLookup, fieldToSearch);
               }
               else {
                       var currentModal = (modal.inUse ? modal2 : modal);
                       if (currentModal.inUse === false) {
                           //Check Client Validations
                           if (!ownerReference.hasValidClientFilter(lookupName, lookupInfo)) { if (finished) finished(false); return; }
                           //Show External Lookup
                           currentModal.showModal(specializedLookup.moduleName, specializedLookup.uiSettings, ownerReference.getLookupDisplay(lookupName), ['Ok', 'Cancelar'], 'large').then(function (r, data) {
                           if (r == 'Ok') {
                               if (!currentModal.internalUIs || currentModal.internalUIs.length != 1) { if (finished) finished(false); return; }
                               var lookupVM = currentModal[currentModal.internalUIs[0]]; 
                               if (!lookupVM) return; 
                               if (typeof lookupVM == 'function') lookupVM = lookupVM(); 
                               currentModal[currentModal.internalUIs[0]] = null; 
                               var selectedItems = lookupVM.getSpecializedLookupItems(); 
                               if (vm.status() == 'C' && selectedItems != null && selectedItems.length > 1) { 
                                   var results = '';
                                   $.each(selectedItems, function (index, item) { results += (index == 0 ? '' : ',') + (typeof item[lookupFieldName] == 'function' ? item[lookupFieldName]() : item[lookupFieldName]).toString().trim() });
                                   results = '[' + results + ']'
                                   ownerReference[fieldToSearch](results);
                                   if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                       vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                   else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                   document.dispatchEvent(dataUpdateEvent);
                                   if (finished) finished(true, results);
                               }
                               else if (selectedItems.length > 0) { dataContext['finalizeAll' + lookupName](ownerReference, selectedItems, '', lookupInfo); }
                           }
                           if (finished) finished(r === 'Ok');
                       });
                   }
                   if (finished) finished(false);
               }
           };
           ownerReference.clearLookUp = function (lookupName) {
               return eval('dataContext.clear' + lookupName + '(ownerReference)');
           };
       }
       //#endregion Lookup Extended Methods
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           var parent = getAbsoluteValue(ownerReference.TcsParametro);
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
           if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroValorGpeconList === 'function') && parent.TcsParametroValorGpeconList().length == 0) parent.setCurrentDetails('TcsParametroValorGpecon');
       };
       ownerReference.setParentAsModified = function() {
       var parent = getAbsoluteValue(ownerReference.TcsParametro);
       if (parent) {
           if (parent.isUnchanged()) {
               parent.setModified(); 
           }
           parent.setParentAsModified();
       }
       };
       ownerReference.getParent = function() {
           return getAbsoluteValue(ownerReference.TcsParametro);
       };
       ownerReference.getSelfList = function() {
           var parent = ownerReference.getParent();
           if (!isNullOrEmpty(parent)) {
               return getAbsoluteValue(parent.TcsParametroValorGpeconList);
           } else { return null; }
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','DescGrupoEconomico','IdGpecon','IdGpeconString','IdParametro','IdParametroValor','LxDatatypeParametro','PossuiVariacao','UidTabela','ValorParametro','ValorParametroBool' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Client Events
       ownerReference.OnAdded = function () {
    var entity = ownerReference;
setAbsoluteValue(entity, 'UidTabela', 'E2622A3A-2EA0-46D9-9E50-917A1ACF474A')
setAbsoluteValue(entity, 'PossuiVariacao', true)

if (getAbsoluteValue(vm.currentDataItem().LxDatatypeParametro)){
	setAbsoluteValue(entity, 'ValorParametroBool', false)
	setAbsoluteValue(entity, 'ValorParametro', false)
}
vm.notifyPresentation("TcsParametroValorGpeconList");
       }
       ownerReference.OnPropertyChanged = function (propertyName, oldValue, newValue) {
    if (propertyName == 'ValorParametro' && getAbsoluteValue(vm.currentDataItem().LxDatatypeParametro) == 1){
			if (isNaN(parseInt(newValue))){
				app.showMessage('Valor inválido.', 'Atenção', ['Ok']).then(function (){
					var value = (isNaN(parseInt(oldValue)) ? 0 : oldValue);
					setAbsoluteValue(ownerReference, 'ValorParametro', value);
					notifyPresentation('')
			});
	}
}

if (propertyName == 'ValorParametroBool'){
	setAbsoluteValue(ownerReference, 'ValorParametro', newValue);
}
       }
    //#endregion Client Events
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxDatatypeParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValorParametro', newValue));
                   if (value != ownerReference.LxDatatypeParametro()) {
                       ownerReference.LxDatatypeParametro(value);
                   }
            });
    
           ownerReference.LxDatatypeParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametro(0); return; }
                   var value = dataDomains.getName('TipoValorParametro', newValue);
                   if (value != ownerReference.LxDatatypeParametroName()) {
                       ownerReference.LxDatatypeParametroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorGpecon", null, TcsParametroValorGpeconInitializer);
    
    // Configure TcsParametroValorFilial data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorFilial",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,CodigoFilial: { dataType: DataType.String, maxLength: 18, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 18})]  }
    ,IdFilialPfj: { dataType: DataType.Int32, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdFilialString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,LxDatatypeParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,LxDatatypeParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,NomeFilial: { dataType: DataType.String, maxLength: 60, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 60})]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,ValorParametroBool: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns a single parent and associates with Details
    TcsParametro: { entityTypeName: "TcsParametro:#Linx.Framework.BV.Parametro", isScalar: true, foreignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroValorFilial" }
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorFilial'] = {ChaveSelecao: 'LookUpParametroFilial', CodigoFilial: 'LookUpParametroFilial', IdFilialPfj: 'LookUpParametroFilial', IdFilialString: 'LookUpParametroFilial', NomeFilial: 'LookUpParametroFilial'};
    var TcsParametroValorFilialInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorFilial') : ko.observable(getNextSequence('TcsParametroValorFilial')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorFilial{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorFilialByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['CodigoFilial'] = 'S';
       ownerReference.serverDataType['IdFilialPfj'] = 'I';
       ownerReference.serverDataType['IdFilialString'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['LxDatatypeParametro'] = 'Y';
       ownerReference.serverDataType['NomeFilial'] = 'S';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['ValorParametroBool'] = 'B';
       ownerReference.typeName = 'TcsParametroValorFilial';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorFilial !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorFilial() != ownerReference) { cacheElements[idxR].TcsParametroValorFilial(ownerReference); }
           }
       }
       //#region Lookup Extended Methods
       if (isPOCO !== true) {
           ownerReference.getLookupPropertyName = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null && !isNullOrEmpty(property.lookupPropertyName) ? property.lookupPropertyName : propertyName);
           }
           ownerReference.getLookupVisibleColumns = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null ? property.lookupVisibleColumns : '');
           }
           ownerReference.getLookUpClientFilterExpressions = function (lookupName, lookupInfo) {
               return '';
           };
        
           ownerReference.getLookupDisplay = function (lookupName) {
               var displayName = '';
               return 'Seleção' + displayName;
           };
        
           ownerReference.getSpecializedLookup = function (lookupName, lookupInfo, fieldToSearch, valueToSearch, ownerReference, allowMultiSelectionInSearch) {
               var specializedLookup = '';
               return specializedLookup;
           };
        
           ownerReference.getSubQueryFilterFromLookUpParametroFilial = function (propertyName) {
               var filter = '';
               return filter;
           }
           ownerReference.canGetClientFilter = function (lookupName) {
               return true;
           }
           ownerReference.validatedlookupsArray = [];
           ownerReference.internalLookupSearch = function (lookupName, fieldToSearch, operation, querySucceeded, lookupInfo, valueToSearch, beforeGettingLookup, referencefield) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); querySucceeded(null); return lookupInfo; }
               if (isNullOrEmpty(lookupInfo.lastJEntityExpression)) {
                   if (isNullOrEmpty(referencefield)) referencefield = fieldToSearch;
                   if ((typeof valueToSearch) === 'undefined')
                       valueToSearch = getAbsoluteValue(ownerReference[referencefield]);
                   var extraFilters = '';
                   if (ownerReference.canGetClientFilter(lookupName)) {
                       extraFilters = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
                       dataContext.lastClientFilterExpressions[lookupName] = extraFilters;
                       if (extraFilters === 'Error') { querySucceeded(null); return lookupInfo; }
                       if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                           var customFilter = ownerReference['BeforeGet' + lookupName + 'Query'](fieldToSearch, lookupInfo);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                       if (typeof ownerReference['getSubQueryFilterFrom' + lookupName] == 'function') {
                           var customFilter = ownerReference['getSubQueryFilterFrom' + lookupName](referencefield);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                   }
                   var completeExpression = getLookUpJEntityExpression(lookupName, ownerReference, fieldToSearch, valueToSearch, extraFilters, referencefield, app, lookupInfo.vm.useLikeCommandAsDefault);
                   if (completeExpression === 'Error') { querySucceeded(null); return lookupInfo; }
                   lookupInfo.lastJEntityExpression = completeExpression;
               }
               switch (operation) {
                   case 'F':
                       lookupInfo.pageSkip = 0;
                       break;
                   case 'B':
                       lookupInfo.pageSkip = lookupInfo.pageSkip - 1;
                       break;
                   case 'N':
                       lookupInfo.pageSkip = lookupInfo.pageSkip + 1;
                       break;
                   case 'L':
                       lookupInfo.pageSkip = lookupInfo.totalPages();
                       break;
                   default:
               }
        
               var e = { cancel: false, lookupName: lookupName, jEntitySearch: lookupInfo.lastJEntityExpression, entity: ownerReference, viewModel: lookupInfo.vm };
               if (beforeGettingLookup) beforeGettingLookup(e);
               if (e.cancel) { querySucceeded(null); return lookupInfo; }
               if(lookupInfo.lastJEntityExpression !== e.jEntitySearch)
                   lookupInfo.lastJEntityExpression = e.jEntitySearch;
               if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(true);
               var returnQueryResult = function (data) { lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount); querySucceeded(data); };
               eval('dataContext.get' + lookupName + 'ByEntitySearch(lookupInfo.lastJEntityExpression, (isNullOrEmpty(lookupInfo.fieldToSort) ? fieldToSearch : lookupInfo.fieldToSort), lookupInfo.pageSize*lookupInfo.pageSkip, lookupInfo.pageSize, lookupInfo.sortDirection, fieldToSearch).then(returnQueryResult).fail(queryFailed)').fin(function(){ if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(false); });
               return lookupInfo;
           };
        
           ownerReference.hasValidClientFilter = function (lookupName, lookupInfo) {
               var checkClientFilter = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
               if (checkClientFilter === 'Error') { return false; }
               if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                   checkClientFilter = ownerReference['BeforeGet' + lookupName + 'Query']('', lookupInfo);
                   if (checkClientFilter === 'Error') { return false; }
               }
               return true;
           }
        
           ownerReference.executeLookUp = function (lookupName, fieldToSearch, beforeGettingLookup, vm, valueToSearch, finished, comboCallBack, allowMultiSelectionInSearch) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); if (finished) finished(false, null); return; }
               var lookupFieldName = ownerReference.getLookupPropertyName(fieldToSearch);
               vm.dataBind('', true);
               var lookupInfo = new lookupInformation();
               lookupInfo.visibleColumns = ownerReference.getLookupVisibleColumns(fieldToSearch);
               lookupInfo.vm = vm;
               lookupInfo.isMultiSelection = lookupName.in(['LookTcsParametroUsuario', 'LookUpParametroRede', 'LookUpParametroGpecon', 'LookUpParametroFilial', 'LookUpParametroLoja', 'LookUpTcsTabelaAutorizacaoC']);
               var specializedLookup = ownerReference.getSpecializedLookup(lookupName, lookupInfo, lookupFieldName, valueToSearch, ownerReference, allowMultiSelectionInSearch);
               if (isNullOrEmpty(specializedLookup)) {
                       ownerReference.internalLookupSearch(lookupName, lookupFieldName, 'F',
                           function querySucceeded(data) {
                               if (typeof ownerReference['OnLoading' + lookupName + 'Query'] == 'function') {
                                   ownerReference['OnLoading' + lookupName + 'Query'](data);
                               }
                               if ((typeof comboCallBack) === 'function') {
                                   return comboCallBack(data ? data.results : null);
                               }
                               else if (data == null || data.results == null || data.results.length == 0) {
                                   if (finished) finished(false, null);
                                   ownerReference.clearLookUp(lookupName);
                                   if (data != null) app.showMessage('A informação de Lookup [' + ownerReference.getDisplayName(fieldToSearch) + '] não foi encontrada!', 'Informação', ['Ok']);
                                   return;
                               }
                               lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount);
                               showLookUp(dataContext, ownerReference, ownerReference.getLookupDisplay(lookupName), lookupName, lookupFieldName, ownerReference.internalLookupSearch, lookupInfo, 
                                   function (confirm, values) {
                                       var results = '';
                                       if (values != null && values.length > 1) {
                                           $.each(values, function (index, item) { results += (index == 0 ? '' : ',') + item[lookupFieldName].toString().trim() });
                                           results = '[' + results + ']';
                                           ownerReference[fieldToSearch](results);
                                           if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                               vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                           else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                           document.dispatchEvent(dataUpdateEvent);
                                       }
                                       if (finished) finished(confirm, results);
                                   }, data.results, allowMultiSelectionInSearch);
                       }, lookupInfo, valueToSearch, beforeGettingLookup, fieldToSearch);
               }
               else {
                       var currentModal = (modal.inUse ? modal2 : modal);
                       if (currentModal.inUse === false) {
                           //Check Client Validations
                           if (!ownerReference.hasValidClientFilter(lookupName, lookupInfo)) { if (finished) finished(false); return; }
                           //Show External Lookup
                           currentModal.showModal(specializedLookup.moduleName, specializedLookup.uiSettings, ownerReference.getLookupDisplay(lookupName), ['Ok', 'Cancelar'], 'large').then(function (r, data) {
                           if (r == 'Ok') {
                               if (!currentModal.internalUIs || currentModal.internalUIs.length != 1) { if (finished) finished(false); return; }
                               var lookupVM = currentModal[currentModal.internalUIs[0]]; 
                               if (!lookupVM) return; 
                               if (typeof lookupVM == 'function') lookupVM = lookupVM(); 
                               currentModal[currentModal.internalUIs[0]] = null; 
                               var selectedItems = lookupVM.getSpecializedLookupItems(); 
                               if (vm.status() == 'C' && selectedItems != null && selectedItems.length > 1) { 
                                   var results = '';
                                   $.each(selectedItems, function (index, item) { results += (index == 0 ? '' : ',') + (typeof item[lookupFieldName] == 'function' ? item[lookupFieldName]() : item[lookupFieldName]).toString().trim() });
                                   results = '[' + results + ']'
                                   ownerReference[fieldToSearch](results);
                                   if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                       vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                   else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                   document.dispatchEvent(dataUpdateEvent);
                                   if (finished) finished(true, results);
                               }
                               else if (selectedItems.length > 0) { dataContext['finalizeAll' + lookupName](ownerReference, selectedItems, '', lookupInfo); }
                           }
                           if (finished) finished(r === 'Ok');
                       });
                   }
                   if (finished) finished(false);
               }
           };
           ownerReference.clearLookUp = function (lookupName) {
               return eval('dataContext.clear' + lookupName + '(ownerReference)');
           };
       }
       //#endregion Lookup Extended Methods
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           var parent = getAbsoluteValue(ownerReference.TcsParametro);
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
           if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroValorFilialList === 'function') && parent.TcsParametroValorFilialList().length == 0) parent.setCurrentDetails('TcsParametroValorFilial');
       };
       ownerReference.setParentAsModified = function() {
       var parent = getAbsoluteValue(ownerReference.TcsParametro);
       if (parent) {
           if (parent.isUnchanged()) {
               parent.setModified(); 
           }
           parent.setParentAsModified();
       }
       };
       ownerReference.getParent = function() {
           return getAbsoluteValue(ownerReference.TcsParametro);
       };
       ownerReference.getSelfList = function() {
           var parent = ownerReference.getParent();
           if (!isNullOrEmpty(parent)) {
               return getAbsoluteValue(parent.TcsParametroValorFilialList);
           } else { return null; }
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','CodigoFilial','IdFilialPfj','IdFilialString','IdParametro','IdParametroValor','LxDatatypeParametro','NomeFilial','PossuiVariacao','UidTabela','ValorParametro','ValorParametroBool' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Client Events
       ownerReference.OnAdded = function () {
    var entity = ownerReference;
setAbsoluteValue(entity, 'UidTabela', 'AC554A67-E9F8-4F59-BD6A-BB9762BD20C1')
setAbsoluteValue(entity, 'PossuiVariacao', true)

if (getAbsoluteValue(vm.currentDataItem().LxDatatypeParametro)){
	setAbsoluteValue(entity, 'ValorParametroBool', false)
	setAbsoluteValue(entity, 'ValorParametro', false)
}
vm.notifyPresentation("TcsParametroValorFilialList");
       }
       ownerReference.OnPropertyChanged = function (propertyName, oldValue, newValue) {
    if (propertyName == 'ValorParametro' && getAbsoluteValue(vm.currentDataItem().LxDatatypeParametro) == 1){
			if (isNaN(parseInt(newValue))){
				app.showMessage('Valor inválido.', 'Atenção', ['Ok']).then(function (){
					var value = (isNaN(parseInt(oldValue)) ? 0 : oldValue);
					setAbsoluteValue(ownerReference, 'ValorParametro', value);
					notifyPresentation('')
			});
	}
}

if (propertyName == 'ValorParametroBool'){
	setAbsoluteValue(ownerReference, 'ValorParametro', newValue);
}
       }
    //#endregion Client Events
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxDatatypeParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValorParametro', newValue));
                   if (value != ownerReference.LxDatatypeParametro()) {
                       ownerReference.LxDatatypeParametro(value);
                   }
            });
    
           ownerReference.LxDatatypeParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametro(0); return; }
                   var value = dataDomains.getName('TipoValorParametro', newValue);
                   if (value != ownerReference.LxDatatypeParametroName()) {
                       ownerReference.LxDatatypeParametroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorFilial", null, TcsParametroValorFilialInitializer);
    
    // Configure TcsParametroValorLjvLoja data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorLjvLoja",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,CodLoja: { dataType: DataType.String, maxLength: 20, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 20})]  }
    ,DescLoja: { dataType: DataType.String, maxLength: 60, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 60})]  }
    ,IdLoja: { dataType: DataType.Int32, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdLojaString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,LxDatatypeParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,LxDatatypeParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,ValorParametroBool: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns a single parent and associates with Details
    TcsParametro: { entityTypeName: "TcsParametro:#Linx.Framework.BV.Parametro", isScalar: true, foreignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroValorLjvLoja" }
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorLjvLoja'] = {ChaveSelecao: 'LookUpParametroLoja', CodLoja: 'LookUpParametroLoja', DescLoja: 'LookUpParametroLoja', IdLoja: 'LookUpParametroLoja', IdLojaString: 'LookUpParametroLoja'};
    var TcsParametroValorLjvLojaInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorLjvLoja') : ko.observable(getNextSequence('TcsParametroValorLjvLoja')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorLjvLoja{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorLjvLojaByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['CodLoja'] = 'S';
       ownerReference.serverDataType['DescLoja'] = 'S';
       ownerReference.serverDataType['IdLoja'] = 'I';
       ownerReference.serverDataType['IdLojaString'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['LxDatatypeParametro'] = 'Y';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['ValorParametroBool'] = 'B';
       ownerReference.typeName = 'TcsParametroValorLjvLoja';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorLjvLoja !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorLjvLoja() != ownerReference) { cacheElements[idxR].TcsParametroValorLjvLoja(ownerReference); }
           }
       }
       //#region Lookup Extended Methods
       if (isPOCO !== true) {
           ownerReference.getLookupPropertyName = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null && !isNullOrEmpty(property.lookupPropertyName) ? property.lookupPropertyName : propertyName);
           }
           ownerReference.getLookupVisibleColumns = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null ? property.lookupVisibleColumns : '');
           }
           ownerReference.getLookUpClientFilterExpressions = function (lookupName, lookupInfo) {
               return '';
           };
        
           ownerReference.getLookupDisplay = function (lookupName) {
               var displayName = '';
               return 'Seleção' + displayName;
           };
        
           ownerReference.getSpecializedLookup = function (lookupName, lookupInfo, fieldToSearch, valueToSearch, ownerReference, allowMultiSelectionInSearch) {
               var specializedLookup = '';
               return specializedLookup;
           };
        
           ownerReference.getSubQueryFilterFromLookUpParametroLoja = function (propertyName) {
               var filter = '';
               return filter;
           }
           ownerReference.canGetClientFilter = function (lookupName) {
               return true;
           }
           ownerReference.validatedlookupsArray = [];
           ownerReference.internalLookupSearch = function (lookupName, fieldToSearch, operation, querySucceeded, lookupInfo, valueToSearch, beforeGettingLookup, referencefield) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); querySucceeded(null); return lookupInfo; }
               if (isNullOrEmpty(lookupInfo.lastJEntityExpression)) {
                   if (isNullOrEmpty(referencefield)) referencefield = fieldToSearch;
                   if ((typeof valueToSearch) === 'undefined')
                       valueToSearch = getAbsoluteValue(ownerReference[referencefield]);
                   var extraFilters = '';
                   if (ownerReference.canGetClientFilter(lookupName)) {
                       extraFilters = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
                       dataContext.lastClientFilterExpressions[lookupName] = extraFilters;
                       if (extraFilters === 'Error') { querySucceeded(null); return lookupInfo; }
                       if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                           var customFilter = ownerReference['BeforeGet' + lookupName + 'Query'](fieldToSearch, lookupInfo);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                       if (typeof ownerReference['getSubQueryFilterFrom' + lookupName] == 'function') {
                           var customFilter = ownerReference['getSubQueryFilterFrom' + lookupName](referencefield);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                   }
                   var completeExpression = getLookUpJEntityExpression(lookupName, ownerReference, fieldToSearch, valueToSearch, extraFilters, referencefield, app, lookupInfo.vm.useLikeCommandAsDefault);
                   if (completeExpression === 'Error') { querySucceeded(null); return lookupInfo; }
                   lookupInfo.lastJEntityExpression = completeExpression;
               }
               switch (operation) {
                   case 'F':
                       lookupInfo.pageSkip = 0;
                       break;
                   case 'B':
                       lookupInfo.pageSkip = lookupInfo.pageSkip - 1;
                       break;
                   case 'N':
                       lookupInfo.pageSkip = lookupInfo.pageSkip + 1;
                       break;
                   case 'L':
                       lookupInfo.pageSkip = lookupInfo.totalPages();
                       break;
                   default:
               }
        
               var e = { cancel: false, lookupName: lookupName, jEntitySearch: lookupInfo.lastJEntityExpression, entity: ownerReference, viewModel: lookupInfo.vm };
               if (beforeGettingLookup) beforeGettingLookup(e);
               if (e.cancel) { querySucceeded(null); return lookupInfo; }
               if(lookupInfo.lastJEntityExpression !== e.jEntitySearch)
                   lookupInfo.lastJEntityExpression = e.jEntitySearch;
               if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(true);
               var returnQueryResult = function (data) { lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount); querySucceeded(data); };
               eval('dataContext.get' + lookupName + 'ByEntitySearch(lookupInfo.lastJEntityExpression, (isNullOrEmpty(lookupInfo.fieldToSort) ? fieldToSearch : lookupInfo.fieldToSort), lookupInfo.pageSize*lookupInfo.pageSkip, lookupInfo.pageSize, lookupInfo.sortDirection, fieldToSearch).then(returnQueryResult).fail(queryFailed)').fin(function(){ if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(false); });
               return lookupInfo;
           };
        
           ownerReference.hasValidClientFilter = function (lookupName, lookupInfo) {
               var checkClientFilter = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
               if (checkClientFilter === 'Error') { return false; }
               if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                   checkClientFilter = ownerReference['BeforeGet' + lookupName + 'Query']('', lookupInfo);
                   if (checkClientFilter === 'Error') { return false; }
               }
               return true;
           }
        
           ownerReference.executeLookUp = function (lookupName, fieldToSearch, beforeGettingLookup, vm, valueToSearch, finished, comboCallBack, allowMultiSelectionInSearch) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); if (finished) finished(false, null); return; }
               var lookupFieldName = ownerReference.getLookupPropertyName(fieldToSearch);
               vm.dataBind('', true);
               var lookupInfo = new lookupInformation();
               lookupInfo.visibleColumns = ownerReference.getLookupVisibleColumns(fieldToSearch);
               lookupInfo.vm = vm;
               lookupInfo.isMultiSelection = lookupName.in(['LookTcsParametroUsuario', 'LookUpParametroRede', 'LookUpParametroGpecon', 'LookUpParametroFilial', 'LookUpParametroLoja', 'LookUpTcsTabelaAutorizacaoC']);
               var specializedLookup = ownerReference.getSpecializedLookup(lookupName, lookupInfo, lookupFieldName, valueToSearch, ownerReference, allowMultiSelectionInSearch);
               if (isNullOrEmpty(specializedLookup)) {
                       ownerReference.internalLookupSearch(lookupName, lookupFieldName, 'F',
                           function querySucceeded(data) {
                               if (typeof ownerReference['OnLoading' + lookupName + 'Query'] == 'function') {
                                   ownerReference['OnLoading' + lookupName + 'Query'](data);
                               }
                               if ((typeof comboCallBack) === 'function') {
                                   return comboCallBack(data ? data.results : null);
                               }
                               else if (data == null || data.results == null || data.results.length == 0) {
                                   if (finished) finished(false, null);
                                   ownerReference.clearLookUp(lookupName);
                                   if (data != null) app.showMessage('A informação de Lookup [' + ownerReference.getDisplayName(fieldToSearch) + '] não foi encontrada!', 'Informação', ['Ok']);
                                   return;
                               }
                               lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount);
                               showLookUp(dataContext, ownerReference, ownerReference.getLookupDisplay(lookupName), lookupName, lookupFieldName, ownerReference.internalLookupSearch, lookupInfo, 
                                   function (confirm, values) {
                                       var results = '';
                                       if (values != null && values.length > 1) {
                                           $.each(values, function (index, item) { results += (index == 0 ? '' : ',') + item[lookupFieldName].toString().trim() });
                                           results = '[' + results + ']';
                                           ownerReference[fieldToSearch](results);
                                           if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                               vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                           else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                           document.dispatchEvent(dataUpdateEvent);
                                       }
                                       if (finished) finished(confirm, results);
                                   }, data.results, allowMultiSelectionInSearch);
                       }, lookupInfo, valueToSearch, beforeGettingLookup, fieldToSearch);
               }
               else {
                       var currentModal = (modal.inUse ? modal2 : modal);
                       if (currentModal.inUse === false) {
                           //Check Client Validations
                           if (!ownerReference.hasValidClientFilter(lookupName, lookupInfo)) { if (finished) finished(false); return; }
                           //Show External Lookup
                           currentModal.showModal(specializedLookup.moduleName, specializedLookup.uiSettings, ownerReference.getLookupDisplay(lookupName), ['Ok', 'Cancelar'], 'large').then(function (r, data) {
                           if (r == 'Ok') {
                               if (!currentModal.internalUIs || currentModal.internalUIs.length != 1) { if (finished) finished(false); return; }
                               var lookupVM = currentModal[currentModal.internalUIs[0]]; 
                               if (!lookupVM) return; 
                               if (typeof lookupVM == 'function') lookupVM = lookupVM(); 
                               currentModal[currentModal.internalUIs[0]] = null; 
                               var selectedItems = lookupVM.getSpecializedLookupItems(); 
                               if (vm.status() == 'C' && selectedItems != null && selectedItems.length > 1) { 
                                   var results = '';
                                   $.each(selectedItems, function (index, item) { results += (index == 0 ? '' : ',') + (typeof item[lookupFieldName] == 'function' ? item[lookupFieldName]() : item[lookupFieldName]).toString().trim() });
                                   results = '[' + results + ']'
                                   ownerReference[fieldToSearch](results);
                                   if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                       vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                   else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                   document.dispatchEvent(dataUpdateEvent);
                                   if (finished) finished(true, results);
                               }
                               else if (selectedItems.length > 0) { dataContext['finalizeAll' + lookupName](ownerReference, selectedItems, '', lookupInfo); }
                           }
                           if (finished) finished(r === 'Ok');
                       });
                   }
                   if (finished) finished(false);
               }
           };
           ownerReference.clearLookUp = function (lookupName) {
               return eval('dataContext.clear' + lookupName + '(ownerReference)');
           };
       }
       //#endregion Lookup Extended Methods
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           var parent = getAbsoluteValue(ownerReference.TcsParametro);
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
           if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroValorLjvLojaList === 'function') && parent.TcsParametroValorLjvLojaList().length == 0) parent.setCurrentDetails('TcsParametroValorLjvLoja');
       };
       ownerReference.setParentAsModified = function() {
       var parent = getAbsoluteValue(ownerReference.TcsParametro);
       if (parent) {
           if (parent.isUnchanged()) {
               parent.setModified(); 
           }
           parent.setParentAsModified();
       }
       };
       ownerReference.getParent = function() {
           return getAbsoluteValue(ownerReference.TcsParametro);
       };
       ownerReference.getSelfList = function() {
           var parent = ownerReference.getParent();
           if (!isNullOrEmpty(parent)) {
               return getAbsoluteValue(parent.TcsParametroValorLjvLojaList);
           } else { return null; }
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','CodLoja','DescLoja','IdLoja','IdLojaString','IdParametro','IdParametroValor','LxDatatypeParametro','PossuiVariacao','UidTabela','ValorParametro','ValorParametroBool' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Client Events
       ownerReference.OnAdded = function () {
    var entity = ownerReference;
setAbsoluteValue(entity, 'UidTabela', '513CBC3E-FF6A-4B56-BCC5-350C63BF902F')
setAbsoluteValue(entity, 'PossuiVariacao', true)

if (getAbsoluteValue(vm.currentDataItem().LxDatatypeParametro)){
	setAbsoluteValue(entity, 'ValorParametroBool', false)
	setAbsoluteValue(entity, 'ValorParametro', false)
}
vm.notifyPresentation("TcsParametroValorLjvLojaList");





       }
       ownerReference.OnPropertyChanged = function (propertyName, oldValue, newValue) {
    if (propertyName == 'ValorParametro' && getAbsoluteValue(vm.currentDataItem().LxDatatypeParametro) == 1){
			if (isNaN(parseInt(newValue))){
				app.showMessage('Valor inválido.', 'Atenção', ['Ok']).then(function (){
					var value = (isNaN(parseInt(oldValue)) ? 0 : oldValue);
					setAbsoluteValue(ownerReference, 'ValorParametro', value);
					notifyPresentation('')
			});
	}
}

if (propertyName == 'ValorParametroBool'){
	setAbsoluteValue(ownerReference, 'ValorParametro', newValue);
}
       }
    //#endregion Client Events
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxDatatypeParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValorParametro', newValue));
                   if (value != ownerReference.LxDatatypeParametro()) {
                       ownerReference.LxDatatypeParametro(value);
                   }
            });
    
           ownerReference.LxDatatypeParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametro(0); return; }
                   var value = dataDomains.getName('TipoValorParametro', newValue);
                   if (value != ownerReference.LxDatatypeParametroName()) {
                       ownerReference.LxDatatypeParametroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorLjvLoja", null, TcsParametroValorLjvLojaInitializer);
    
    // Configure TcsParametroValorVariacaoGenerica data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorVariacaoGenerica",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,LxDatatypeParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,LxDatatypeParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,NomeTabela: { dataType: DataType.String, maxLength: 250, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 250})]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,ValorParametroBool: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns a single parent and associates with Details
    TcsParametro: { entityTypeName: "TcsParametro:#Linx.Framework.BV.Parametro", isScalar: true, foreignKeyNames: ["IdParametro"], associationName: "FK_TcsParametro_TcsParametroValorVariacaoGenerica" }
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorVariacaoGenerica'] = {NomeTabela: 'LookUpTcsTabelaAutorizacaoC', UidTabela: 'LookUpTcsTabelaAutorizacaoC'};
    var TcsParametroValorVariacaoGenericaInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorVariacaoGenerica') : ko.observable(getNextSequence('TcsParametroValorVariacaoGenerica')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorVariacaoGenerica{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorVariacaoGenericaByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['LxDatatypeParametro'] = 'Y';
       ownerReference.serverDataType['NomeTabela'] = 'S';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['ValorParametroBool'] = 'B';
       ownerReference.typeName = 'TcsParametroValorVariacaoGenerica';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorVariacaoGenerica !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorVariacaoGenerica() != ownerReference) { cacheElements[idxR].TcsParametroValorVariacaoGenerica(ownerReference); }
           }
       }
       //#region Lookup Extended Methods
       if (isPOCO !== true) {
           ownerReference.getLookupPropertyName = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null && !isNullOrEmpty(property.lookupPropertyName) ? property.lookupPropertyName : propertyName);
           }
           ownerReference.getLookupVisibleColumns = function(propertyName) {
              var property = getEntityProperty(ownerReference.typeName, propertyName);
              return (property != null ? property.lookupVisibleColumns : '');
           }
           ownerReference.getLookUpClientFilterExpressions = function (lookupName, lookupInfo) {
               return '';
           };
        
           ownerReference.getLookupDisplay = function (lookupName) {
               var displayName = '';
               return 'Seleção' + displayName;
           };
        
           ownerReference.getSpecializedLookup = function (lookupName, lookupInfo, fieldToSearch, valueToSearch, ownerReference, allowMultiSelectionInSearch) {
               var specializedLookup = '';
               return specializedLookup;
           };
        
           ownerReference.getSubQueryFilterFromLookUpTcsTabelaAutorizacaoC = function (propertyName) {
               var filter = '';
               return filter;
           }
           ownerReference.canGetClientFilter = function (lookupName) {
               return true;
           }
           ownerReference.validatedlookupsArray = [];
           ownerReference.internalLookupSearch = function (lookupName, fieldToSearch, operation, querySucceeded, lookupInfo, valueToSearch, beforeGettingLookup, referencefield) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); querySucceeded(null); return lookupInfo; }
               if (isNullOrEmpty(lookupInfo.lastJEntityExpression)) {
                   if (isNullOrEmpty(referencefield)) referencefield = fieldToSearch;
                   if ((typeof valueToSearch) === 'undefined')
                       valueToSearch = getAbsoluteValue(ownerReference[referencefield]);
                   var extraFilters = '';
                   if (ownerReference.canGetClientFilter(lookupName)) {
                       extraFilters = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
                       dataContext.lastClientFilterExpressions[lookupName] = extraFilters;
                       if (extraFilters === 'Error') { querySucceeded(null); return lookupInfo; }
                       if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                           var customFilter = ownerReference['BeforeGet' + lookupName + 'Query'](fieldToSearch, lookupInfo);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                       if (typeof ownerReference['getSubQueryFilterFrom' + lookupName] == 'function') {
                           var customFilter = ownerReference['getSubQueryFilterFrom' + lookupName](referencefield);
                           if (customFilter === 'Error') { querySucceeded(null); return lookupInfo; }
                           if (!isNullOrEmpty(customFilter)) { extraFilters = (isNullOrEmpty(extraFilters) ? '' : extraFilters + ';') + customFilter; }
                       }
                   }
                   var completeExpression = getLookUpJEntityExpression(lookupName, ownerReference, fieldToSearch, valueToSearch, extraFilters, referencefield, app, lookupInfo.vm.useLikeCommandAsDefault);
                   if (completeExpression === 'Error') { querySucceeded(null); return lookupInfo; }
                   lookupInfo.lastJEntityExpression = completeExpression;
               }
               switch (operation) {
                   case 'F':
                       lookupInfo.pageSkip = 0;
                       break;
                   case 'B':
                       lookupInfo.pageSkip = lookupInfo.pageSkip - 1;
                       break;
                   case 'N':
                       lookupInfo.pageSkip = lookupInfo.pageSkip + 1;
                       break;
                   case 'L':
                       lookupInfo.pageSkip = lookupInfo.totalPages();
                       break;
                   default:
               }
        
               var e = { cancel: false, lookupName: lookupName, jEntitySearch: lookupInfo.lastJEntityExpression, entity: ownerReference, viewModel: lookupInfo.vm };
               if (beforeGettingLookup) beforeGettingLookup(e);
               if (e.cancel) { querySucceeded(null); return lookupInfo; }
               if(lookupInfo.lastJEntityExpression !== e.jEntitySearch)
                   lookupInfo.lastJEntityExpression = e.jEntitySearch;
               if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(true);
               var returnQueryResult = function (data) { lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount); querySucceeded(data); };
               eval('dataContext.get' + lookupName + 'ByEntitySearch(lookupInfo.lastJEntityExpression, (isNullOrEmpty(lookupInfo.fieldToSort) ? fieldToSearch : lookupInfo.fieldToSort), lookupInfo.pageSize*lookupInfo.pageSkip, lookupInfo.pageSize, lookupInfo.sortDirection, fieldToSearch).then(returnQueryResult).fail(queryFailed)').fin(function(){ if (lookupInfo.vm) lookupInfo.vm.dataToolbar.isBusy(false); });
               return lookupInfo;
           };
        
           ownerReference.hasValidClientFilter = function (lookupName, lookupInfo) {
               var checkClientFilter = ownerReference.getLookUpClientFilterExpressions(lookupName, lookupInfo);
               if (checkClientFilter === 'Error') { return false; }
               if (typeof ownerReference['BeforeGet' + lookupName + 'Query'] == 'function') {
                   checkClientFilter = ownerReference['BeforeGet' + lookupName + 'Query']('', lookupInfo);
                   if (checkClientFilter === 'Error') { return false; }
               }
               return true;
           }
        
           ownerReference.executeLookUp = function (lookupName, fieldToSearch, beforeGettingLookup, vm, valueToSearch, finished, comboCallBack, allowMultiSelectionInSearch) {
               if (!lookupName || !fieldToSearch) { console.warn('lookupName or fieldToSearch is Empty!'); if (finished) finished(false, null); return; }
               var lookupFieldName = ownerReference.getLookupPropertyName(fieldToSearch);
               vm.dataBind('', true);
               var lookupInfo = new lookupInformation();
               lookupInfo.visibleColumns = ownerReference.getLookupVisibleColumns(fieldToSearch);
               lookupInfo.vm = vm;
               lookupInfo.isMultiSelection = lookupName.in(['LookTcsParametroUsuario', 'LookUpParametroRede', 'LookUpParametroGpecon', 'LookUpParametroFilial', 'LookUpParametroLoja', 'LookUpTcsTabelaAutorizacaoC']);
               var specializedLookup = ownerReference.getSpecializedLookup(lookupName, lookupInfo, lookupFieldName, valueToSearch, ownerReference, allowMultiSelectionInSearch);
               if (isNullOrEmpty(specializedLookup)) {
                       ownerReference.internalLookupSearch(lookupName, lookupFieldName, 'F',
                           function querySucceeded(data) {
                               if (typeof ownerReference['OnLoading' + lookupName + 'Query'] == 'function') {
                                   ownerReference['OnLoading' + lookupName + 'Query'](data);
                               }
                               if ((typeof comboCallBack) === 'function') {
                                   return comboCallBack(data ? data.results : null);
                               }
                               else if (data == null || data.results == null || data.results.length == 0) {
                                   if (finished) finished(false, null);
                                   ownerReference.clearLookUp(lookupName);
                                   if (data != null) app.showMessage('A informação de Lookup [' + ownerReference.getDisplayName(fieldToSearch) + '] não foi encontrada!', 'Informação', ['Ok']);
                                   return;
                               }
                               lookupInfo.totalRecords = (isNullOrEmpty(data.inlineCount) ? data.results.length : data.inlineCount);
                               showLookUp(dataContext, ownerReference, ownerReference.getLookupDisplay(lookupName), lookupName, lookupFieldName, ownerReference.internalLookupSearch, lookupInfo, 
                                   function (confirm, values) {
                                       var results = '';
                                       if (values != null && values.length > 1) {
                                           $.each(values, function (index, item) { results += (index == 0 ? '' : ',') + item[lookupFieldName].toString().trim() });
                                           results = '[' + results + ']';
                                           ownerReference[fieldToSearch](results);
                                           if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                               vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                           else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                           document.dispatchEvent(dataUpdateEvent);
                                       }
                                       if (finished) finished(confirm, results);
                                   }, data.results, allowMultiSelectionInSearch);
                       }, lookupInfo, valueToSearch, beforeGettingLookup, fieldToSearch);
               }
               else {
                       var currentModal = (modal.inUse ? modal2 : modal);
                       if (currentModal.inUse === false) {
                           //Check Client Validations
                           if (!ownerReference.hasValidClientFilter(lookupName, lookupInfo)) { if (finished) finished(false); return; }
                           //Show External Lookup
                           currentModal.showModal(specializedLookup.moduleName, specializedLookup.uiSettings, ownerReference.getLookupDisplay(lookupName), ['Ok', 'Cancelar'], 'large').then(function (r, data) {
                           if (r == 'Ok') {
                               if (!currentModal.internalUIs || currentModal.internalUIs.length != 1) { if (finished) finished(false); return; }
                               var lookupVM = currentModal[currentModal.internalUIs[0]]; 
                               if (!lookupVM) return; 
                               if (typeof lookupVM == 'function') lookupVM = lookupVM(); 
                               currentModal[currentModal.internalUIs[0]] = null; 
                               var selectedItems = lookupVM.getSpecializedLookupItems(); 
                               if (vm.status() == 'C' && selectedItems != null && selectedItems.length > 1) { 
                                   var results = '';
                                   $.each(selectedItems, function (index, item) { results += (index == 0 ? '' : ',') + (typeof item[lookupFieldName] == 'function' ? item[lookupFieldName]() : item[lookupFieldName]).toString().trim() });
                                   results = '[' + results + ']'
                                   ownerReference[fieldToSearch](results);
                                   if (vm.entitySearchRange[ownerReference.typeName + fieldToSearch] === undefined)
                                       vm.entitySearchRange[ownerReference.typeName + fieldToSearch] = ko.observable(results);
                                   else vm.entitySearchRange[ownerReference.typeName + fieldToSearch](results);
                                   document.dispatchEvent(dataUpdateEvent);
                                   if (finished) finished(true, results);
                               }
                               else if (selectedItems.length > 0) { dataContext['finalizeAll' + lookupName](ownerReference, selectedItems, '', lookupInfo); }
                           }
                           if (finished) finished(r === 'Ok');
                       });
                   }
                   if (finished) finished(false);
               }
           };
           ownerReference.clearLookUp = function (lookupName) {
               return eval('dataContext.clear' + lookupName + '(ownerReference)');
           };
       }
       //#endregion Lookup Extended Methods
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           var parent = getAbsoluteValue(ownerReference.TcsParametro);
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
           if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroValorVariacaoGenericaList === 'function') && parent.TcsParametroValorVariacaoGenericaList().length == 0) parent.setCurrentDetails('TcsParametroValorVariacaoGenerica');
       };
       ownerReference.setParentAsModified = function() {
       var parent = getAbsoluteValue(ownerReference.TcsParametro);
       if (parent) {
           if (parent.isUnchanged()) {
               parent.setModified(); 
           }
           parent.setParentAsModified();
       }
       };
       ownerReference.getParent = function() {
           return getAbsoluteValue(ownerReference.TcsParametro);
       };
       ownerReference.getSelfList = function() {
           var parent = ownerReference.getParent();
           if (!isNullOrEmpty(parent)) {
               return getAbsoluteValue(parent.TcsParametroValorVariacaoGenericaList);
           } else { return null; }
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','IdParametro','IdParametroValor','LxDatatypeParametro','NomeTabela','PossuiVariacao','UidTabela','ValorParametro','ValorParametroBool' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Client Events
       ownerReference.BeforeGetLookUpTcsTabelaAutorizacaoCQuery = function (fieldToSearch, lookupInfo) {
    if (!vm.dataToolbar.canUndo())
	return;
	
var dataItem = vm.currentDataItem().TcsParametroTabelaSelecaoList();
var condition = "";

for (var i = 0; i < dataItem.length; i++) {
	if (jQuery.inArray(getAbsoluteValue(dataItem[i].UidTabela).toUpperCase(), ["B24D57B5-BE89-4335-8017-9E581F44E06D", "18E96E62-AB34-41E9-99FA-B6F755CAD3D2", "E2622A3A-2EA0-46D9-9E50-917A1ACF474A", "513CBC3E-FF6A-4B56-BCC5-350C63BF902F", "AC554A67-E9F8-4F59-BD6A-BB9762BD20C1"]) < 0) {
	condition = condition + (condition.length > 0 ? ";||#" : "") + "UidTabela#==#G" + getAbsoluteValue(dataItem[i].UidTabela);	
	}
};

condition = condition.length > 0 ? condition : "UidTabela#==#G00000000-0000-0000-0000-000000000000";

return "LookUpTcsTabelaAutorizacaoB{" + condition + "}";

       }
       ownerReference.OnPropertyChanged = function (propertyName, oldValue, newValue) {
    if (propertyName == 'ValorParametro' && getAbsoluteValue(vm.currentDataItem().LxDatatypeParametro) == 1){
			if (isNaN(parseInt(newValue))){
				app.showMessage('Valor inválido.', 'Atenção', ['Ok']).then(function (){
					var value = (isNaN(parseInt(oldValue)) ? 0 : oldValue);
					setAbsoluteValue(ownerReference, 'ValorParametro', value);
					notifyPresentation('')
			});
	}
}

if (propertyName == 'ValorParametroBool'){
	setAbsoluteValue(ownerReference, 'ValorParametro', newValue);
}
       }
       ownerReference.OnAdded = function () {
    var entity = ownerReference;
setAbsoluteValue(entity, 'PossuiVariacao', true)

if (getAbsoluteValue(vm.currentDataItem().LxDatatypeParametro)){
	setAbsoluteValue(entity, 'ValorParametroBool', false)
	setAbsoluteValue(entity, 'ValorParametro', false)
}
       }
    //#endregion Client Events
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxDatatypeParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValorParametro', newValue));
                   if (value != ownerReference.LxDatatypeParametro()) {
                       ownerReference.LxDatatypeParametro(value);
                   }
            });
    
           ownerReference.LxDatatypeParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametro(0); return; }
                   var value = dataDomains.getName('TipoValorParametro', newValue);
                   if (value != ownerReference.LxDatatypeParametroName()) {
                       ownerReference.LxDatatypeParametroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorVariacaoGenerica", null, TcsParametroValorVariacaoGenericaInitializer);
    
    // Configure TcsParametroValorP data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorP",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,LxDatatypeParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxDatatypeParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,ValorParametroData: { dataType: DataType.DateTime, isNullable: false, isPartOfKey: false, defaultValue: '', validators: [ Validator.hasValueValidator]  }
    ,ValorParametroBool: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorP'] = {};
    var TcsParametroValorPInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorP') : ko.observable(getNextSequence('TcsParametroValorP')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorP{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorPByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['LxDatatypeParametro'] = 'Y';
       ownerReference.serverDataType['ValorParametroData'] = 'T';
       ownerReference.serverDataType['ValorParametroBool'] = 'B';
       ownerReference.typeName = 'TcsParametroValorP';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorP !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorP() != ownerReference) { cacheElements[idxR].TcsParametroValorP(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'IdParametro','IdParametroValor','PossuiVariacao','ValorParametro','LxDatatypeParametro','ValorParametroData','ValorParametroBool' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxDatatypeParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValorParametro', newValue));
                   if (value != ownerReference.LxDatatypeParametro()) {
                       ownerReference.LxDatatypeParametro(value);
                   }
            });
    
           ownerReference.LxDatatypeParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametro(0); return; }
                   var value = dataDomains.getName('TipoValorParametro', newValue);
                   if (value != ownerReference.LxDatatypeParametroName()) {
                       ownerReference.LxDatatypeParametroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorP", null, TcsParametroValorPInitializer);
    
    // Configure ParametroValor data type
    metadataStore.addEntityType({
    shortName: "ParametroValor",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.None,
    dataProperties: {
    TituloParametro: { dataType: DataType.String, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,ValorParametro: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,TipoValorParametro: { dataType: DataType.None, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['ParametroValor'] = {};
    var ParametroValorInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('ParametroValor') : ko.observable(getNextSequence('ParametroValor')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'ParametroValor{' + 'TituloParametro#==#S' + getAbsoluteValue(ownerReference.TituloParametro).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getParametroValorByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['TituloParametro'] = 'S';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['TipoValorParametro'] = 'S';
       ownerReference.typeName = 'ParametroValor';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'TituloParametro' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].ParametroValor !== 'function') { return; }
               else  if (cacheElements[idxR].ParametroValor() != ownerReference) { cacheElements[idxR].ParametroValor(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'TituloParametro','ValorParametro','TipoValorParametro' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("ParametroValor", null, ParametroValorInitializer);
    
    // Configure TcsParametroValorVariacaoP data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorVariacaoP",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorVariacaoP'] = {};
    var TcsParametroValorVariacaoPInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorVariacaoP') : ko.observable(getNextSequence('TcsParametroValorVariacaoP')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorVariacaoP{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorVariacaoPByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.typeName = 'TcsParametroValorVariacaoP';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorVariacaoP !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorVariacaoP() != ownerReference) { cacheElements[idxR].TcsParametroValorVariacaoP(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
           setAbsoluteValue(ownerReference, 'PossuiVariacao', true);
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','IdParametro','IdParametroValor','PossuiVariacao','UidTabela','ValorParametro' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorVariacaoP", null, TcsParametroValorVariacaoPInitializer);
    
    // Configure ParametroValorVariacao data type
    metadataStore.addEntityType({
    shortName: "ParametroValorVariacao",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.None,
    dataProperties: {
    TituloParametro: { dataType: DataType.String, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,NomeTabela: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,ChaveVariacao: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,ValorVariacao: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['ParametroValorVariacao'] = {};
    var ParametroValorVariacaoInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('ParametroValorVariacao') : ko.observable(getNextSequence('ParametroValorVariacao')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'ParametroValorVariacao{' + 'TituloParametro#==#S' + getAbsoluteValue(ownerReference.TituloParametro).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getParametroValorVariacaoByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['TituloParametro'] = 'S';
       ownerReference.serverDataType['NomeTabela'] = 'S';
       ownerReference.serverDataType['ChaveVariacao'] = 'S';
       ownerReference.serverDataType['ValorVariacao'] = 'S';
       ownerReference.typeName = 'ParametroValorVariacao';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'TituloParametro' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].ParametroValorVariacao !== 'function') { return; }
               else  if (cacheElements[idxR].ParametroValorVariacao() != ownerReference) { cacheElements[idxR].ParametroValorVariacao(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'TituloParametro','NomeTabela','ChaveVariacao','ValorVariacao' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("ParametroValorVariacao", null, ParametroValorVariacaoInitializer);
    
    // Configure TcsUsuarioParametro data type
    metadataStore.addEntityType({
    shortName: "TcsUsuarioParametro",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    IdUsuario: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,NomeUsuario: { dataType: DataType.String, maxLength: 250, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 250})]  }
    ,UidUsuarioString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsUsuarioParametro'] = {};
    var TcsUsuarioParametroInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsUsuarioParametro') : ko.observable(getNextSequence('TcsUsuarioParametro')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsUsuarioParametro{' + 'IdUsuario#==#L' + getAbsoluteValue(ownerReference.IdUsuario).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsUsuarioParametroByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['IdUsuario'] = 'L';
       ownerReference.serverDataType['NomeUsuario'] = 'S';
       ownerReference.serverDataType['UidUsuarioString'] = 'S';
       ownerReference.typeName = 'TcsUsuarioParametro';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdUsuario' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsUsuarioParametro !== 'function') { return; }
               else  if (cacheElements[idxR].TcsUsuarioParametro() != ownerReference) { cacheElements[idxR].TcsUsuarioParametro(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'IdUsuario','NomeUsuario','UidUsuarioString' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("TcsUsuarioParametro", null, TcsUsuarioParametroInitializer);
    
    // Configure TbcBandeiraRedeParametro data type
    metadataStore.addEntityType({
    shortName: "TbcBandeiraRedeParametro",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    CodBandeiraRede: { dataType: DataType.String, maxLength: 25, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 25})]  }
    ,DescBandeiraRede: { dataType: DataType.String, maxLength: 60, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 60})]  }
    ,IdBandeiraRedeParam: { dataType: DataType.Int32, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,IdBandeiraRedeString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TbcBandeiraRedeParametro'] = {};
    var TbcBandeiraRedeParametroInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TbcBandeiraRedeParametro') : ko.observable(getNextSequence('TbcBandeiraRedeParametro')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TbcBandeiraRedeParametro{' + 'IdBandeiraRedeParam#==#I' + getAbsoluteValue(ownerReference.IdBandeiraRedeParam).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTbcBandeiraRedeParametroByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['CodBandeiraRede'] = 'S';
       ownerReference.serverDataType['DescBandeiraRede'] = 'S';
       ownerReference.serverDataType['IdBandeiraRedeParam'] = 'I';
       ownerReference.serverDataType['IdBandeiraRedeString'] = 'S';
       ownerReference.typeName = 'TbcBandeiraRedeParametro';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdBandeiraRedeParam' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TbcBandeiraRedeParametro !== 'function') { return; }
               else  if (cacheElements[idxR].TbcBandeiraRedeParametro() != ownerReference) { cacheElements[idxR].TbcBandeiraRedeParametro(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'CodBandeiraRede','DescBandeiraRede','IdBandeiraRedeParam','IdBandeiraRedeString' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("TbcBandeiraRedeParametro", null, TbcBandeiraRedeParametroInitializer);
    
    // Configure TbcGrupoEconomicoParametro data type
    metadataStore.addEntityType({
    shortName: "TbcGrupoEconomicoParametro",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    DescGrupoEconomico: { dataType: DataType.String, maxLength: 60, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 60})]  }
    ,IdGpecon: { dataType: DataType.Int32, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,IdGpeconString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TbcGrupoEconomicoParametro'] = {};
    var TbcGrupoEconomicoParametroInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TbcGrupoEconomicoParametro') : ko.observable(getNextSequence('TbcGrupoEconomicoParametro')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TbcGrupoEconomicoParametro{' + 'IdGpecon#==#I' + getAbsoluteValue(ownerReference.IdGpecon).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTbcGrupoEconomicoParametroByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['DescGrupoEconomico'] = 'S';
       ownerReference.serverDataType['IdGpecon'] = 'I';
       ownerReference.serverDataType['IdGpeconString'] = 'S';
       ownerReference.typeName = 'TbcGrupoEconomicoParametro';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdGpecon' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
           if (idGpecon > 0) setAbsoluteValue(ownerReference, 'IdGpecon', idGpecon);
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TbcGrupoEconomicoParametro !== 'function') { return; }
               else  if (cacheElements[idxR].TbcGrupoEconomicoParametro() != ownerReference) { cacheElements[idxR].TbcGrupoEconomicoParametro(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'DescGrupoEconomico','IdGpecon','IdGpeconString' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("TbcGrupoEconomicoParametro", null, TbcGrupoEconomicoParametroInitializer);
    
    // Configure TbcFilialParametro data type
    metadataStore.addEntityType({
    shortName: "TbcFilialParametro",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    CodigoFilial: { dataType: DataType.String, maxLength: 18, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 18})]  }
    ,IdFilialPfj: { dataType: DataType.Int32, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,IdFilialString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,NomeFilial: { dataType: DataType.String, maxLength: 60, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 60})]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TbcFilialParametro'] = {};
    var TbcFilialParametroInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TbcFilialParametro') : ko.observable(getNextSequence('TbcFilialParametro')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TbcFilialParametro{' + 'IdFilialPfj#==#I' + getAbsoluteValue(ownerReference.IdFilialPfj).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTbcFilialParametroByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['CodigoFilial'] = 'S';
       ownerReference.serverDataType['IdFilialPfj'] = 'I';
       ownerReference.serverDataType['IdFilialString'] = 'S';
       ownerReference.serverDataType['NomeFilial'] = 'S';
       ownerReference.typeName = 'TbcFilialParametro';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdFilialPfj' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TbcFilialParametro !== 'function') { return; }
               else  if (cacheElements[idxR].TbcFilialParametro() != ownerReference) { cacheElements[idxR].TbcFilialParametro(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'CodigoFilial','IdFilialPfj','IdFilialString','NomeFilial' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("TbcFilialParametro", null, TbcFilialParametroInitializer);
    
    // Configure TcsParametroValorVariacaoGenericaP data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorVariacaoGenericaP",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorVariacaoGenericaP'] = {};
    var TcsParametroValorVariacaoGenericaPInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorVariacaoGenericaP') : ko.observable(getNextSequence('TcsParametroValorVariacaoGenericaP')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorVariacaoGenericaP{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorVariacaoGenericaPByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.typeName = 'TcsParametroValorVariacaoGenericaP';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorVariacaoGenericaP !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorVariacaoGenericaP() != ownerReference) { cacheElements[idxR].TcsParametroValorVariacaoGenericaP(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
           setAbsoluteValue(ownerReference, 'PossuiVariacao', true);
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','IdParametro','IdParametroValor','PossuiVariacao','UidTabela','ValorParametro' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorVariacaoGenericaP", null, TcsParametroValorVariacaoGenericaPInitializer);
    
    // Configure TcsParametroValorP1 data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorP1",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorP1'] = {};
    var TcsParametroValorP1Initializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorP1') : ko.observable(getNextSequence('TcsParametroValorP1')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorP1{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorP1ByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.typeName = 'TcsParametroValorP1';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorP1 !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorP1() != ownerReference) { cacheElements[idxR].TcsParametroValorP1(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
           setAbsoluteValue(ownerReference, 'PossuiVariacao', false);
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'IdParametro','IdParametroValor','PossuiVariacao','ValorParametro' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorP1", null, TcsParametroValorP1Initializer);
    
    // Configure ParametroInfo data type
    metadataStore.addEntityType({
    shortName: "ParametroInfo",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.None,
    dataProperties: {
    IdTcsAmbiente: { dataType: DataType.Int32, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,TituloParametro: { dataType: DataType.String, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,ValorParametro: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['ParametroInfo'] = {};
    var ParametroInfoInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('ParametroInfo') : ko.observable(getNextSequence('ParametroInfo')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'ParametroInfo{' + 'IdTcsAmbiente#==#I' + getAbsoluteValue(ownerReference.IdTcsAmbiente).toString() + ';TituloParametro#==#S' + getAbsoluteValue(ownerReference.TituloParametro).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getParametroInfoByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['IdTcsAmbiente'] = 'I';
       ownerReference.serverDataType['TituloParametro'] = 'S';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.typeName = 'ParametroInfo';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdTcsAmbiente','TituloParametro' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].ParametroInfo !== 'function') { return; }
               else  if (cacheElements[idxR].ParametroInfo() != ownerReference) { cacheElements[idxR].ParametroInfo(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'IdTcsAmbiente','TituloParametro','ValorParametro' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("ParametroInfo", null, ParametroInfoInitializer);
    
    // Configure TcsParametroValorP2 data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorP2",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorP2'] = {};
    var TcsParametroValorP2Initializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorP2') : ko.observable(getNextSequence('TcsParametroValorP2')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorP2{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorP2ByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.typeName = 'TcsParametroValorP2';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorP2 !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorP2() != ownerReference) { cacheElements[idxR].TcsParametroValorP2(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','IdParametro','IdParametroValor','PossuiVariacao','UidTabela','ValorParametro' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorP2", null, TcsParametroValorP2Initializer);
    
    // Configure TcsParametroValorLojaP data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorLojaP",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,LxDatatypeParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxDatatypeParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,CodLoja: { dataType: DataType.String, maxLength: 20, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 20})]  }
    ,DescLoja: { dataType: DataType.String, maxLength: 60, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 60})]  }
    ,IdLoja: { dataType: DataType.Int32, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdLojaString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,ValorParametroBool: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorLojaP'] = {};
    var TcsParametroValorLojaPInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorLojaP') : ko.observable(getNextSequence('TcsParametroValorLojaP')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorLojaP{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorLojaPByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['LxDatatypeParametro'] = 'Y';
       ownerReference.serverDataType['CodLoja'] = 'S';
       ownerReference.serverDataType['DescLoja'] = 'S';
       ownerReference.serverDataType['IdLoja'] = 'I';
       ownerReference.serverDataType['IdLojaString'] = 'S';
       ownerReference.serverDataType['ValorParametroBool'] = 'B';
       ownerReference.typeName = 'TcsParametroValorLojaP';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorLojaP !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorLojaP() != ownerReference) { cacheElements[idxR].TcsParametroValorLojaP(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','IdParametro','IdParametroValor','PossuiVariacao','UidTabela','ValorParametro','LxDatatypeParametro','CodLoja','DescLoja','IdLoja','IdLojaString','ValorParametroBool' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxDatatypeParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValorParametro', newValue));
                   if (value != ownerReference.LxDatatypeParametro()) {
                       ownerReference.LxDatatypeParametro(value);
                   }
            });
    
           ownerReference.LxDatatypeParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametro(0); return; }
                   var value = dataDomains.getName('TipoValorParametro', newValue);
                   if (value != ownerReference.LxDatatypeParametroName()) {
                       ownerReference.LxDatatypeParametroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorLojaP", null, TcsParametroValorLojaPInitializer);
    
    // Configure LjvLojaParametro data type
    metadataStore.addEntityType({
    shortName: "LjvLojaParametro",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    CodLoja: { dataType: DataType.String, maxLength: 20, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 20})]  }
    ,DescLoja: { dataType: DataType.String, maxLength: 60, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 60})]  }
    ,IdLoja: { dataType: DataType.Int32, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,IdLojaString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['LjvLojaParametro'] = {};
    var LjvLojaParametroInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('LjvLojaParametro') : ko.observable(getNextSequence('LjvLojaParametro')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'LjvLojaParametro{' + 'IdLoja#==#I' + getAbsoluteValue(ownerReference.IdLoja).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getLjvLojaParametroByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['CodLoja'] = 'S';
       ownerReference.serverDataType['DescLoja'] = 'S';
       ownerReference.serverDataType['IdLoja'] = 'I';
       ownerReference.serverDataType['IdLojaString'] = 'S';
       ownerReference.typeName = 'LjvLojaParametro';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdLoja' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].LjvLojaParametro !== 'function') { return; }
               else  if (cacheElements[idxR].LjvLojaParametro() != ownerReference) { cacheElements[idxR].LjvLojaParametro(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'CodLoja','DescLoja','IdLoja','IdLojaString' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    };
    metadataStore.registerEntityTypeCtor("LjvLojaParametro", null, LjvLojaParametroInitializer);
    
    // Configure TcsParametroValorUsuarioP data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorUsuarioP",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,LxDatatypeParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxDatatypeParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,IdUsuario: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,NomeUsuario: { dataType: DataType.String, maxLength: 250, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 250})]  }
    ,UidUsuarioString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,ValorParametroBool: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorUsuarioP'] = {};
    var TcsParametroValorUsuarioPInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorUsuarioP') : ko.observable(getNextSequence('TcsParametroValorUsuarioP')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorUsuarioP{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorUsuarioPByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['LxDatatypeParametro'] = 'Y';
       ownerReference.serverDataType['IdUsuario'] = 'L';
       ownerReference.serverDataType['NomeUsuario'] = 'S';
       ownerReference.serverDataType['UidUsuarioString'] = 'S';
       ownerReference.serverDataType['ValorParametroBool'] = 'B';
       ownerReference.typeName = 'TcsParametroValorUsuarioP';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorUsuarioP !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorUsuarioP() != ownerReference) { cacheElements[idxR].TcsParametroValorUsuarioP(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','IdParametro','IdParametroValor','PossuiVariacao','UidTabela','ValorParametro','LxDatatypeParametro','IdUsuario','NomeUsuario','UidUsuarioString','ValorParametroBool' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxDatatypeParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValorParametro', newValue));
                   if (value != ownerReference.LxDatatypeParametro()) {
                       ownerReference.LxDatatypeParametro(value);
                   }
            });
    
           ownerReference.LxDatatypeParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametro(0); return; }
                   var value = dataDomains.getName('TipoValorParametro', newValue);
                   if (value != ownerReference.LxDatatypeParametroName()) {
                       ownerReference.LxDatatypeParametroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorUsuarioP", null, TcsParametroValorUsuarioPInitializer);
    
    // Configure TcsParametroValorRedeP data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorRedeP",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,LxDatatypeParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxDatatypeParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,CodBandeiraRede: { dataType: DataType.String, maxLength: 25, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 25})]  }
    ,DescBandeiraRede: { dataType: DataType.String, maxLength: 60, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 60})]  }
    ,IdBandeiraRedeParam: { dataType: DataType.Int32, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdBandeiraRedeString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,ValorParametroBool: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorRedeP'] = {};
    var TcsParametroValorRedePInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorRedeP') : ko.observable(getNextSequence('TcsParametroValorRedeP')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorRedeP{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorRedePByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['LxDatatypeParametro'] = 'Y';
       ownerReference.serverDataType['CodBandeiraRede'] = 'S';
       ownerReference.serverDataType['DescBandeiraRede'] = 'S';
       ownerReference.serverDataType['IdBandeiraRedeParam'] = 'I';
       ownerReference.serverDataType['IdBandeiraRedeString'] = 'S';
       ownerReference.serverDataType['ValorParametroBool'] = 'B';
       ownerReference.typeName = 'TcsParametroValorRedeP';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorRedeP !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorRedeP() != ownerReference) { cacheElements[idxR].TcsParametroValorRedeP(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','IdParametro','IdParametroValor','PossuiVariacao','UidTabela','ValorParametro','LxDatatypeParametro','CodBandeiraRede','DescBandeiraRede','IdBandeiraRedeParam','IdBandeiraRedeString','ValorParametroBool' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxDatatypeParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValorParametro', newValue));
                   if (value != ownerReference.LxDatatypeParametro()) {
                       ownerReference.LxDatatypeParametro(value);
                   }
            });
    
           ownerReference.LxDatatypeParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametro(0); return; }
                   var value = dataDomains.getName('TipoValorParametro', newValue);
                   if (value != ownerReference.LxDatatypeParametroName()) {
                       ownerReference.LxDatatypeParametroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorRedeP", null, TcsParametroValorRedePInitializer);
    
    // Configure TcsParametroValorGpeconP data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorGpeconP",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,LxDatatypeParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxDatatypeParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,DescGrupoEconomico: { dataType: DataType.String, maxLength: 60, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 60})]  }
    ,IdGpecon: { dataType: DataType.Int32, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdGpeconString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,ValorParametroBool: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorGpeconP'] = {};
    var TcsParametroValorGpeconPInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorGpeconP') : ko.observable(getNextSequence('TcsParametroValorGpeconP')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorGpeconP{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorGpeconPByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['LxDatatypeParametro'] = 'Y';
       ownerReference.serverDataType['DescGrupoEconomico'] = 'S';
       ownerReference.serverDataType['IdGpecon'] = 'I';
       ownerReference.serverDataType['IdGpeconString'] = 'S';
       ownerReference.serverDataType['ValorParametroBool'] = 'B';
       ownerReference.typeName = 'TcsParametroValorGpeconP';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
           if (idGpecon > 0) setAbsoluteValue(ownerReference, 'IdGpecon', idGpecon);
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorGpeconP !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorGpeconP() != ownerReference) { cacheElements[idxR].TcsParametroValorGpeconP(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','IdParametro','IdParametroValor','PossuiVariacao','UidTabela','ValorParametro','LxDatatypeParametro','DescGrupoEconomico','IdGpecon','IdGpeconString','ValorParametroBool' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxDatatypeParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValorParametro', newValue));
                   if (value != ownerReference.LxDatatypeParametro()) {
                       ownerReference.LxDatatypeParametro(value);
                   }
            });
    
           ownerReference.LxDatatypeParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametro(0); return; }
                   var value = dataDomains.getName('TipoValorParametro', newValue);
                   if (value != ownerReference.LxDatatypeParametroName()) {
                       ownerReference.LxDatatypeParametroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorGpeconP", null, TcsParametroValorGpeconPInitializer);
    
    // Configure TcsParametroValorFilialP data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorFilialP",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,LxDatatypeParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxDatatypeParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,CodigoFilial: { dataType: DataType.String, maxLength: 18, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 18})]  }
    ,IdFilialPfj: { dataType: DataType.Int32, isNullable: false, isPartOfKey: false, validators: [ ]  }
    ,IdFilialString: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,NomeFilial: { dataType: DataType.String, maxLength: 60, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 60})]  }
    ,ValorParametroBool: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorFilialP'] = {};
    var TcsParametroValorFilialPInitializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorFilialP') : ko.observable(getNextSequence('TcsParametroValorFilialP')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorFilialP{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorFilialPByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['LxDatatypeParametro'] = 'Y';
       ownerReference.serverDataType['CodigoFilial'] = 'S';
       ownerReference.serverDataType['IdFilialPfj'] = 'I';
       ownerReference.serverDataType['IdFilialString'] = 'S';
       ownerReference.serverDataType['NomeFilial'] = 'S';
       ownerReference.serverDataType['ValorParametroBool'] = 'B';
       ownerReference.typeName = 'TcsParametroValorFilialP';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorFilialP !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorFilialP() != ownerReference) { cacheElements[idxR].TcsParametroValorFilialP(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','IdParametro','IdParametroValor','PossuiVariacao','UidTabela','ValorParametro','LxDatatypeParametro','CodigoFilial','IdFilialPfj','IdFilialString','NomeFilial','ValorParametroBool' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxDatatypeParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValorParametro', newValue));
                   if (value != ownerReference.LxDatatypeParametro()) {
                       ownerReference.LxDatatypeParametro(value);
                   }
            });
    
           ownerReference.LxDatatypeParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametro(0); return; }
                   var value = dataDomains.getName('TipoValorParametro', newValue);
                   if (value != ownerReference.LxDatatypeParametroName()) {
                       ownerReference.LxDatatypeParametroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorFilialP", null, TcsParametroValorFilialPInitializer);
    
    // Configure TcsParametroValorVariacaoGenericaP1 data type
    metadataStore.addEntityType({
    shortName: "TcsParametroValorVariacaoGenericaP1",
    namespace: "Linx.Framework.BV.Parametro",
    autoGeneratedKeyType: AutoGeneratedKeyType.Identity,
    dataProperties: {
    ChaveSelecao: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,IdParametro: { dataType: DataType.Int64, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,IdParametroValor: { dataType: DataType.Int64, isNullable: false, isPartOfKey: true, validators: [ Validator.hasValueValidator]  }
    ,PossuiVariacao: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,UidTabela: { dataType: DataType.Guid, isNullable: true, isPartOfKey: false, validators: [ ]  }
    ,ValorParametro: { dataType: DataType.String, maxLength: 250, isNullable: true, isPartOfKey: false, validators: [ Validator.maxLength( {maxLength: 250})]  }
    ,LxDatatypeParametro: { dataType: DataType.Byte, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator]  }
    ,LxDatatypeParametroName: { dataType: DataType.String, isNullable: false, isPartOfKey: false, validators: [] }
    ,NomeTabela: { dataType: DataType.String, maxLength: 250, isNullable: false, isPartOfKey: false, validators: [ Validator.hasValueValidator, Validator.maxLength( {maxLength: 250})]  }
    ,ValorParametroBool: { dataType: DataType.Boolean, isNullable: true, isPartOfKey: false, validators: [ ]  }
                    },
    navigationProperties: {
    // Returns collections of details and associates with Parent
                          }
    });
    lookUpProperties['TcsParametroValorVariacaoGenericaP1'] = {};
    var TcsParametroValorVariacaoGenericaP1Initializer = function (ownerReference, isPOCO) {
       ownerReference.RowDataId = (isPOCO === true ? getNextSequence('TcsParametroValorVariacaoGenericaP1') : ko.observable(getNextSequence('TcsParametroValorVariacaoGenericaP1')));
       ownerReference.setRemovedLookupFields = function(removedFields) {
           for (var idxLUp in entitylookUps[ownerReference.typeName]) {
               var hasKeyValue = false;
               var luName = entitylookUps[ownerReference.typeName][idxLUp];
               var luMeta = metadataInfo[luName];
               for (var idxProp in luMeta) {
                   var prop = luMeta[idxProp];
                   if (!isNullOrEmpty(prop.relatedKey) && prop.isPartOfKey) {
                       hasKeyValue = !isNullOrEmpty(getAbsoluteValue(ownerReference[prop.relatedKey]));
                       break;
                   }
               }
               if (hasKeyValue) {
                   for (var idxProp in luMeta) {
                       var prop = luMeta[idxProp];
                       if (!isNullOrEmpty(prop.relatedKey) && !prop.isPartOfKey) {
                           removedFields.push(prop.relatedKey);
                       }
                   }
               }
           }
       }
       ownerReference.getJExpression = function(listFilterRange, removedFields, noDetails) {
           if (ownerReference.excludedFilters && ownerReference.excludedFilters.length > 0) { if (removedFields instanceof Array) removedFields = removedFields.concat(ownerReference.excludedFilters); else removedFields = ownerReference.excludedFilters; }
           ownerReference.setRemovedLookupFields(removedFields);
           var jExpression = getJEntityExpression(ownerReference, app, listFilterRange, removedFields, vm.useLikeCommandAsDefault, ownerReference.getQbeZeroFields());
           if (jExpression === 'Error') return jExpression;
           return jExpression;
      };
       ownerReference.createOriginal = function(propertyName, oldValue) {
           ownerReference.original = ownerReference.getPrimitiveDTO();
           if (propertyName) ownerReference.original[propertyName] = oldValue;
       }
       ownerReference.restoreOriginal = function() {
           if (!isNullOrEmpty(ownerReference.original)) {
              enableChangeTrack = false;
              var properties = metadataInfo[ownerReference.typeName];
              for (var i = 0; i < properties.length; i++) {
                  var propertyName = properties[i].key;
                  if ((typeof ownerReference.original[propertyName]) !== 'undefined') ownerReference[propertyName] = ownerReference.original[propertyName];
              }
              delete ownerReference.original;
              enableChangeTrack = true;
           } else if(ownerReference.ChangeState === 'D') ownerReference.ChangeState = 'U';
       }
       if (isPOCO === true) {
           ownerReference.getValidationErrors = function(propertyName) {
               var errors = [];
               if (!vm.canReportErrors) return errors;
               if (!ownerReference.ChangeState || ['I', 'U'].indexOf(ownerReference.ChangeState) < 0) return errors;
               var properties = metadataInfo[ownerReference.typeName];
               for (var i = 0; i < properties.length; i++) {
                   var prop = properties[i];
                   if (isNullOrEmpty(propertyName) || prop.key == propertyName) {
                       if (prop.isRequired === true && !prop.isPartOfKey && isNullOrEmpty(ownerReference[prop.key]) && !(prop.isQbeZero === true && ownerReference[prop.key] == 0)) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] é requerido.');
                       if (prop.validateMaxLength === true && prop.maxLength > 0 && !isNullOrEmpty(ownerReference[prop.key]) && ownerReference[prop.key].length > prop.maxLength) errors.push('O campo [' + prop.headerText + (managerAuth.shellMode=='DEV' ? ' (' + ownerReference.typeName + '.' + prop.key + ')' : '') + '] permite no máximo ' + prop.maxLength.toString() + ' caractere(s).');
                   }
               }
               return errors;
           }
       }
       ownerReference.getQbeZeroFields = function() {
           var result = [];
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               if (properties[i].isQbeZero) {
                   result.push(properties[i].key);
               }
           }
           return result;
       }
       ownerReference.getPrimitiveDTO = function(loadDetails) {
           var command = '';
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
               command += (command === '' ? '' : ', ') + properties[i].key + ': getAbsoluteValue(ownerReference.' + properties[i].key + ')';
               if (properties[i].isDomain && properties[i].key.length > 4) command += (command === '' ? '' : ', ') + strLeft(properties[i].key, properties[i].key.length - 4) + ': getAbsoluteValue(ownerReference.' + strLeft(properties[i].key, properties[i].key.length - 4) + ')';
           }
           eval('var result = { ' + command + ' };');
           return result;
       };
       ownerReference.getAllDetailChanges = function() {
           var result = [];
           return result;
       };
       ownerReference.copyDataFrom = function(originData, copyDetails) {
           enableChangeTrack = false;
           var properties = metadataInfo[ownerReference.typeName];
           for (var i = 0; i < properties.length; i++) {
                setAbsoluteValue(ownerReference, properties[i].key, getAbsoluteValue(originData[properties[i].key]));
           }
       enableChangeTrack = true;
       };
          ownerReference.commitDetailsVisualPendings = function() {
          }
          ownerReference.refreshData = function(noWait, succeeded) {
             var filterByKey = 'TcsParametroValorVariacaoGenericaP1{' + 'IdParametroValor#==#L' + getAbsoluteValue(ownerReference.IdParametroValor).toString() + '}';
             if (!ownerReference.isPOCO && ownerReference.entityAspect && !ownerReference.isDetached() && !ownerReference.isUnchanged()) ownerReference.entityAspect.setUnchanged();
             return dataContext.getTcsParametroValorVariacaoGenericaP1ByEntitySearchNoAssociations(filterByKey, 0, 0, false, true, ownerReference.isPOCO === true, '', querySucceeded);
             function querySucceeded(data) {
                if (ownerReference.isPOCO && data.results.length > 0) {  for (var idx = 0; idx < data.results.length; idx++) { ownerReference.copyDataFrom(data.results[idx]); } }
                if (succeeded) { succeeded(data); }
                if (data.results.length == 0) { return; }
                if (!noWait || ownerReference.atLeastOneDetailLoaded()) { vm.clearInnerUIs(ownerReference); ownerReference.fillDetails(true, '', false, noWait); }
           }
          }
       if (isPOCO === true) {
           ownerReference.isPOCO = true;
           ownerReference.enableDetailsDataTack = function(breezeReference) {
              if (breezeReference) breezeReference.setCurrentDetails();
           };
       }
       ownerReference.isAdded = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Added;
       });
       ownerReference.isDeleted = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Deleted;
       });
       ownerReference.isModified = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Modified;
       });
       ownerReference.isDetached = (isPOCO === true ? function() { return false; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Detached;
       });
       ownerReference.isUnchanged = (isPOCO === true ? function() { return true; } : function() {
           return ownerReference.entityAspect.entityState === breeze.EntityState.Unchanged;
       });
       ownerReference.setModified = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setModified();
       });
       ownerReference.setUnchanged = (isPOCO === true ? function() {  } : function() {
           ownerReference.entityAspect.setUnchanged();
       });
       ownerReference.serverDataType = [];
       ownerReference.serverDataType['ChaveSelecao'] = 'S';
       ownerReference.serverDataType['IdParametro'] = 'L';
       ownerReference.serverDataType['IdParametroValor'] = 'L';
       ownerReference.serverDataType['PossuiVariacao'] = 'B';
       ownerReference.serverDataType['UidTabela'] = 'G';
       ownerReference.serverDataType['ValorParametro'] = 'S';
       ownerReference.serverDataType['LxDatatypeParametro'] = 'Y';
       ownerReference.serverDataType['NomeTabela'] = 'S';
       ownerReference.serverDataType['ValorParametroBool'] = 'B';
       ownerReference.typeName = 'TcsParametroValorVariacaoGenericaP1';
       ownerReference.isPrimaryKey = function(propertyName) {
           var keys = [ 'IdParametroValor' ];
           return keys.indexOf(propertyName) >= 0;
       }
       ownerReference.getDisplayName = function(propertyName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          return (property != null ? property.headerText : propertyName);
       }
       ownerReference.setDisplayName = function(propertyName, displayName) {
          var property = getEntityProperty(ownerReference.typeName, propertyName);
          if (property != null) property.headerText = displayName;
       }
       ownerReference.setBandeiraRede = function (idBandeiraRede) {
       };
       ownerReference.setGpecon = function (idGpecon) {
       };
       ownerReference.UpdateIndependentRelation = function(detailName) {
           var cacheElements = dataContext.getEntities(detailName);
           for (var idxR = 0; idxR < cacheElements.length; idxR++) {
               if (typeof cacheElements[idxR].TcsParametroValorVariacaoGenericaP1 !== 'function') { return; }
               else  if (cacheElements[idxR].TcsParametroValorVariacaoGenericaP1() != ownerReference) { cacheElements[idxR].TcsParametroValorVariacaoGenericaP1(ownerReference); }
           }
       }
       ownerReference.setDefaults = function () {
            //Adjust default value for QBE Zero Properties
            var qbeZeroProperties = ownerReference.getQbeZeroFields();
            for (var i = 0; i < qbeZeroProperties.length; i++) {
                   setAbsoluteValue(ownerReference, qbeZeroProperties[i], 0);
            }
       };
       ownerReference.delete = function() {
           if (ownerReference.isDetached()) {
               app.showMessage('A informação selecionada não pode ser excluída!', 'Alerta', ['Ok']);
               return;
           }
           if (ownerReference.setParentAsModified) ownerReference.setParentAsModified();
           if (ownerReference.entityAspect) ownerReference.entityAspect.setDeleted(); // mark for deletion
       };
       ownerReference.setParentAsModified = function() {
       };
       ownerReference.getParent = function() {
           return null;
       };
       ownerReference.getSelfList = function() {
           return vm.dataView();
       };
       ownerReference.namespace = 'Linx.Framework.BV.Parametro';
       ownerReference.myProperties = [ 'ChaveSelecao','IdParametro','IdParametroValor','PossuiVariacao','UidTabela','ValorParametro','LxDatatypeParametro','NomeTabela','ValorParametroBool' ];
       ownerReference.queryRequiredProperties = {  };
       ownerReference.excludedFilters = [];
       ownerReference.getCurrentElements = function() {
           var result = [ ownerReference ];
           return result;
       };
       ownerReference.checkForSendingAllRowsToServer = function() {
       };
       ownerReference.detailsLoaded = function() {
           return true;
       }
       ownerReference.atLeastOneDetailLoaded = function() {
           return true;
       }
       ownerReference.adjustDetailsLoaded = function(value) {
       }
       ownerReference.fillDetails = function(force, detailName, noInnerUIs, noWait, callback, customParentRelation) {
          if (typeof force === 'undefined') force = false;
          if (force) vm.clearInnerUIs(ownerReference);
          if (!noInnerUIs) vm.queryInnerUIs(ownerReference);
          if (callback) { callback(); }
       };
       //Select first element as a current item of each detail
       ownerReference.setCurrentDetails = function(detailName, clearing) {
       };
       ownerReference.notifyEmptyDetails = function(detailName) {
       };
    //#region Extended Domain Names
       if (isPOCO !== true) {
           ownerReference.LxDatatypeParametroName.subscribe(
               function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametroName(''); return; }
                   var value = (dataDomains.getId('TipoValorParametro', newValue));
                   if (value != ownerReference.LxDatatypeParametro()) {
                       ownerReference.LxDatatypeParametro(value);
                   }
            });
    
           ownerReference.LxDatatypeParametro.subscribe(
           function (newValue) {
                   if (newValue == null) { ownerReference.LxDatatypeParametro(0); return; }
                   var value = dataDomains.getName('TipoValorParametro', newValue);
                   if (value != ownerReference.LxDatatypeParametroName()) {
                       ownerReference.LxDatatypeParametroName(value);
               }
           });
       }
    //#endregion Extended Domain Names
    };
    metadataStore.registerEntityTypeCtor("TcsParametroValorVariacaoGenericaP1", null, TcsParametroValorVariacaoGenericaP1Initializer);
    //#endregion Classes Map
    //#region Context Definition
    
    //#region Get LookUps
    
    var getLookTcsParametroUsuarioByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookTcsParametroUsuarioByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getLookUpParametroRedeByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpParametroRedeByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getLookUpParametroGpeconByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpParametroGpeconByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getLookUpParametroLojaByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpParametroLojaByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getLookUpParametroFilialByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpParametroFilialByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getLookUpTcsTabelaAutorizacaoCByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpTcsTabelaAutorizacaoCByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getLookUpTcsAplicativoByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpTcsAplicativoByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    var lookUpExternalManagers = [];
    
    var getLookUpTcsParametroGrupoAutorizacaoByEntitySearch = function (jEntitySearch, order, skip, take, direction, lookupField) {
        var query = EntityQuery.from('GetLookUpTcsParametroGrupoAutorizacaoByEntitySearch').noTracking(true);
        query = (direction === 'descending' ? query.orderByDesc(order) : query.orderBy(order));
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ propertyName: (isNullOrEmpty(lookupField) ? order : lookupField), jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        if (!lookUpExternalManagers['LinxFrameworkParametroAutorizacao']) {
           lookUpExternalManagers['LinxFrameworkParametroAutorizacao'] = new breeze.EntityManager({ dataService: new breeze.DataService({
               serviceName: getServiceAddress('LinxFrameworkParametroAutorizacao'),
               hasServerMetadata: false
           }) });
        }
        return lookUpExternalManagers['LinxFrameworkParametroAutorizacao'].executeQuery(query)
        .fail(queryFailed);
    };
    //#endregion
    //#region Get KPI Ranges
    //#endregion
    
    //#region Get Combo LookUp
    var getResultsCombo = function (lookupName, fieldName, current, callback) {
        if (typeof current.executeLookUp === 'function') {
           current.executeLookUp(lookupName, fieldName, null, vm, null, null, function (result) {
               if (callback) callback(result);
           });
        }
    };
    var clientFilterHasModified = function clientFilterHasModified(lookupName, current) {
        var lastFilter = dataContext.lastClientFilterExpressions[lookupName];
        if (lastFilter === 'Error') return true;
        var currentFilter = current.getLookUpClientFilterExpressions(lookupName, null);
        return lastFilter != currentFilter;
    };
    //#endregion Get Combo LookUp
    
    //#region Get Business Entities
    
    var getBmEntityProperties = function (entityName, parentDataPath) {
        return manager.executeQuery(EntityQuery.from('GetBmEntityProperties').withParameters({ entityName: entityName, parentDataPath: parentDataPath }).noTracking(true))
        .fail(queryFailed);
    };
    
    var clearTcsParametro = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsParametro');
        var refTcsParametro = manager.createEntity('TcsParametro', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsParametro ] });
        return true;
    };
    
    var getTcsParametro = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametro').noTracking(noTracking)
        .orderBy('IdParametro asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametro asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametro asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorP = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsParametroValorP');
        var refTcsParametroValorP = manager.createEntity('TcsParametroValorP', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsParametroValorP ] });
        return true;
    };
    
    var getTcsParametroValorP = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorP').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorPByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorPEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorPByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorPByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearParametroValor = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('ParametroValor');
        var refParametroValor = manager.createEntity('ParametroValor', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refParametroValor ] });
        return true;
    };
    
    var getParametroValor = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetParametroValor').noTracking(noTracking)
        .orderBy('TituloParametro asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getParametroValorByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddParametroValorEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetParametroValorByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'TituloParametro asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetParametroValorByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'TituloParametro asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorVariacaoP = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsParametroValorVariacaoP');
        var refTcsParametroValorVariacaoP = manager.createEntity('TcsParametroValorVariacaoP', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsParametroValorVariacaoP ] });
        return true;
    };
    
    var getTcsParametroValorVariacaoP = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorVariacaoP').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorVariacaoPByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorVariacaoPEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorVariacaoPByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorVariacaoPByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroTabelaSelecao = function (idBandeiraRede, complete) {
        clearAll();
        if (complete) complete({ results: [] });
        return true;
    };
    
    var getTcsParametroTabelaSelecao = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroTabelaSelecao').noTracking(noTracking)
        .orderBy('DescTabela asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroTabelaSelecaoByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroTabelaSelecaoEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroTabelaSelecaoByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'DescTabela asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroTabelaSelecaoByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'DescTabela asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValor = function (idBandeiraRede, complete) {
        clearAll();
        if (complete) complete({ results: [] });
        return true;
    };
    
    var getTcsParametroValor = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValor').noTracking(noTracking)
        .orderBy('IdParametro asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametro asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametro asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearParametroValorVariacao = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('ParametroValorVariacao');
        var refParametroValorVariacao = manager.createEntity('ParametroValorVariacao', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refParametroValorVariacao ] });
        return true;
    };
    
    var getParametroValorVariacao = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetParametroValorVariacao').noTracking(noTracking)
        .orderBy('TituloParametro asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getParametroValorVariacaoByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddParametroValorVariacaoEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetParametroValorVariacaoByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'TituloParametro asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetParametroValorVariacaoByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'TituloParametro asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsUsuarioParametro = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsUsuarioParametro');
        var refTcsUsuarioParametro = manager.createEntity('TcsUsuarioParametro', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsUsuarioParametro ] });
        return true;
    };
    
    var getTcsUsuarioParametro = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsUsuarioParametro').noTracking(noTracking)
        .orderBy('IdUsuario asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsUsuarioParametroByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsUsuarioParametroEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsUsuarioParametroByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdUsuario asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsUsuarioParametroByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdUsuario asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorUsuario = function (idBandeiraRede, complete) {
        clearAll();
        if (complete) complete({ results: [] });
        return true;
    };
    
    var getTcsParametroValorUsuario = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorUsuario').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorUsuarioByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorUsuarioEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorUsuarioByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorUsuarioByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTbcBandeiraRedeParametro = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TbcBandeiraRedeParametro');
        var refTbcBandeiraRedeParametro = manager.createEntity('TbcBandeiraRedeParametro', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTbcBandeiraRedeParametro ] });
        return true;
    };
    
    var getTbcBandeiraRedeParametro = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTbcBandeiraRedeParametro').noTracking(noTracking)
        .orderBy('IdBandeiraRedeParam asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTbcBandeiraRedeParametroByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTbcBandeiraRedeParametroEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTbcBandeiraRedeParametroByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdBandeiraRedeParam asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTbcBandeiraRedeParametroByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdBandeiraRedeParam asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorRede = function (idBandeiraRede, complete) {
        clearAll();
        if (complete) complete({ results: [] });
        return true;
    };
    
    var getTcsParametroValorRede = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorRede').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorRedeByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorRedeEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorRedeByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorRedeByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTbcGrupoEconomicoParametro = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TbcGrupoEconomicoParametro');
        var refTbcGrupoEconomicoParametro = manager.createEntity('TbcGrupoEconomicoParametro', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTbcGrupoEconomicoParametro ] });
        return true;
    };
    
    var getTbcGrupoEconomicoParametro = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTbcGrupoEconomicoParametro').noTracking(noTracking)
        .orderBy('IdGpecon asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTbcGrupoEconomicoParametroByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTbcGrupoEconomicoParametroEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTbcGrupoEconomicoParametroByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdGpecon asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTbcGrupoEconomicoParametroByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdGpecon asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorGpecon = function (idBandeiraRede, complete) {
        clearAll();
        if (complete) complete({ results: [] });
        return true;
    };
    
    var getTcsParametroValorGpecon = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorGpecon').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorGpeconByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorGpeconEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorGpeconByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorGpeconByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTbcFilialParametro = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TbcFilialParametro');
        var refTbcFilialParametro = manager.createEntity('TbcFilialParametro', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTbcFilialParametro ] });
        return true;
    };
    
    var getTbcFilialParametro = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTbcFilialParametro').noTracking(noTracking)
        .orderBy('IdFilialPfj asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTbcFilialParametroByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTbcFilialParametroEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTbcFilialParametroByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdFilialPfj asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTbcFilialParametroByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdFilialPfj asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorFilial = function (idBandeiraRede, complete) {
        clearAll();
        if (complete) complete({ results: [] });
        return true;
    };
    
    var getTcsParametroValorFilial = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorFilial').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorFilialByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorFilialEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorFilialByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorFilialByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorLjvLoja = function (idBandeiraRede, complete) {
        clearAll();
        if (complete) complete({ results: [] });
        return true;
    };
    
    var getTcsParametroValorLjvLoja = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorLjvLoja').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorLjvLojaByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorLjvLojaEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorLjvLojaByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorLjvLojaByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorVariacaoGenericaP = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsParametroValorVariacaoGenericaP');
        var refTcsParametroValorVariacaoGenericaP = manager.createEntity('TcsParametroValorVariacaoGenericaP', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsParametroValorVariacaoGenericaP ] });
        return true;
    };
    
    var getTcsParametroValorVariacaoGenericaP = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorVariacaoGenericaP').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorVariacaoGenericaPByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorVariacaoGenericaPEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorVariacaoGenericaPByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorVariacaoGenericaPByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorVariacaoGenerica = function (idBandeiraRede, complete) {
        clearAll();
        if (complete) complete({ results: [] });
        return true;
    };
    
    var getTcsParametroValorVariacaoGenerica = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorVariacaoGenerica').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorVariacaoGenericaByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorVariacaoGenericaEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorVariacaoGenericaByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorVariacaoGenericaByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorP1 = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsParametroValorP1');
        var refTcsParametroValorP1 = manager.createEntity('TcsParametroValorP1', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsParametroValorP1 ] });
        return true;
    };
    
    var getTcsParametroValorP1 = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorP1').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorP1ByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorP1EntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorP1ByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorP1ByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearParametroInfo = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('ParametroInfo');
        var refParametroInfo = manager.createEntity('ParametroInfo', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refParametroInfo ] });
        return true;
    };
    
    var getParametroInfo = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetParametroInfo').noTracking(noTracking)
        .orderBy('IdTcsAmbiente asc, TituloParametro asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getParametroInfoByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddParametroInfoEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetParametroInfoByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsAmbiente asc, TituloParametro asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetParametroInfoByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdTcsAmbiente asc, TituloParametro asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorP2 = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsParametroValorP2');
        var refTcsParametroValorP2 = manager.createEntity('TcsParametroValorP2', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsParametroValorP2 ] });
        return true;
    };
    
    var getTcsParametroValorP2 = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorP2').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorP2ByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorP2EntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorP2ByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorP2ByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorLojaP = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsParametroValorLojaP');
        var refTcsParametroValorLojaP = manager.createEntity('TcsParametroValorLojaP', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsParametroValorLojaP ] });
        return true;
    };
    
    var getTcsParametroValorLojaP = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorLojaP').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorLojaPByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorLojaPEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorLojaPByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorLojaPByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearLjvLojaParametro = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('LjvLojaParametro');
        var refLjvLojaParametro = manager.createEntity('LjvLojaParametro', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refLjvLojaParametro ] });
        return true;
    };
    
    var getLjvLojaParametro = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetLjvLojaParametro').noTracking(noTracking)
        .orderBy('IdLoja asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getLjvLojaParametroByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddLjvLojaParametroEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetLjvLojaParametroByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdLoja asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetLjvLojaParametroByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdLoja asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorUsuarioP = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsParametroValorUsuarioP');
        var refTcsParametroValorUsuarioP = manager.createEntity('TcsParametroValorUsuarioP', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsParametroValorUsuarioP ] });
        return true;
    };
    
    var getTcsParametroValorUsuarioP = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorUsuarioP').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorUsuarioPByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorUsuarioPEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorUsuarioPByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorUsuarioPByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorRedeP = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsParametroValorRedeP');
        var refTcsParametroValorRedeP = manager.createEntity('TcsParametroValorRedeP', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsParametroValorRedeP ] });
        return true;
    };
    
    var getTcsParametroValorRedeP = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorRedeP').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorRedePByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorRedePEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorRedePByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorRedePByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorGpeconP = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsParametroValorGpeconP');
        var refTcsParametroValorGpeconP = manager.createEntity('TcsParametroValorGpeconP', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsParametroValorGpeconP ] });
        return true;
    };
    
    var getTcsParametroValorGpeconP = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorGpeconP').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorGpeconPByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorGpeconPEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorGpeconPByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorGpeconPByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorFilialP = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsParametroValorFilialP');
        var refTcsParametroValorFilialP = manager.createEntity('TcsParametroValorFilialP', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsParametroValorFilialP ] });
        return true;
    };
    
    var getTcsParametroValorFilialP = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorFilialP').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorFilialPByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorFilialPEntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorFilialPByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorFilialPByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    
    var clearTcsParametroValorVariacaoGenericaP1 = function (idBandeiraRede, complete) {
        clearAll();
        resetSequence('TcsParametroValorVariacaoGenericaP1');
        var refTcsParametroValorVariacaoGenericaP1 = manager.createEntity('TcsParametroValorVariacaoGenericaP1', {}, breeze.EntityState.Unchanged);
        if (complete) complete({ results: [ refTcsParametroValorVariacaoGenericaP1 ] });
        return true;
    };
    
    var getTcsParametroValorVariacaoGenericaP1 = function (predicate, preserveCurrentState, noTracking) {
        if (!preserveCurrentState) clearAll();
        var query = EntityQuery.from('GetTcsParametroValorVariacaoGenericaP1').noTracking(noTracking)
        .orderBy('IdParametroValor asc')
        ;
    
        if ((typeof predicate !== 'undefined') && predicate !== null)
            query = query.where(predicate);
    
        return manager.executeQuery(query)
        .fail(queryFailed);
    };
    
    var getTcsParametroValorVariacaoGenericaP1ByEntitySearchNoAssociations = function (jEntitySearch, skip, take, returnInlineCount, preserveCurrentState, noTracking, orderByDef, succeeded, complete) {
        if (!preserveCurrentState) clearAll();
           
        var hasEntitySearch = (typeof jEntitySearch !== 'undefined') && jEntitySearch !== null;
        if (hasEntitySearch && jEntitySearch.length > 1000) {
           $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/AddTcsParametroValorVariacaoGenericaP1EntitySearchId'),
               globalError: true,
               headers: managerAuth.getHeaders(),
               contentType: 'application/json',
               async: true,
               cache: false,
               data: JSON.stringify([jEntitySearch]),
               success: function (response) {
                   var query = EntityQuery.from('GetTcsParametroValorVariacaoGenericaP1ByEntitySearchIdNoAssociations').noTracking(noTracking)
                           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
                               ;
    
               query = query.withParameters({ entitySearchId: response });
    
                   if (take > 0)
                       query = query.skip(skip).take(take);
                   if (returnInlineCount)
                       query = query.inlineCount(true);
    
                   return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
               }
            });
    }
    else {
           var query = EntityQuery.from('GetTcsParametroValorVariacaoGenericaP1ByEntitySearchNoAssociations').noTracking(noTracking)
           .orderBy((isNullOrEmpty(orderByDef) ? 'IdParametroValor asc' : orderByDef))
           ;
    
           if (hasEntitySearch)
               query = query.withParameters({ jEntitySearch: jEntitySearch });
           if (take > 0)
               query = query.skip(skip).take(take);
           if (returnInlineCount)
                query = query.inlineCount(true);
    
           return manager.executeQuery(query).fail(queryFailed).then(succeeded).fin(complete);
       };
    }
    //#endregion
    
    // Create the data update event.
    var dataUpdateEvent = document.createEvent('Event');
    // Define that the event name is 'ParametroContext_DataUpdate'.
    var contextUpdtEvt = 'ParametroContext_DataUpdate_' + getNewGuid();
    dataUpdateEvent.initEvent(contextUpdtEvt, true, true);
    
    //#region LookUps Finalizers
     var finalizeAllLookUpTcsAplicativo = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            if (propertyName === '' || propertyName === 'IdTcsAplicativo') {
               if (selectedElement.hasOwnProperty('IdTcsAplicativo') && replaceTo.hasOwnProperty('IdTcsAplicativo'))
               {
                   setAbsoluteValue(replaceTo, 'IdTcsAplicativo', getAbsoluteValue(selectedElement['IdTcsAplicativo']));
               }
               else if (replaceTo.hasOwnProperty('IdTcsAplicativo')) {
                   setAbsoluteValue(replaceTo, 'IdTcsAplicativo', 0);
               }
            }
            if (propertyName === '' || propertyName === 'DescricaoAplicativo') {
               if (selectedElement.hasOwnProperty('DescricaoAplicativo') && replaceTo.hasOwnProperty('DescricaoAplicativo'))
               {
                   setAbsoluteValue(replaceTo, 'DescricaoAplicativo', getAbsoluteValue(selectedElement['DescricaoAplicativo']));
               }
               else if (replaceTo.hasOwnProperty('DescricaoAplicativo')) {
                   setAbsoluteValue(replaceTo, 'DescricaoAplicativo', null);
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpTcsAplicativo'))
                replaceTo.validatedlookupsArray.push('LookUpTcsAplicativo');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
    };
    
    function clearLookUpTcsAplicativo(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'IdTcsAplicativo', 0);
        setAbsoluteValue(replaceTo, 'DescricaoAplicativo', null);
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
     var finalizeAllLookUpTcsParametroGrupoAutorizacao = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            if (propertyName === '' || propertyName === 'DescGrupoParametro') {
               if (selectedElement.hasOwnProperty('DescGrupoParametro') && replaceTo.hasOwnProperty('DescGrupoParametro'))
               {
                   setAbsoluteValue(replaceTo, 'DescGrupoParametro', getAbsoluteValue(selectedElement['DescGrupoParametro']));
               }
               else if (replaceTo.hasOwnProperty('DescGrupoParametro')) {
                   setAbsoluteValue(replaceTo, 'DescGrupoParametro', '');
               }
            }
            if (propertyName === '' || propertyName === 'IdGrupoParametro') {
               if (selectedElement.hasOwnProperty('IdGrupoParametro') && replaceTo.hasOwnProperty('IdGrupoParametro'))
               {
                   setAbsoluteValue(replaceTo, 'IdGrupoParametro', getAbsoluteValue(selectedElement['IdGrupoParametro']));
               }
               else if (replaceTo.hasOwnProperty('IdGrupoParametro')) {
                   setAbsoluteValue(replaceTo, 'IdGrupoParametro', 0);
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpTcsParametroGrupoAutorizacao'))
                replaceTo.validatedlookupsArray.push('LookUpTcsParametroGrupoAutorizacao');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
    };
    
    function clearLookUpTcsParametroGrupoAutorizacao(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'DescGrupoParametro', '');
        setAbsoluteValue(replaceTo, 'IdGrupoParametro', 0);
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
     var finalizeAllLookUpTcsTabelaAutorizacaoSelecao = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            if (propertyName === '' || propertyName === 'DescTabela') {
               if (selectedElement.hasOwnProperty('DescTabela') && replaceTo.hasOwnProperty('DescTabela'))
               {
                   setAbsoluteValue(replaceTo, 'DescTabela', getAbsoluteValue(selectedElement['DescTabela']));
               }
               else if (replaceTo.hasOwnProperty('DescTabela')) {
                   setAbsoluteValue(replaceTo, 'DescTabela', '');
               }
            }
            if (propertyName === '' || propertyName === 'NomeTabela') {
               if (selectedElement.hasOwnProperty('NomeTabela') && replaceTo.hasOwnProperty('NomeTabela'))
               {
                   setAbsoluteValue(replaceTo, 'NomeTabela', getAbsoluteValue(selectedElement['NomeTabela']));
               }
               else if (replaceTo.hasOwnProperty('NomeTabela')) {
                   setAbsoluteValue(replaceTo, 'NomeTabela', '');
               }
            }
            if (propertyName === '' || propertyName === 'UidTabela') {
               if (selectedElement.hasOwnProperty('UidTabela') && replaceTo.hasOwnProperty('UidTabela'))
               {
                   setAbsoluteValue(replaceTo, 'UidTabela', getAbsoluteValue(selectedElement['UidTabela']));
               }
               else if (replaceTo.hasOwnProperty('UidTabela')) {
                   setAbsoluteValue(replaceTo, 'UidTabela', '00000000-0000-0000-0000-000000000000');
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpTcsTabelaAutorizacaoSelecao'))
                replaceTo.validatedlookupsArray.push('LookUpTcsTabelaAutorizacaoSelecao');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
    };
    
    function clearLookUpTcsTabelaAutorizacaoSelecao(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'DescTabela', '');
        setAbsoluteValue(replaceTo, 'NomeTabela', '');
        setAbsoluteValue(replaceTo, 'UidTabela', '00000000-0000-0000-0000-000000000000');
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
     var finalizeAllLookTcsParametroUsuario = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        var originalRow = replaceTo;
        var hasDisconsideredElement = false;
        var parent = getAbsoluteValue(replaceTo.TcsParametro);
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            var isInvalidElement = false;
            var curList = parent.TcsParametroValorUsuarioList();
            for (var idx = 0; idx < curList.length; idx++) {
               if (curList[idx] != originalRow && getAbsoluteValue(curList[idx].IdUsuario) === getAbsoluteValue(selectedElement['IdUsuario'])) {
                   isInvalidElement = true;
                   hasDisconsideredElement = true;
                   break;
               }
            }
            if (isInvalidElement) continue;
            if (isUsedOriginalRow) {
                replaceTo = lookupInfo.vm.createTcsParametroValorUsuario(parent);
            }
            else {
                isUsedOriginalRow = true;
            }
            if (propertyName === '' || propertyName === 'IdUsuario') {
               if (selectedElement.hasOwnProperty('IdUsuario') && replaceTo.hasOwnProperty('IdUsuario'))
               {
                   setAbsoluteValue(replaceTo, 'IdUsuario', getAbsoluteValue(selectedElement['IdUsuario']));
               }
               else if (replaceTo.hasOwnProperty('IdUsuario')) {
                   setAbsoluteValue(replaceTo, 'IdUsuario', 0);
               }
            }
            if (propertyName === '' || propertyName === 'NomeUsuario') {
               if (selectedElement.hasOwnProperty('NomeUsuario') && replaceTo.hasOwnProperty('NomeUsuario'))
               {
                   setAbsoluteValue(replaceTo, 'NomeUsuario', getAbsoluteValue(selectedElement['NomeUsuario']));
               }
               else if (replaceTo.hasOwnProperty('NomeUsuario')) {
                   setAbsoluteValue(replaceTo, 'NomeUsuario', '');
               }
            }
            if (propertyName === '' || propertyName === 'ChaveSelecao') {
               if (selectedElement.hasOwnProperty('ChaveSelecao') && replaceTo.hasOwnProperty('ChaveSelecao'))
               {
                   setAbsoluteValue(replaceTo, 'ChaveSelecao', getAbsoluteValue(selectedElement['ChaveSelecao']));
               }
               else if (replaceTo.hasOwnProperty('ChaveSelecao')) {
                   setAbsoluteValue(replaceTo, 'ChaveSelecao', null);
               }
            }
            if (propertyName === '' || propertyName === 'UidUsuarioString') {
               if (selectedElement.hasOwnProperty('UidUsuarioString') && replaceTo.hasOwnProperty('UidUsuarioString'))
               {
                   setAbsoluteValue(replaceTo, 'UidUsuarioString', getAbsoluteValue(selectedElement['UidUsuarioString']));
               }
               else if (replaceTo.hasOwnProperty('UidUsuarioString')) {
                   setAbsoluteValue(replaceTo, 'UidUsuarioString', '');
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookTcsParametroUsuario'))
                replaceTo.validatedlookupsArray.push('LookTcsParametroUsuario');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
        if (hasDisconsideredElement) {
           clearLookTcsParametroUsuario(replaceTo);
           app.showMessage('Itens que já estão sendo utilizados foram desconsiderados nessa seleção!', 'Informação', ['Ok']);
        }
    };
    
    function clearLookTcsParametroUsuario(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'IdUsuario', 0);
        setAbsoluteValue(replaceTo, 'NomeUsuario', '');
        setAbsoluteValue(replaceTo, 'ChaveSelecao', null);
        setAbsoluteValue(replaceTo, 'UidUsuarioString', '');
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
     var finalizeAllLookUpParametroRede = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        var originalRow = replaceTo;
        var hasDisconsideredElement = false;
        var parent = getAbsoluteValue(replaceTo.TcsParametro);
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            var isInvalidElement = false;
            var curList = parent.TcsParametroValorRedeList();
            for (var idx = 0; idx < curList.length; idx++) {
               if (curList[idx] != originalRow && getAbsoluteValue(curList[idx].IdBandeiraRedeParam) === getAbsoluteValue(selectedElement['IdBandeiraRedeParam'])) {
                   isInvalidElement = true;
                   hasDisconsideredElement = true;
                   break;
               }
            }
            if (isInvalidElement) continue;
            if (isUsedOriginalRow) {
                replaceTo = lookupInfo.vm.createTcsParametroValorRede(parent);
            }
            else {
                isUsedOriginalRow = true;
            }
            if (propertyName === '' || propertyName === 'IdBandeiraRedeParam') {
               if (selectedElement.hasOwnProperty('IdBandeiraRedeParam') && replaceTo.hasOwnProperty('IdBandeiraRedeParam'))
               {
                   setAbsoluteValue(replaceTo, 'IdBandeiraRedeParam', getAbsoluteValue(selectedElement['IdBandeiraRedeParam']));
               }
               else if (replaceTo.hasOwnProperty('IdBandeiraRedeParam')) {
                   setAbsoluteValue(replaceTo, 'IdBandeiraRedeParam', 0);
               }
            }
            if (propertyName === '' || propertyName === 'CodBandeiraRede') {
               if (selectedElement.hasOwnProperty('CodBandeiraRede') && replaceTo.hasOwnProperty('CodBandeiraRede'))
               {
                   setAbsoluteValue(replaceTo, 'CodBandeiraRede', getAbsoluteValue(selectedElement['CodBandeiraRede']));
               }
               else if (replaceTo.hasOwnProperty('CodBandeiraRede')) {
                   setAbsoluteValue(replaceTo, 'CodBandeiraRede', '');
               }
            }
            if (propertyName === '' || propertyName === 'DescBandeiraRede') {
               if (selectedElement.hasOwnProperty('DescBandeiraRede') && replaceTo.hasOwnProperty('DescBandeiraRede'))
               {
                   setAbsoluteValue(replaceTo, 'DescBandeiraRede', getAbsoluteValue(selectedElement['DescBandeiraRede']));
               }
               else if (replaceTo.hasOwnProperty('DescBandeiraRede')) {
                   setAbsoluteValue(replaceTo, 'DescBandeiraRede', '');
               }
            }
            if (propertyName === '' || propertyName === 'ChaveSelecao') {
               if (selectedElement.hasOwnProperty('ChaveSelecao') && replaceTo.hasOwnProperty('ChaveSelecao'))
               {
                   setAbsoluteValue(replaceTo, 'ChaveSelecao', getAbsoluteValue(selectedElement['ChaveSelecao']));
               }
               else if (replaceTo.hasOwnProperty('ChaveSelecao')) {
                   setAbsoluteValue(replaceTo, 'ChaveSelecao', null);
               }
            }
            if (propertyName === '' || propertyName === 'IdBandeiraRedeString') {
               if (selectedElement.hasOwnProperty('IdBandeiraRedeString') && replaceTo.hasOwnProperty('IdBandeiraRedeString'))
               {
                   setAbsoluteValue(replaceTo, 'IdBandeiraRedeString', getAbsoluteValue(selectedElement['IdBandeiraRedeString']));
               }
               else if (replaceTo.hasOwnProperty('IdBandeiraRedeString')) {
                   setAbsoluteValue(replaceTo, 'IdBandeiraRedeString', '');
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpParametroRede'))
                replaceTo.validatedlookupsArray.push('LookUpParametroRede');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
        if (hasDisconsideredElement) {
           clearLookUpParametroRede(replaceTo);
           app.showMessage('Itens que já estão sendo utilizados foram desconsiderados nessa seleção!', 'Informação', ['Ok']);
        }
    };
    
    function clearLookUpParametroRede(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'IdBandeiraRedeParam', 0);
        setAbsoluteValue(replaceTo, 'CodBandeiraRede', '');
        setAbsoluteValue(replaceTo, 'DescBandeiraRede', '');
        setAbsoluteValue(replaceTo, 'ChaveSelecao', null);
        setAbsoluteValue(replaceTo, 'IdBandeiraRedeString', '');
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
     var finalizeAllLookUpParametroGpecon = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        var originalRow = replaceTo;
        var hasDisconsideredElement = false;
        var parent = getAbsoluteValue(replaceTo.TcsParametro);
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            var isInvalidElement = false;
            var curList = parent.TcsParametroValorGpeconList();
            for (var idx = 0; idx < curList.length; idx++) {
               if (curList[idx] != originalRow && getAbsoluteValue(curList[idx].IdGpecon) === getAbsoluteValue(selectedElement['IdGpecon'])) {
                   isInvalidElement = true;
                   hasDisconsideredElement = true;
                   break;
               }
            }
            if (isInvalidElement) continue;
            if (isUsedOriginalRow) {
                replaceTo = lookupInfo.vm.createTcsParametroValorGpecon(parent);
            }
            else {
                isUsedOriginalRow = true;
            }
            if (propertyName === '' || propertyName === 'IdGpecon') {
               if (selectedElement.hasOwnProperty('IdGpecon') && replaceTo.hasOwnProperty('IdGpecon'))
               {
                   setAbsoluteValue(replaceTo, 'IdGpecon', getAbsoluteValue(selectedElement['IdGpecon']));
               }
               else if (replaceTo.hasOwnProperty('IdGpecon')) {
                   setAbsoluteValue(replaceTo, 'IdGpecon', 0);
               }
            }
            if (propertyName === '' || propertyName === 'DescGrupoEconomico') {
               if (selectedElement.hasOwnProperty('DescGrupoEconomico') && replaceTo.hasOwnProperty('DescGrupoEconomico'))
               {
                   setAbsoluteValue(replaceTo, 'DescGrupoEconomico', getAbsoluteValue(selectedElement['DescGrupoEconomico']));
               }
               else if (replaceTo.hasOwnProperty('DescGrupoEconomico')) {
                   setAbsoluteValue(replaceTo, 'DescGrupoEconomico', '');
               }
            }
            if (propertyName === '' || propertyName === 'IdGpeconString') {
               if (selectedElement.hasOwnProperty('IdGpeconString') && replaceTo.hasOwnProperty('IdGpeconString'))
               {
                   setAbsoluteValue(replaceTo, 'IdGpeconString', getAbsoluteValue(selectedElement['IdGpeconString']));
               }
               else if (replaceTo.hasOwnProperty('IdGpeconString')) {
                   setAbsoluteValue(replaceTo, 'IdGpeconString', '');
               }
            }
            if (propertyName === '' || propertyName === 'ChaveSelecao') {
               if (selectedElement.hasOwnProperty('ChaveSelecao') && replaceTo.hasOwnProperty('ChaveSelecao'))
               {
                   setAbsoluteValue(replaceTo, 'ChaveSelecao', getAbsoluteValue(selectedElement['ChaveSelecao']));
               }
               else if (replaceTo.hasOwnProperty('ChaveSelecao')) {
                   setAbsoluteValue(replaceTo, 'ChaveSelecao', null);
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpParametroGpecon'))
                replaceTo.validatedlookupsArray.push('LookUpParametroGpecon');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
        if (hasDisconsideredElement) {
           clearLookUpParametroGpecon(replaceTo);
           app.showMessage('Itens que já estão sendo utilizados foram desconsiderados nessa seleção!', 'Informação', ['Ok']);
        }
    };
    
    function clearLookUpParametroGpecon(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'IdGpecon', 0);
        setAbsoluteValue(replaceTo, 'DescGrupoEconomico', '');
        setAbsoluteValue(replaceTo, 'IdGpeconString', '');
        setAbsoluteValue(replaceTo, 'ChaveSelecao', null);
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
     var finalizeAllLookUpParametroFilial = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        var originalRow = replaceTo;
        var hasDisconsideredElement = false;
        var parent = getAbsoluteValue(replaceTo.TcsParametro);
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            var isInvalidElement = false;
            var curList = parent.TcsParametroValorFilialList();
            for (var idx = 0; idx < curList.length; idx++) {
               if (curList[idx] != originalRow && getAbsoluteValue(curList[idx].IdFilialPfj) === getAbsoluteValue(selectedElement['IdFilialPfj'])) {
                   isInvalidElement = true;
                   hasDisconsideredElement = true;
                   break;
               }
            }
            if (isInvalidElement) continue;
            if (isUsedOriginalRow) {
                replaceTo = lookupInfo.vm.createTcsParametroValorFilial(parent);
            }
            else {
                isUsedOriginalRow = true;
            }
            if (propertyName === '' || propertyName === 'CodigoFilial') {
               if (selectedElement.hasOwnProperty('CodigoFilial') && replaceTo.hasOwnProperty('CodigoFilial'))
               {
                   setAbsoluteValue(replaceTo, 'CodigoFilial', getAbsoluteValue(selectedElement['CodigoFilial']));
               }
               else if (replaceTo.hasOwnProperty('CodigoFilial')) {
                   setAbsoluteValue(replaceTo, 'CodigoFilial', null);
               }
            }
            if (propertyName === '' || propertyName === 'IdFilialPfj') {
               if (selectedElement.hasOwnProperty('IdFilialPfj') && replaceTo.hasOwnProperty('IdFilialPfj'))
               {
                   setAbsoluteValue(replaceTo, 'IdFilialPfj', getAbsoluteValue(selectedElement['IdFilialPfj']));
               }
               else if (replaceTo.hasOwnProperty('IdFilialPfj')) {
                   setAbsoluteValue(replaceTo, 'IdFilialPfj', 0);
               }
            }
            if (propertyName === '' || propertyName === 'IdFilialString') {
               if (selectedElement.hasOwnProperty('IdFilialString') && replaceTo.hasOwnProperty('IdFilialString'))
               {
                   setAbsoluteValue(replaceTo, 'IdFilialString', getAbsoluteValue(selectedElement['IdFilialString']));
               }
               else if (replaceTo.hasOwnProperty('IdFilialString')) {
                   setAbsoluteValue(replaceTo, 'IdFilialString', '');
               }
            }
            if (propertyName === '' || propertyName === 'NomeFilial') {
               if (selectedElement.hasOwnProperty('NomeFilial') && replaceTo.hasOwnProperty('NomeFilial'))
               {
                   setAbsoluteValue(replaceTo, 'NomeFilial', getAbsoluteValue(selectedElement['NomeFilial']));
               }
               else if (replaceTo.hasOwnProperty('NomeFilial')) {
                   setAbsoluteValue(replaceTo, 'NomeFilial', null);
               }
            }
            if (propertyName === '' || propertyName === 'ChaveSelecao') {
               if (selectedElement.hasOwnProperty('ChaveSelecao') && replaceTo.hasOwnProperty('ChaveSelecao'))
               {
                   setAbsoluteValue(replaceTo, 'ChaveSelecao', getAbsoluteValue(selectedElement['ChaveSelecao']));
               }
               else if (replaceTo.hasOwnProperty('ChaveSelecao')) {
                   setAbsoluteValue(replaceTo, 'ChaveSelecao', null);
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpParametroFilial'))
                replaceTo.validatedlookupsArray.push('LookUpParametroFilial');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
        if (hasDisconsideredElement) {
           clearLookUpParametroFilial(replaceTo);
           app.showMessage('Itens que já estão sendo utilizados foram desconsiderados nessa seleção!', 'Informação', ['Ok']);
        }
    };
    
    function clearLookUpParametroFilial(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'CodigoFilial', null);
        setAbsoluteValue(replaceTo, 'IdFilialPfj', 0);
        setAbsoluteValue(replaceTo, 'IdFilialString', '');
        setAbsoluteValue(replaceTo, 'NomeFilial', null);
        setAbsoluteValue(replaceTo, 'ChaveSelecao', null);
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
     var finalizeAllLookUpParametroLoja = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        var originalRow = replaceTo;
        var hasDisconsideredElement = false;
        var parent = getAbsoluteValue(replaceTo.TcsParametro);
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            var isInvalidElement = false;
            var curList = parent.TcsParametroValorLjvLojaList();
            for (var idx = 0; idx < curList.length; idx++) {
               if (curList[idx] != originalRow && getAbsoluteValue(curList[idx].IdLoja) === getAbsoluteValue(selectedElement['IdLoja'])) {
                   isInvalidElement = true;
                   hasDisconsideredElement = true;
                   break;
               }
            }
            if (isInvalidElement) continue;
            if (isUsedOriginalRow) {
                replaceTo = lookupInfo.vm.createTcsParametroValorLjvLoja(parent);
            }
            else {
                isUsedOriginalRow = true;
            }
            if (propertyName === '' || propertyName === 'CodLoja') {
               if (selectedElement.hasOwnProperty('CodLoja') && replaceTo.hasOwnProperty('CodLoja'))
               {
                   setAbsoluteValue(replaceTo, 'CodLoja', getAbsoluteValue(selectedElement['CodLoja']));
               }
               else if (replaceTo.hasOwnProperty('CodLoja')) {
                   setAbsoluteValue(replaceTo, 'CodLoja', '');
               }
            }
            if (propertyName === '' || propertyName === 'DescLoja') {
               if (selectedElement.hasOwnProperty('DescLoja') && replaceTo.hasOwnProperty('DescLoja'))
               {
                   setAbsoluteValue(replaceTo, 'DescLoja', getAbsoluteValue(selectedElement['DescLoja']));
               }
               else if (replaceTo.hasOwnProperty('DescLoja')) {
                   setAbsoluteValue(replaceTo, 'DescLoja', '');
               }
            }
            if (propertyName === '' || propertyName === 'IdLoja') {
               if (selectedElement.hasOwnProperty('IdLoja') && replaceTo.hasOwnProperty('IdLoja'))
               {
                   setAbsoluteValue(replaceTo, 'IdLoja', getAbsoluteValue(selectedElement['IdLoja']));
               }
               else if (replaceTo.hasOwnProperty('IdLoja')) {
                   setAbsoluteValue(replaceTo, 'IdLoja', 0);
               }
            }
            if (propertyName === '' || propertyName === 'IdLojaString') {
               if (selectedElement.hasOwnProperty('IdLojaString') && replaceTo.hasOwnProperty('IdLojaString'))
               {
                   setAbsoluteValue(replaceTo, 'IdLojaString', getAbsoluteValue(selectedElement['IdLojaString']));
               }
               else if (replaceTo.hasOwnProperty('IdLojaString')) {
                   setAbsoluteValue(replaceTo, 'IdLojaString', '');
               }
            }
            if (propertyName === '' || propertyName === 'ChaveSelecao') {
               if (selectedElement.hasOwnProperty('ChaveSelecao') && replaceTo.hasOwnProperty('ChaveSelecao'))
               {
                   setAbsoluteValue(replaceTo, 'ChaveSelecao', getAbsoluteValue(selectedElement['ChaveSelecao']));
               }
               else if (replaceTo.hasOwnProperty('ChaveSelecao')) {
                   setAbsoluteValue(replaceTo, 'ChaveSelecao', null);
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpParametroLoja'))
                replaceTo.validatedlookupsArray.push('LookUpParametroLoja');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
        if (hasDisconsideredElement) {
           clearLookUpParametroLoja(replaceTo);
           app.showMessage('Itens que já estão sendo utilizados foram desconsiderados nessa seleção!', 'Informação', ['Ok']);
        }
    };
    
    function clearLookUpParametroLoja(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'CodLoja', '');
        setAbsoluteValue(replaceTo, 'DescLoja', '');
        setAbsoluteValue(replaceTo, 'IdLoja', 0);
        setAbsoluteValue(replaceTo, 'IdLojaString', '');
        setAbsoluteValue(replaceTo, 'ChaveSelecao', null);
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
     var finalizeAllLookUpTcsTabelaAutorizacaoC = function (replaceTo, selectedElements, propertyName, lookupInfo) {
        if (!replaceTo || !selectedElements)
            return;
        if (!Array.isArray(selectedElements)) {
            selectedElements = [selectedElements];
        }
        var parent = getAbsoluteValue(replaceTo.TcsParametro);
        isFinalizingLookup(true);
        if (!propertyName)
            propertyName = '';
        var isUsedOriginalRow = false;
        for (var i = 0; i < selectedElements.length; i++)
        {
            var selectedElement = selectedElements[i];
            if (isUsedOriginalRow) {
                replaceTo = lookupInfo.vm.createTcsParametroValorVariacaoGenerica(parent);
            }
            else {
                isUsedOriginalRow = true;
            }
            if (propertyName === '' || propertyName === 'UidTabela') {
               if (selectedElement.hasOwnProperty('UidTabela') && replaceTo.hasOwnProperty('UidTabela'))
               {
                   setAbsoluteValue(replaceTo, 'UidTabela', getAbsoluteValue(selectedElement['UidTabela']));
               }
               else if (replaceTo.hasOwnProperty('UidTabela')) {
                   setAbsoluteValue(replaceTo, 'UidTabela', null);
               }
            }
            if (propertyName === '' || propertyName === 'NomeTabela') {
               if (selectedElement.hasOwnProperty('NomeTabela') && replaceTo.hasOwnProperty('NomeTabela'))
               {
                   setAbsoluteValue(replaceTo, 'NomeTabela', getAbsoluteValue(selectedElement['NomeTabela']));
               }
               else if (replaceTo.hasOwnProperty('NomeTabela')) {
                   setAbsoluteValue(replaceTo, 'NomeTabela', '');
               }
            }
            if (replaceTo.validatedlookupsArray && !replaceTo.validatedlookupsArray.contains('LookUpTcsTabelaAutorizacaoC'))
                replaceTo.validatedlookupsArray.push('LookUpTcsTabelaAutorizacaoC');
        }
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        document.dispatchEvent(dataUpdateEvent);
        isFinalizingLookup(false);
    };
    
    function clearLookUpTcsTabelaAutorizacaoC(replaceTo) {
        if (!replaceTo)
            return;
        isClearingLookup(true);
        setAbsoluteValue(replaceTo, 'UidTabela', null);
        setAbsoluteValue(replaceTo, 'NomeTabela', '');
        isClearingLookup(false);
        //Trigger context data update event
        if (replaceTo.isPOCO) vm.refreshCurrentBind();
        setTimeout(function () {document.dispatchEvent(dataUpdateEvent);}, 100);
    }
    //#endregion
    
    var cancelChanges = function(dataForUndo) {
        if (dataForUndo && dataForUndo.length > 0) {
            dataForUndo.forEach(function(e) { e.restoreOriginal(); } ); 
        } else {
            manager.rejectChanges();
        }
    };
    
    var hasEmptyRequiredFilters = function() {
        return false;
    };
    
    var getEntityProperty = function (entityName, propertyName) {
        for (var i = 0; i < metadataInfo[entityName].length; i++) {
            if (metadataInfo[entityName][i].key === propertyName)
                return metadataInfo[entityName][i];
        }
        return null;
    };
    var getViewInfo = function (entityName) {
        var result = [];
        if (metadataInfo[entityName])
        {
            var viewInfoElements = metadataInfo[entityName];
            for (var i = 0; i < viewInfoElements.length; i++) {
                var selectedElement = viewInfoElements[i];
                if (!selectedElement.hidden && (selectedElement.dataType === 'string' || selectedElement.dataType === 'number'))
                {
                    if (strLeft(selectedElement.key, 3) === 'Cod' || strLeft(selectedElement.key, 2) === 'Id' || strLeft(selectedElement.key, 6) === 'Numero' || strLeft(selectedElement.key, 6) === 'Number') {
                        result.push(selectedElement);
                    }
                }
            }
            for (var i = 0; i < viewInfoElements.length; i++) {
                var selectedElement = viewInfoElements[i];
                if (!selectedElement.hidden && (selectedElement.dataType === 'string')) {
                    if (strLeft(selectedElement.key, 4) === 'Nome' || strLeft(selectedElement.key, 4) === 'Name' || strLeft(selectedElement.key, 4) === 'Desc' || strLeft(selectedElement.key, 6) === 'Titulo' || strLeft(selectedElement.key, 5) === 'Title') {
                        result.push(selectedElement);
                    }
                }
            }
            for (var i = 0; i < viewInfoElements.length; i++) {
                var selectedElement = viewInfoElements[i];
                if (!selectedElement.hidden && selectedElement.dataType === 'date')
                {                
                   result.push(selectedElement);
                }
            }
        }
        return result;
    };
    
    var getChanges = function() {
       return manager.getEntities(null, [breeze.EntityState.Added, breeze.EntityState.Modified, breeze.EntityState.Deleted]);
    };
    
    var hasValidationErrors = function(savingData) {
        if (savingData instanceof Array) {
           for (var idx = 0; idx < savingData.length; idx++) {
               var entity = savingData[idx];
               if (entity.ChangeState && entity.getValidationErrors && ['I', 'U'].indexOf(entity.ChangeState) >=0) {
                  var errors = entity.getValidationErrors();
                  if (errors.length > 0) {
                       showModalAlert('Campos obrigatórios não estão preenchidos.', errors);
                       return true;
                   }
               }
           }
        }
        else {
           var changes = manager.getEntities(null, [breeze.EntityState.Added, breeze.EntityState.Modified]);
           for (var idxChange = 0; idxChange < changes.length; idxChange++) {
              changes[idxChange].setParentAsModified();
           }
           changes = manager.getEntities(null, [breeze.EntityState.Added, breeze.EntityState.Modified]);
           for (var idxChange = 0; idxChange < changes.length; idxChange++) {
              var entity = changes[idxChange];
              var isOk = entity.entityAspect.validateEntity();
              if (!isOk) {
                  var errors = entity.entityAspect.getValidationErrors();
                  var strErrors = [];
                  for (var idx = 0; idx < errors.length; idx++) {
                      var errorMsg = errors[idx].errorMessage;
                      var propName = strExtract(errorMsg, "'", "'");
                      var propDisplay = entity.getDisplayName(propName);
                      errorMsg = errorMsg.replace("'" + propName + "'", "'" + propDisplay + "'" + (managerAuth.shellMode=='DEV' ? " (" + entity.typeName + "." + propName + ")": ""));
                      strErrors.push(translateError(errorMsg));
                  }
                  showModalAlert('Campos obrigatórios não estão preenchidos.', strErrors);
                  return true;
              }
           }
        }
        return false;
    };
    
    var saveChanges = function(saveSucceeded, saveFailed, fin, saveNoTRack) {
        if (saveNoTRack === true) {
            var dataForSaving = JSON.stringify(_.map(vm.getDataForSaving(), function(entity){ return entity.getPrimitiveDTO(entity.ChangeState != 'D'); }));
            return $.ajax({
               type: 'POST',
               crossDomain: true,
               url: getServiceAddress('LinxFrameworkParametro/Save' + vm.rootDataTypeName),
               globalError: false,
               contentType: 'application/json',
               async: true,
               cache: false,
               data: dataForSaving,
               success: function (response) {
                      success(response);
               },
               error: function (jqXHR, textStatus, errorThrown) {
                      failed({ message: jqXHR.responseJSON.ExceptionMessage });
               }
            });
        }
        else {
            return manager.saveChanges()
                   .fail(failed).then(success);
        }
    
        function success(result) {
            if (fin) fin();
            if (saveNoTRack === true && result.length > 0) {
               for (var idx = 0; idx < result.length; idx++) { dataContext.initializePOCO(result[idx], vm.rootDataTypeName); }
            }
            else if (result != null && result.keyMappings != null && result.keyMappings.length > 0) {
                for (var idx = 0; idx < result.keyMappings.length; idx++) {
                    if (result.keyMappings[idx].realValue == null) {
                       var entity = manager.getEntityByKey(result.keyMappings[idx].entityTypeName, result.keyMappings[idx].tempValue);
                       if (entity) manager.detachEntity(entity);
                    }
                }
                manager.acceptChanges();
            }
            if (saveSucceeded)
                saveSucceeded(result);
        }
    
        function failed(error) {
            if (fin) fin();
            if (error.message.indexOf('Internal Error in key fixup - unable to locate entity') == -1 && error.message.indexOf('An entity with this key is already in the cache:') == -1) {
               if (saveFailed)
                   saveFailed(error);
               var msg = error.message.replace('Fail by saving data:', '');
               showModalAlert('Falha ao salvar informações.', [ msg ]);
               error.message = msg;
               throw error;
           } else {
               manager.acceptChanges();
           }
        }
    };
    
    var acceptChanges = function () {
        return manager.acceptChanges();
    };
    
    var getEntities = function (entityName) {
        return manager.getEntities(entityName);
    };
    
    var notifyPresentation = function(dataSourceName) {
          if (dataContext.dataForUpdate !== '') {
           setTimeout(function () { notifyPresentation(dataSourceName); }, 100);
           return;
          }
          dataContext.dataForUpdate = dataSourceName;
          document.dispatchEvent(dataUpdateEvent);
          dataContext.dataForUpdate = '';
    };
    
    var getEntityInCache = function (entityName, propertiesReference) {
        var keys = [];
        if (!isNullOrEmpty(propertiesReference)) {
            for (var i = 0; i < metadataInfo[entityName].length; i++) {
                if (metadataInfo[entityName][i].isPartOfKey && !isNullOrEmpty(propertiesReference[metadataInfo[entityName][i].key]))
                    keys.push(propertiesReference[metadataInfo[entityName][i].key]);
            }
            if (keys.length == 0)
                return null;
            else {
                return manager.getEntityByKey(entityName, (keys.length == 1 ? keys[0] : keys));
            }
        }
        else
            return null;
    }
    
    var createEntity = function(entityName, initialValues, unchanged) {
        var entity = getEntityInCache(entityName, initialValues);
        if (!isNullOrEmpty(entity))
            entity.entityAspect.entityState == (unchanged === true ? breeze.EntityState.Unchanged : breeze.EntityState.Added);
        else 
            entity = manager.createEntity(entityName, initialValues, (unchanged === true ? breeze.EntityState.Unchanged : breeze.EntityState.Added));
        return entity;
    };
    
    var createFreeEntity = function(entityName) {
       return manager.createEntity(entityName, {}, breeze.EntityState.Detached);
    }
    
    var createTcsParametro = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametro', { IndicaEnviaPdv: false, IndicaParametroLinx: false, PermiteVariacaoPorEntidade: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorP = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorP', { PossuiVariacao: false, ValorParametroBool: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createParametroValor = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('ParametroValor', { TituloParametro: (-1 * getSequence('ParametroValor')).toString() });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorVariacaoP = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorVariacaoP', { PossuiVariacao: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroTabelaSelecao = function(parent, noCurrent) {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroTabelaSelecao', { TcsParametro: parent, EntityUniqueKey: getNewGuid() });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        if (noCurrent !== true) parent.currentTcsParametroTabelaSelecao(entity);
        if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroTabelaSelecaoList === 'function') && parent.TcsParametroTabelaSelecaoList().length == 0) parent.setCurrentDetails('TcsParametroTabelaSelecao');
        if (entity.setParentAsModified) entity.setParentAsModified();
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValor = function(parent, noCurrent) {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValor', { TcsParametro: parent, EntityUniqueKey: getNewGuid(), PossuiVariacao: false, ValorParametroBool: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        if (noCurrent !== true) parent.currentTcsParametroValor(entity);
        if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroValorList === 'function') && parent.TcsParametroValorList().length == 0) parent.setCurrentDetails('TcsParametroValor');
        if (entity.setParentAsModified) entity.setParentAsModified();
        enableChangeTrack = true;
        return entity;
    };
    
    var createParametroValorVariacao = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('ParametroValorVariacao', { TituloParametro: (-1 * getSequence('ParametroValorVariacao')).toString() });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsUsuarioParametro = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsUsuarioParametro');
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorUsuario = function(parent, noCurrent) {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorUsuario', { TcsParametro: parent, PossuiVariacao: false, ValorParametroBool: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        if (noCurrent !== true) parent.currentTcsParametroValorUsuario(entity);
        if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroValorUsuarioList === 'function') && parent.TcsParametroValorUsuarioList().length == 0) parent.setCurrentDetails('TcsParametroValorUsuario');
        if (entity.setParentAsModified) entity.setParentAsModified();
        enableChangeTrack = true;
        return entity;
    };
    
    var createTbcBandeiraRedeParametro = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TbcBandeiraRedeParametro');
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorRede = function(parent, noCurrent) {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorRede', { TcsParametro: parent, PossuiVariacao: false, ValorParametroBool: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        if (noCurrent !== true) parent.currentTcsParametroValorRede(entity);
        if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroValorRedeList === 'function') && parent.TcsParametroValorRedeList().length == 0) parent.setCurrentDetails('TcsParametroValorRede');
        if (entity.setParentAsModified) entity.setParentAsModified();
        enableChangeTrack = true;
        return entity;
    };
    
    var createTbcGrupoEconomicoParametro = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TbcGrupoEconomicoParametro');
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorGpecon = function(parent, noCurrent) {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorGpecon', { TcsParametro: parent, PossuiVariacao: false, ValorParametroBool: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        if (noCurrent !== true) parent.currentTcsParametroValorGpecon(entity);
        if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroValorGpeconList === 'function') && parent.TcsParametroValorGpeconList().length == 0) parent.setCurrentDetails('TcsParametroValorGpecon');
        if (entity.setParentAsModified) entity.setParentAsModified();
        enableChangeTrack = true;
        return entity;
    };
    
    var createTbcFilialParametro = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TbcFilialParametro');
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorFilial = function(parent, noCurrent) {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorFilial', { TcsParametro: parent, PossuiVariacao: false, ValorParametroBool: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        if (noCurrent !== true) parent.currentTcsParametroValorFilial(entity);
        if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroValorFilialList === 'function') && parent.TcsParametroValorFilialList().length == 0) parent.setCurrentDetails('TcsParametroValorFilial');
        if (entity.setParentAsModified) entity.setParentAsModified();
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorLjvLoja = function(parent, noCurrent) {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorLjvLoja', { TcsParametro: parent, PossuiVariacao: false, ValorParametroBool: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        if (noCurrent !== true) parent.currentTcsParametroValorLjvLoja(entity);
        if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroValorLjvLojaList === 'function') && parent.TcsParametroValorLjvLojaList().length == 0) parent.setCurrentDetails('TcsParametroValorLjvLoja');
        if (entity.setParentAsModified) entity.setParentAsModified();
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorVariacaoGenericaP = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorVariacaoGenericaP', { PossuiVariacao: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorVariacaoGenerica = function(parent, noCurrent) {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorVariacaoGenerica', { TcsParametro: parent, PossuiVariacao: false, ValorParametroBool: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        if (noCurrent !== true) parent.currentTcsParametroValorVariacaoGenerica(entity);
        if (parent && (typeof parent.setCurrentDetails === 'function') && (typeof parent.TcsParametroValorVariacaoGenericaList === 'function') && parent.TcsParametroValorVariacaoGenericaList().length == 0) parent.setCurrentDetails('TcsParametroValorVariacaoGenerica');
        if (entity.setParentAsModified) entity.setParentAsModified();
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorP1 = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorP1', { PossuiVariacao: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createParametroInfo = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('ParametroInfo', { IdTcsAmbiente: (-1 * getSequence('ParametroInfo')), TituloParametro: (-1 * getSequence('ParametroInfo')).toString() });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorP2 = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorP2', { PossuiVariacao: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorLojaP = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorLojaP', { PossuiVariacao: false, ValorParametroBool: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createLjvLojaParametro = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('LjvLojaParametro');
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorUsuarioP = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorUsuarioP', { PossuiVariacao: false, ValorParametroBool: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorRedeP = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorRedeP', { PossuiVariacao: false, ValorParametroBool: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorGpeconP = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorGpeconP', { PossuiVariacao: false, ValorParametroBool: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorFilialP = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorFilialP', { PossuiVariacao: false, ValorParametroBool: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var createTcsParametroValorVariacaoGenericaP1 = function() {
        //Create entity instance
        enableChangeTrack = false;
        var entity = createEntity('TcsParametroValorVariacaoGenericaP1', { PossuiVariacao: false, ValorParametroBool: false });
        entity.setDefaults();
        if (typeof entity.OnAdding == 'function') {
            if (!entity.OnAdding()) { dataContext.deleteEntity(entity); return; }
        }
        enableChangeTrack = true;
        return entity;
    };
    
    var deleteEntity = function (entity) {
        entity.delete();
    };
    
    var detachEntity = function (entity) {
        if (!entity.isDetached()) entity.entityAspect.setDetached();
    };
    
    var attachEntity = function (entity) {
        manager.attachEntity(entity);
    };
    
    var clearAll = function () {
        manager.rejectChanges();
        manager.clear();
    };
    
    var executeQuery = function (getMethod, jEntitySearch, order, skip, take, noTracking, callBack) {
        var query = EntityQuery.from(getMethod).noTracking(noTracking)
        .orderBy(order);
    
        if ((typeof jEntitySearch !== 'undefined') && jEntitySearch !== null)
            query = query.withParameters({ jEntitySearch: jEntitySearch });
    
        if (take > 0)
           query = query.skip(skip).take(take);
        query = query.inlineCount(true);
    
        return manager.executeQuery(query)
        .fail(queryFailed).then(function (data) {
            if (callBack) {
                callBack(data.results);
            }
        });
    };
    var exportToExcel = function(entityName, jEntitySearch, translatedJEntitySearch, complete, columnsVisible) {
        var info = jQuery.grep(dataExportInfo[vm.rootDataTypeName], function (item, i) { return (item.name === entityName);});
        if (info == null || info.length === 0) {
            app.showMessage('Erro na exportação', 'Alerta', ['Ok']);
            return;
        }
        $.ajax({
           type: 'POST',
           crossDomain: true,
           url: getServiceAddress(info[0].actionExport),
           globalError: true,
           headers: managerAuth.getHeaders(),
           contentType: 'application/json',
           async: true,
           cache: false,
           data: JSON.stringify([jEntitySearch, translatedJEntitySearch, columnsVisible]),
           success: function (response) {
               if (response.startsWith('~/FileDownload/')) {
                    saveURL(getBaseServiceAddress(info[0].actionExport) + response.substr(1), entityName + '.xlsx');
               } else {
                    saveExcelBlob(entityName + '.xlsx', response);
               }
           },
           complete: function (jqXHR, textStatus) {
               if(complete) complete();
           },
           error: function (jqXHR, textStatus, errorThrown) {
           }
        });
    };
    var exportReportDataSource = function(complete) {
        $.ajax({
           type: 'GET',
           crossDomain: true,
           headers: managerAuth.getHeaders(),
           url: getServiceAddress("LinxFrameworkParametro/GetReportDataSource"),
           success: function (response) {
                  saveExcelBlob('datasource.ldsx', response);
           },
           complete: function (jqXHR, textStatus) {
                  if(complete) complete();
           },
           error: function (jqXHR, textStatus, errorThrown) {
                  alert('Erro na exportação do data source');
           }
        });
    };
    var exportTemplateReport = function(reportPath, complete) {
        $.ajax({
           type: 'GET',
           crossDomain: true,
           headers: managerAuth.getHeaders(),
           url: getServiceAddress("LinxFrameworkParametro/GetTemplateReport"),
           data: { reportPath: reportPath },
           success: function (response) {
                  saveExcelBlob(reportPath + '.lrtx', response);
           },
           complete: function (jqXHR, textStatus) {
                  if(complete) complete();
           },
           error: function (jqXHR, textStatus, errorThrown) {
                  alert('Erro na exportação do data source');
           }
        });
    };
    var exportToReport = function(reportName, entityName, jEntitySearch, translatedJEntitySearch, complete, columnsVisible, exportMedia) {
        var info = jQuery.grep(dataExportInfo[vm.rootDataTypeName], function (item, i) { return (item.name === entityName);});
        if (info == null || info.length === 0) {
            app.showMessage('Erro na exportação', 'Alerta', ['Ok']);
            return;
        }
        $.ajax({
           type: 'POST',
           crossDomain: true,
           headers: managerAuth.getHeaders(),
           url: getServiceAddress(info[0].actionReport),
           globalError: true,
           contentType: 'application/json',
           async: true,
           cache: false,
           data: JSON.stringify([ reportName, jEntitySearch, translatedJEntitySearch, columnsVisible, getServiceAddress(''), exportMedia ]),
           success: function (response) {
                  saveExcelBlob(entityName + '.lrtx', response);
           },
           complete: function (jqXHR, textStatus) {
                  if(complete) complete();
           },
           error: function (jqXHR, textStatus, errorThrown) {
           }
        });
    };
    
    var hasChanges = ko.observable(false);
    
    manager.hasChangesChanged.subscribe(function(eventArgs) {
        hasChanges(eventArgs.hasChanges);
    });
    
    //#region Internal methods
    
    function queryFailed(error) {
    }
    
    function log(msg, data) {
        logger.log(msg, data, system.getModuleId(dataContext), true);
    }
    
    function logError(msg, error) {
         logger.logError(msg, error, system.getModuleId(dataContext), true);
    }
    
    function loadParameters() {
     dataParameters.isLoaded = true;
    }
    
    //#endregion Internal methods
    //#region getWithBinding
    var createDTOEmpty = function (entityName) {
        var entityType = manager.metadataStore.getEntityType(entityName);
        var entity = {};
        for (var idx = 0; idx < entityType.dataProperties.length; idx++) {
            var prop = entityType.dataProperties[idx];
            entity[prop.name] = prop.defaultValue;
        }
        for (var idx = 0; idx < entityType.navigationProperties.length; idx++) {
            var navigationName = entityType.navigationProperties[idx].name;
            if (navigationName.endsWith('List'))
                entity['current' + navigationName.replace('List', '')] = null;
        }
        entity.isEmptyEntity = true;
        return entity;
    };
    var getWithBinding = function (binding, entityName) {
        if (typeof binding === 'function' && binding() != null)
            return binding;
        else
            return createDTOEmpty(entityName);
    }
    //#endregion getWithBinding
    
        var vm = null;
        var dataContext = {
            dataForUpdate: '',
            getPivotLayouts: getPivotLayouts,
            getServiceAddress: getServiceAddress,
            getBaseServiceAddress: getBaseServiceAddress,
            getDataFeedUrl: getDataFeedUrl,
            getDataServiceUrl: getDataServiceUrl,
            setServiceBusUrl: setServiceBusUrl,
            initializePOCO: initializePOCO,
            getWithBinding: getWithBinding,
            managerAuth: managerAuth,
            hasDataFeed: true,
            getAccessGroup: getAccessGroup,
            getNewGuid: getNewGuid,
            metadataInfo: metadataInfo,
            dataExportInfo: dataExportInfo,
            entityNames: entityNames,
            lookUpNames: lookUpNames,
            lookUpProperties: lookUpProperties,
            metadataStore: metadataStore,
            cancelChanges: cancelChanges,
            saveChanges: saveChanges,
            getChanges: getChanges,
            hasValidationErrors: hasValidationErrors,
            getEntityProperty: getEntityProperty,
            getViewInfo: getViewInfo,
            createEntity: createEntity,
            notifyPresentation: notifyPresentation,
            createFreeEntity: createFreeEntity,
            createTcsParametro: createTcsParametro,
            createTcsParametroValorP: createTcsParametroValorP,
            createParametroValor: createParametroValor,
            createTcsParametroValorVariacaoP: createTcsParametroValorVariacaoP,
            createTcsParametroTabelaSelecao: createTcsParametroTabelaSelecao,
            createTcsParametroValor: createTcsParametroValor,
            createParametroValorVariacao: createParametroValorVariacao,
            createTcsUsuarioParametro: createTcsUsuarioParametro,
            createTcsParametroValorUsuario: createTcsParametroValorUsuario,
            createTbcBandeiraRedeParametro: createTbcBandeiraRedeParametro,
            createTcsParametroValorRede: createTcsParametroValorRede,
            createTbcGrupoEconomicoParametro: createTbcGrupoEconomicoParametro,
            createTcsParametroValorGpecon: createTcsParametroValorGpecon,
            createTbcFilialParametro: createTbcFilialParametro,
            createTcsParametroValorFilial: createTcsParametroValorFilial,
            createTcsParametroValorLjvLoja: createTcsParametroValorLjvLoja,
            createTcsParametroValorVariacaoGenericaP: createTcsParametroValorVariacaoGenericaP,
            createTcsParametroValorVariacaoGenerica: createTcsParametroValorVariacaoGenerica,
            createTcsParametroValorP1: createTcsParametroValorP1,
            createParametroInfo: createParametroInfo,
            createTcsParametroValorP2: createTcsParametroValorP2,
            createTcsParametroValorLojaP: createTcsParametroValorLojaP,
            createLjvLojaParametro: createLjvLojaParametro,
            createTcsParametroValorUsuarioP: createTcsParametroValorUsuarioP,
            createTcsParametroValorRedeP: createTcsParametroValorRedeP,
            createTcsParametroValorGpeconP: createTcsParametroValorGpeconP,
            createTcsParametroValorFilialP: createTcsParametroValorFilialP,
            createTcsParametroValorVariacaoGenericaP1: createTcsParametroValorVariacaoGenericaP1,
            deleteEntity: deleteEntity,
            acceptChanges: acceptChanges,
            getEntities: getEntities,
            detachEntity: detachEntity,
            attachEntity: attachEntity,
            executeQuery: executeQuery,
            sharedData: [],
            clearAll: clearAll,
            hasChanges: hasChanges,
            dataDomains: dataDomains,
            dataParameters: dataParameters,
            loadParameters: loadParameters,
            exportToExcel: exportToExcel,
            exportToReport: exportToReport,
            exportReportDataSource: exportReportDataSource,
            exportTemplateReport: exportTemplateReport,
            businessAssemblyName: businessAssemblyName,
            controllerName: controllerName,
            getResultsCombo: getResultsCombo,
            clientFilterHasModified: clientFilterHasModified,
            lastClientFilterExpressions: {},
            breeze: breeze,
            contextUpdtEvt: contextUpdtEvt,
            setCurrentViewModel: function(vModel) { vm = vModel; },
                getLookTcsParametroUsuarioByEntitySearch: getLookTcsParametroUsuarioByEntitySearch,
            getLookUpParametroRedeByEntitySearch: getLookUpParametroRedeByEntitySearch,
            getLookUpParametroGpeconByEntitySearch: getLookUpParametroGpeconByEntitySearch,
            getLookUpParametroLojaByEntitySearch: getLookUpParametroLojaByEntitySearch,
            getLookUpParametroFilialByEntitySearch: getLookUpParametroFilialByEntitySearch,
            getLookUpTcsTabelaAutorizacaoCByEntitySearch: getLookUpTcsTabelaAutorizacaoCByEntitySearch,
            getLookUpTcsAplicativoByEntitySearch: getLookUpTcsAplicativoByEntitySearch,
        getLookUpTcsParametroGrupoAutorizacaoByEntitySearch: getLookUpTcsParametroGrupoAutorizacaoByEntitySearch,
            getBmEntityProperties: getBmEntityProperties,
            clearTcsParametro: clearTcsParametro,
            getTcsParametro: getTcsParametro,
            getTcsParametroByEntitySearchNoAssociations: getTcsParametroByEntitySearchNoAssociations,
            clearTcsParametroValorP: clearTcsParametroValorP,
            getTcsParametroValorP: getTcsParametroValorP,
            getTcsParametroValorPByEntitySearchNoAssociations: getTcsParametroValorPByEntitySearchNoAssociations,
            clearParametroValor: clearParametroValor,
            getParametroValor: getParametroValor,
            getParametroValorByEntitySearchNoAssociations: getParametroValorByEntitySearchNoAssociations,
            clearTcsParametroValorVariacaoP: clearTcsParametroValorVariacaoP,
            getTcsParametroValorVariacaoP: getTcsParametroValorVariacaoP,
            getTcsParametroValorVariacaoPByEntitySearchNoAssociations: getTcsParametroValorVariacaoPByEntitySearchNoAssociations,
            clearTcsParametroTabelaSelecao: clearTcsParametroTabelaSelecao,
            getTcsParametroTabelaSelecao: getTcsParametroTabelaSelecao,
            getTcsParametroTabelaSelecaoByEntitySearchNoAssociations: getTcsParametroTabelaSelecaoByEntitySearchNoAssociations,
            clearTcsParametroValor: clearTcsParametroValor,
            getTcsParametroValor: getTcsParametroValor,
            getTcsParametroValorByEntitySearchNoAssociations: getTcsParametroValorByEntitySearchNoAssociations,
            clearParametroValorVariacao: clearParametroValorVariacao,
            getParametroValorVariacao: getParametroValorVariacao,
            getParametroValorVariacaoByEntitySearchNoAssociations: getParametroValorVariacaoByEntitySearchNoAssociations,
            clearTcsUsuarioParametro: clearTcsUsuarioParametro,
            getTcsUsuarioParametro: getTcsUsuarioParametro,
            getTcsUsuarioParametroByEntitySearchNoAssociations: getTcsUsuarioParametroByEntitySearchNoAssociations,
            clearTcsParametroValorUsuario: clearTcsParametroValorUsuario,
            getTcsParametroValorUsuario: getTcsParametroValorUsuario,
            getTcsParametroValorUsuarioByEntitySearchNoAssociations: getTcsParametroValorUsuarioByEntitySearchNoAssociations,
            clearTbcBandeiraRedeParametro: clearTbcBandeiraRedeParametro,
            getTbcBandeiraRedeParametro: getTbcBandeiraRedeParametro,
            getTbcBandeiraRedeParametroByEntitySearchNoAssociations: getTbcBandeiraRedeParametroByEntitySearchNoAssociations,
            clearTcsParametroValorRede: clearTcsParametroValorRede,
            getTcsParametroValorRede: getTcsParametroValorRede,
            getTcsParametroValorRedeByEntitySearchNoAssociations: getTcsParametroValorRedeByEntitySearchNoAssociations,
            clearTbcGrupoEconomicoParametro: clearTbcGrupoEconomicoParametro,
            getTbcGrupoEconomicoParametro: getTbcGrupoEconomicoParametro,
            getTbcGrupoEconomicoParametroByEntitySearchNoAssociations: getTbcGrupoEconomicoParametroByEntitySearchNoAssociations,
            clearTcsParametroValorGpecon: clearTcsParametroValorGpecon,
            getTcsParametroValorGpecon: getTcsParametroValorGpecon,
            getTcsParametroValorGpeconByEntitySearchNoAssociations: getTcsParametroValorGpeconByEntitySearchNoAssociations,
            clearTbcFilialParametro: clearTbcFilialParametro,
            getTbcFilialParametro: getTbcFilialParametro,
            getTbcFilialParametroByEntitySearchNoAssociations: getTbcFilialParametroByEntitySearchNoAssociations,
            clearTcsParametroValorFilial: clearTcsParametroValorFilial,
            getTcsParametroValorFilial: getTcsParametroValorFilial,
            getTcsParametroValorFilialByEntitySearchNoAssociations: getTcsParametroValorFilialByEntitySearchNoAssociations,
            clearTcsParametroValorLjvLoja: clearTcsParametroValorLjvLoja,
            getTcsParametroValorLjvLoja: getTcsParametroValorLjvLoja,
            getTcsParametroValorLjvLojaByEntitySearchNoAssociations: getTcsParametroValorLjvLojaByEntitySearchNoAssociations,
            clearTcsParametroValorVariacaoGenericaP: clearTcsParametroValorVariacaoGenericaP,
            getTcsParametroValorVariacaoGenericaP: getTcsParametroValorVariacaoGenericaP,
            getTcsParametroValorVariacaoGenericaPByEntitySearchNoAssociations: getTcsParametroValorVariacaoGenericaPByEntitySearchNoAssociations,
            clearTcsParametroValorVariacaoGenerica: clearTcsParametroValorVariacaoGenerica,
            getTcsParametroValorVariacaoGenerica: getTcsParametroValorVariacaoGenerica,
            getTcsParametroValorVariacaoGenericaByEntitySearchNoAssociations: getTcsParametroValorVariacaoGenericaByEntitySearchNoAssociations,
            clearTcsParametroValorP1: clearTcsParametroValorP1,
            getTcsParametroValorP1: getTcsParametroValorP1,
            getTcsParametroValorP1ByEntitySearchNoAssociations: getTcsParametroValorP1ByEntitySearchNoAssociations,
            clearParametroInfo: clearParametroInfo,
            getParametroInfo: getParametroInfo,
            getParametroInfoByEntitySearchNoAssociations: getParametroInfoByEntitySearchNoAssociations,
            clearTcsParametroValorP2: clearTcsParametroValorP2,
            getTcsParametroValorP2: getTcsParametroValorP2,
            getTcsParametroValorP2ByEntitySearchNoAssociations: getTcsParametroValorP2ByEntitySearchNoAssociations,
            clearTcsParametroValorLojaP: clearTcsParametroValorLojaP,
            getTcsParametroValorLojaP: getTcsParametroValorLojaP,
            getTcsParametroValorLojaPByEntitySearchNoAssociations: getTcsParametroValorLojaPByEntitySearchNoAssociations,
            clearLjvLojaParametro: clearLjvLojaParametro,
            getLjvLojaParametro: getLjvLojaParametro,
            getLjvLojaParametroByEntitySearchNoAssociations: getLjvLojaParametroByEntitySearchNoAssociations,
            clearTcsParametroValorUsuarioP: clearTcsParametroValorUsuarioP,
            getTcsParametroValorUsuarioP: getTcsParametroValorUsuarioP,
            getTcsParametroValorUsuarioPByEntitySearchNoAssociations: getTcsParametroValorUsuarioPByEntitySearchNoAssociations,
            clearTcsParametroValorRedeP: clearTcsParametroValorRedeP,
            getTcsParametroValorRedeP: getTcsParametroValorRedeP,
            getTcsParametroValorRedePByEntitySearchNoAssociations: getTcsParametroValorRedePByEntitySearchNoAssociations,
            clearTcsParametroValorGpeconP: clearTcsParametroValorGpeconP,
            getTcsParametroValorGpeconP: getTcsParametroValorGpeconP,
            getTcsParametroValorGpeconPByEntitySearchNoAssociations: getTcsParametroValorGpeconPByEntitySearchNoAssociations,
            clearTcsParametroValorFilialP: clearTcsParametroValorFilialP,
            getTcsParametroValorFilialP: getTcsParametroValorFilialP,
            getTcsParametroValorFilialPByEntitySearchNoAssociations: getTcsParametroValorFilialPByEntitySearchNoAssociations,
            clearTcsParametroValorVariacaoGenericaP1: clearTcsParametroValorVariacaoGenericaP1,
            getTcsParametroValorVariacaoGenericaP1: getTcsParametroValorVariacaoGenericaP1,
            getTcsParametroValorVariacaoGenericaP1ByEntitySearchNoAssociations: getTcsParametroValorVariacaoGenericaP1ByEntitySearchNoAssociations,
                finalizeAllLookUpTcsAplicativo: finalizeAllLookUpTcsAplicativo,
            clearLookUpTcsAplicativo: clearLookUpTcsAplicativo,
            finalizeAllLookUpTcsParametroGrupoAutorizacao: finalizeAllLookUpTcsParametroGrupoAutorizacao,
            clearLookUpTcsParametroGrupoAutorizacao: clearLookUpTcsParametroGrupoAutorizacao,
            finalizeAllLookUpTcsTabelaAutorizacaoSelecao: finalizeAllLookUpTcsTabelaAutorizacaoSelecao,
            clearLookUpTcsTabelaAutorizacaoSelecao: clearLookUpTcsTabelaAutorizacaoSelecao,
            finalizeAllLookTcsParametroUsuario: finalizeAllLookTcsParametroUsuario,
            clearLookTcsParametroUsuario: clearLookTcsParametroUsuario,
            finalizeAllLookUpParametroRede: finalizeAllLookUpParametroRede,
            clearLookUpParametroRede: clearLookUpParametroRede,
            finalizeAllLookUpParametroGpecon: finalizeAllLookUpParametroGpecon,
            clearLookUpParametroGpecon: clearLookUpParametroGpecon,
            finalizeAllLookUpParametroFilial: finalizeAllLookUpParametroFilial,
            clearLookUpParametroFilial: clearLookUpParametroFilial,
            finalizeAllLookUpParametroLoja: finalizeAllLookUpParametroLoja,
            clearLookUpParametroLoja: clearLookUpParametroLoja,
            finalizeAllLookUpTcsTabelaAutorizacaoC: finalizeAllLookUpTcsTabelaAutorizacaoC,
            clearLookUpTcsTabelaAutorizacaoC: clearLookUpTcsTabelaAutorizacaoC
        };
    loadParameters();
    return dataContext;
    //#endregion Context Definition
}
return result;
});
