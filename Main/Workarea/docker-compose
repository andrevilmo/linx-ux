version: '2'
services:
    site:
        build: .
        container_name: my_workarea_site
        environment:
            NODE_ENV: production
        ports:
            - 80


################
### cmds
################
#$ docker-compose up --build

#$ docker-compose start
#$ docker-compose stop